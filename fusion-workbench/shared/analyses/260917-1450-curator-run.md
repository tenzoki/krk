# Kuratorlauf 260917-1450

---
**Status:** Complete
**Datum:** 260917
**HEAD:** `5501401cb57ab8f3426a39803423df0c466596f2`
**Modus:** survey (260917-1450), danach anwenden (260917-1526, Freigabe: alle elf Einträge)
**Anker des vorigen Laufs:** `6f57c9fc19bcf048436938f01ed22b6089e21ddf` (2026-08-29, `chore(workbench): archive stale files (tier-1)`), gelesen aus `.cadence-anchors`, Schlüssel `last_curator_run`. Eine Laufdatei jenes Durchgangs steht in keinem der Analysespeicher; der Lauf ist allein über den Anker und über den Commit `c9b19f4 docs: apply the approved normative-surface changes` (2026-08-29) belegt.
**Umfang:** anchored. Gelesen wurden `6f57c9f..HEAD` (149 Commits) sowie Datensätze mit Stempel ab `260829`.
**Placement:** off. Diese Ausgabe hat keine Umsetzungs-Einträge vorgeschlagen und führt deshalb keinen Abschnitt zur Platzierung.
**Einträge:** 11 vorgeschlagen (8 auf Stufe 1, 3 auf Stufe 2, 0 Entfernungen einer Festlegung, 0 Verschiebungen, 0 Zusammenlegungen), 1 Bewerber.

---

## Die drei normativen Flächen dieses Projekts

Zwei der drei sind hier leer, und das ist keine Lücke, sondern der Bestand. Das Projekt hat weder ein Verzeichnis `./rules/` noch `.claude/rules/`; `ls -d rules .claude/rules` meldet beide als nicht vorhanden. Damit bleiben die Entscheidungsdatensätze unter `fusion-workbench/shared/decisions` und `fusion-workbench/circles/*/decisions` sowie `CLAUDE.md`. Alle elf vorgeschlagenen Änderungen betreffen `CLAUDE.md`; an keinem Entscheidungsdatensatz ist eine Änderung vorgeschlagen.

## Was gelesen wurde, Quelle für Quelle

| # | Quelle | Bestand | Gelesen |
|---|---|---|---|
| 1 | Arbeitseinheiten (`circles/`) | 27 Container | 27 nach Name und Zustand aufgenommen, 3 Datensätze ganz gelesen (die drei ohne Marker). Der Plan und der Prüfbericht der Runde 24 sind nach Name und Marker aufgenommen und **nicht gelesen** |
| 2 | Entscheidungsdatensätze | 222 (60 gemeinsam, 162 in Circles) | 222 nach Name und Marker aufgenommen, 44 mit Stempel ab `260829` einzeln geprüft, 5 ganz oder in Teilen gelesen |
| 3 | git | 149 Commits seit dem Anker | `git log --follow 6f57c9f..HEAD -- CLAUDE.md` → 6 Commits, alle gelesen. Für Regeldateien entfällt der Lauf, weil es keine gibt |
| 4 | Prüfberichte und Analysen | 107 Prüfberichte, 11 Analysen | 2 Prüfberichte und 5 Analysen liegen nach dem Anker. Die 5 Analysen sind ganz gelesen und Satz für Satz gegen `CLAUDE.md` gehalten worden; sie haben L06 bestätigt und L11 beigesteuert. Die 2 Prüfberichte sind nach Name aufgenommen und **nicht gelesen** |
| 5 | `orchestrator-events.jsonl` | 2 575 Zeilen | nur stützend gelesen, keine Aussage ruht darauf |
| 6 | Archiv | 5 Durchgänge, 162 Dateien | der jüngste Durchgang ist `260829-1256-safe-cleanup-tier-1` und liegt auf dem Anker; **seit dem Anker ist nichts archiviert worden**, also null neu gelesen |
| 7 | `**Provenance:**`-Köpfe von Regeldateien | **null** | es gibt keine projekteigenen Regeldateien |

Keine Quelle war unlesbar. Die beiden leeren sind Quelle 6 (im gebundenen Umfang) und Quelle 7 (überhaupt).

Daneben sind die offenen Defektdatensätze als Gegenprobe gelesen worden, nicht als vierte Fläche: 110 offene Defekte, davon 20 mit Stempel ab `260829` einzeln geprüft. Drei davon halten denselben Befund fest, den ein Eintrag unten vorschlägt (L04, L05, L06); **kein offener Defekt widerspricht einem Eintrag dieser Liste.**

Die zweite Gegenprobe gilt den Markern: Für jeden der 20 Defekte sind seine Datensatzverweise aufgelöst und die Marker der getroffenen Entscheidungen gelesen worden. Genau ein offener Defekt verweist auf eine als umgesetzt (`_i_`) markierte Entscheidung, nämlich `260905-2254` auf `260826-1811_i_wie-wird-die-vollstaendigkeit-einer-alle-liste-neben-einer-aufzaehlung-gehalten.md`. **Das ist kein Paar zum Melden**: der Defekt beschreibt die Umsetzung nicht als fehlend, sondern nennt die Entscheidung als benachbarte offene Frage, und die Umsetzung steht im Baum (`grep -rn 'jede_alle_liste_fuehrt_genau_die_varianten_ihrer_aufzaehlung' crates` → vier Treffer, darunter der Durchlauf selbst in `crates/krk-core/tests/baum.rs`). Kein Marker ist hier vor- oder zurückzusetzen, und das wäre ohnehin Arbeit des Abgleichers.

## Größe der Flächen

| Fläche | Vorher | Nachher |
|---|---|---|
| `CLAUDE.md` | 71 010 Bytes, 213 Zeilen | 74 535 Bytes, 213 Zeilen (`wc -c -l CLAUDE.md` gegen `git show HEAD:CLAUDE.md \| wc -c -l`) |
| Entscheidungsdatensätze | 222 Dateien | 222 Dateien, keine Änderung vorgeschlagen und keine angewandt |
| projekteigene Regeldateien | nicht vorhanden | nicht vorhanden |

Die elf Einträge fassen zusammen 3 409 Bytes an Bestandstext an und schreiben 6 938 zurück, also 4,8 Prozent der Datei berührt und keine Verschiebung. **Der Schwellenwert von 20 Prozent ist nicht erreicht**, eine gesonderte Rückfrage zum Ausmaß entfällt.

**Entfernungen einer Festlegung: null auf jeder Stufe.** Kein Eintrag dieses Laufs nimmt eine Festlegung heraus; L11 hebt eine Ausnahme auf und bindet danach mehr Stellen als vorher.

## Vergleichszählung je Flächenpaar

Keine dieser Zahlen behauptet Vollständigkeit über den Korpus. Jede nennt die Auswahlregel und das, was sie erreicht hat.

**1. Entscheidung gegen Entscheidung.** Auswahlregel: die 44 Datensätze mit Stempel ab dem Ankerdatum gegen die 36 lebenden (`_o_` und `_a_`) Datensätze beider Speicher, also 1 584 geordnete Paare aufgezählt. Gelesen wurden die Paare, deren Dateinamen zwei oder mehr unterscheidende Wortteile teilen: 8 Paare, alle 8 geöffnet. Das stärkste ist `260910-0818_a_…-zusage-l4` gegen `260819-2216_a_…-zusage-l7`, sechs geteilte Wortteile; beide beantworten dieselbe Frage für verschiedene Runden und antworten gleich. **Kein Widerspruch in den acht Paaren, die die Regel erreicht hat.**

**2. Entscheidung gegen Regeldatei.** Null Paare: es gibt keine projekteigene Regeldatei.

**3. Entscheidung gegen `CLAUDE.md`.** Auswahlregel: die 40 Datensatzverweise, die `CLAUDE.md` selbst führt, sowie die 44 Datensätze ab dem Anker. Alle 40 Verweise sind im Werkbankbaum aufgelöst worden (`find` je Basisname, Marker ausgesternt): **40 von 40 lösen eindeutig auf, keiner doppelt, keiner tot**, und jeder Marker, den `CLAUDE.md` ausdrücklich nennt, stimmt mit dem Dateinamen überein. Zwei Widersprüche: L01 und L11. Für L11 ist der Verweis nicht erschlossen, sondern ausgeschrieben: der Datensatz `260818-0201_*` nennt in seiner eigenen Kopfzeile `**Cross-references:**` den Abschnitt von `CLAUDE.md`, den er überholt.

**4. Regeldatei gegen Regeldatei.** Null Paare.

**5. Regeldatei gegen `CLAUDE.md`.** Null Paare.

**6. `CLAUDE.md` gegen sich selbst und gegen den Baum.** Alle 213 Zeilen gelesen. Jedes Kommando, das die Datei als Erhebungsmittel nennt, ist ausgeführt worden; jede benannte Datei, Probe, Konstante und Aufzählung ist im Baum gesucht worden. Acht Einträge kommen von hier, sämtlich Stufe 1. Ausdrücklich geprüft und **richtig befunden** sind unter anderem: die zehn Zeitzusagen in `crates/krk-bench/src/messen.rs`, die vier Mitglieder mit `#![deny(unsafe_code)]` gegen `members`, die genau zwei Dateien mit `#![allow(unsafe_code)]`, die eine Hülle um `NSPasteboard`, die drei Stellen mit `Bereich::ALLE.len()`, die drei anerkannten Prüfordner-Fassungen samt der vierten in `xtask`, die drei Stellen der Betriebsregel außerhalb dieser Datei, die fünf Merkmale von `gix`, die zwei von `zip`, das fehlende Bibliotheksziel in `krk-ui`, der eine Rufer von `rueckschritt`, die drei Kommandos in `immer_erreichbar`, `anlegen_falls_fehlt` in genau zwei Dateien, die Abwesenheit von `setLevelsOfUndo` im Code, `std::env::temp_dir()` an genau einer Stelle für den Messplan (`plan_schreiben`, `crates/krk-bench/src/messen.rs:1959`) und `Ordnermodell::neu` als die eine Stelle der Deep-Vorbelegung. Von den vierzig Verweisen und den rund dreißig ausgeführten Kommandos und Symbolsuchen haben **acht** eine falsche Aussage getroffen; alles andere stimmt mit dem Baum überein.

## Vorgelegter Bestandstext

An keinem Entscheidungsdatensatz ist eine Änderung vorgeschlagen, deshalb ist hier kein Datensatzinhalt festzuhalten. Der Bestandstext jedes vorgeschlagenen Eingriffs an `CLAUDE.md` steht im jeweiligen Eintrag unter **Vorher** und ist beim Anwenden Zeichen für Zeichen gegen die Platte zu halten.

## Rückwegs-Zusage

`CLAUDE.md` liegt unter Versionsverwaltung. Jeder angewandte Eintrag ist mit `git checkout -- CLAUDE.md` zurückzunehmen, solange nichts committet ist.

## Die Änderungsliste

Elf Einträge, keiner davon eine Entfernung einer Festlegung: acht auf Stufe 1 (L02 bis L09) und drei auf Stufe 2 (L01, L10, L11). Keine Verschiebung, keine Zusammenlegung, keine Stufe 3.

### L01 — L4 steht seit dem 260910-0818 wieder auf den Gegenständen der späteren Messrunde

- **Fläche:** `CLAUDE.md`
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 2
- **Beleg:** `circles/260910-0707-krk-meldet-neuerungen-in-readers-settings-keymap/decisions/260910-0818_*_schuldet-diese-runde-einen-abnahmelauf-gegen-die-zusage-l4.md` `Answered:` — „Möglichkeit 1: kein eigener Abnahmelauf, L4 kommt auf die Gegenstände der späteren Messrunde; derselbe Weg wie bei L7 in der Runde 14“, ruled by user. Der Satz, den der Datensatz überholt, ist „**Drei davon sind mit dem Lauf vom 260810 erledigt** (L1, L4 und L7 halten)“: von den dreien steht L7 seit dem 260819-2242 und L4 seit dem 260910-0818 wieder daneben. Der Datensatz nennt die Stelle selbst: „CLAUDE.md führt das Ergebnis seither unter „Maximen““.
- **Folgengruppe:** tier-2
- **Entfernte Festlegung:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Drei davon sind mit dem Lauf vom 260810 erledigt** (L1, L4 und L7 halten). Offen bleibt der vierte, die Geschwindigkeit der Syntaxhervorhebung aus C3 — sie gehört zu keiner der zehn Zusagen, kommt aus einer fremden Kiste und ist auf dem Referenzgerät weiterhin ungemessen. **L7 steht seit dem 260819-2242 wieder daneben:** die Runde 14 legt Arbeit innerhalb seiner Endbedingung nach und verzichtet dafür auf einen eigenen Abnahmelauf, also kommt die Zusage auf die Gegenstände der späteren Messrunde zurück (`shared/decisions/260819-2216_*_schuldet-diese-runde-einen-abnahmelauf-gegen-die-zusage-l7.md`).

**Nachher:**

> **Drei davon sind mit dem Lauf vom 260810 erledigt** (L1, L4 und L7 halten). Offen bleibt der vierte, die Geschwindigkeit der Syntaxhervorhebung aus C3 — sie gehört zu keiner der zehn Zusagen, kommt aus einer fremden Kiste und ist auf dem Referenzgerät weiterhin ungemessen. **L7 steht seit dem 260819-2242 wieder daneben:** die Runde 14 legt Arbeit innerhalb seiner Endbedingung nach und verzichtet dafür auf einen eigenen Abnahmelauf, also kommt die Zusage auf die Gegenstände der späteren Messrunde zurück (`shared/decisions/260819-2216_*_schuldet-diese-runde-einen-abnahmelauf-gegen-die-zusage-l7.md`). **L4 steht seit dem 260910-0818 aus demselben Grund daneben:** die Runde 24 legt beim Start drei weitere Ablagedateien nach, verzichtet ebenso auf einen eigenen Abnahmelauf und schiebt die Zusage auf dieselbe spätere Messrunde (`circles/260910-0707-krk-meldet-neuerungen-in-readers-settings-keymap/decisions/260910-0818_*_schuldet-diese-runde-einen-abnahmelauf-gegen-die-zusage-l4.md`). Von den dreien hält damit nur noch L1 ohne Vorbehalt.

### L02 — Der Zustandsmarker `_t_` gibt es nicht mehr, und der Glob über `_*_circle.md` sieht die Runden ab der 24. nicht

- **Fläche:** `CLAUDE.md`
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `ls fusion-workbench/circles/*/_*_circle.md` → 24 Dateien, `ls -d fusion-workbench/circles/*/` → 27 Verzeichnisse. Die drei ohne Marker sind `260910-0707-krk-meldet-neuerungen-in-readers-settings-keymap`, `260911-1825-fusion-leseprofile-auf-fusion-11-nachziehen` und `260917-1415-kontextmenue-traegt-open-with`; `grep -h '^\*\*Status:\*\*' fusion-workbench/circles/*/2*-*.md` → `done`, `open`, `done`. `ls fusion-workbench/circles/*/_t_circle.md` → kein Treffer. Die Form ist im Datensatz der Runde 25 ausgeschrieben (`circles/260911-1825-fusion-leseprofile-auf-fusion-11-nachziehen/260911-1825-fusion-leseprofile-auf-fusion-11-nachziehen.md`, Abschnitt „Die eine Ursache hinter fast allem“) und in `$FUSION_PLUGIN_ROOT/rules/fusion-workbench-conventions.md` `## Backlog entries — work items` normiert.
- **Folgengruppe:** tier-1
- **Entfernte Festlegung:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Wie viele Runden gefahren sind und wie jede geschlossen hat, sagt der Dateibestand und nicht diese Zeile:** `ls fusion-workbench/circles/*/_*_circle.md`. Beschränkt (`_b_`) und kohärent (`_c_`) geschlossene Runden zählen als gefahren, vorgesehene (`_a_`) und zurückgestellte (`_d_`) nicht. Eine Runde, die gerade läuft, trägt `_t_`: gebaut, solange sie steht, aber noch nicht geschlossen. **Nicht jede Runde hat einen Circle-Datensatz, und jede Zählung über `circles/` in dieser Datei liefert deshalb eine Untergrenze:** die Runde 18 ist ohne aktiven Circle gefahren, ihre Arbeit liegt nach der Herkunftsregel vollständig unter `fusion-workbench/shared/`, und keiner der Globs dieser Datei sieht sie (`shared/issues/260826-0149_*_die-runde-18-hat-keinen-circle-datensatz-und-jede-zaehlung-ueber-circles-uebergeht-sie.md`). Die Tabelle darunter ist ein Verweisregister für die Pfadregel im Absatz danach.

**Nachher:**

> **Wie viele Runden gefahren sind und wie jede geschlossen hat, sagt der Dateibestand und nicht diese Zeile, und die Werkbank führt dafür seit fusion 11 zwei Formen nebeneinander.** Die älteren Runden tragen ihren Zustand als Buchstaben im Dateinamen ihres Datensatzes: `ls fusion-workbench/circles/*/_*_circle.md`, wobei beschränkt (`_b_`) und kohärent (`_c_`) geschlossene als gefahren zählen, vorgesehene (`_a_`) und zurückgestellte (`_d_`) nicht. Die neueren tragen keinen Buchstaben mehr; ihr Datensatz heißt wie sein Verzeichnis und führt den Zustand als Kopfzeile `**Status:**` mit den Werten `open`, `claimed`, `paused`, `done` und `dropped`, erhoben mit `grep -h '^\*\*Status:\*\*' fusion-workbench/circles/*/2*-*.md`. Einen eigenen Wert für „gebaut, solange sie steht, aber noch nicht geschlossen“ gibt es darin nicht mehr; an die Stelle des früheren `_t_` tritt `claimed`. **Jeder Glob über `_*_circle.md` in dieser Datei liefert deshalb eine Untergrenze**, und zwar aus zwei Gründen: die Runde 18 ist ohne Circle-Datensatz gefahren und liegt nach der Herkunftsregel vollständig unter `fusion-workbench/shared/` (`shared/issues/260826-0149_*_die-runde-18-hat-keinen-circle-datensatz-und-jede-zaehlung-ueber-circles-uebergeht-sie.md`), und jede Runde ab der 24. führt ihren Zustand in der Kopfzeile statt im Namen. Die Tabelle darunter ist ein Verweisregister für die Pfadregel im Absatz danach und endet bei der Runde 23.

### L03 — Die Aufzählung der Runden, die der Glob nicht sieht, nennt nur die Runde 18

- **Fläche:** `CLAUDE.md`
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `ls fusion-workbench/circles/*/_[bc]_circle.md` → 22 Dateien, jüngste `260830-1045-git-bereich-liest-status-branch-verlauf/_b_circle.md` (Runde 23). Die Runden 24 und 25 tragen `**Status:** done` in der Kopfzeile und fehlen in der Liste; gemessen mit demselben `grep -h` wie in L02.
- **Folgengruppe:** tier-1
- **Entfernte Festlegung:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> welche das sind, sagt `ls fusion-workbench/circles/*/_[bc]_circle.md` gegen das Datum `260810`, und die Runde 18 kommt ohne Circle-Datensatz dazu; keine davon ist gegen die zehn Zusagen gemessen.

**Nachher:**

> welche das sind, sagt `ls fusion-workbench/circles/*/_[bc]_circle.md` gegen das Datum `260810`; dazu kommen die Runden, die dieser Glob nicht sieht, also die Runde 18 ohne Circle-Datensatz und jede Runde ab der 24. mit ihrem Zustand in der Kopfzeile. Keine davon ist gegen die zehn Zusagen gemessen.

### L04 — Der Glob über `_a_circle.md` kann vorgesehene Arbeit nicht mehr melden

- **Fläche:** `CLAUDE.md`
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `ls fusion-workbench/circles/*/_a_circle.md` → kein Treffer, während `grep -l '^\*\*Status:\*\* open' fusion-workbench/circles/*/2*-*.md` → `circles/260917-1415-kontextmenue-traegt-open-with/260917-1415-kontextmenue-traegt-open-with.md`. Der offene Defekt `shared/issues/260910-0739_*_claude-md-sagt-es-gebe-keinen-vorgesehenen-circle-seit-heute-gibt-es-einen.md` hält denselben Befund fest und schreibt die Abnahme aus: „Der Abschnitt nennt kein Ergebnis des Kommandos mehr, oder er nennt eines, das der Baum trägt.“
- **Folgengruppe:** tier-1
- **Entfernte Festlegung:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Welche Circles vorgesehen und nicht gefahren sind, sagt `ls fusion-workbench/circles/*/_a_circle.md` und nicht diese Zeile.** Seit dem 260821-2202 gibt das Kommando nichts aus. Der Web-Betrachter im Vorschaufenster

**Nachher:**

> **Welche Arbeit vorgesehen und nicht gefahren ist, sagt der Dateibestand und nicht diese Zeile.** `ls fusion-workbench/circles/*/_a_circle.md` gibt seit dem 260821-2202 nichts mehr aus und kann für eine nach der Umstellung auf fusion 11 angelegte Arbeitseinheit auch nichts mehr ausgeben, weil deren Datensatz keinen Zustandsbuchstaben trägt; erhoben wird sie deshalb über die Kopfzeile, mit `grep -l '^\*\*Status:\*\* open' fusion-workbench/circles/*/2*-*.md`. Der Web-Betrachter im Vorschaufenster

### L05 — Die Freigabe des Rückgängig-Blocks ist gemessen, nicht ungemessen

- **Fläche:** `CLAUDE.md`
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `grep -rn 'der_verwalter_gibt_den_block_auf_allen_vier_wegen_frei' crates` → `crates/krk-ui/src/appkit/editor.rs:983` (Doc) und `:4363` (die Probe); `grep -n 'Freigabe des Blocks' crates/krk-ui/src/appkit/editor.rs` → `971:/// # Die Freigabe des Blocks ist seit dem 260907 gemessen`. Der offene Defekt `shared/issues/260908-0838_*_claude-md-nennt-die-freigabe-des-rueckgaengig-blocks-ungemessen-seit-heute-haelt-sie-eine-probe.md` hält denselben Befund fest und verlangt ausdrücklich, den Verweis auf `260810-1341` stehen zu lassen.
- **Folgengruppe:** tier-1
- **Entfernte Festlegung:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> Ob die Freigabe eines angemeldeten Blocks wirklich abträgt, ist in diesem Baum durch nichts gemessen; der Datensatz dazu ist als Lage angenommen geschlossen, nicht behoben (`issues/260810-1341_*_die-freigabe-des-angemeldeten-rueckgaengig-blocks-ist-geschlossen-und-nicht-gemessen.md`).

**Nachher:**

> **Die Freigabe eines angemeldeten Blocks ist seit dem 260907 gemessen.** `der_verwalter_gibt_den_block_auf_allen_vier_wegen_frei` (`krk-ui/src/appkit/editor.rs`, Prüfmodul) meldet einen Block an, der eine `Stapellast` hält, und fährt die vier Wege des Kopfes von `Stapellast` je an einem eigenen `NSUndoManager`; alle vier geben frei, gemessen am Zähler. Ohne offenen `autoreleasepool` fällt der Zähler auf keinem der vier Wege, und der Editor bekommt seinen von der Ereignisschleife der Anwendung, je Ereignis einen. Der ältere Datensatz hält den Stand von davor fest und ist als Lage angenommen geschlossen (`issues/260810-1341_*_die-freigabe-des-angemeldeten-rueckgaengig-blocks-ist-geschlossen-und-nicht-gemessen.md`); seine Lage besteht nicht mehr.

### L06 — Das genannte Zählkommando für `ohne_warten_oeffnen` zählt Prosa mit

- **Fläche:** `CLAUDE.md`
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `grep -rn 'ohne_warten_oeffnen(' crates/krk-core/src | wc -l` → 9 Zeilen; der Lauf, den `verzeichnis/sys.rs` selbst nennt, gibt 6 aus, nämlich `operation/entpacken.rs:137`, `operation/zippen.rs:389`, `text/datei.rs:492`, `:694`, `:776`, `verzeichnis/sys.rs:261`. Der offene Defekt `shared/issues/260905-2254_*_das-zaehlkommando-fuer-ohne-warten-oeffnen-gibt-elf-zeilen-fuer-sechs-rufer-aus.md` nennt die Quelldatei behoben und `CLAUDE.md` ausdrücklich offen.
- **Folgengruppe:** tier-1
- **Entfernte Festlegung:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Wie viele Aufrufer die Hülle hat und wo sie liegen, sagt `grep -rn 'ohne_warten_oeffnen(' crates/krk-core/src` und nicht diese Zeile**

**Nachher:**

> **Wie viele Aufrufer die Hülle hat und wo sie liegen, sagt der Lauf, den der Modulkopf und der Doc-Kommentar von `ohne_warten_oeffnen` selbst nennen, und nicht diese Zeile**: `awk 'FNR==1{probe=0} /^#\[cfg\(test\)\]/{probe=1} !probe && /ohne_warten_oeffnen\(/ && !/^[[:space:]]*\/\// && !/^pub fn/ {print FILENAME":"FNR}' $(find crates/krk-core/src -name '*.rs' | sort)`. Ein bloßes `grep` über dasselbe Muster zählt die Definition, das Prüfmodul und jede Prosastelle mit und hebt sein Ergebnis mit jedem weiteren Satz über die Hülle (`shared/issues/260905-2254_*_das-zaehlkommando-fuer-ohne-warten-oeffnen-gibt-elf-zeilen-fuer-sechs-rufer-aus.md`)

### L07 — `UNLESBARE_ALLE_LISTEN` führt einen Eintrag, nicht zwei

- **Fläche:** `CLAUDE.md`
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `awk '/const UNLESBARE_ALLE_LISTEN/,/^\];/' crates/krk-core/tests/baum.rs` → `const UNLESBARE_ALLE_LISTEN: [(&str, &str, &str); 1] = [( "krk-core/src/ablage/pfade.rs", "Datei", … )];` — ein Eintrag.
- **Folgengruppe:** tier-1
- **Entfernte Festlegung:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Zwei Listen kann er nicht lesen, und sie stehen mit Grund in `UNLESBARE_ALLE_LISTEN`** in derselben Datei, statt als stiller Übersprung im Code.

**Nachher:**

> **Welche Listen er nicht lesen kann, sagt `UNLESBARE_ALLE_LISTEN`** in derselben Datei, je Eintrag die Datei, die Aufzählung und den Grund, statt als stiller Übersprung im Code; erhoben mit `awk '/const UNLESBARE_ALLE_LISTEN/,/^\];/' crates/krk-core/tests/baum.rs`.

### L08 — KRK meldet seit der Runde 24 sehr wohl, was eine neue Fassung mitbringt

- **Fläche:** `CLAUDE.md`
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `crates/krk-core/src/ablage/neuerungen.rs` (Modulkopf: „Was eine neue Fassung von KRK an den von Hand gepflegten Ablagedateien mitbringt, das die Datei des Nutzers noch nicht fuehrt“), `crates/krk-ui/src/appkit/anwendung.rs:499` ruft `neuerungen::startzeile`, `crates/krk-ui/src/appkit/blaetter/neuerungen.rs` zeigt das Blatt, `crates/krk-core/src/tasten/belegung.rs:860` führt `Kommando::NeuerungenZeigen`. Die Runde steht auf `**Status:** done`, ihr Plan `circles/260910-0707-…/planning/260910-0818_c_plan-krk-meldet-neuerungen-in-readers-settings-keymap.md` auf `_c_`.
- **Folgengruppe:** tier-1
- **Entfernte Festlegung:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> und eine Meldung darüber gibt es nicht: eine unveränderte Datei ist nicht beschädigt.

**Nachher:**

> und seit der Runde 24 meldet KRK beim Start, welche Namen die Auslieferungsfassung führt und die Nutzerdatei nicht, und umgekehrt (`krk-core/src/ablage/neuerungen.rs`, `startzeile`; das Blatt auf Abruf hängt an `Kommando::NeuerungenZeigen`). Geschrieben wird dabei nichts: die Meldung ergänzt die Datei nicht, weil eine unveränderte Datei nicht beschädigt ist.

### L09 — Die Belegform „Abgleiche unter `history/`“ endet mit der Runde 24

- **Fläche:** `CLAUDE.md`
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `ls fusion-workbench/circles/260911-1825-fusion-leseprofile-auf-fusion-11-nachziehen/` → nur der Datensatz und `issues/`; weder `planning/` noch `history/`. `$FUSION_PLUGIN_ROOT/rules/fusion-workbench-conventions.md` `## Session history`: „The history store is closed to writes. No agent writes a session log“.
- **Folgengruppe:** tier-1
- **Entfernte Festlegung:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> Jede Runde hat ihre Planschritte vollständig auf `[DONE]` und jede behauptete Erledigung einzeln gegen den Baum gelesen — die Belege sind die Abgleiche unter `history/` des jeweiligen Circles.

**Nachher:**

> Jede Runde bis zur 24. hat ihre Planschritte vollständig auf `[DONE]` und jede behauptete Erledigung einzeln gegen den Baum gelesen; die Belege sind die Abgleiche unter `history/` des jeweiligen Circles. **Diese Belegform endet dort**: fusion schreibt seit der Fassung 11 keine Sitzungsprotokolle mehr, und was eine spätere Runde geprüft hat, steht in ihren Commits und in den Datensätzen, die sie hinterlassen hat.

### L10 — Die Unterscheidung zwischen `_b_` und `_c_` endet mit der Runde 23

- **Fläche:** `CLAUDE.md`
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 2
- **Beleg:** `$FUSION_PLUGIN_ROOT/rules/fusion-workbench-conventions.md` `## Backlog entries — work items`: „`dropped` covers in one value what an issue's `_c_` and `_d_` and the six-marker Circle vocabulary's `_c_`, `_b_`, `_s_` and `_d_` distinguished by marker“; die fünf Werte sind `open`, `claimed`, `paused`, `done`, `dropped`. Der überholte Satz ist „Beide Listen lassen jede Runde ohne Circle-Datensatz aus.“, der die Grenze der beiden Globs nur halb nennt: sie endet bei der Runde 23, gemessen mit `ls fusion-workbench/circles/*/_[bc]_circle.md | tail -1`.
- **Folgengruppe:** tier-2
- **Entfernte Festlegung:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> Beide Listen lassen jede Runde ohne Circle-Datensatz aus.

**Nachher:**

> Beide Listen lassen jede Runde ohne Circle-Datensatz aus und enden bei der Runde 23: **die Unterscheidung zwischen beschränktem und kohärentem Abschluss gibt es seit fusion 11 nicht mehr**, der Zustand einer späteren Runde ist `done` und sagt über den Abnahmelauf nichts.

### L11 — Die Ortsregel für den Marker in Zitaten ist durch eine Gestaltregel ersetzt

- **Fläche:** `CLAUDE.md`
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 2
- **Beleg:** `shared/decisions/260818-0201_*_does-a-cross-references-line-between-records-write-the-marker-in-the-star-form.md`, Marker `_a_`, `Answered:`-Zeile: „der Platzhalter gilt fuer jedes Zitat und nicht nur fuer die Querverweiszeile; eine Aussage ueber ein Zitat bekommt eine von aussen erkennbare Gestalt; ruled by user, Kai Stalmann". Derselbe Datensatz nennt in seiner Kopfzeile `**Cross-references:**` ausdrücklich „`CLAUDE.md` `## Bindende Grundlage: die Entscheidungsdatensätze` (the location rule)", benennt den überholten Satz also selbst. Niedergelegt ist die Neufassung in `$FUSION_PLUGIN_ROOT/rules/fusion-workbench-conventions.md` `## Filename Patterns` (Sternform für jedes Zitat, keine Ausnahme nach Speicher) und `## Marker globs` (eine Aussage über ein Zitat nennt Datei und Zeile oder zäunt den Wortlaut ein). Der überholte Satz ist „und die Ausnahme gilt je Datei nach ihrem Ort, nicht je Absatz: `history/`, `reviews/`, `analyses/`, `issues/`, `decisions/`, `messungen/` und `spikes/`".
- **Folgengruppe:** tier-2
- **Entfernte Festlegung:** die Ausnahme nach dem Speicherort. Sie verschwindet als Ausnahme, nicht als Zusage: die Sternform bindet danach **mehr** Stellen als vorher, und der Fall, den die Ortsregel schützen sollte, bleibt über die Gestaltregel geschützt. Die Begründung „die Ortsregel ist entscheidbar, eine Regel je Absatz wäre es nicht" fällt mit ihr; die Gestaltregel ist an derselben Stelle entscheidbar, weil sie an der Schreibweise der zitierenden Zeile hängt und nicht am Sinn des Absatzes.
- **Rückweg:** `git checkout -- CLAUDE.md`
- **Folge außerhalb dieses Auftrags:** zwei Doc-Kommentare in `xtask/src/release.rs` (Zeilen 1492 und 1526) nennen die Ortsregel beim Namen und begründen mit ihr, warum `fusion-workbench/` aus zwei Quellbaum-Läufen fällt. Der Schluss bleibt richtig, der Regelname stimmt nach diesem Eintrag nicht mehr. `xtask/` ist Anwendungscode und gehört dem `coder`; abgelegt als `260917-1526_*_zwei-doc-kommentare-in-xtask-berufen-sich-auf-die-ortsregel-die-claude-md-nicht-mehr-fuehrt.md`.

**Vorher:**

> **Aufzeichnungen eines Standes behalten ihren damaligen Marker**, und die Ausnahme gilt je Datei nach ihrem Ort, nicht je Absatz: `history/`, `reviews/`, `analyses/`, `issues/`, `decisions/`, `messungen/` und `spikes/`. Die Ortsregel ist entscheidbar, eine Regel je Absatz wäre es nicht und lieferte bei jedem Durchgang einen anderen Bestand. Daneben bleibt jede Stelle stehen, an der der Marker die Aussage selbst ist — eine Befundtabelle mit den Spalten „zitiert" und „ist", die Beschreibung einer Umbenennung: dort löschte die Sternform den Inhalt.

**Nachher:**

> **Aufzeichnungen eines Standes behalten ihren damaligen Marker, und welche Stelle ihn behält, entscheidet ihre Gestalt und nicht der Speicher, in dem die Datei liegt.** Die Sternform gilt seit dem 260906-2147 für **jedes** Zitat, ohne Ausnahme nach Speicher (`shared/decisions/260818-0201_*_does-a-cross-references-line-between-records-write-the-marker-in-the-star-form.md`, beantwortet; niedergelegt in `$FUSION_PLUGIN_ROOT/rules/fusion-workbench-conventions.md` `## Filename Patterns`). Stehen bleibt, was keine Adresse ist, sondern eine Aussage über eine: eine Befundtabelle mit den Spalten „zitiert" und „ist", die Beschreibung einer Umbenennung. Eine solche Stelle nennt die zitierende Zeile mit Datei und Zeilennummer oder zäunt den Wortlaut in einen Codeblock ein, statt den Marker als Adresse auszuschreiben; die Regel dazu steht in `## Marker globs` derselben Datei.
## Bewerber — nicht zur Freigabe vorgelegt und nicht anzuwenden

### C01 — Das Rundenregister endet bei der Runde 23, der Baum trägt zwei weitere gefahrene Runden

- **Fläche:** `CLAUDE.md`, die Rundentabelle nach dem Abschnitt „Worum es geht“
- **Folgengruppe:** Bewerber
- **Warum Bewerber:** Die Tabelle führt die Runden 1 bis 23. `circles/260910-0707-krk-meldet-neuerungen-in-readers-settings-keymap` und `circles/260911-1825-fusion-leseprofile-auf-fusion-11-nachziehen` stehen beide auf `**Status:** done` und fehlen darin, während der Absatz zu den Leseprofilen schon von „der Runde 24“ spricht. Dass die erste von beiden die Runde 24 ist, steht damit in `CLAUDE.md` selbst; **dass die zweite die Runde 25 ist, steht nirgends** und wäre eine Zuschreibung dieses Laufs, `inference:` aus der zeitlichen Folge, die die Tabelle bisher befolgt. Eine Zeile zu schreiben hieße außerdem, ihren Gegenstandstext neu zu formulieren, und das ist neuer Inhalt und keine Berichtigung einer falschen Aussage. L02 schließt die Auskunftslücke ohne diese Zuschreibung, indem es den Leser auf die Kopfzeile verweist. Wer die zwei Zeilen dennoch will, vergibt die Nummern und beauftragt sie gesondert.

## Was außerhalb dieses Auftrags liegt und wem es gehört

**Zwei Rundendatensätze tragen eine Kopfzeile `**Status:**`, die ihrem Dateinamen widerspricht.** `circles/260809-2040-tastenbelegung-als-markdown-in-downloads/_b_circle.md` und `circles/260804-0933-eingebauter-web-betrachter-im-vorschaufenster/_d_circle.md` sagen beide `anticipated`. Der Befund ist gefunden, gemessen und offen abgelegt (`circles/260911-1825-fusion-leseprofile-auf-fusion-11-nachziehen/issues/260912-2158_*_zwei-rundendatensaetze-tragen-eine-status-kopfzeile-die-ihrem-eigenen-dateinamen-widerspricht.md`); die Behebung hängt an einer Nutzerfrage, die der Datensatz ausschreibt. Arbeitseinheiten-Datensätze sind keine der drei Flächen dieses Auftrags, und ihre Pflege gehört dem Orchestrator. Hier wird nichts daran geändert und **kein zweiter Datensatz dafür angelegt**, weil der erste schon steht.

**Drei offene Defekte an `CLAUDE.md` werden von dieser Liste bedient, aber nicht geschlossen.** `260910-0739` (L04), `260908-0838` (L05) und die `CLAUDE.md`-Hälfte von `260905-2254` (L06) erfüllen ihre eigene Abnahme, sobald die Einträge angewandt sind. Das Umlegen des Markers auf `_c_` ist Abgleicharbeit und steht dem Abgleicher zu, nicht diesem Lauf.

## Ergebnisse je Eintrag

Der Durchgang vom 260917-1526 hat alle elf freigegebenen Einträge angewandt, gegen HEAD `5501401cb57ab8f3426a39803423df0c466596f2`. Je Eintrag ist der Bestandstext vor dem Schreiben von der Platte gelesen und Zeichen für Zeichen gegen den Block **Vorher** gehalten worden; alle elf standen genau einmal in der Datei. Nach dem Schreiben ist die Datei erneut gelesen und jeder Block **Nachher** Zeichen für Zeichen darin wiedergefunden worden, ebenfalls genau einmal.

| Eintrag | Ergebnis | Bestandstext gefunden | Text nach dem Schreiben wiedergefunden |
|---|---|---|---|
| L01 | applied | 1× | 1× |
| L02 | applied | 1× | 1× |
| L03 | applied | 1× | 1× |
| L04 | applied | 1× | 1× |
| L05 | applied | 1× | 1× |
| L06 | applied | 1× | 1× |
| L07 | applied | 1× | 1× |
| L08 | applied | 1× | 1× |
| L09 | applied | 1× | 1× |
| L10 | applied | 1× | 1× |
| L11 | applied | 1× | 1× |

Kein Eintrag steht auf `skipped`, `stale` oder `failed`. Der Bewerber C01 ist nicht vorgelegt und nicht angewandt worden.

`CLAUDE.md` ist die einzige angefasste Datei; `git diff --stat -- CLAUDE.md` meldet zehn geänderte Zeilen. Zurückgenommen wird der ganze Lauf mit `git checkout -- CLAUDE.md`, solange nichts committet ist. Nichts ist committet; das bleibt Sache des Nutzers.

**Keine Probe und kein Prüfstück im Baum hängt an der Größe oder am Wortlaut dieser Datei.** Erhoben mit `grep -rn 'CLAUDE\.md' crates xtask`: einundzwanzig Treffer, sämtlich Doc-Kommentare und Modulköpfe, kein Treffer in Zeichenkettenform und damit kein Leser der Datei. Eine Nachführung ist also niemandem geschuldet, bis auf die eine unter L11 abgelegte.
