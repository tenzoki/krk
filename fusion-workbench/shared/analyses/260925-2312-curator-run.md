# Kuratorlauf 260925-2312

---
**Status:** Complete
**Datum:** 260925
**HEAD:** `0d4485ebfa8c5cecfc313d831c9d096eb7d29e76`
**Modus:** survey, danach apply (Freigabe `all`)
**Voriger Lauf:** `260917-1450-curator-run.md`, 260917, HEAD `5501401cb57ab8f3426a39803423df0c466596f2`; derselbe Commit steht als `last_curator_run` in `.cadence-anchors` (gelesen mit `fusion-cadence-anchor get last_curator_run`).
**Umfang:** anchored. Gelesen wurden `5501401..HEAD` (24 Commits) und die Datensätze mit Stempel ab `260917-1415`.
**Placement:** off. **Edges:** off. Ein Abschnitt zur Platzierung und einer zu Kanten zwischen Arbeitseinheiten entfallen deshalb.
**Einträge:** 2 vorgeschlagen (2 auf Stufe 1; keine Entfernung einer Festlegung, keine Stufe 2 oder 3, keine Verschiebung, keine Zusammenlegung), 3 Bewerber.

---

## Die drei normativen Flächen dieses Projekts

Zwei der drei sind leer, wie im vorigen Lauf: `ls -d rules .claude/rules` meldet beide als nicht vorhanden. Es bleiben die Entscheidungsdatensätze unter `fusion-workbench/shared/decisions` und `fusion-workbench/work-packages/*/decisions` sowie `CLAUDE.md`. Beide Einträge betreffen `CLAUDE.md`; an keinem Entscheidungsdatensatz ist eine Änderung vorgeschlagen.

Seit dem vorigen Lauf hat die Werkbank die Speichernamen von fusion 12 übernommen (`769b867`: `circles/` heißt `work-packages/`, `planning/` heißt `plans/`), und `CLAUDE.md` ist nachgezogen (`dae2b70`). Dieser Lauf liest deshalb unter den neuen Namen; die Datensätze selbst tragen in ihrem Inhalt weiter die alten Pfade.

## Was gelesen wurde, Quelle für Quelle

| # | Quelle | Bestand | Gelesen |
|---|---|---|---|
| 1 | Arbeitseinheiten (`work-packages/`) | 27 Container | 27 nach Name und Zustand aufgenommen (24 mit Markerdatensatz, 3 mit Kopfzeile `**Status:**`, alle drei `done`). Ganz gelesen: 1, `260917-1415-kontextmenue-traegt-open-with`, der einzige nach dem Anker angelegte |
| 2 | Entscheidungsdatensätze | 225 (37 davon `_o_` oder `_a_`) | 3 mit Stempel nach dem Anker ganz gelesen (`260918-0842_*`, `260925-1901_*`, `260918-0856_*`); dazu `260818-0201_*` ganz, weil `CLAUDE.md` sich auf seine Antwort stützt |
| 3 | git | 24 Commits seit dem Anker | `git log --follow 5501401..HEAD -- CLAUDE.md` → 4 Commits (`f2245e9`, `6f82761`, `dae2b70`, `99bff53`), alle gelesen; `f2245e9` ist die Anwendung des vorigen Laufs. Dazu `769b867` (Umbenennung der Speicher) und `c0dbbed` (Lesezeichen) als Stoff für die Prüfung gegen den Baum. Für Regeldateien entfällt der Lauf |
| 4 | Prüfberichte und Analysen | 107 Prüfberichte, 12 Analysen | nach dem Anker **null** Prüfberichte und eine Analyse, die Laufdatei des vorigen Durchgangs; sie ist gelesen und ihr Bewerber C01 hier übernommen |
| 5 | `orchestrator-events.jsonl` | 2 614 Zeilen | nicht gelesen; keine Aussage dieses Laufs brauchte eine Stütze von dort |
| 6 | Archiv | 5 Durchgänge | der jüngste ist `260829-1256-safe-cleanup-tier-1`; **seit dem Anker ist nichts archiviert worden**, also null neu gelesen. Einzeln geöffnet wurde ein älterer archivierter Defekt, den L02 zitiert |
| 7 | `**Provenance:**`-Köpfe von Regeldateien | **null** | es gibt keine projekteigenen Regeldateien |

Keine Quelle war unlesbar. Leer im gebundenen Umfang sind die Prüfberichte (Quelle 4), das Archiv (Quelle 6) und überhaupt die Regeldateien (Quelle 7).

**Gegenprobe über die offenen Defekte:** 110 offene Defekte (`find fusion-workbench/shared/issues fusion-workbench/work-packages/*/issues -maxdepth 1 -name '*_o_*.md' | wc -l`). Nach dem Anker ist kein offener dazugekommen; die vier Defekte mit späterem Stempel sind alle geschlossen. Eine Suche nach `fusion 13` und `planning/` in den offenen Defekten trifft drei, die den alten Pfad als Adresse führen und nichts über das Lesen alter Speicher behaupten. **Kein offener Defekt widerspricht einem Eintrag dieser Liste.** Ein Paar aus umgesetzter Entscheidung und offenem Defekt, der die Umsetzung als fehlend beschreibt, ist im gebundenen Umfang nicht entstanden.

## Größe der Flächen

| Fläche | Vorher | Nachher, falls beide Einträge angewandt werden |
|---|---|---|
| `CLAUDE.md` | 76 722 Bytes, 213 Zeilen (`wc -c -l CLAUDE.md`) | 77 179 Bytes, 213 Zeilen |
| Entscheidungsdatensätze | 225 Dateien, 1 391 065 Bytes | unverändert |
| projekteigene Regeldateien | nicht vorhanden | nicht vorhanden |

Die zwei Einträge ersetzen zusammen 187 Bytes Bestandstext durch 644. **Entfernt oder verschoben wird nichts**, also 0 Prozent jeder Fläche; der Schwellenwert von 20 Prozent ist nicht berührt, eine gesonderte Rückfrage zum Ausmaß entfällt.

**Entfernungen einer Festlegung: null auf jeder Stufe.**

## Vergleichszählung je Flächenpaar

Keine dieser Zahlen behauptet Vollständigkeit über den Korpus. Jede nennt ihre Auswahlregel und das, was die Regel erreicht hat.

**1. Entscheidung gegen Entscheidung.** Auswahlregel: die 3 Entscheidungen mit Stempel nach dem Anker gegen die 37 lebenden (`find fusion-workbench/shared/decisions fusion-workbench/work-packages/*/decisions -name '*_[oa]_*.md' | wc -l`), ohne das Paar eines Datensatzes mit sich selbst: 110 Paare. Gelesen würden die Paare, deren Dateinamen zwei oder mehr unterscheidende Wortteile teilen; die Regel erreicht **null** Paare. Von Hand dazugenommen und gelesen: `260918-0842_*` gegen `260813-1534_*` und `260821-1115_*`, beide abgeschlossen, weil alle drei die Auslieferungskette betreffen. `260918-0842_*` nennt `260813-1534_*` selbst als nicht überholt und beantwortet eine andere Frage. **Kein Widerspruch in den Paaren, die Regel und Handauswahl erreicht haben.**

**2. Entscheidung gegen Regeldatei.** Null Paare: es gibt keine projekteigene Regeldatei.

**3. Entscheidung gegen `CLAUDE.md`.** Auswahlregel: jeder Datensatzverweis, den `CLAUDE.md` führt, sowie die 3 Entscheidungen nach dem Anker. `grep -oE '`[^`]*[0-9]{6}-[0-9]{4}[_-][^`]*`' CLAUDE.md | sort -u` liefert 78 Spannen; 47 davon enden auf `.md`, und jede ist mit `find fusion-workbench -name <basename>` aufgelöst worden: 45 lösen auf genau eine Datei auf, die zwei Pfade auf einen Rundendatensatz (`…/_*_circle.md`, `…/_b_circle.md`) auf genau die genannte, der gekürzte Verweis `260810-2132_*_…` über seinen Stempel auf genau eine. **Kein Verweis ist tot, und jeder Marker, den `CLAUDE.md` in Worten nennt, stimmt mit dem Dateinamen** (geprüft unter anderem: `260819-1440` beantwortet und nicht umgesetzt, `260810-2132` zurückgestellt, `260810-1044` zurückgestellt, `260826-1302` und beide `260907-0826` offen, `260826-1811`, `260826-0859`, `260826-0923`, `260811-2050` umgesetzt). Die drei neuen Entscheidungen widersprechen `CLAUDE.md` nicht: `260918-0842_*` ist dort seit `6f82761` wiedergegeben, `260918-0856_*` deckt sich mit „die Anwendungen, die das System für den angeklickten Eintrag nennt“, und `260925-1901_*` betrifft eine Stelle, über die `CLAUDE.md` nichts sagt.

Daneben stehen vier Verweise als nackter Stempel ohne Namen (`260810-0423`, `260810-1247`, `260815-0211`, `260826-1221`). Die ersten beiden lösen auf je einen Datensatz auf; `260815-0211` auf acht Dateien und `260826-1221` auf zwanzig, darunter der gemeinte im Archiv. Daraus kommen L02 und der Bewerber C02.

**4. Regeldatei gegen Regeldatei.** Null Paare.

**5. Regeldatei gegen `CLAUDE.md`.** Null Paare.

**6. `CLAUDE.md` gegen sich selbst und gegen den Baum.** Alle 213 Zeilen gelesen, gegen HEAD. Ausgeführt wurden die Erhebungskommandos, die die Datei selbst nennt (Rundenglobs, Kopfzeilen `**Status:**`, die zehn Zeitzusagen, Version und Tags, `EDITORGRENZE`, `#![deny(unsafe_code)]` gegen `members`, die zwei Ausnahmen, `UNLESBARE_ALLE_LISTEN`, `Kontextbefehl::ALLE`, `immer_erreichbar`, der Rumpf von `ist_eigene_textflaeche` unter `grep -A13`, die Stellen der Betriebsregel, die Stellen der C-Freiheits-Zusage, die Zahl der Leseprofile und der Symbol-PNGs, das Ziel `check:` im `Makefile`), dazu rund 90 Suchen nach benannten Funktionen, Proben, Konstanten und Dateien. **Alle stimmen mit dem Baum überein**, auch nach den zwei Code-Commits seit dem Anker (`ea9af88` „Öffnen mit“, `c0dbbed` Lesezeichen): der Absatz zum Kontextmenü trägt die zwei Selektoren `kontextbefehl:` und `oeffnenMit:` richtig, `NSPasteboard` wird weiter allein in `appkit/zwischenablage.rs` angesprochen. Die eine falsche Aussage steht in dem Satz, den `dae2b70` zur Umbenennung hinzugefügt hat: L01.

## Vorgelegter Bestandstext

An keinem Entscheidungsdatensatz ist eine Änderung vorgeschlagen, deshalb ist hier kein Datensatzinhalt festzuhalten. Der Bestandstext jedes Eingriffs an `CLAUDE.md` steht im jeweiligen Eintrag unter **Before** und ist beim Anwenden Zeichen für Zeichen gegen die Platte zu halten; beide stehen heute genau einmal in der Datei (`grep -c` je Bestandstext → 1).

## Die Änderungsliste

### L01 — Alte Speichernamen liest fusion nur, solange ihr Verzeichnis steht, und hier steht keins mehr

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Abschnitt „Worum es geht“, der Absatz vor der Rundentabelle
- **Tier:** 1
- **Citation:** Der Satz behauptet, die alten Pfade in den Datensätzen würden „bis fusion 13 beide gelesen“. Geprüft:
  ```
  $ ls -d fusion-workbench/circles fusion-workbench/shared/planning
  ls: fusion-workbench/circles: No such file or directory
  ls: fusion-workbench/shared/planning: No such file or directory
  $ "$FUSION_PLUGIN_ROOT/bin/fusion-stores"   # Kopf, Zeilen zu LEGACY_*
  #   LEGACY_CONTAINER_STORE  each one's v11 name, read beside the new name
  #   LEGACY_PLAN_STORE       while that directory exists and never written
  ```
  Der alte Name wird nur gelesen, solange sein Verzeichnis besteht; `769b867` hat beide Verzeichnisse umbenannt, also liest fusion hier keinen alten Namen mehr. Ein Leser, der einen Pfad `circles/…` aus einem Datensatz öffnet, greift ins Leere; erreichbar ist der Datensatz allein über seinen Dateinamen. Nach der Regel „ableiten statt berichtigen“ nennt der neue Text die zwei Kommandos und keinen Bestand.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> die Datensätze darin tragen ihre alten Dateinamen und in ihrem Inhalt weiter die alten Pfade, die bis fusion 13 beide gelesen werden.

**After:**
> die Datensätze darin tragen ihre alten Dateinamen und in ihrem Inhalt weiter die alten Pfade. Einen Speicher unter seinem alten Namen liest fusion bis zur Fassung 13 nur, solange dessen Verzeichnis steht, und hier steht keins mehr (`ls -d fusion-workbench/circles fusion-workbench/shared/planning` meldet beide als nicht vorhanden): ein alter Pfad aus einem Datensatz wird deshalb über seinen Dateinamen gesucht (`find fusion-workbench -name '<name>'`, den Marker ausgesternt) und nicht geöffnet.

### L02 — Der Verweis auf den Defekt zum Schwungleser trifft zwanzig Dateien, und der gemeinte liegt im Archiv

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Abschnitt „Was man nicht sieht, wenn man es nicht weiß“, Absatz „Die Prüfung dessen, was da geöffnet wurde, steht am Deskriptor und nicht am Pfad.“
- **Tier:** 1
- **Citation:** Der Satz verweist auf „den Defekt `260826-1221`“ als den, bis zu dem `Schwungleser::oeffnen` mit `File::open` an einer benannten Röhre hängen blieb. Geprüft:
  ```
  $ find fusion-workbench -name '260826-1221*' | wc -l
  20
  $ find fusion-workbench -name '260826-1221*schwungleser*'
  fusion-workbench/archive/260827-1534-safe-cleanup-tier-1/shared/issues/260826-1221_c_der-schwungleser-oeffnet-mit-file-open-und-haengt-an-einer-benannten-roehre-fuer-immer.md
  ```
  Der Stempel benennt 18 Defekte, einen Entscheid und zwei Prüfberichte; der gemeinte Defekt ist am 260827 archiviert worden. Welcher gemeint ist, ist nicht erschlossen: sein Titel sagt wörtlich, was der Satz über ihn sagt, und der Fix `9c02863` („der Schwungleser oeffnet ueber die Huelle und haengt an keiner benannten Roehre mehr“) ist der Commit, der den Stempel in `CLAUDE.md` gebracht hat (`git log -S'Defekt `260826-1221`' -- CLAUDE.md`). Die neue Form ist die speicherlose mit ausgesterntem Marker, dieselbe, die `CLAUDE.md` im Abschnitt zur Auslieferung schon führt.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> der bis zum Defekt `260826-1221` als einziger Öffner

**After:**
> der bis zum Defekt `260826-1221_*_der-schwungleser-oeffnet-mit-file-open-und-haengt-an-einer-benannten-roehre-fuer-immer.md` als einziger Öffner

## Bewerber — nicht zur Freigabe vorgelegt und nicht anzuwenden

### C01 — Das Rundenregister endet bei der Runde 23, der Baum trägt drei weitere gefahrene Runden

- **Surface:** CLAUDE.md, die Rundentabelle im Abschnitt „Worum es geht“
- **Consequence group:** candidate
- **Warum Bewerber:** übernommen aus `260917-1450-curator-run.md`, C01, und seither um eine Runde gewachsen. `260910-0707-krk-meldet-neuerungen-in-readers-settings-keymap`, `260911-1825-fusion-leseprofile-auf-fusion-11-nachziehen` und `260917-1415-kontextmenue-traegt-open-with` stehen auf `**Status:** done` und fehlen in der Tabelle. Die Tabelle sagt das selbst („endet bei der Runde 23“), also ist keine Aussage falsch. Eine Zeile zu ergänzen hieße, Rundennummern zu vergeben, die nur für die 24 in `CLAUDE.md` steht, und einen Gegenstandstext neu zu formulieren: neuer Inhalt und keine Berichtigung. Wer die Zeilen will, vergibt die Nummern und beauftragt sie gesondert.

### C02 — Der nackte Stempel `260815-0211` trifft acht Dateien

- **Surface:** CLAUDE.md, Absatz „Der Durchlauf über den Unterbaum hält genau einen Verzeichnisdeskriptor“
- **Consequence group:** candidate
- **Warum Bewerber:** `find fusion-workbench -name '260815-0211*' | wc -l` → 8 (sieben Defekte der Runde 10 und ein Prüfbericht). Gemeint ist `inference:` `260815-0211_*_ein-deskriptormangel-des-prozesses-wird-zu-einem-dauerhaften-kein-treffer-darunter.md`, weil der Satz danach genau diesen Mangel beschreibt. Anders als bei L02 spricht der Satz aber von einer „zweiten Gestalt“, nicht vom Titel selbst, und der Datensatz liegt weiter im Speicher seiner Runde. Die Zuordnung ist erschlossen und nicht geprüft; der Bewerber wird deshalb nicht vorgelegt.

### C03 — 48 Verweise in `CLAUDE.md` tragen einen Speicherabschnitt, den die Zitierregel von fusion als Verstoß meldet

- **Surface:** CLAUDE.md, der ganze Text, und die Pfadregel nach der Rundentabelle
- **Consequence group:** candidate
- **Warum Bewerber:** `"$FUSION_PLUGIN_ROOT/bin/fusion-citation-check" | grep -c 'CLAUDE.md:'` → 48 Zeilen, jede `store-prefixed`. Die Pfadregel in `CLAUDE.md` („Pfade mit dem Vorsatz `shared/` oder `work-packages/` sind relativ zu `fusion-workbench/` zu lesen“, und die Runde 18 „immer in der langen Form mit `shared/`“) schreibt diese Form ausdrücklich vor; `$FUSION_PLUGIN_ROOT/rules/fusion-workbench-conventions.md` `## Filename Patterns` nennt jeden Speicherabschnitt im Zitat einen Verstoß. Ein Agent, der einen Datensatz der Runde 18 in `CLAUDE.md` zitiert, kann nicht beiden folgen. **Die zweite Seite ist keine der drei Flächen dieses Laufs**, sondern eine Regel des Plugins, und der Entscheid `260818-0201_*`, auf den sich `CLAUDE.md` stützt, hat nur den Platzhalter für den Marker beschlossen und den Speicherabschnitt ausdrücklich nicht gefragt („was diese Frage gar nicht gestellt hatte“). Alle 48 lösen heute auf. Welche der zwei Formen hier gilt, ist eine Nutzerfrage; eine Änderung ohne sie hieße, die Pfadregel über eine Lesart hinweg umzuschreiben.

## Was außerhalb dieses Auftrags liegt und wem es gehört

**Drei offene Defekte an `CLAUDE.md` sind seit dem vorigen Lauf erfüllt und stehen weiter offen**: `260908-0838_*_claude-md-nennt-die-freigabe-des-rueckgaengig-blocks-ungemessen-seit-heute-haelt-sie-eine-probe.md`, `260910-0739_*_claude-md-sagt-es-gebe-keinen-vorgesehenen-circle-seit-heute-gibt-es-einen.md` und die `CLAUDE.md`-Hälfte von `260905-2254_*_das-zaehlkommando-fuer-ohne-warten-oeffnen-gibt-elf-zeilen-fuer-sechs-rufer-aus.md`. Den Marker umzulegen gehört dem Abgleich (`/fusion:reconcile`), nicht diesem Lauf. Ebenso trägt `260918-0842_*` den Marker „beantwortet“, obwohl `6f82761` die Antwort in `CLAUDE.md` und `README.md` niedergelegt hat; ob das als Umsetzung zählt, entscheidet derselbe Abgleich.

**Neun lebende Entscheidungen im gemeinsamen Speicher führen in ihrem Text Pfade unter `circles/`** (`grep -rl 'circles/' fusion-workbench/shared/decisions/*_[oa]_*.md | wc -l` → 9). Das ist der Stand, den `/fusion:migrate` bewusst stehen lässt, und kein Widerspruch zwischen zwei Flächen; L01 sagt dem Leser, wie er einen solchen Pfad auflöst.

## Ergebnisse je Eintrag

Angewandt am 260925 im Modus `apply`, Freigabe `all`, gegen HEAD `0d4485ebfa8c5cecfc313d831c9d096eb7d29e76`. Vor jedem Eingriff ist der Bestandstext von der Platte gelesen worden (je genau ein Treffer in `CLAUDE.md`), nach jedem der geschriebene Bereich gegen den Text unter **After** gehalten, Byte für Byte (je genau ein Treffer, 499 und 145 Bytes).

- L01 — applied
- L02 — applied
- C01 — not-offered
- C02 — not-offered
- C03 — not-offered

`CLAUDE.md` danach: 77 179 Bytes, 213 Zeilen (`wc -c -l CLAUDE.md`), wie in „Größe der Flächen“ vorausgesagt. Rücknahme: `git checkout -- CLAUDE.md`. Committet ist nichts.
