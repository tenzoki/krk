# Kuratorenlauf — 260826-1637

**Status:** Complete — Erhebung am 260826-1637 gegen `caa3f23`; Anwendung am 260826-1650 gegen denselben Baumstand, alle fünf Einträge angewandt, `CLAUDE.md` liegt geändert im Arbeitsbaum.
**Datum:** 260826-1637 (Erhebung)
**Git HEAD beim Lesen:** `caa3f23` (`docs(workbench): der Sitzungsbericht der Vollbaum-Durchsicht ist vollstaendig`, 2026-08-26 16:32:57 +0200). `CLAUDE.md` ist im Arbeitsbaum unverändert gegenüber HEAD (`git status --porcelain -- CLAUDE.md` ist leer).
**Modus:** survey, danach apply. Der erste Durchgang hat an keine Fläche geschrieben; der zweite (`**Mode:** apply`, Freigabe `all`) hat die fünf Einträge auf `CLAUDE.md` angewandt.
**Vorheriger Kuratorenlauf:** `shared/history/260826-1100-curator-run.md`, Baumstand `7af4c36`, ein Eintrag, angewandt in `88f18ed`. Davor `260826-1009`, `260826-0818`, `260821-2231`, `260820-2127`, `260820-1119`, `260819-1500` und `260814-1405`.

**Was seit dem vorigen Lauf dazugekommen ist.** Zwischen `7af4c36` und `caa3f23` liegen elf Commits. `88f18ed` hat den Eintrag des vorigen Laufs angewandt, `004ff72` den Sitzungsbericht dazu abgelegt; die neun Commits von `f22a036` bis `caa3f23` tragen die Vollbaum-Durchsicht der Sitzung 260826-1114: fünfzehn Durchsichtsberichte, 122 Defektdatensätze und vier Entscheidungsdatensätze, dazu die zwei Schlussabgleiche `260826-1452-reconciliation.md` und `260826-1633-reconciliation.md`. Daneben hat der Archivlauf `archive/260826-1637-safe-cleanup-tier-1` unmittelbar vor diesem Lauf 63 Dateien aus `shared/` verschoben; keine davon wird in `CLAUDE.md` zitiert (Abschnitt 5).

**Gegenstand dieses Laufs.** Der ganze Bestand der drei Flächen, mit Schwerpunkt auf dem, was die Vollbaum-Durchsicht über `CLAUDE.md` gesagt hat: der Abgleich `260826-1452` hat einen Datensatz ausdrücklich an den Curator überwiesen, und die Durchsicht hat weitere Datensätze mit `**Betroffen:** CLAUDE.md` abgelegt. Jede Aussage daraus ist hier am Baum nachgeprüft und nicht geglaubt.

**Sprache dieser Datei.** Deutsch. `CLAUDE.md:3` deklariert `**Language:** de` als einzige Sprachzeile, und nach `## Project language` der Konventionen steuert sie beide Flächen. Gelesen sind `stilwerk/chat-voice-de.yaml` und `stilwerk/default-voice-de.yaml`.

---

## 1. Die acht Belegquellen, und was in jeder gelesen wurde

| # | Quelle | Gelesen |
|---|---|---|
| 1 | Circle-Datensätze (`circles/`, 19 Verzeichnisse, 19 Datensätze) | **19 durchsucht, 0 vollständig gelesen.** `grep -c 'CLAUDE.md'` trifft elf Datensätze; keiner trägt eine Aussage über eine der fünf hier vorgeschlagenen Stellen. Die Marker aller 19 sind gegen die Rundentabelle von `CLAUDE.md` gehalten (Abschnitt 4). |
| 2 | Entscheidungsdatensätze (`shared/decisions/` 31, `circles/*/decisions/` 145, zusammen 176) | **31 Köpfe gelesen** (Titel und `Answered:`/`Superseded by:`-Zeile jedes gemeinsamen Datensatzes), dazu vollständig `260819-2216_i_wird-die-vorschauflaeche-…` und die Köpfe der vier heute gefilterten (`260826-1221`, `-1223`, `-1225`, `-1302`). Von den Circle-Datensätzen sind die neun gelesen, die `CLAUDE.md` zitiert, auf Existenz und Marker. |
| 3 | Sitzungsberichte (`shared/history/` 146, `circles/*/history/` 415, zusammen 561) | **3 gelesen**: der vorige Kuratorenlauf `260826-1100-curator-run.md` (Kopf und Quellentabelle), der Abgleich `260826-1452-reconciliation.md` (die Zeile, die den Datensatz `260826-1306` an den Curator überweist) und der Kopf von `260826-1633-reconciliation.md`. |
| 4 | Git-Historie | `git log --follow -- CLAUDE.md` vollständig: 32 Commits von `c0682ff` (2026-08-02) bis `88f18ed`. `git blame` auf die fünf betroffenen Zeilen: Zeilen 34, 127 und 129 stammen aus `a355347` (260814-1939), Zeile 115 aus `90f8ac1` (260820-2147), Zeile 123 aus `fb50fcd` (260826). `git log -S'struct Wegwerfwurzel'` auf `xtask/src/release.rs` (erster Treffer `ac95acf`, 260807). `git show -s` auf `ffb702c` (260811, die Schließung von `260810-1925`). |
| 5 | Durchsichten und Untersuchungen (`shared/reviews/` 36, `shared/analyses/` 4) | **0 vollständig gelesen**, 19 Berichte vom 260826 durchsucht; fünfzehn nennen `CLAUDE.md`. Ihre Befunde sind über die Defektdatensätze erreicht, die sie abgelegt haben, und dort am Baum nachgeprüft. |
| 6 | `orchestrator-events.jsonl` (1689 Zeilen) | **Durchsucht, 9 Treffer auf `curator`.** Die letzten drei Zeilen (Gate der Sitzung 260826-1114, die Übergabe an `/fusion:cleanup`) sind gelesen. Bestätigend; kein Eintrag stützt sich darauf. |
| 7 | Archiv (`archive/`, drei Läufe, 139 `.md`-Dateien) | **Durchsucht, 0 gelesen.** 43 Dateien nennen `CLAUDE.md`. Der Lauf `260826-1637` hat unmittelbar vor diesem Lauf 63 Dateien verschoben; die Zitatprüfung in Abschnitt 5 hat jede in `CLAUDE.md` genannte Datei auch gegen das Archiv gehalten, und keine ist dorthin gewandert. |
| 8 | `**Provenance:**`-Köpfe projekteigener Regeldateien | **0 — die Fläche fehlt.** `ls rules .claude/rules` meldet beide als nicht vorhanden. |

Der Defektbestand als Gegenprobe: `shared/issues/` trägt 199 offene Datensätze, `circles/*/issues/` 116. Alle 65 offenen, die `CLAUDE.md` nennen (`grep -li 'CLAUDE.md'` über beide Speicher), sind auf ihren Bezug zu den fünf Einträgen durchgesehen; die zehn mit `**Betroffen:** CLAUDE.md` oder einer wörtlichen Zeilenangabe sind vollständig gelesen. Kein offener Defekt behauptet das Gegenteil eines der fünf Einträge; drei stützen je einen (Abschnitt 6), und drei sind für sich gegenstandslos geworden (Abschnitt 8).

## 2. Flächengrößen

| Fläche | Vorher | Nachher (bei voller Freigabe) |
|---|---|---|
| `CLAUDE.md` | 50.484 Bytes, 195 Zeilen | 52.095 Bytes, 195 Zeilen |
| Entscheidungsdatensätze (`shared/decisions/`) | 31 Dateien, 185.717 Bytes, 2.456 Zeilen | unverändert |
| Projekteigene Regeldateien | Fläche fehlt | Fläche fehlt |

**Sperrschwelle:** kein Eintrag löscht. Vier der fünf Einträge verlängern ihre Zeile, einer (L04) nimmt 26 Zeichen einer Aufzählung heraus und fügt einen Verweis hinzu. Das größte Vorher-Stück, das ein Eintrag ersetzt, sind 1.467 Bytes von 50.484, also 2,9 Prozent; die Schwelle von 20 Prozent ist nicht berührt.

## 3. Die sechs Flächenpaare

| Paar | Auswahlregel | Paare erzeugt | Gelesen | Befund |
|---|---|---|---|---|
| Entscheidung gegen Entscheidung | jeder gemeinsame Datensatz mit `Answered:`-Zeile oder `Superseded by:`-Zeile gegen jeden offenen (`_o_`) gemeinsamen Datensatz gleichen Gegenstands; Gegenstand nach Titelwort (`ls shared/decisions`, 31 Dateien) | 3 (`260826-0859_o` gegen `260826-0923_o`; `260819-2216_i_wird-die-vorschauflaeche` gegen `circles/260812-1000-…/decisions/260812-1000_s_was-tut-ein-link-…`; `260821-2202_a` gegen `circles/260804-0933-…/_d_circle.md`) | 3 | keiner überholt einen anderen: die zwei offenen Fragen zur Schwelle sind beide offen, die Überholung des Link-Datensatzes ist mit `_s_` schon vollzogen, die Absage des Web-Betrachters ist als `_d_` abgelegt und `CLAUDE.md` schreibt genau das aus |
| Entscheidung gegen Regeldatei | — | 0 | 0 | Fläche fehlt |
| Entscheidung gegen `CLAUDE.md` | jeder in `CLAUDE.md` zitierte Datensatz (`grep -oE` über Rückstrich-Pfade, 14 Entscheidungsdatensätze) auf Existenz, Marker und die Aussage, die `CLAUDE.md` über ihn macht | 14 | 14 | alle vorhanden, jeder Marker passt zur Aussage (`_d_` bei „zurückgestellt“, `_a_` bei „der Nutzer hat gewählt“, `_o_` bei „ist offen“, `_i_` bei „gewählt und gebaut“) |
| Regeldatei gegen Regeldatei | — | 0 | 0 | Fläche fehlt |
| Regeldatei gegen `CLAUDE.md` | — | 0 | 0 | Fläche fehlt |
| `CLAUDE.md` gegen sich selbst | die Rundentabelle gegen den Absatz darunter; jede Zahl, jeder Name und jeder Pfad gegen den Baum (Abschnitt 5) | 1 Aussagenpaar, 57 Pfade, 41 Bezeichner | alle | ein Befund: die Pfadregel unter der Tabelle ist für die Zeile 18 nicht mehr total (L05) |

Der Befund „kein lebender Datensatz überholt einen anderen“ gilt für die Paare, die die Auswahlregel erreicht hat, und nicht für den Bestand von 176 Datensätzen.

## 4. Was am Baum nachgeprüft ist und hält

Jeder Pfad, den `CLAUDE.md` in Rückstrichen nennt, ist mit einem Glob-Skript gegen den Baum und gegen das Archiv gehalten (`grep -oE '`[^`]*(circles|shared|messungen|crates|resources|xtask|spikes|iconset)/[^`]*`' CLAUDE.md`, 57 Pfade, dazu die sechs Kurzform-Zitate der Runden 1 und 2): **alle 63 vorhanden, keiner archiviert.** 41 Bezeichner (Funktionen, Proben, Konstanten, Aufzählungen) sind mit `grep -rl --include='*.rs'` gesucht: **40 vorhanden**, und der eine ohne Treffer, `ist_editorflaeche`, wird in `CLAUDE.md` ausdrücklich als der alte Name genannt. Die Zählungen halten: `Wirkungsbereich` 7, `Bereich` 5, `Fokus` 5, `Art` 6 Werte mit `Zippen` und `Entpacken`, `Kommando` ohne Git-Variante, zehn `"L<n>"` in `messen.rs`, `Cargo.lock` ohne `cc` und ohne anderes `-sys` als `windows-sys`, `zip` mit `deflate-flate2` und `unreserved` und `flate2 = "1"` daneben, Untergrenzen-Abschnitt in 39 von 41 Dateien unter `appkit/` mit genau `koordinaten.rs` und `mod.rs` ohne, `ohne_warten_oeffnen` mit fünf Rufern in `text/datei.rs` und `operation/{zippen,entpacken}.rs`, `#![deny(unsafe_code)]` an den drei Kistenwurzeln und `#![allow(unsafe_code)]` allein in `verzeichnis/sys.rs` und `appkit/mod.rs`, `krk-ui` ohne `src/lib.rs`, `rust-toolchain.toml` auf 1.97.1, `MACOSX_DEPLOYMENT_TARGET = "15.0"`, `xtask` mit `bundle`, `version`, `release`, `beglaubigen` und `veroeffentlichen`, die `README.md` mit `### Versionsstufen`, `### Nur beglaubigen` und `### Die acht Stationen`, sieben PNGs unter `iconset/`, `Ordnermodell::neu` mit `tief: true`. Die Circle-Marker: 13 `_b_`, 5 `_c_`, 2 `_d_`, 0 `_a_`, 0 `_t_` — wie die Prosa sagt.

## 5. Pre-edit content

Dieser Lauf schlägt keine Änderung an einem Entscheidungsdatensatz vor. Der Abschnitt ist deshalb leer, und das ist eine Aussage und keine Auslassung.

## 6. Das Ledger

Fünf Einträge zur Freigabe, drei Tier 1, zwei Konsolidierungen; keine Einschränkung wird entfernt. Dazu drei Kandidaten, die nicht zur Freigabe stehen.

### L01 — `CLAUDE.md` nennt `Messplanwaechter::neu`, das es nicht gibt, und `cargo test` als zweiten Greifer auf den Messplan, der es seit dem 260811 nicht mehr ist

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Zeile 129 (Abschnitt „Was man nicht sieht, wenn man es nicht weiß“)
- **Tier:** 1
- **Citation:** `grep -n 'fn neu\|impl Messplanwaechter' crates/krk-bench/src/messen.rs` → allein `1573:impl Messplanwaechter {`; es gibt kein `neu`, der Bauer heißt `in_verzeichnis` (`messen.rs:1620`). `grep -n 'plan_schreiben\|plan_in_verzeichnis_schreiben' crates/krk-bench/src/messen.rs` → `plan_schreiben` hat außerhalb von Doc-Kommentaren genau einen Rufer, `Gesamtlauf::fahren` (`:1029`); die Probe `der_messplan_traegt_die_pruefsitzung_in_der_serialisierung_der_sitzung` (`:2705`) ruft `plan_in_verzeichnis_schreiben` mit einem `Wegwerfordner` (`:2720`), die zweite Messplan-Probe `Messplanwaechter::in_verzeichnis` (`:2769`). Bestätigend: der Doc-Kommentar `messen.rs:1603-1619` („Der Testlauf war bis zum 260811 der zweite Beteiligte und ist es nicht mehr“), der `Resolved:`-Vermerk des geschlossenen Datensatzes `shared/issues/260810-1925_c_…` („eine fremde `krk-messplan-999999.toml` im echten Temporärverzeichnis überlebt ein volles `make check`“, geschlossen in `ffb702c` am 260811), und der offene Datensatz `shared/issues/260826-1306_o_claude-md-nennt-cargo-test-als-zweiten-greifer-…`, den der Abgleich `260826-1452` an den Curator überwiesen hat. Die Zeile stammt aus `a355347` (260814), also aus der Zeit nach der Schließung: sie war beim Schreiben schon falsch.
- **Consequence group:** tier-1
- **Constraint removed:** none — die Voraussetzung „nie zwei Läufe zugleich“ bleibt stehen, präzisiert auf Messläufe, so wie der Doc-Kommentar im Code sie führt; der Leitsatz wird vom falschen Greifer (`make check`) auf den wirklichen (der nächste Messlauf) gestellt
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> **Ein `make check` löscht den Messplan eines gleichzeitig laufenden Messlaufs.** `Messplanwaechter::neu` (`krk-bench/src/messen.rs`) räumt beim Anlegen jede fremde `krk-messplan-*.toml` im Temporärverzeichnis ab. So kommt der Plan auch nach einem Strg+C weg, an das kein `Drop` heranreicht; gewählt hat das der Nutzer als Option 4 von `shared/decisions/260810-1850_*_wie-kommt-der-messplan-bei-strg-c-weg-ohne-die-zusage-der-sitzungssicherung-zu-brechen.md`. **Vorausgesetzt ist dabei, dass nie zwei Läufe zugleich auf dieses Verzeichnis greifen**, und der zweite Greifer ist nicht nur ein zweiter Messlauf: die Probe `der_messplan_traegt_die_pruefsitzung_…` ruft `plan_schreiben`, also räumt auch `cargo test` ab (`shared/issues/260810-1925_*_eine-probe-schreibt-ins-echte-temporaerverzeichnis-und-raeumt-dort-jetzt-fremde-messplaene-ab.md`).

**After:**
> **Der nächste Messlauf löscht den Messplan eines gleichzeitig laufenden Messlaufs.** `Messplanwaechter::in_verzeichnis` (`krk-bench/src/messen.rs`) räumt beim Anlegen jede fremde `krk-messplan-*.toml` im Temporärverzeichnis ab. So kommt der Plan auch nach einem Strg+C weg, an das kein `Drop` heranreicht; gewählt hat das der Nutzer als Option 4 von `shared/decisions/260810-1850_*_wie-kommt-der-messplan-bei-strg-c-weg-ohne-die-zusage-der-sitzungssicherung-zu-brechen.md`. **Vorausgesetzt ist dabei, dass nie zwei Messläufe zugleich auf dieses Verzeichnis greifen.** `cargo test` und damit `make check` sind seit dem 260811 kein zweiter Greifer mehr: die Probe `der_messplan_traegt_die_pruefsitzung_…` rief bis dahin `plan_schreiben` und traf das echte Temporärverzeichnis; seither geht jede Probe über `plan_in_verzeichnis_schreiben` und einen `Wegwerfordner`, und `std::env::temp_dir()` steht für den Messplan an genau einer Stelle, in `plan_schreiben` (`shared/issues/260810-1925_*_eine-probe-schreibt-ins-echte-temporaerverzeichnis-und-raeumt-dort-jetzt-fremde-messplaene-ab.md`, geschlossen).

### L02 — Die Begründung für `certify-only.sh` behauptet einen Abbruch an Station 1, den `stand_pruefen` in dieser Lage nicht auslöst

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Zeile 115 (Abschnitt „Bauen und prüfen“)
- **Tier:** 1
- **Citation:** `sed -n 290,320p xtask/src/release.rs` → `stand_pruefen` fragt zweierlei, einen passenden Tag auf HEAD (`git tag --points-at HEAD`, „einer davon muss passen, und mehrere stören nicht“) und einen sauberen Arbeitsbaum (`git status --porcelain --untracked-files=no`), und ist grün, wenn beides gilt. Ein an der Beglaubigung gescheiterter Lauf bewegt HEAD nicht und ändert keine verfolgte Datei; `xtask/src/version.rs:55-63` schreibt es selbst aus („ein zweiter Lauf desselben `./release.sh 0.2.0` faellt hier durch, ohne etwas zu tun, und faehrt gleich weiter zu `release`“), und `Vorhaben::NichtsZuTun` (`version.rs:148-154`) ist der Zweig dafür. `xtask/src/beglaubigung.rs:16-17` nennt als einzige Stelle die vollständige Bedingung: „und der Arbeitsbaum ist inzwischen ein anderer“. Der offene Datensatz `shared/issues/260826-1441_o_zwei-dokumentationsstaende-zum-zweiten-release-sh-…` führt die fünf Stellen mit derselben Begründung; die vier außerhalb von `CLAUDE.md` (`certify-only.sh:22-24`, `Makefile:157-160`, `xtask/src/main.rs:130-132`, `README.md:304-310`) liegen außerhalb dieses Remits und sind über jenen Datensatz Arbeit des `coder`.
- **Consequence group:** tier-1
- **Constraint removed:** none — der Weg, sein Zweck und seine Grenze bleiben; ersetzt wird allein die Begründung, warum ein zweites `./release.sh` in dieser Lage der falsche Handgriff ist: nicht ein Abbruch, sondern ein zweiter Bau und eine zweite Einreichung
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> **Seit dem 260820 steht daneben ein zweiter Weg mit denselben vier Schichten, der nur beglaubigt:** `./certify-only.sh <version>` → `make beglaubigen VERSION=…` → `cargo xtask beglaubigen <version>`. Er ist für den Lauf gedacht, der erst an der Beglaubigung gescheitert ist — etwa am Zeitüberlauf des Uploads zu Apple —, während das fertige, signierte Bündel unter `target/KRK.app` liegt und allein das Ticket fehlt; ein zweites `./release.sh <zahl>` bräche in dieser Lage an Station 1 ab, weil der Tag nach dem ersten Lauf nicht mehr allein auf HEAD steht. **Er prüft weder Tag noch Arbeitsbaum, und das ist der Zweck und keine Nachlässigkeit:** ein so beglaubigtes Bündel ist nicht durch die Vorprüfungen der Auslieferungskette gegangen. Gebaut wird nichts; die Zahl im Argument hält `xtask` gegen `CFBundleShortVersionString` der `Info.plist` **im gebauten Bündel** und nicht gegen die `Cargo.toml`, damit kein Bündel von vorgestern bei Apple landet. Im Einzelnen steht der Weg in `README.md` unter `### Nur beglaubigen`.

**After:**
> **Seit dem 260820 steht daneben ein zweiter Weg mit denselben vier Schichten, der nur beglaubigt:** `./certify-only.sh <version>` → `make beglaubigen VERSION=…` → `cargo xtask beglaubigen <version>`. Er ist für den Lauf gedacht, der erst an der Beglaubigung gescheitert ist — etwa am Zeitüberlauf des Uploads zu Apple —, während das fertige, signierte Bündel unter `target/KRK.app` liegt und allein das Ticket fehlt; ein zweites `./release.sh <zahl>` übersetzte in dieser Lage beide Ziele neu und reichte dasselbe Bündel ein zweites Mal ein, und an Station 1 bräche es nur ab, wenn seit dem ersten Lauf etwas eingetragen oder geändert ist: `stand_pruefen` (`xtask/src/release.rs`) fragt allein nach einem passenden Tag auf HEAD und einem sauberen Arbeitsbaum, und mehrere Tags auf HEAD stören nicht (`shared/issues/260826-1441_*_zwei-dokumentationsstaende-zum-zweiten-release-sh-nach-einem-scheitern-widersprechen-sich-und-der-code-gibt-dem-selteneren-recht.md`). **Er prüft weder Tag noch Arbeitsbaum, und das ist der Zweck und keine Nachlässigkeit:** ein so beglaubigtes Bündel ist nicht durch die Vorprüfungen der Auslieferungskette gegangen. Gebaut wird nichts; die Zahl im Argument hält `xtask` gegen `CFBundleShortVersionString` der `Info.plist` **im gebauten Bündel** und nicht gegen die `Cargo.toml`, damit kein Bündel von vorgestern bei Apple landet. Im Einzelnen steht der Weg in `README.md` unter `### Nur beglaubigen`.

### L03 — „genau drei Fassungen“ des Prüfordners: `xtask` trägt seit `ac95acf` eine vierte, die die Zählprobe nicht sieht

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Zeile 127 (Abschnitt „Was man nicht sieht, wenn man es nicht weiß“)
- **Tier:** 1
- **Citation:** `sed -n 903,932p xtask/src/release.rs` → `struct Wegwerfwurzel` mit `impl Drop` (`remove_dir_all`) und `fn wegwerfwurzel`, das `std::env::temp_dir().join(format!("krk-xtask-test-{zweck}-{}-{laufnummer}", std::process::id()))` bildet: Prozesskennung, Laufnummer, Abräumen in `Drop`, also alle drei Merkmale, die der Satz in `CLAUDE.md` als die Fassung beschreibt. `git log -S'struct Wegwerfwurzel' -- xtask/src/release.rs` → erster Treffer `ac95acf` (260807); der Satz „genau drei“ stammt aus `a355347` (260814) und war beim Schreiben schon falsch. `grep -n 'genau_drei_pruefordner_fassungen\|join("crates")' crates/krk-core/tests/baum.rs crates/krk-core/tests/gemeinsam/mod.rs` → die Probe steht in `baum.rs:114`, und `quelldateien()` hängt in `gemeinsam/mod.rs:277` `crates` an die Wurzel, liest `xtask/` also nie. Der offene Datensatz `shared/issues/260826-1302_o_eine-vierte-pruefordner-fassung-steht-in-xtask-und-die-zaehlprobe-c4-6-kann-sie-nicht-sehen.md` stellt die Frage, ob die vierte anerkannt wird oder fällt, als Nutzerfrage; dieser Eintrag entscheidet sie nicht, sondern schreibt den Stand.
- **Consequence group:** tier-1
- **Constraint removed:** none — „eine je Kiste, und das soll so bleiben“ und „statt eine vierte zu schreiben“ bleiben wörtlich; hinzu kommt der Umstand, dass der Baum die Regel an einer Stelle bricht und die Probe es nicht melden kann
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> **Der Messplatz liegt unter `~/Library/Caches/krk-messplatz`**, nicht unter `/tmp`. Prüfordner einzelner Testläufe gehören dagegen nicht dorthin: sie tragen Prozesskennung und Laufnummer und räumen sich in `Drop` selbst auf. **Es gibt genau drei Fassungen, eine je Kiste, und das soll so bleiben:** `crates/krk-core/tests/gemeinsam/mod.rs` für alle Abnahmeproben des Kerns, `crates/krk-ui/src/pruefordner.rs` für die Proben jener Kiste, `crates/krk-bench/src/wegwerfordner.rs` unter dem Namen `Wegwerfordner`. Wer einen Prüfordner braucht, nimmt die Fassung seiner Kiste, statt eine vierte zu schreiben.

**After:**
> **Der Messplatz liegt unter `~/Library/Caches/krk-messplatz`**, nicht unter `/tmp`. Prüfordner einzelner Testläufe gehören dagegen nicht dorthin: sie tragen Prozesskennung und Laufnummer und räumen sich in `Drop` selbst auf. **Es gibt drei anerkannte Fassungen, eine je Kiste unter `crates/`, und das soll so bleiben:** `crates/krk-core/tests/gemeinsam/mod.rs` für alle Abnahmeproben des Kerns, `crates/krk-ui/src/pruefordner.rs` für die Proben jener Kiste, `crates/krk-bench/src/wegwerfordner.rs` unter dem Namen `Wegwerfordner`. Wer einen Prüfordner braucht, nimmt die Fassung seiner Kiste, statt eine vierte zu schreiben. **Eine vierte steht trotzdem seit `ac95acf` im Prüfmodul von `xtask/src/release.rs` (`Wegwerfwurzel`)**, und die Zählprobe `genau_drei_pruefordner_fassungen_stehen_im_baum` (`crates/krk-core/tests/baum.rs`) sieht sie nicht, weil `gemeinsam::quelldateien()` allein `crates/` liest; ob sie als vierte anerkannt wird oder fällt, ist eine offene Nutzerfrage (`shared/issues/260826-1302_*_eine-vierte-pruefordner-fassung-steht-in-xtask-und-die-zaehlprobe-c4-6-kann-sie-nicht-sehen.md`).

### L04 — Die Ablage-Aufzählung neben dem Zeiger, der sie ersetzen soll, fällt; der Zeiger bleibt

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Zeile 123 (Abschnitt „Was man nicht sieht, wenn man es nicht weiß“)
- **Tier:** consolidation
- **Citation:** `awk '/pub const ALLE/,/\];/' crates/krk-core/src/ablage/pfade.rs` → sieben Werte; die Prosa nennt sechs Glieder (die zwei Zettel zusammengefasst) und stimmt heute. Der offene Datensatz `shared/issues/260826-0923_o_die-ablage-aufzaehlung-steht-neben-dem-zeiger-der-sie-ersetzen-sollte-und-veraltet-mit-der-achten-datei.md` benennt die Lage: die Stelle trägt Zeiger und Kopie zugleich, und dieselbe Datei behandelt dieselbe Fehlerklasse bei `Kommando`, `ohne_warten_oeffnen` und den drei gezählten Aufzählungen anders. Der Eintrag nimmt die billigste der drei dort genannten Formen: die Kopie fällt, der Zeiger und der Grund bleiben. **Keine Einschränkung wird entfernt**: die Betriebsregel, der Ort, die Untersuchung und die zwei Stellen der Regel stehen unverändert.
- **Consequence group:** consolidation
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> **KRKs Bestand liegt außerhalb des Bündels, und ein Löschwerkzeug nimmt ihn mit.** Alles, was sich die Anwendung merkt, hält sie unter `~/Library/Application Support/KRK/`: die Lesezeichen, die gesicherte Sitzung, die abweichende Tastenbelegung, die Einstellungen, die von Hand gepflegten Leseprofile und die zwei Notizzettel. **Welche Dateien das sind, sagt `Datei::ALLE` (`crates/krk-core/src/ablage/pfade.rs`) und nicht diese Zeile**; die Aufzählung hier hat die Einstellungen und die Leseprofile übergangen, seit es sie gibt. Werkzeuge, die eine App samt ihrer Stützdateien entfernen, löschen diesen Ordner mit; am 17.08. ist genau das geschehen, drei Minuten und 46 Sekunden nach einem Auslieferungslauf (`shared/analyses/260820-2242-lesezeichenverlust-nach-installation.md`, dreizehn Beweisstücke, kein Schreiber im Baum fasst die Datei an). **Die Betriebsregel daraus lautet: die neue Fassung über die alte kopieren und die alte nicht vorher löschen.** Sie steht seit der Runde 15 an zwei Stellen, an denen der Nutzer sie beim Installieren liest, und beide sind für ihn und nicht für einen Agenten geschrieben: in den ersten dreißig Zeilen der `README.md` und im festen Text jeder Releaseseite, der als Konstante `RELEASETEXT` in `xtask/src/veroeffentlichung.rs` an genau einer Stelle steht und dessen Aussagen die Probe `der_releasetext_traegt_jede_seiner_aussagen` einzeln hält. Wer den Text ändert, ändert ihn dort und nicht in einer Kopie.

**After:**
> **KRKs Bestand liegt außerhalb des Bündels, und ein Löschwerkzeug nimmt ihn mit.** Alles, was sich die Anwendung merkt, hält sie unter `~/Library/Application Support/KRK/`. **Welche Dateien das sind, sagt `Datei::ALLE` (`crates/krk-core/src/ablage/pfade.rs`) und nicht diese Zeile**: eine Aufzählung an dieser Stelle hat die Einstellungen und die Leseprofile übergangen, seit es sie gibt, und veraltet mit der nächsten Ablagedatei (`shared/issues/260826-0923_*_die-ablage-aufzaehlung-steht-neben-dem-zeiger-der-sie-ersetzen-sollte-und-veraltet-mit-der-achten-datei.md`). Werkzeuge, die eine App samt ihrer Stützdateien entfernen, löschen diesen Ordner mit; am 17.08. ist genau das geschehen, drei Minuten und 46 Sekunden nach einem Auslieferungslauf (`shared/analyses/260820-2242-lesezeichenverlust-nach-installation.md`, dreizehn Beweisstücke, kein Schreiber im Baum fasst die Datei an). **Die Betriebsregel daraus lautet: die neue Fassung über die alte kopieren und die alte nicht vorher löschen.** Sie steht seit der Runde 15 an zwei Stellen, an denen der Nutzer sie beim Installieren liest, und beide sind für ihn und nicht für einen Agenten geschrieben: in den ersten dreißig Zeilen der `README.md` und im festen Text jeder Releaseseite, der als Konstante `RELEASETEXT` in `xtask/src/veroeffentlichung.rs` an genau einer Stelle steht und dessen Aussagen die Probe `der_releasetext_traegt_jede_seiner_aussagen` einzeln hält. Wer den Text ändert, ändert ihn dort und nicht in einer Kopie.

### L05 — Die Pfadregel unter der Rundentabelle bekommt den Zweig für `shared/`, `circles/` und die Zeile ohne Circle

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Zeile 34 (Absatz unter der Rundentabelle)
- **Tier:** consolidation
- **Citation:** `CLAUDE.md` gegen sich selbst (Flächenpaar 6): Zeile 32 der Tabelle trägt in der Circle-Spalte „— kein Circle-Datensatz, alles unter `fusion-workbench/shared/`“, und die Regel in Zeile 34 kennt nur „der jeweils genannte Circle“ und „ohne Nennung die Runde 2“; für die Zeile 18 liefert sie das falsche Verzeichnis. `grep -oE '`[^`]*(planning|decisions|issues|analyses)/[^`]*`' CLAUDE.md | sort -u` → jedes heutige Zitat der Runde 18 trägt den Vorsatz `shared/`, die vier Kurzform-Zitate gehören der Runde 2; die Regel geht heute nicht schief, und die Lücke wächst mit der nächsten Runde ohne Circle. Datensatz: `shared/issues/260826-0923_o_die-pfadregel-nach-der-rundentabelle-ist-fuer-die-neue-zeile-18-nicht-mehr-total.md`. Der Absatz sagt außerdem nirgends, wogegen ein Pfad mit `shared/` aufzulösen ist. **Der Eintrag entfernt nichts** und fügt der Fallunterscheidung die zwei fehlenden Zweige hinzu.
- **Consequence group:** consolidation
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> Pfade der Form `planning/…`, `decisions/…`, `analyses/…` und `issues/…` sind relativ zum Verzeichnis des **jeweils genannten** Circles zu lesen. **Ohne Nennung gilt die Runde 2** — sie hat die meisten der hier zitierten Datensätze hervorgebracht. Alle Speicher binden weiter, auch die der vorgesehenen Circles, die diese Tabelle nicht führt.

**After:**
> Pfade der Form `planning/…`, `decisions/…`, `analyses/…` und `issues/…` sind relativ zum Verzeichnis des **jeweils genannten** Circles zu lesen. **Ohne Nennung gilt die Runde 2** — sie hat die meisten der hier zitierten Datensätze hervorgebracht. Pfade mit dem Vorsatz `shared/` oder `circles/` sind relativ zu `fusion-workbench/` zu lesen; eine Tabellenzeile ohne Circle-Verzeichnis, heute die Runde 18, nennt keinen Circle, und ihre Datensätze stehen deshalb immer in der langen Form mit `shared/` und nie in der Kurzform (`shared/issues/260826-0923_*_die-pfadregel-nach-der-rundentabelle-ist-fuer-die-neue-zeile-18-nicht-mehr-total.md`). Alle Speicher binden weiter, auch die der vorgesehenen Circles, die diese Tabelle nicht führt.

## 7. Kandidaten — nicht zur Freigabe

### K01 — „die Messstrecke meldet `NICHT_IM_VORDERGRUND` statt Zahlen“ gilt nur für die Sitzungsstrecke

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Zeile 125 („Der Abnahmelauf verlangt KRK im Vordergrund“)
- **Tier:** candidate
- **Citation:** `grep -rn NICHT_IM_VORDERGRUND crates --include='*.rs'` → entschieden allein in `messmodus.rs:744` (`messung_unmoeglich`). Der offene Datensatz `shared/issues/260826-1442_o_die-spannenstrecke-hat-keinen-vordergrundvorbehalt-…` belegt am Baum, dass `--messmodus spannen` (L1, L2, L3, L10) den Vorbehalt nicht trägt und im Hintergrund nach zehn Sekunden mit der Geduldsmeldung abbricht.
- **Warum Kandidat:** der Datensatz schlägt eine Codeänderung vor, die den Vorbehalt auf beide Strecken hebt; danach wäre der Satz in `CLAUDE.md` wieder wahr. Eine Änderung an der Prosa vor dieser Entscheidung schriebe den Defekt fest. Arbeit des `coder` über jenen Datensatz; der Satz wird wieder vorgelegt, wenn der Datensatz geschlossen oder abgelehnt ist.

### K02 — `CLAUDE.md` trägt die Betriebsregel gegen den Datenverlust beim Installieren, aber nicht die zweite Regel derselben Bauart zur `readers.toml`

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Abschnitt „Was man nicht sieht, wenn man es nicht weiß“
- **Tier:** candidate
- **Citation:** `shared/issues/260826-0149_o_claude-md-sagt-nichts-ueber-die-fuenf-neuerungen-der-runde-18-an-der-vorschau.md` (reconciler); `README.md:44-73`. Die Tabellenzeile 18, die der Datensatz verlangt, steht seit `fb50fcd`; offen ist der Absatz über den Handgriff (Leseprofile erreichen einen Nutzer, der KRK schon gestartet hat, erst nach dem Beiseitelegen der `readers.toml`) und über die Abweichung zwischen Leseläufen und Verzeichnisöffnungen.
- **Warum Kandidat:** eine Ergänzung ist keiner der zwei Gründe dieses Agenten — kein Widerspruch, keine Überholung —, und der Datensatz sagt selbst, die Wahl, was in die Datei gehört, gehört dem Nutzer. Wer den Absatz will, schreibt ihn; dieser Lauf schlägt ihn nicht vor.

### K03 — Die `deny(unsafe_code)`-Aussage ist wahr, wie sie dasteht, und zwei offene Datensätze wollen ihren Umfang ändern

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Zeile 95
- **Tier:** candidate
- **Citation:** `grep -rn '#!\[\(deny\|allow\)(unsafe_code)\]' crates` → drei Kistenwurzeln, zwei Ausnahmen, wie die Zeile sagt. `shared/issues/260826-1302_o_die-probenziele-des-kerns-tragen-kein-deny-unsafe-code-…` (die Probenziele unter `tests/` sind eigene Kisten ohne die Grenze) und `shared/issues/260826-1450_o_xtask-traegt-kein-deny-unsafe-code.md` (`xtask` fehlt) nennen je zwei Wege, einen an der Prosa und einen am Code.
- **Warum Kandidat:** keine falsche Aussage; welcher der zwei Wege je Datensatz gilt, ist eine Codeentscheidung, die vor der Prosa fällt. Arbeit des `coder`.

## 8. Was dieser Lauf nicht darf, und wer es darf

- **Drei offene Defektdatensätze sind gegenstandslos geworden, und ihre Marker gehören dem `reconciler`:** `shared/issues/260825-1859_o_claude-md-nennt-fuer-zip-das-eine-merkmal-deflate-flate2-es-sind-zwei.md` (Zeile 83 nennt heute beide Merkmale), `circles/260823-2208-…/issues/260824-1852_o_zwei-aussagen-in-claude-md-sind-mit-dieser-runde-falsch-geworden-…` (die Rufer von `ohne_warten_oeffnen` stehen ohne Zahl, die Leseprofile stehen in Zeile 123) und, bei voller Freigabe dieses Laufs, `shared/issues/260826-1306_o_…` (L01), `260826-1441_o_…` zur Hälfte (L02 deckt `CLAUDE.md`, nicht die vier anderen Stellen), `260826-0923_o_die-ablage-aufzaehlung-…` (L04) und `260826-0923_o_die-pfadregel-…` (L05). Dieser Lauf rührt keinen Marker an.
- **Vier Stellen außerhalb des Remits tragen dieselbe falsche Begründung wie L02:** `certify-only.sh`, `Makefile`, `xtask/src/main.rs`, `README.md`. Der Datensatz `260826-1441_o_…` führt sie mit Zeilennummern; ein zweiter Datensatz wäre eine Kopie. Arbeit des `coder`.
- **Kein Fixture außerhalb des Remits hängt an `CLAUDE.md`**; die Größenänderung bricht keine Probe dieses Projekts.
- **Kein Commit.** Die Änderungen bleiben bei Freigabe im Arbeitsbaum; `git checkout -- CLAUDE.md` nimmt sie zurück.

## 9. Ausgang

Anwendungsdurchgang am 260826-1650, Baumstand beim Schreiben `caa3f23` (unverändert gegenüber der Erhebung; `git status --porcelain -- CLAUDE.md` war vor dem Schreiben leer). Freigabe: `all`. Vor jedem Eintrag ist der Before-Text von der Platte gelesen und genau einmal gefunden worden; nach dem Schreiben steht jeder After-Text genau einmal und byteweise wie im Ledger auf der Platte, und kein Before-Text ist mehr vorhanden.

- L01: applied
- L02: applied
- L03: applied
- L04: applied
- L05: applied
- K01, K02, K03: Kandidaten, nicht zur Freigabe gestellt, nicht angewandt.

`CLAUDE.md` nachher: 52.095 Bytes, 195 Zeilen — wie in Abschnitt 2 vorhergesagt. `git diff --stat -- CLAUDE.md`: 5 Einfügungen, 5 Löschungen. Kein Entscheidungsdatensatz und keine Regeldatei ist berührt. Kein Commit; Rücknahme mit `git checkout -- CLAUDE.md`. Was daraus außerhalb dieses Remits folgt, steht unverändert in Abschnitt 8: die Marker der gegenstandslos gewordenen Defekte gehören dem `reconciler`, die vier Stellen mit der Begründung aus L02 dem `coder`.
