# Kuratorlauf: die bewilligten Änderungen an CLAUDE.md anwenden

**Status:** Complete
**Filed by:** curator, Kai Stalmann <kai@stalmann.org>
**HEAD:** `fe5fe9cb812cf748a46fbd8856017b2555759e37`
**Modus:** apply
**Voriger Kuratorlauf:** `260829-1257-curator-run.md`
**Ankerwert `last_curator_run`:** `6f57c9fc19bcf048436938f01ed22b6089e21ddf`

## Wie dieser Lauf zu seiner Änderungsliste gekommen ist

**Die Auftragsform weicht von der Regelform ab, und das ist hier festgehalten statt
verschwiegen.** Der Auftrag trägt weder `**Mode:** apply` noch eine `**Ledger:**`-Zeile,
und im Verlaufsspeicher steht keine Laufdatei des vorangegangenen Durchgangs C1: die
jüngste ist `260829-1257-curator-run.md`. Die Erhebung von C1 hat also keine Datei
hinterlassen, an der dieser Lauf sich prüfen könnte.

Der Auftrag selbst schreibt jede bewilligte Änderung einzeln aus und ordnet ausdrücklich
an, jede Stelle vor der Änderung neu am Baum zu prüfen. Genau das ist geschehen: die
Änderungsliste unten ist neu erhoben und nicht aus einem Bericht übernommen. Die
Bewilligungsmenge ist die Aufzählung des Auftrags, Punkt für Punkt, samt der einen
Abweichung, die der Nutzer an der Rundentabelle gewählt hat.

**Ein Ordner der Voraufnahme ist damit nicht erfüllt** — die Laufdatei von C1 fehlt —, und
was an ihre Stelle tritt, ist die Neuerhebung. Wer diesen Lauf prüfen will, prüft die
Belege in den Einträgen und nicht eine Übereinstimmung mit einem Vorgängertext.

## Was gelesen wurde

Dieser Lauf ist keine Erhebung über die acht Quellen, sondern die Anwendung einer
bewilligten Liste mit Neuprüfung je Stelle. Gelesen wurden:

| Quelle | gelesen |
|---|---|
| 1 Circle-Datensätze | 0 — keine Aussage dieses Laufs hängt an einem |
| 2 Entscheidungsdatensätze | 9 (`260826-0859`, `260826-0923`, `260826-1811`, `260811-2050`, `260821-1115`, `260806-1303`, `260819-1440`, `260826-1225`, `260907-2340`) |
| 3 Sitzungsverläufe | 1 Auflistung des Speichers, 0 Dateien im Rumpf gelesen |
| 4 git | HEAD und Arbeitsbaumstand; keine `git log --follow`-Läufe, weil jede Aussage am heutigen Baum entscheidbar war |
| 5 Durchsichten und Analysen | 0 |
| 6 `orchestrator-events.jsonl` | 0 |
| 7 Archivspeicher | 0 gelesen; die Auflösung der Datensatzverweise hat zwei Archivtreffer angezeigt, keiner davon trägt eine Aussage dieses Laufs |
| 8 Provenienzköpfe der Regeldateien | 5 (die vom Ladeprogramm ausgegebenen), keine geändert |

Dazu 14 Quelldateien des Baums, `Makefile`, `Cargo.toml`, `resources/default-keymap.toml`,
sechs Befunddatensätze und der Plan der Runde 13.

**Was nicht gelesen wurde, ist damit gesagt:** dieser Lauf hat den Bestand der Circles,
der Analysen und des Archivs nicht durchgesehen. Für eine Erhebung wäre das ein Mangel;
für die Anwendung einer Liste, deren jede Aussage am Quelltext entscheidbar ist, ist es
die Grenze der Aufgabe.

## Vergleichszählungen über die sechs Flächenpaare

**Dieser Lauf hat keine Widerspruchssuche gefahren.** Die sechs Paare sind nicht
abgeglichen, und für keines steht hier eine Zahl. Die Änderungen dieses Laufs kommen
sämtlich aus einer bewilligten Liste, deren Belege am Quelltext hängen, nicht aus einem
Flächenvergleich. Wer eine Widerspruchsauskunft braucht, braucht eine Erhebung.

Eine Fläche ist dabei nachweislich leer: das Projekt führt weder `./rules/` noch
`.claude/rules/`. Es gibt keine projekteigenen Regeldateien, und die drei Paare, die eine
solche Datei brauchen, haben nichts, worüber sie laufen könnten.

## Flächenumfänge

| Fläche | vorher | nachher |
|---|---|---|
| `CLAUDE.md` | 207 Zeilen, 63 894 Bytes | 213 Zeilen, 70 269 Bytes |
| Projekteigene Regeldateien | nicht vorhanden | nicht vorhanden |
| Entscheidungsdatensätze | unverändert | unverändert |

## Voraufnahme der Entscheidungsdatensätze

**Keiner wird geändert.** Dieser Lauf fasst keinen Entscheidungsdatensatz an, weder im
Rumpf noch am Marker. Geändert werden `CLAUDE.md` und fünf Befunddatensätze unter
`shared/issues/`; alle sind unter Versionsverwaltung, und der Rückweg steht je Eintrag.

## Wie Vorher und Nachher hier zitiert sind

Je Eintrag steht der **geänderte Satz** und nicht der ganze Absatz, in dem er steht. Der
Absatz umfasst an mehreren Stellen fünfhundert Wörter, von denen die Änderung zwanzig
berührt, und ein zweimal vollständig abgeschriebener Absatz machte den Eintrag unprüfbar
statt prüfbarer. Der zitierte Satz ist der Bereich, der vor dem Schreiben von der Platte
gelesen und danach byteweise gegengelesen wurde.

## Die Änderungsliste

### L01 — Rundentabelle Runde 13: Tastenname fällt

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 27
- **Stufe:** 1
- **Beleg:** `CLAUDE.md:83` verlangt, dass Tastenzuordnungen in `resources/default-keymap.toml` stehen und nicht in dieser Datei; die Zeile hält sich nicht daran. Der Funktionsname steht in `resources/default-keymap.toml:310` als „Anderes Dateifenster auf diesen Ordner stellen".
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> `opt+cmd+s` gleicht die Ordner der zwei Dateifenster an, Abwurf aus fremden Anwendungen

**Nachher:**
> die Ordner der zwei Dateifenster angleichen, Abwurf aus fremden Anwendungen

### L02 — Rundentabelle Runde 20: Zoom-Tastennamen fallen

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 34
- **Stufe:** 1
- **Beleg:** dieselbe Regel `CLAUDE.md:83`. Die Namen stehen in `resources/default-keymap.toml:841,846,851` als „Vorschau vergrößern", „Vorschau verkleinern", „Vorschau in Ausgangsgröße".
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> den drei Zoombefehlen `cmd+plus`, `cmd+minus` und `cmd+0`

**Nachher:**
> den drei Zoombefehlen Vergrößern, Verkleinern und Ausgangsgröße

### L03 — Rundentabelle Runde 21: `cmd+v` fällt

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 35
- **Stufe:** 1
- **Beleg:** `resources/default-keymap.toml:524-526` trägt das Einfügen in den Filtertext als `filter_einfuegen` auf `cmd+f`; die Kommentare bei `:88-92` schreiben den Wechsel vom 260907-2009 aus. Befund `260907-2046_o_…`.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> `cmd+v` im Dateifenster hängt die Zwischenablage an den Filtertext an, `*` im Filtertext ist ein Platzhalter

**Nachher:**
> Einfügen aus der Zwischenablage in den Filtertext, `*` im Filtertext ist ein Platzhalter

### L04 — Rundentabelle Runde 22: `cmd+c` und `cmd+x` fallen

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 36
- **Stufe:** 1
- **Beleg:** dieselbe Regel `CLAUDE.md:83`. Die Namen stehen in `resources/default-keymap.toml:1212,1218` als „Ausschneiden" und „Kopieren". Der Verzeichnisname der Runde bleibt unberührt.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> `cmd+c` und `cmd+x` im Dateifenster legen Dateiverweise für andere Anwendungen ab

**Nachher:**
> Kopieren und Ausschneiden im Dateifenster legen Dateiverweise für andere Anwendungen ab

### L05 — Baumbild: „vier Mitglieder" auf die Quelle umgestellt

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 55
- **Stufe:** 1
- **Beleg:** `awk '/^members = \[/,/^\]/' Cargo.toml` gibt vier Mitglieder aus; die Zahl stimmt heute und fällt mit dem fünften Mitglied. `rules/critical-stance.md` §5.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> ├── Cargo.toml            # Workspace mit vier Mitgliedern, Version an einer Stelle

**Nachher:**
> ├── Cargo.toml            # Workspace, Mitglieder unter `members`, Version an einer Stelle

### L06 — Baumbild: `leseprofil/` und `operation/` fehlen

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeilen 58-60
- **Stufe:** 1
- **Beleg:** `ls crates/krk-core/src` führt `ablage git leseprofil lib.rs operation stapelumbenennen tasten text verzeichnis zwischenablage.rs`; das Baumbild nennt `leseprofil/` und `operation/` nicht.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> │                         #   Tastennormalisierung, Ablage, `text/`, `stapelumbenennen/`,
> │                         #   `git/`

**Nachher:**
> │                         #   Tastennormalisierung, Ablage, `text/`, `stapelumbenennen/`,
> │                         #   `git/`, `leseprofil/`, `operation/`

### L07 — Baumbild: `HowTo.md`, `certify-only.sh` und `rustfmt.toml` fehlen

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeilen 65-74
- **Stufe:** 1
- **Beleg:** `ls -1` in der Projektwurzel führt alle drei. `HowTo.md` reist seit dem 260905 im Releasepaket mit (`CLAUDE.md:129`), `certify-only.sh` ist die Hülle des zweiten Wegs (`CLAUDE.md:121`), und `rustfmt.toml` steuert `cargo fmt`.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> ├── release.sh            # zweite Hülle: ein Kommando, ein Argument, keine Logik

**Nachher:**
> ├── release.sh            # zweite Hülle: ein Kommando, ein Argument, keine Logik
> ├── certify-only.sh       # dritte Hülle: beglaubigt ein fertiges Bündel, baut nichts
> ├── rustfmt.toml          # die Formatierungsregeln, die `cargo fmt` durchsetzt

(dazu `HowTo.md` vor `idea.txt`)

### L08 — Baumbild: „die sieben PNGs" auf ihr Zählkommando

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 70
- **Stufe:** 1
- **Beleg:** `ls iconset/*.png` gibt sieben Dateien aus; die Zahl stimmt heute. `rules/critical-stance.md` §5.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> ├── iconset/              # die sieben PNGs, aus denen xtask die .icns baut

**Nachher:**
> ├── iconset/              # die PNGs, aus denen xtask die .icns baut (`ls iconset/*.png`)

### L09 — Der Marker am Spec ist beantwortet, nicht offen

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 77
- **Stufe:** 2
- **Beleg:** `260819-1440_a_was-sagt-der-marker-c-an-einem-spec-gebaut-oder-abgenommen.md`, Zeile 132: `Answered: 260905-2008-orchestrator-session.md` `## Fuenf weitere Entscheidungen am 260907-0823 beantwortet` — der Zustand folgt der belegten Bauarbeit, die Abnahme bekommt eine eigene Kopfzeile. Der Marker steht auf `_a_`, und `CLAUDE.md:183` legt fest, dass `_a_` „beantwortet, aber noch nicht in Code umgesetzt" heißt.
- **Folgengruppe:** tier-2
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> welche Lesart der Marker an einem **Spec** trägt, ist offen (`shared/decisions/260819-1440_*_was-sagt-der-marker-c-an-einem-spec-gebaut-oder-abgenommen.md`)

**Nachher:**
> welche Lesart der Marker an einem **Spec** trägt, ist seit dem 260907-0823 beantwortet und noch nicht umgesetzt (`shared/decisions/260819-1440_*_was-sagt-der-marker-c-an-einem-spec-gebaut-oder-abgenommen.md`, `_a_`): der Zustand folgt der belegten Bauarbeit, und die Abnahme bekommt eine eigene Kopfzeile

### L10 — „rund 16 MB" auf die Konstante umgestellt

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 81
- **Stufe:** 1
- **Beleg:** `grep -n 'pub const EDITORGRENZE' crates/krk-core/src/text/datei.rs` → `186:pub const EDITORGRENZE: u64 = 16 * 1024 * 1024;`
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> er nimmt Textdateien bis rund 16 MB an

**Nachher:**
> er nimmt Textdateien bis `krk_core::text::datei::EDITORGRENZE` an; welche Größe das ist, sagt `grep -n 'pub const EDITORGRENZE' crates/krk-core/src/text/datei.rs` und keine Zahl hier

### L11 — Zeile 83: drei Änderungen in einem Absatz

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 83
- **Stufe:** 1
- **Beleg:** (a) `awk '/pub enum Kontextbefehl/,/^}/' crates/krk-ui/src/kommandos/kontextmenue.rs` führt vier Werte, nicht drei — die Modulköpfe von `appkit/tabelle.rs` und `kommandos/mod.rs` schreiben deshalb „die eigenen Einträge". (b) `ls iconset/*.png` → sieben. (c) `crates/krk-ui/src/appkit/tabelle.rs`, Doc-Kommentar an `in_zeile_einsteigen`: ein Ordner ohne Leserecht geht durch den Einstieg hindurch, `Schwungleser::oeffnen` scheitert mit `EACCES`, und `tabs::lesemeldungen_einziehen` macht daraus die Meldung; Nutzerentscheid vom 260907-1210, `260815-1749_*_meldet-der-doppelklick-auf-einen-ordner-ohne-leserecht-oder-schweigt-er-wie-heute.md`, gehalten von `tabs::tests::ein_ordner_ohne_leserecht_meldet_sich_aus_dem_lesevorgang`.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> sie stand bis zur Runde 17 auf zwei und ist mit den drei eigenen Einträgen des Kontextmenüs falsch geworden
> … (Ordner: hineingehen; unerreichbares Verknüpfungsziel: Meldung in der Statuszeile; sonst: an das System)
> … aus den sieben PNGs unter `iconset/` erzeugt

**Nachher:**
> sie stand bis zur Runde 17 auf zwei und ist mit den eigenen Einträgen des Kontextmenüs falsch geworden
> … (Ordner: hineingehen; Ordner ohne Leserecht: hineingehen, und der Lesevorgang meldet das fehlende Recht; unerreichbares Verknüpfungsziel: Meldung in der Statuszeile; sonst: an das System)
> … aus den PNGs unter `iconset/` erzeugt (`ls iconset/*.png`)

### L12 — `make check` fährt fünf Kommandos, nicht vier

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 115
- **Stufe:** 1
- **Beleg:** `grep -n '^check:' Makefile` → `73:check: build test fmt-check lint doc ## Die fuenf Abnahmekommandos, in der Reihenfolge der Schritte`. Fünf Voraussetzungen, und das Ziel `doc` fährt `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` (`Makefile:71`).
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> `make check` fährt die vier Abnahmekommandos in einem Zug

**Nachher:**
> `make check` fährt die fünf Abnahmekommandos in einem Zug, in der Reihenfolge des Codeblocks darüber

### L13 — Der Codeblock unterschlägt das fünfte Kommando

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Codeblock Zeilen 105-111
- **Stufe:** 1
- **Beleg:** wie L12. Aus diesem Block wird abgeschrieben, bevor jemand einen Lauf fährt; ohne die Zeile läuft `cargo doc` nie, und `make check` bricht dann an einer Stelle ab, die der Abschreibende nicht kennt.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> cargo fmt --all --check
> cargo xtask bundle          # baut und signiert target/KRK.app im Profil release

**Nachher:**
> cargo fmt --all --check
> RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps
> cargo xtask bundle          # baut und signiert target/KRK.app im Profil release

### L14 — Die Hülle für den Veröffentlichungsbefehl ist entschieden, nicht offen

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 123
- **Stufe:** 2
- **Beleg:** `260821-1115_i_bekommt-der-veroeffentlichungsbefehl-eine-eigene-huelle-wie-certify-only-sh.md`, Zeile 87: Möglichkeit 1, keine neue Hülle; Zeile 90 nennt den Baum als Umsetzung. Marker `_i_`.
- **Folgengruppe:** tier-2
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> **Für diesen dritten Weg gibt es bewusst noch keine Hülle**, weder ein Skript noch ein Ziel im `Makefile`; ob er eine bekommt, ist offen (`shared/decisions/260821-1115_*_bekommt-der-veroeffentlichungsbefehl-eine-eigene-huelle-wie-certify-only-sh.md`), und solange nicht, trägt der Aufruf den vollen Pfad zu `cargo`.

**Nachher:**
> **Für diesen dritten Weg gibt es bewusst keine Hülle**, weder ein Skript noch ein Ziel im `Makefile`; der Nutzer hat das am 260907-1301 so entschieden (`shared/decisions/260821-1115_*_bekommt-der-veroeffentlichungsbefehl-eine-eigene-huelle-wie-certify-only-sh.md`, Möglichkeit 1), und der Aufruf trägt deshalb den vollen Pfad zu `cargo`. Ein Ziel im `Makefile` lässt sich in zwei Zeilen nachziehen, sobald der Weg zum ersten Mal wirklich gebraucht wird.

### L15 — Station 1 stellt zwei Vorabfragen für Station 8

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 123
- **Stufe:** 1
- **Beleg:** `xtask/src/release.rs:220` ruft `veroeffentlichung::gh_pruefen()?`, `:225` ruft `veroeffentlichung::release_frei_pruefen(&bundle::wurzel(), bundle::VERSION)?`. Beide stehen in Station 1, beide dienen Station 8. Befund `260906-0008_o_…`.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> deshalb fragt schon Station 1 nach `gh`, obwohl erst Station 8 es braucht

**Nachher:**
> deshalb stellt schon Station 1 zwei Vorabfragen, die erst Station 8 braucht: `gh_pruefen` fragt, ob `gh` da und angemeldet ist, und `release_frei_pruefen` fragt, ob auf der Gegenseite nicht schon ein Release `v<zahl>` steht (beide in `xtask/src/release.rs`, gerufen aus `veroeffentlichung`)

### L16 — Der Vordergrund beim Abnahmelauf ist entschieden, nicht offen

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 135
- **Stufe:** 2
- **Beleg:** `260806-1303_*_wie-kommt-krk-fuer-den-abnahmelauf-in-den-vordergrund.md` (im Circle der Runde 1), Zeile 92: Möglichkeit 1, es bleibt beim Abbruch, und die Messanleitung schreibt die Bedingung hin; Zeile 95 nennt `crates/krk-ui/src/messmodus.rs` als Umsetzung. Marker `_i_`.
- **Folgengruppe:** tier-2
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> Die Frage dazu ist offen (`circles/260802-0842-krk-mac-dateimanager-editor-git/decisions/260806-1303_*_wie-kommt-krk-fuer-den-abnahmelauf-in-den-vordergrund.md`).

**Nachher:**
> Der Nutzer hat am 260907-2009 entschieden, dass es beim Abbruch bleibt und die Messanleitung die Bedingung hinschreibt (`circles/260802-0842-krk-mac-dateimanager-editor-git/decisions/260806-1303_*_wie-kommt-krk-fuer-den-abnahmelauf-in-den-vordergrund.md`, Möglichkeit 1); die Gegenmöglichkeit lohnt erst, wenn die Messreihe je ohne den Nutzer laufen soll, und bräuchte dann einen zweiten Ausgabeweg für die Messzeilen.

### L17 — Der Verweis auf den Plan der Runde 13 beschreibt einen alten Stand

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 143
- **Stufe:** 1
- **Beleg:** Der Abschnitt „Was der Übersetzer einfordert, und was er nicht einfordert" steht in `260818-1633_*_plan-ordner-angleichen-und-abwurf-aus-fremden-apps.md` (im Circle der Runde 13) auf Zeile 185 und beschreibt den Stand jener Runde. Die heutige Auskunft steht im Doc-Kommentar von `jede_alle_liste_fuehrt_genau_die_varianten_ihrer_aufzaehlung` (`crates/krk-core/tests/baum.rs:833` und der Kopf darüber ab `:783`). Der Plan wird nicht angefasst: er ist Aufzeichnung einer geschlossenen Runde, und ob solche Texte angefasst werden, ist offen (`260907-2340_o_…`).
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> steht im Plan der Runde 13 (…, Abschnitt „Was der Übersetzer einfordert, und was er nicht einfordert").

**Nachher:**
> steht im Plan der Runde 13 (…, Abschnitt „Was der Übersetzer einfordert, und was er nicht einfordert") — **und zwar für den Stand jener Runde.** Was heute gilt, sagt der Doc-Kommentar über `jede_alle_liste_fuehrt_genau_die_varianten_ihrer_aufzaehlung` (`crates/krk-core/tests/baum.rs`); der Plan ist Aufzeichnung und wird nicht nachgezogen.

### L18 — Das Kontextmenü ohne Zahl

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 145
- **Stufe:** 1
- **Beleg:** `awk '/pub enum Kontextbefehl/,/^}/' crates/krk-ui/src/kommandos/kontextmenue.rs` führt `Zippen`, `Entpacken`, `ImFinderOeffnen`, `ImFinderAnzeigen`. Das Menü trägt sie plus das Teilen. Der Modulkopf bei `:235` sagt ausdrücklich: „**Die Feldbreite in der Typangabe hält den Bau nicht an.**" Abnahmebedingung des Befunds `260907-0726_o_…`: keine Zahl, sondern ein Zeiger auf `Kontextbefehl::ALLE` samt Erhebungsbefehl.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine — die Aussage des Absatzes (der zweite Weg hat keine Taste, `Kontextbefehl` ist die Sperre gegen den wirkungslosen Menüeintrag) steht unverändert
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> Das Kontextmenü der Dateiliste trägt vier Einträge: das Teilen, … und daneben Zip, Unzip und „Im Finder öffnen", die keines sind. Die drei tragen weder … Ein vierter Wert hält damit den Bau an, statt still nichts zu tun.

**Nachher:** siehe die Datei; der Absatz nennt keine Zahl mehr, zeigt auf `Kontextbefehl::ALLE` mit dem Erhebungsbefehl und schreibt aus, dass die Feldbreite der Liste den Bau **nicht** anhält, wohl aber `titel`, `menuemarke` und der Ausführungszweig.

### L19 — Die `ALLE`-Listen: drei Bauformen sind nachgezogen, die Frage ist entschieden

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 147
- **Stufe:** 1
- **Beleg:** `crates/krk-ui/src/appkit/aufteilung.rs:266` (`rahmen: [Retained<NSBox>; Bereich::ALLE.len()]`), `:390` (`gemessene_breiten(&self) -> [f64; Bereich::ALLE.len()]`), `crates/krk-ui/src/fenstermodell.rs:1063` (`breiten_uebernehmen(gemessen: [f64; Bereich::ALLE.len()], …)`). Der Durchlauf `jede_alle_liste_fuehrt_genau_die_varianten_ihrer_aufzaehlung` steht in `crates/krk-core/tests/baum.rs:833`, seine zwei eingetragenen Ausnahmen in `UNLESBARE_ALLE_LISTEN` (`:759`), und „Sie liest allein `crates/`" steht in seinem Kopf (`:820`). Entscheid `260826-1811_i_…`, Zeile 56. Befund `260907-0859_o_…`.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> **Ob eine `ALLE`-Liste neben ihrer Aufzählung vollständig bleibt, entscheidet ihre Bauform, und drei der vier Bauformen im Baum entscheiden es nicht.** … Welche Bauform künftig gilt, ist eine offene Nutzerfrage (…).

**Nachher:** siehe die Datei; der Absatz nennt die heutige Bauform, den Durchlauf, seine zwei Ausnahmen und seine Grenze auf `crates/`.

### L20 — „Es sind seit der Runde 14 zwei"

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 149
- **Stufe:** 1
- **Beleg:** `crates/krk-ui/src/appkit/anwendung.rs:2849-2862`: `ist_eigene_textflaeche` fragt Editorfläche und Vorschaufläche. Die zwei Namen stehen im Rumpf; die Zahl daneben ist die zweite Quelle derselben Menge (`rules/critical-stance.md` §5).
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> **Es sind seit der Runde 14 zwei**: die Textfläche des eingebauten Editors und die Textanzeige der Vorschau.

**Nachher:**
> **Seit der Runde 14 sind es die Textfläche des eingebauten Editors und die Textanzeige der Vorschau**, und welche es sind, sagt der Rumpf von `ist_eigene_textflaeche` und keine Zahl hier (`grep -n -A13 'fn ist_eigene_textflaeche' crates/krk-ui/src/appkit/anwendung.rs`).

### L21 — Der Filterabsatz: vier falsche Aussagen

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 155
- **Stufe:** 1
- **Beleg:** `awk '/pub const ZEICHENSCHWELLE/' crates/krk-core/src/verzeichnis/filter.rs` → `pub const ZEICHENSCHWELLE: usize = 3;` (`filter.rs:257`). Eine Funktion `inhaltsschwelle` gibt es nicht mehr; der eine Rufer ist `Ordnermodell::schwelle_erreicht` (`modell.rs:1229`), und der bedient beide Frager, `tief_wirkt` und `inhalt_wirkt` (`filter.rs`, Modulkopf). Entscheide `260826-0859_i_…` (Zeile 47) und `260826-0923_i_…` (Zeile 123). Der Durchlauf beginnt ab drei Zeichen, nicht beim ersten Anschlag. „Deep ab Werk auf ein" bleibt richtig (`modell.rs:446`, `tief: true`).
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> `inhaltsschwelle` sagt, ab welcher Länge des Filtertexts auch der Inhalt gelesen wird … **Seit dem 260826 steht das Ankreuzfeld „Deep" ab Werk auf ein, und damit stößt schon der erste Anschlag im Dateifenster den Durchlauf über den Unterbaum an** … An demselben Wert hängt die Schwelle des Inhaltsfilters, die `inhaltsschwelle` nach dem Stand der tiefen Suche staffelt; ob die Kopplung so bleibt, ist offen (…).

**Nachher:** siehe die Datei; eine Schwelle mit heutigem Namen, ein Rufer, keine Staffelung, kein Verweis auf eine offene Frage.

### L22 — „also 16 MB" am Stapelbudget

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 165
- **Stufe:** 1
- **Beleg:** wie L10. Der Satz nennt die Konstante schon; die Zahl daneben ist ihre zweite Quelle.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> ist `krk_core::text::datei::EDITORGRENZE`, also 16 MB, und ein `const _: () = assert!(…)` daneben

**Nachher:**
> ist `krk_core::text::datei::EDITORGRENZE`, und ein `const _: () = assert!(…)` daneben

### L23 — Die Untergrenzen-Angabe hält ein Prüflauf

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Zeile 179
- **Stufe:** 1
- **Beleg:** `crates/krk-core/tests/baum.rs:1238` `jede_appkit_datei_mit_frameworkimport_traegt_den_untergrenzen_abschnitt` und `:1276` `jeder_frameworkimport_steht_namentlich_im_untergrenzen_abschnitt`. Der Doc-Kommentar der ersten (`:1219-1232`) schreibt aus, dass die Ausnahme eine Eigenschaft ist und keine Liste, und dass die Richtigkeit der Zahl eine Zusage des Menschen bleibt. Entscheid `260811-2050_i_…`, Zeile 73 und 76. Befund `260907-2300_o_…`.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine — die Warnung, dass der Übersetzer die Untergrenze nicht hält, bleibt
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> **Die Gegenmaßnahme ist eine Gewohnheit und kein Werkzeug** … außer zweien: `koordinaten.rs` und `mod.rs`, beide begründet … Ob und wie weit die Angabe prüfbar gemacht wird, ist offen (…).

**Nachher:** siehe die Datei; zwei Proben halten die eine Hälfte, der Mensch die andere, und die Ausnahme ist eine Eigenschaft.

### L24 — Die Schreibweisenregel fehlt im Abschnitt `## Sprache`

- **Fläche:** CLAUDE.md
- **Datei:** `CLAUDE.md`, Abschnitt `## Sprache`
- **Stufe:** 2
- **Beleg:** `260826-1225_i_welche-schreibweise-gilt-fuer-nutzersichtbare-deutsche-meldungen-umlaut-oder-umschrift.md`, Zeile 116 (Antwort vom 260907-0703: Umlaute in allem, was ein Mensch liest; Umschrift für Kommentare und Bezeichner) und Zeile 119 (umgesetzt, 61 Zeichenketten, acht Proben auf den Wortlaut). Die Naht selbst hält keine Probe; der Datensatz nennt die offene Frage dazu (`260907-0826_*_wie-wird-die-naht-zwischen-umlaut-und-umschrift-gehalten-jetzt-da-sie-eine-regel-ist.md`).
- **Folgengruppe:** tier-2
- **Entfernte Vorschrift:** keine — Zusatz
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:** (kein Satz — der Abschnitt sagt zur Schreibweise nichts)

**Nachher:** ein Absatz nach „Prosa in diesem Projekt ist deutsch."

## Ausgänge

Alle vierundzwanzig Einträge sind angewandt. Kein Eintrag ist übersprungen, keiner
veraltet, keiner fehlgeschlagen. Vor jedem Schreiben ist der Vorher-Text von der Platte
gelesen und danach gegengelesen worden.

| Eintrag | Ausgang |
|---|---|
| L01 bis L24 | angewandt |

**Eine Berichtigung während des Laufs.** L18 hat im ersten Durchgang die Typangabe
`[Kontextbefehl; 4]` wörtlich zitiert und damit eine gegenwärtige Zahl der
`Kontextbefehl`-Werte in die Datei zurückgeholt, die die Abnahmebedingung des Befunds
`260907-0726_*_…` gerade ausschließt. Das Gegenlesen hat es gefangen; der Satz nennt
jetzt keine Zahl. Der Eintrag steht als angewandt, weil der Endstand der bewilligte ist.

## Entfernte Zahlen und Vorschriften

**Keine einzige Vorschrift ist entfernt worden.** Sechs Zahlen sind auf ihr
Erhebungskommando oder auf ihre Quelle umgestellt, und jede ist hier in einer Zeile
benannt, damit eine spätere Suche nach einer verschwundenen Angabe sie findet:

- die vier Mitglieder des Workspace, jetzt `members` in der Wurzel-`Cargo.toml`
- die sieben PNGs unter `iconset/`, jetzt `ls iconset/*.png`
- die 16 MB des Editors, zweimal, jetzt `krk_core::text::datei::EDITORGRENZE`
- die zwei eigenen Textflächen, jetzt der Rumpf von `ist_eigene_textflaeche`
- die Zahl der Kontextmenü-Einträge, jetzt `Kontextbefehl::ALLE`
- die Länge der Zeichenschwelle des Filters, jetzt `filter::ZEICHENSCHWELLE`

Vier Tastennamen sind aus der Rundentabelle gefallen (`opt+cmd+s`, `cmd+plus`/`cmd+minus`/`cmd+0`,
`cmd+v`, `cmd+c`/`cmd+x`) und durch den Funktionsnamen ersetzt. Die Verzeichnisnamen der
Runden sind unberührt; der Name der Runde 22 trägt die Tasten weiter als historische
Benennung.

## Geprüfte Erhebungskommandos

Jedes neu in die Datei geschriebene Kommando ist vor dem Schreiben gefahren:

| Kommando | Ausgabe |
|---|---|
| ``awk '/pub enum Kontextbefehl/,/^}/' crates/krk-ui/src/kommandos/kontextmenue.rs`` | die Aufzählung mit `Zippen`, `Entpacken`, `ImFinderOeffnen`, `ImFinderAnzeigen` |
| ``awk '/pub const ZEICHENSCHWELLE/' crates/krk-core/src/verzeichnis/filter.rs`` | `pub const ZEICHENSCHWELLE: usize = 3;` |
| `ls iconset/*.png` | sieben Dateien, `icon-16` bis `icon-1024` |
| `grep -n 'pub const EDITORGRENZE' crates/krk-core/src/text/datei.rs` | `186:pub const EDITORGRENZE: u64 = 16 * 1024 * 1024;` |
| `grep -n -A13 'fn ist_eigene_textflaeche' crates/krk-ui/src/appkit/anwendung.rs` | der Rumpf mit `editorflaeche \|\| vorschauflaeche` |
| `grep -n '^check:' Makefile` | `73:check: build test fmt-check lint doc` |

## Befunde

Geschlossen: `260907-0726`, `260907-0859`, `260907-2046`, `260907-2300`, `260908-0002`
— je mit `Resolved:`-Vermerk und Umbenennung auf `_c_`.

Offen geblieben: `260906-0008`. Der Befund hat zwei Hälften, und die `README.md`-Hälfte
liegt außerhalb dieses Auftrags. Der Datensatz trägt jetzt einen Vermerk, welche Hälfte
erledigt ist und welche nicht; der Marker bleibt `_o_`.

## Was dieser Lauf nicht getan hat

- **Den Ankerwert `last_curator_run` nicht fortgeschrieben.** Der Anker bindet den
  Lesebereich der *nächsten* Erhebung. Ihn nach einem Lauf zu setzen, der die acht
  Quellen gar nicht durchgesehen hat, hieße der nächsten Erhebung sagen, alles vor
  diesem HEAD sei schon gesehen worden — und genau das wäre falsch. Er steht unverändert
  auf `6f57c9fc19bcf048436938f01ed22b6089e21ddf`.
- **Nicht committet.** Es liegen Änderungen im Arbeitsbaum.
- **Keine Datei außer `CLAUDE.md` und den sechs Befunddatensätzen angefasst.** `README.md`,
  `HowTo.md` und der Plan der Runde 13 sind gelesen und nicht geändert worden.

---
**Status:** Complete
