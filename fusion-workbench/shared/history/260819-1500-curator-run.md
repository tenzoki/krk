# Kuratorenlauf — 260819-1500

**Status:** Complete — die Erhebung ist gefahren, das Gate ist mit `all` beantwortet, und alle dreizehn Einträge sind angewandt. Abschnitt 11 trägt die Ergebnisse je Eintrag.
**Datum:** 260819-1500 (Erhebung)
**Git HEAD beim Lesen:** `82c020a` (`chore(workbench): der Abgleich zum Sitzungsabschluss, Verdikt kohaerent`)
**Modus:** survey (260819-1500), danach apply (260819, derselbe Baumstand `82c020a`, `CLAUDE.md` unverändert seit der Erhebung). Die Zustimmung des Nutzers lautete `all`.
**Vorheriger Kuratorenlauf:** `shared/history/260814-1405-curator-run.md`, Baumstand `43dfe90`, zwölf Einträge, alle angewandt.

**Sprache dieser Datei.** Sie ist deutsch, wie der vorige Kuratorenlauf und die 163 Entscheidungsdatensätze daneben. `CLAUDE.md:4` deklariert seit `c75972c` `**Artifact language:** en`, und nach `rules/fusion-workbench-conventions.md` `## Project language` gölte für diese Datei damit Englisch. Der Widerspruch zwischen dieser Zeile und `CLAUDE.md:178` ist der Gegenstand des Entscheidungsdatensatzes, den dieser Durchgang angelegt hat; die Sprache dieser Datei mitten in den offenen Streit hinein zu wechseln, hieße ihn still zu entscheiden. Die Wahl ist damit benannt und nicht stillschweigend getroffen.

---

## 1. Was sich seit dem vorigen Lauf geändert hat

Der vorige Lauf las den Baum bei `43dfe90` am 260814. Seither sind **161 Commits** gelandet (`git log --oneline --since=2026-08-14 | wc -l`), drei Runden sind gefahren und geschlossen (11, 12, 13), eine vierte ist zurückgestellt worden, und die ausgelieferte Version ist von `v0.4.1` auf `v0.5.4` gestiegen. Die Erhebung dieses Durchgangs ist trotzdem nicht auf dieses Intervall beschränkt: `git log --follow` ist über die ganze Historie von `CLAUDE.md` gelesen worden (23 Commits), nicht über ein Fenster ab dem 260814.

## 2. Die drei normativen Flächen

| # | Fläche | Befund |
|---|---|---|
| 1 | Entscheidungsdatensätze unter `$SCAN_DECISIONS` = `shared/decisions/` und `circles/*/decisions/` | vorhanden, **163 Dateien**, alle lesbar |
| 2 | Projekteigene Regeldateien `./rules/` und `.claude/rules/` | **abwesend**. `ls rules` und `ls .claude/rules` melden beide „No such file or directory"; `.claude/` trägt allein `settings.local.json`. Es gibt in diesem Projekt keine projekteigenen Regeldateien. |
| 3 | `CLAUDE.md` im Projektwurzelverzeichnis | vorhanden, 178 Zeilen, 34274 Bytes, lesbar |

Keine Fläche war unlesbar. Für Fläche 2 gilt damit der Fall „abwesend" aus `agents/curator.md` `### Readable, absent, unreadable`: es wird null gemeldet, und die beiden anderen Flächen sind vollständig gefahren.

## 3. Evidenzquellen — was gelesen wurde

| # | Quelle | Bestand | Gelesen |
|---|---|---|---|
| 1 | Circle-Datensätze (`circles/`) | 15 | 15 Markerzustände; 3 vollständig (Directive und Schließungsnotiz der Runden 11, 12, 13) |
| 2 | Entscheidungsdatensätze | 163 | 163 Marker; 1 vollständig (`260819-1440_o_was-sagt-der-marker-c-…`); die Zeilen `Answered:`/`Implemented:`/`Superseded by:` von 11 weiteren |
| 3 | Sitzungsprotokolle (`history/`) | 442 | 1 vollständig (`shared/history/260819-1440-reconciliation.md`, der breite Abgleich von heute); 1 Kopf (`260814-1405-curator-run.md`) |
| 4 | `git log --follow` | — | `CLAUDE.md`: 23 Commits, ganze Historie. Projekteigene Regeldateien: entfällt, es gibt keine. Dazu `git log --since=2026-08-14` (161), `git show c75972c`, `git tag -l` |
| 5 | Durchsichten und Analysen | 59 + 3 | 0 vollständig, beide Bestände aufgelistet und nach Datum geprüft. **Das ist eine Lücke dieses Durchgangs** und unten unter „Was nicht gelesen wurde" benannt |
| 6 | `orchestrator-events.jsonl` | 1216 Zeilen | 0. Die Quelle ist nur bestätigend und trägt keinen Befund dieses Laufs |
| 7 | Archivspeicher `$WORKBENCH/archive` | **0 Dateien** — das Verzeichnis besteht und ist leer (`ls -A fusion-workbench/archive` liefert nichts). Es ist ausdrücklich gelesen und nicht übersprungen worden | — |
| 8 | `**Provenance:**`-Kopfzeilen auf Regeldateien | **0** — es gibt keine projekteigenen Regeldateien, also auch keine Kopfzeilen | — |
| — | Defektkorpus (Gegenprobe, keine vierte Fläche) | 585, davon 133 offen | 6 offene Datensätze vollständig, alle mit `CLAUDE.md` als Gegenstand |

**Was nicht gelesen wurde.** Die 59 Durchsichten und 3 Analysen sind aufgelistet, aber keine ist im Volltext gelesen worden; die jüngste Durchsicht im gemeinsamen Speicher stammt vom 260815, die jüngeren liegen in den Circles der Runden 12 und 13 und sind über deren Schließungsnotizen und über die Defektdatensätze erreicht worden, die sie hervorgebracht haben. Von den 442 Sitzungsprotokollen ist eines gelesen. Ein Befund, der allein in einem ungelesenen Protokoll oder einer ungelesenen Durchsicht stünde, hätte dieser Durchgang nicht gefunden.

## 4. Flächengrößen

| Fläche | Bytes vorher | Zeilen vorher | Bytes nachher | Zeilen nachher |
|---|---|---|---|---|
| Entscheidungsdatensätze (163 Dateien zusammen) | 1021054 | 11526 | unverändert | unverändert |
| Projekteigene Regeldateien | 0 | 0 | 0 | 0 |
| `CLAUDE.md` | 34274 | 178 | 37896 | 181 |

**Blast-Radius.** Die vorgeschlagenen Streichungen entfernen zusammen rund 1,1 kB aus `CLAUDE.md`, also **etwa 3 Prozent** der Datei, und aus keiner anderen Fläche etwas. Die Schwelle von 20 Prozent je Fläche ist nicht erreicht; es gibt keine gesonderte Rückfrage zum Ausmaß.

## 5. Vergleichszählungen je Flächenpaar

**Die Auswahlregel steht bei jedem Paar dabei, und das Verdikt gilt für die Paare, die die Regel erreicht hat, nicht für den Korpus.**

| Paar | Auswahlregel | Paare erzeugt | Gelesen | Verdikt |
|---|---|---|---|---|
| 1. Entscheidung gegen Entscheidung | Nur bindende Datensätze (`_a_` und `_i_`, 122 von 163; `_o_` ist eine Frage und keine Aussage, `_s_`/`_d_` sind Historie). Daraus die Paare mit **mindestens zwei gemeinsamen Inhaltstoken im Slug** nach Abzug einer Stoppwortliste | **11** von 7381 möglichen | 11 (Titel), 4 davon zusätzlich mit ihren Zeilen `Answered:`/`Implemented:` | kein Widerspruch. Alle elf Paare sind ergänzende Fragen zu derselben Fähigkeit, keine konkurrierenden Antworten |
| 2. Entscheidung gegen Regeldatei | entfällt — es gibt keine projekteigenen Regeldateien | 0 | 0 | — |
| 3. Entscheidung gegen `CLAUDE.md` | Jeder Entscheidungsdatensatz, den `CLAUDE.md` zitiert (8), plus jeder Datensatz, dessen Gegenstand eine Aussage von `CLAUDE.md` trägt (7 weitere, über Slug-Suche zu den Stichworten Tag, Löschen, Filter, Sprache, Marker) | **15** | 15 | **ein Widerspruch**: `CLAUDE.md:106` gegen `shared/decisions/260813-1534_i_…` (Eintrag L07) |
| 4. Regeldatei gegen Regeldatei | entfällt | 0 | 0 | — |
| 5. Regeldatei gegen `CLAUDE.md` | entfällt | 0 | 0 | — |
| 6. `CLAUDE.md` gegen sich selbst | Alle 178 Zeilen gegeneinander gelesen, mit besonderer Aufmerksamkeit für Kopfdeklarationen gegen Fließtext | ganze Datei | ganze Datei | **ein Widerspruch**, unauflösbar durch mich: `:4` gegen `:178`. Als offener Entscheidungsdatensatz abgelegt, siehe Abschnitt 8 |

**Die Auswahlregel für Paar 1 im Wortlaut, damit sie nachfahrbar ist.** Aus dem Dateinamen jedes bindenden Datensatzes wird der Slug genommen, an Bindestrichen zerlegt, auf Token von mindestens fünf Zeichen gekürzt und gegen eine Stoppwortliste deutscher Funktionswörter und generischer Fragewörter (`geschieht`, `traegt`, `bekommt`, `steht`, `kommt`, …) gefiltert. Zwei Datensätze bilden ein Kandidatenpaar, wenn ihre Tokenmengen sich in mindestens zwei Token schneiden. Diese Regel findet Datensätze, die über **dieselbe Sache** sprechen; sie findet **nicht** zwei Datensätze, die dieselbe Sache mit verschiedenen Wörtern benennen. Das ist die bekannte Lücke dieses Verdikts.

## 6. Vorabinhalt geänderter Entscheidungsdatensätze

**Keiner.** Dieser Durchgang schlägt an keinem der 163 Entscheidungsdatensätze eine Änderung vor, weder am Rumpf noch am Marker noch am Dateinamen. Es ist deshalb kein Vorabinhalt festzuhalten. Der einzige Schreibvorgang im Entscheidungsspeicher ist der **neue** offene Datensatz aus Abschnitt 8, und ein neuer Datensatz ändert keine bestehende Aussage.

---

## 7. Das Änderungsbuch

Dreizehn Einträge, alle auf `CLAUDE.md`. Gruppen in der Reihenfolge, in der sie am Gate erscheinen: zuerst die Streichung einer Vorschrift, dann Tier 3, Tier 2, Tier 1, zuletzt Zusammenführungen.

### Streichung einer Vorschrift (1 Eintrag)

### L07 — „den Tag setzt der Nutzer, nicht das Werkzeug" ist am 260813 zurückgenommen worden

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 106
- **Tier:** 2
- **Zitat:** `shared/decisions/260813-1534_i_darf-das-bauwerkzeug-den-tag-setzen-und-die-auslieferung-in-einem-kommando-fahren.md`, Zeile `Answered:` — Nutzerwort vom 260813-1534: „Wir nehmen den Entscheid einfach zurück, das Werkzeug soll taggen, releasen und fertig", und Zeile `Implemented:` — `801f5cc`. Der Datensatz nennt den überholten Vorgänger ausdrücklich: `circles/260813-0939-titelleiste-fuehrt-version-und-semantische-tags/decisions/260813-0939_s_wer-setzt-den-ersten-tag-v0-1-0-und-wann.md`, der den Marker `_s_` trägt. Der Satz in `CLAUDE.md`, den er überholt, ist wörtlich „den Tag setzt der Nutzer, nicht das Werkzeug". **Am Baum nachgeprüft:** `grep -n 'fn taggen\|NurTaggen\|SetzenEintragenTaggen' xtask/src/version.rs` liefert `83: NurTaggen`, `85: SetzenEintragenTaggen`, `216: fn taggen(...)` — von drei Vorhaben taggen zwei. Corroborierend, nicht tragend: der offene Defekt `shared/issues/260816-1232_o_claude-md-sagt-den-tag-setze-der-nutzer-seit-dem-260813-setzt-ihn-das-werkzeug.md`, dessen Nachtrag vom 260819-1230 den vorhergesagten Schaden als eingetreten meldet.
- **Konsequenzgruppe:** Streichung einer Vorschrift
- **Gestrichene Vorschrift:** „den Tag setzt der Nutzer, nicht das Werkzeug" — die Anweisung, vor einer Auslieferung den Tag `v<version>` von Hand auf HEAD zu setzen.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> **Seit der Runde 8 bricht `cargo xtask release` ab, wenn HEAD keinen Tag `v<version>` trägt, der zur `Cargo.toml` passt** — den Tag setzt der Nutzer, nicht das Werkzeug.

**Nachher:**
> **Seit der Runde 8 bricht `cargo xtask release` ab, wenn HEAD keinen Tag `v<version>` trägt, der zur `Cargo.toml` passt.** Den Tag setzt seit dem 260813-1534 das Werkzeug und nicht der Nutzer (`shared/decisions/260813-1534_*_darf-das-bauwerkzeug-den-tag-setzen-und-die-auslieferung-in-einem-kommando-fahren.md`); der Nutzer wählt allein die Zahl, im Argument von `./release.sh <version>`. Wer den Tag von Hand vorwegnimmt, bricht den Lauf an „ein vergebener Name hält den Lauf an" ab.

*Anmerkung zur Vorhalteliste:* Der gestrichene Halbsatz gehört zu einer kritischen Prozedur (dem Auslieferungsweg) und fällt damit unter die Vorhalteliste. Er ist trotzdem angeboten, weil die Ausnahme greift: Tier 2 mit einem ausdrücklichen überholenden Datensatz.

### Tier 3 (0 Einträge)

Keiner. Kein Befund dieses Durchgangs ruht auf einer Bahn statt auf einem Datensatz oder einer Messung.

### Tier 2 (2 Einträge)

### L04 — `_c_` heißt in diesem Projekt nicht mehr „der Nutzer hat den Abnahmelauf gefahren"

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 64
- **Tier:** 2
- **Zitat:** `circles/260817-0833-jeder-loeschweg-mit-rueckfrage-und-nur-noch-papierkorb/_c_circle.md`, Abschnitt `## Closure note`, Absatz „Warum dieses `_c_` etwas anderes bedeutet als das der Runde 8": „Hier ist der Abnahmelauf **nicht** gefahren … **Wer künftig `_c_` als ‚vom Nutzer abgenommen' liest, liest diese Runde falsch.**" Dazu `circles/260818-1615-ordner-angleichen-und-abwurf-aus-fremden-apps/_c_circle.md`, `## Closure note`: „Sie ist die **dritte**, die kohärent schließt, und nach der Runde 8 die zweite, die den Abnahmelauf dafür wirklich gefahren hat." Der Satz in `CLAUDE.md`, den beide überholen, ist „Die eine Ausnahme ist die Runde `260813-0939-…`, in der der Nutzer den Lauf selbst gefahren hat; sie schließt als bisher einzige kohärent (`_c_`)". **Am Dateibestand nachgeprüft:** `ls fusion-workbench/circles/*/_*_circle.md | sed -E 's#.*/_([a-z])_circle.md#\1#' | sort | uniq -c` liefert `1 a`, `10 b`, `3 c`, `1 d`.
- **Konsequenzgruppe:** tier-2
- **Gestrichene Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> **Fast jede gefahrene Runde ist als beschränkter Abschluss (`_b_`) geschlossen, und immer aus demselben Grund: der Abnahmelauf verlangt KRK im Vordergrund und ist damit Nutzerarbeit.** Welche das sind, sagt `ls fusion-workbench/circles/*/_b_circle.md`. Die eine Ausnahme ist die Runde `260813-0939-titelleiste-fuehrt-version-und-semantische-tags`, in der der Nutzer den Lauf selbst gefahren hat; sie schließt als bisher einzige kohärent (`_c_`).

**Nachher:**
> **Die meisten gefahrenen Runden sind als beschränkter Abschluss (`_b_`) geschlossen, und immer aus demselben Grund: der Abnahmelauf verlangt KRK im Vordergrund und ist damit Nutzerarbeit.** Welche das sind, sagt `ls fusion-workbench/circles/*/_b_circle.md`; die kohärent geschlossenen sagt `ls fusion-workbench/circles/*/_c_circle.md`. **Der Marker `_c_` heißt hier nicht „vom Nutzer abgenommen".** In zwei der kohärent geschlossenen Runden hat der Nutzer den Abnahmelauf gefahren (`260813-0939-titelleiste-fuehrt-version-und-semantische-tags` und `260818-1615-ordner-angleichen-und-abwurf-aus-fremden-apps`); in der dritten (`260817-0833-jeder-loeschweg-mit-rueckfrage-und-nur-noch-papierkorb`) ist er **nicht** gefahren, und der Marker steht trotzdem auf kohärent, weil die Directive jener Runde über die zehn Zeitzusagen aus C8 nichts sagt. Die Schließungsnotiz des jeweiligen Circle-Datensatzes schreibt es je Runde aus; welche Lesart der Marker an einem **Spec** trägt, ist offen (`shared/decisions/260819-1440_*_was-sagt-der-marker-c-an-einem-spec-gebaut-oder-abgenommen.md`).

### L13 — der Deskriptorhaushalt des Unterbaumdurchlaufs hat seit der Runde 11 eine zweite Hälfte

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 132
- **Tier:** 2
- **Zitat:** `circles/260816-1321-inhaltsfilter-mit-ankreuzfeld-content/_b_circle.md`, `## Closure note`: „der Durchlauf hält einen Verzeichnis- und höchstens einen Dateideskriptor" — genannt als eine der vier Zusagen, auf die es in jener Runde ankam. Der Satz in `CLAUDE.md`, den sie ergänzt und als vollständige Zusage überholt, ist „Der Durchlauf über den Unterbaum hält genau einen Verzeichnisdeskriptor, gleich wie tief der Baum ist". **Am Baum nachgelesen:** `crates/krk-core/src/verzeichnis/durchlauf.rs:109-114` schreibt es aus: „**Der Inhaltsfilter legt genau einen Deskriptor dazu, und nur während eines Lesens.**" Corroborierend: der offene Defekt `circles/260816-1321-inhaltsfilter-mit-ankreuzfeld-content/issues/260816-1935_o_claude-md-nennt-zwei-filterregeln-…`, Punkt 3.
- **Konsequenzgruppe:** tier-2
- **Gestrichene Vorschrift:** keine — die bestehende Zusage bleibt wörtlich stehen und bekommt ihre zweite Hälfte dazu.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> **Der Durchlauf über den Unterbaum hält genau einen Verzeichnisdeskriptor, gleich wie tief der Baum ist** (`krk-core/src/verzeichnis/durchlauf.rs`). Er liest einen Ordner ganz, merkt dessen Unterordner als **Pfad** auf einem Stapel vor und öffnet den nächsten erst danach.

**Nachher:**
> **Der Durchlauf über den Unterbaum hält genau einen Verzeichnisdeskriptor, gleich wie tief der Baum ist** (`krk-core/src/verzeichnis/durchlauf.rs`). Er liest einen Ordner ganz, merkt dessen Unterordner als **Pfad** auf einem Stapel vor und öffnet den nächsten erst danach. **Seit der Runde 11 legt der Inhaltsfilter genau einen Dateideskriptor dazu, und nur während eines Lesens:** die Datei wird geöffnet, bis zur Grenze gelesen und wieder freigegeben, bevor der nächste Kandidat drankommt. Wer daraus eine Liste offener Dateien macht, holt sich den Defekt `260815-0211` in seiner zweiten Gestalt.

### Tier 1 (10 Einträge)

### L01 — die Zahl der gefahrenen Runden steht als Zahl da und ist zum vierten Mal falsch

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 12
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe:
  ```
  $ ls fusion-workbench/circles/*/_*_circle.md | sed -E 's#.*/_([a-z])_circle.md#\1#' | sort | uniq -c
     1 a
    10 b
     3 c
     1 d
  ```
  Zehn beschränkt und drei kohärent geschlossene Runden sind gefahren, also **dreizehn**; die eine vorgesehene (`_a_`, `260804-0933`) und die eine zurückgestellte (`_d_`, `260816-2255`) sind es nicht. `CLAUDE.md:12` sagt zehn. Corroborierend: `shared/issues/260816-2138_o_claude-md-nennt-zehn-gefahrene-runden-es-sind-elf.md`, dessen eigener Titel schon veraltet ist und dessen vier Nachträge die Zahl von elf über zwölf auf dreizehn fortschreiben.
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> **Zehn Runden sind gefahren.** Wie viele es sind und wie jede geschlossen hat, sagt der Dateibestand und nicht diese Zeile: `ls fusion-workbench/circles/*/_*_circle.md`. Die Tabelle darunter ist ein Verweisregister für die Pfadregel im Absatz danach.

**Nachher:**
> **Wie viele Runden gefahren sind und wie jede geschlossen hat, sagt der Dateibestand und nicht diese Zeile:** `ls fusion-workbench/circles/*/_*_circle.md`. Beschränkt (`_b_`) und kohärent (`_c_`) geschlossene Runden zählen als gefahren, vorgesehene (`_a_`) und zurückgestellte (`_d_`) nicht. Die Tabelle darunter ist ein Verweisregister für die Pfadregel im Absatz danach.

*Anmerkung zur Ableitungsregel:* Der Eintrag schlägt die **Ableitung** vor und nicht die berichtigte Zahl. Der Satz braucht die Zahl für nichts: er führt eine Tabelle ein, und die Tabelle ist das Register. Die Zahl an dieser Stelle ist in vier Tagen viermal falsch geworden, und dieselbe Datei wendet dieselbe Überlegung schon zweimal an (auf `Kommando` und auf die Quote der Untergrenzen-Abschnitte).

### L02 — die Rundentabelle führt zehn Zeilen, drei Runden fehlen

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeilen 14–25
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe:
  ```
  $ ls fusion-workbench/circles/
  … 260816-1321-inhaltsfilter-mit-ankreuzfeld-content
  … 260817-0833-jeder-loeschweg-mit-rueckfrage-und-nur-noch-papierkorb
  … 260818-1615-ordner-angleichen-und-abwurf-aus-fremden-apps
  ```
  Diese drei Verzeichnisse führt die Tabelle nicht. Die Zuordnung zu den Rundennummern 11, 12 und 13 steht in `shared/decisions/260819-1440_o_was-sagt-der-marker-c-an-einem-spec-gebaut-oder-abgenommen.md`, Abschnitt `## Der Bestand am 260819-1440`. Der Gegenstand jeder Zeile ist aus der Schließungsnotiz des jeweiligen Circle-Datensatzes genommen.
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> | 10 | `260814-1551-tippen-filtert-dateiliste-flach-und-tief` | Tippen filtert die Dateiliste, Ankreuzfeld „Deep" für den Unterbaum |

**Nachher:**
> | 10 | `260814-1551-tippen-filtert-dateiliste-flach-und-tief` | Tippen filtert die Dateiliste, Ankreuzfeld „Deep" für den Unterbaum |
> | 11 | `260816-1321-inhaltsfilter-mit-ankreuzfeld-content` | der Filter berücksichtigt den Inhalt, Ankreuzfeld „Content" neben „Deep" |
> | 12 | `260817-0833-jeder-loeschweg-mit-rueckfrage-und-nur-noch-papierkorb` | ein Löschweg in den Papierkorb, Rückfrage vor jedem Vorgang, endgültiges Löschen fällt |
> | 13 | `260818-1615-ordner-angleichen-und-abwurf-aus-fremden-apps` | `opt+cmd+s` gleicht die Ordner der zwei Dateifenster an, Abwurf aus fremden Anwendungen |

### L03 — der Projektstand nennt `v0.4.1`, ausgeliefert ist `v0.5.4`

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 39
- **Tier:** 1
- **Zitat:** Kommandos und Ausgaben:
  ```
  $ grep -n '^version' Cargo.toml
  13:version = "0.5.4"

  $ git tag -l | sort -V | tail -8
  v0.4.1  v0.4.3  v0.4.4  v0.5.0  v0.5.1  v0.5.2  v0.5.3  v0.5.4
  ```
  Nach `v0.4.1` stehen sieben weitere Tags. Corroborierend: `shared/issues/260818-0028_o_claude-md-says-the-bundle-ships-as-v0-4-1-and-four-tags-have-been-set-since.md`, dessen Abgleich vom 260819-1440 den Abstand von vier auf sieben Tags fortschreibt. Derselbe Satz trägt daneben die Spanne „die Runden 2 bis 10", die nach L01 drei Runden zu früh endet.
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> Geprüft am 260815-0600. KRK entsteht in Rust mit AppKit über `objc2`. Der Cargo-Workspace steht, das Bündel `target/KRK.app` baut, trägt sein Symbol, ist signiert und liegt als `v0.4.1` aus. […] **Was die Runden 2 bis 10 hinzugefügt haben, steht in der Tabelle oben und wird hier nicht wiederholt**: eine Aufzählung an dieser Stelle wüchse mit jeder Runde um ein Glied, und die Tabelle führt dieselbe Auskunft schon nach Runden geordnet.

**Nachher:**
> KRK entsteht in Rust mit AppKit über `objc2`. Der Cargo-Workspace steht, das Bündel `target/KRK.app` baut, trägt sein Symbol, ist signiert und wird ausgeliefert. **Welche Version, sagt `grep '^version' Cargo.toml` und der jüngste Tag aus `git tag -l`, nicht diese Zeile** — die Zahl ist seit dem 260815 siebenmal gestiegen. […] **Was die Runden ab der zweiten hinzugefügt haben, steht in der Tabelle oben und wird hier nicht wiederholt**: eine Aufzählung an dieser Stelle wüchse mit jeder Runde um ein Glied, und die Tabelle führt dieselbe Auskunft schon nach Runden geordnet.

*Anmerkung zur Ableitungsregel:* Auch hier die Ableitung statt der berichtigten Zahl. Der Satz braucht die Version, um zu sagen, dass ein signiertes Bündel ausgeliefert wird; welche Zahl es trägt, trägt er nicht. Der Datumsstempel „Geprüft am 260815-0600" fällt mit, weil die Aussagen des Absatzes nach dieser Änderung keine Momentaufnahme mehr sind. Die drei Auslassungspunkte im Vorher- und Nachher-Text stehen für den unveränderten Mittelteil des Absatzes (die Aufzählung dessen, was die Runde 1 gebaut hat); er wird nicht angefasst.

### L05 — die Rangheuristik-Warnung rechnet mit einer Runde gegen neun

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 66
- **Tier:** 1
- **Zitat:** dieselbe Zählung wie L01: drei kohärent, zehn beschränkt geschlossene Runden. Der Satz sagt „die eine Runde … von neun Runden".
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine — die Warnung selbst bleibt vollständig, nur ihre zwei Zahlen fallen.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> Wer eine Rangheuristik über die Circles legt, die allein `_c_` als erfüllte Vorbedingung zählt, bekommt hier eine irreführende Auskunft: sie trennt die eine Runde, deren Abnahmelauf der Nutzer gefahren hat, von neun Runden, die genauso weit gebaut und nur nicht abgenommen sind.

**Nachher:**
> Wer eine Rangheuristik über die Circles legt, die allein `_c_` als erfüllte Vorbedingung zählt, bekommt hier eine irreführende Auskunft: sie trennt die wenigen Runden, deren Abnahmelauf der Nutzer gefahren hat, von den übrigen, die genauso weit gebaut und nur nicht abgenommen sind.

### L06 — der Abnahmelauf der zehn Zusagen liegt vor einer Rundenspanne, die zweimal gewachsen ist

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 78
- **Tier:** 1
- **Zitat:** `ls messungen/` liefert als jüngsten Lauf über alle zehn Zusagen `260810-1918-alle-zusagen.txt`; danach steht dort nur noch `260816-abnahme-inhaltsfilter.md`, eine Abnahmeliste ohne Messstrecke. Die Runden 11, 12 und 13 sind seit dem 260810 dazugekommen, die Spanne „5 bis 10" endet also drei Runden zu früh. Corroborierend: `circles/260817-0833-…/_c_circle.md`, `## Closure note`: „Der Abnahmelauf der zehn Zeitzusagen ist seit dem 260810 nicht gefahren, also inzwischen sieben Runden lang nicht."
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> Das ist der erste vollständig saubere Lauf, und **er liegt vor den Runden 5 bis 10** — keine der sechs ist gegen die zehn Zusagen gemessen.

**Nachher:**
> Das ist der erste vollständig saubere Lauf, und **er liegt vor jeder Runde, die seither geschlossen hat** — welche das sind, sagt `ls fusion-workbench/circles/*/_[bc]_circle.md` gegen das Datum `260810`; keine davon ist gegen die zehn Zusagen gemessen.

*Anmerkung zur Ableitungsregel:* Wieder die Ableitung statt der berichtigten Spanne. Der Satz braucht die Spanne nur, um zu sagen, dass seit dem Lauf ungemessen weitergebaut worden ist; welche Runden genau, sagt der Dateibestand. Die alte Spanne war daneben schon bei ihrer Niederschrift unscharf: die Runde 4 (`260811-1257`) liegt ebenfalls nach dem Lauf vom 260810-1918 und stand trotzdem nicht darin.

### L08 — die Aufzählung der `#[must_use]`-Stellen nennt sieben, der Baum trägt fünfundsiebzig

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 118
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe:
  ```
  $ grep -rn '^\s*#\[must_use' crates/krk-core/src | wc -l
  26
  $ grep -rn '^\s*#\[must_use' crates/krk-ui/src   | wc -l
  48
  $ grep -rn '^\s*#\[must_use' crates/krk-bench/src | wc -l
  1
  ```
  `CLAUDE.md` nennt drei Stellen in `krk-ui` und „vier Stellen in `krk-core`". Es sind 48 in `krk-ui`, 26 in `krk-core` und eine in `krk-bench`.
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** **keine.** Die Vorschrift „ein Rückgabewert, dessen stilles Fallenlassen unbemerkt bliebe, bekommt `#[must_use]`" bleibt wörtlich stehen, ebenso die Bedeutung von `let _ =` und der Hinweis, dass erst `-D warnings` daraus einen Fehler macht. Es fällt allein die Aufzählung der Fundstellen, also eine Messung des Baums.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> So entschieden vom Nutzer am 260811-2140, in der Sache schon am Defekt `260810-0423`. Es tragen: `EditorModell::bearbeiten` (`krk-ui/src/editormodell.rs`), `Auswahlversuch` und `Einzug` (`krk-ui/src/tabs.rs`), dazu vier Stellen in `krk-core`.

**Nachher:**
> So entschieden vom Nutzer am 260811-2140, in der Sache schon am Defekt `260810-0423`. **Wie viele Stellen es tragen, zählt `grep -rn '^\s*#\[must_use' crates/*/src`**; die Zahl wächst mit fast jeder Runde und steht deshalb hier nicht.

### L09 — die Aufzählung der nachzuziehenden Stellen je Kommando lässt die eine weg, die niemand hält

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 120
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe:
  ```
  $ grep -n 'andere => self.bereichskommando' crates/krk-ui/src/appkit/anwendung.rs
  3145:            andere => self.bereichskommando(fokus, andere),
  $ grep -n '_ => return false' crates/krk-ui/src/appkit/tabelle.rs
  1494:            _ => return false,
  ```
  Beide Fallunterscheidungen über `Kommando` enden auf einen Auffangzweig; ein neues Kommando ohne eigenen Zweig übersetzt, besteht jede Probe, steht mit Namen und Kombination im Hauptmenü und tut nichts. Der Absatz in `CLAUDE.md` schließt trotzdem mit „wer eine davon erweitert, bekommt vom Übersetzer die Liste der Stellen, die nachzuziehen sind". Corroborierend: `shared/issues/260818-1635_o_claude-md-nennt-zwei-nachzuziehende-stellen-je-kommando-die-dritte-haelt-kein-uebersetzer.md`, dessen Abgleich vom 260819-1440 die Gesamtrechnung auf 15 Stellen auszählt.
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine — der Eintrag ergänzt.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> Jedes neue Kommando braucht eine Zeile in `Kommando::wirkungsbereich` (`krk-core/src/tasten/belegung.rs`) und in `bereich_des_kommandos` (`krk-ui/src/belegungsmodell.rs`); jede neue Operationsart eine in `schiebt_auffrischung_auf` (`krk-ui/src/auffrischung.rs`).

**Nachher:**
> Jedes neue Kommando braucht eine Zeile in `Kommando::wirkungsbereich` (`krk-core/src/tasten/belegung.rs`) und in `bereich_des_kommandos` (`krk-ui/src/belegungsmodell.rs`); jede neue Operationsart eine in `schiebt_auffrischung_auf` (`krk-ui/src/auffrischung.rs`). **Diese Stellen hält der Übersetzer, und genau deshalb sind sie nicht die gefährlichen.** Der Ausführungszweig hält er nicht: `Anwendungsdelegierter::kommando_ausfuehren` (`krk-ui/src/appkit/anwendung.rs`) und `Tabelle::kommando_ausfuehren` (`krk-ui/src/appkit/tabelle.rs`) enden beide auf einen Auffangzweig. Ein Kommando ohne eigenen Zweig dort übersetzt, besteht jede Probe, steht mit Namen und Kombination im Hauptmenü — und tut nichts. Die vollständige Aufstellung, welche Stellen der Übersetzer hält, welche eine Probe hält und welche gar nichts hält, steht im Plan der Runde 13 (`circles/260818-1615-ordner-angleichen-und-abwurf-aus-fremden-apps/planning/260818-1633_*_plan-ordner-angleichen-und-abwurf-aus-fremden-apps.md`, Abschnitt „Was der Übersetzer einfordert, und was er nicht einfordert").

### L10 — `filter.rs` trägt drei Regeln und keine zwei, und die Ruferzahl ist nicht mehr für alle dieselbe

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 128
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe:
  ```
  $ grep -n '^pub fn ' crates/krk-core/src/verzeichnis/filter.rs
   90:pub fn traegt_ein_dateiname(zeichen: char) -> bool {
  122:pub fn traegt_die_folge(name: &str, filter_klein: &str) -> bool {
  157:pub fn inhaltsschwelle(tief: bool) -> usize {
  ```
  Der Modulkopf derselben Datei sagt es auch so: „Die drei Regeln des Filters". Die Rufer sind verschieden viele: `traegt_ein_dateiname` hat zwei (`krk-ui/src/belegungsmodell.rs`, `krk-ui/src/appkit/tabelle.rs`), `traegt_die_folge` drei (`verzeichnis/durchlauf.rs`, `verzeichnis/inhalt.rs`, `verzeichnis/modell.rs`), `inhaltsschwelle` einen (`verzeichnis/modell.rs`). `CLAUDE.md` sagt „zwei Regeln" und „Jede hat genau zwei Rufer". Corroborierend: `circles/260816-1321-inhaltsfilter-mit-ankreuzfeld-content/issues/260816-1935_o_…`, Punkt 1.
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine — die Zusage „jede Regel steht genau einmal da, und eine Zählprobe hält das fest" bleibt.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> Das Modul heißt jetzt `krk-core/src/verzeichnis/filter.rs` und trägt zwei Regeln, die je genau einmal dastehen: `traegt_ein_dateiname` entscheidet, welches Zeichen aufgenommen wird, und `traegt_die_folge` ist der eine Vergleich. Jede hat genau zwei Rufer, und eine Zählprobe hält das fest — sie hat während der Runde einen dritten gefangen, und behoben wurde die Wurzel und nicht die Zahl in der Probe.

**Nachher:**
> Das Modul heißt jetzt `krk-core/src/verzeichnis/filter.rs` und trägt seit der Runde 11 drei Regeln, die je genau einmal dastehen: `traegt_ein_dateiname` entscheidet, welches Zeichen aufgenommen wird, `traegt_die_folge` ist der eine Vergleich, und `inhaltsschwelle` sagt, ab welcher Länge des Filtertexts auch der Inhalt gelesen wird. Wie viele Rufer jede hat, sagt die Zählprobe `die_zeichenregel_und_der_vergleich_stehen_je_einmal_…` in `crates/krk-core/tests/verzeichnis.rs` und nicht diese Zeile — sie hat während der Runde 10 einen unerlaubten Rufer gefangen, und behoben wurde die Wurzel und nicht die Zahl in der Probe.

### L11 — `bis_zur_grenze_lesen` steht nicht mehr in `krk-ui`

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 136
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe:
  ```
  $ grep -rn 'fn bis_zur_grenze_lesen' crates/
  crates/krk-core/src/text/datei.rs:605:pub fn bis_zur_grenze_lesen(pfad: &Path, grenze: u64) -> Result<Vec<u8>, Lesehindernis> {
  ```
  In `krk-ui/src/vorschaumodell.rs` steht die Funktion nicht; wer dem Verweis folgt, findet dort nichts. Die zwei Aufrufer von `ohne_warten_oeffnen` liegen beide in `crates/krk-core/src/text/datei.rs` (`:421` und `:606`). Corroborierend: `circles/260816-1321-…/issues/260816-1935_o_…`, Punkt 2.
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> **Die Hülle hat zwei Aufrufer**, den Editor (`krk-core/src/text/datei.rs`, `oeffnen`) und seit der Runde 2 auch die Vorschau (`krk-ui/src/vorschaumodell.rs`, `bis_zur_grenze_lesen`).

**Nachher:**
> **Die Hülle hat zwei Aufrufer, und beide liegen seit der Runde 11 in `krk-core/src/text/datei.rs`:** `oeffnen` für den Editor (`:421`) und `bis_zur_grenze_lesen` (`:605`) für die Vorschau und den Inhaltsfilter. Die Vorschau ruft `bis_zur_grenze_lesen` und nicht mehr selbst die Hülle.

### L12 — `bin/fusion-rules` gibt für Langform-Agenten `default-voice-en.yaml` aus

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 176
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe, aus dem Setup dieses Laufs:
  ```
  $ "$FUSION_PLUGIN_ROOT/bin/fusion-rules" curator
  …
  ./fusion-workbench/stilwerk/chat-voice-de.yaml
  ./fusion-workbench/stilwerk/default-voice-en.yaml
  ```
  `CLAUDE.md:176` sagt, der Helfer gebe „für Langform-Agenten `fusion-workbench/stilwerk/default-voice-de.yaml`" aus. Er gibt `default-voice-en.yaml` aus, weil `CLAUDE.md:4` seit `c75972c` `**Artifact language:** en` deklariert und nach `rules/fusion-workbench-conventions.md` `## Project language` das Schreibprofil aus der **Artefaktsprache** auflöst und nicht aus `**Language:**`.
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine. Die Schutzklausel „Zeile nicht umformulieren, nicht verschieben und nicht entfernen" bleibt wörtlich stehen und gilt nach dieser Änderung für beide Deklarationszeilen.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> Die Zeile `**Language:** de` oben deklariert Deutsch als Projektsprache. Sie steuert, welche Stilprofile unter `fusion-workbench/stilwerk/` gelten: `$FUSION_PLUGIN_ROOT/bin/fusion-rules` gibt daraufhin `fusion-workbench/stilwerk/chat-voice-de.yaml` und, für Langform-Agenten, `fusion-workbench/stilwerk/default-voice-de.yaml` aus. Ohne die Zeile fällt die Auflösung still auf `en` zurück.

**Nachher:**
> Die zwei Zeilen oben deklarieren zwei verschiedene Sprachen, und jede steuert ein anderes Stilprofil unter `fusion-workbench/stilwerk/`. `**Language:** de` ist die **Chatsprache** und steuert das Kurzform-Profil, das jeder Agent auf seine Ausgabe im Terminal anwendet; `$FUSION_PLUGIN_ROOT/bin/fusion-rules` gibt dafür `fusion-workbench/stilwerk/chat-voice-de.yaml` aus. `**Artifact language:** en` ist die **Artefaktsprache** und steuert das Langform-Schreibprofil; der Helfer gibt dafür `fusion-workbench/stilwerk/default-voice-en.yaml` aus, und zwar nur an die Agenten, die Langform-Prosa schreiben. Ohne die erste Zeile fällt die Auflösung still auf `en` zurück; ohne die zweite steuert die erste beide Flächen.

**Nicht Gegenstand dieses Eintrags:** ob die Artefaktsprache `en` bleiben soll, wo der ganze Bestand deutsch ist. Das ist der offene Widerspruch aus Abschnitt 8; dieser Eintrag beschreibt allein den Mechanismus richtig, und seine Formulierung bleibt richtig, wie der Nutzer die Frage auch entscheidet.

### Zusammenführungen (0 Einträge)

Keine.

---

## 8. Ein Widerspruch, den ich nicht auflösen darf

**`CLAUDE.md:4` gegen `CLAUDE.md:178`.** Die Kopfzeile `**Artifact language:** en` verlangt nach `rules/fusion-workbench-conventions.md` `## Project language`, dass jede Datei, die für den eigenen Gebrauch des Projekts persistiert — Specs, Pläne, Defekt- und Entscheidungsdatensätze, Sitzungsprotokolle, Durchsichten, Analysen — auf Englisch geschrieben wird. Der Schlusssatz des Abschnitts `## Sprache` sagt „Prosa in diesem Projekt ist deutsch". Ein Agent kann nicht beiden folgen.

**Beide Seiten sind lebendig und vertretbar.** Die Kopfzeile hat der Nutzer selbst gesetzt, von Hand, im Commit `c75972c` vom 260817. Der Schlusssatz beschreibt, was der Bestand tut: Spec, Plan, beide Durchsichten und die Entscheidungsdatensätze der Runde 13 sind deutsch, englisch sind allein elf Defektdatensätze aus zwei `coderev`-Durchgängen. Ich fasse deshalb keine der beiden Zeilen an.

Abgelegt als offener Entscheidungsdatensatz: `shared/decisions/260819-1500_o_gilt-die-artefaktsprache-en-fuer-den-ganzen-bestand-oder-wird-die-deklaration-zurueckgenommen.md`.

Corroborierend, aber nicht tragend: `shared/issues/260817-1610_o_the-language-paragraph-in-claude-md-predates-the-artifact-language-declaration.md`, dessen Abgleich vom 260819-1440 meldet, dass der Widerspruch dem Abgleich von heute unmittelbar Kosten verursacht hat.

## 9. Kandidaten — nicht angeboten und nicht anwendbar

**K01 — zwei überholte Entscheidungsdatensätze tragen die Zeile `Superseded by:` zweimal.**
`circles/260811-1304-statusleiste-mit-bereichsschaltern/decisions/260811-1305_s_ist-die-neue-leiste-die-statuszeile-aus-c1-oder-eine-zweite-flaeche.md` und `circles/260813-0939-titelleiste-fuehrt-version-und-semantische-tags/decisions/260813-0939_s_wer-setzt-den-ersten-tag-v0-1-0-und-wann.md` führen je eine leere Vorlagenzeile `Superseded by:` und darunter die gefüllte. Ein `grep -m1 'Superseded by:'` liest die leere und meldet „nicht überholt". **Warum Kandidat:** Das ist ein Formfehler und kein Widerspruch zwischen bindenden Aussagen und keine geschichtlich begründete Überholung; keiner meiner zwei Änderungsgründe deckt ihn. Er gehört in einen Abgleich.

**K02 — der Datumsstempel „Am 260815" an drei Stellen.**
`CLAUDE.md:74`, `:170` und `:172` datieren ihre Aussagen auf den 260815. Alle drei sind heute nachgeprüft und **halten**: `Wirkungsbereich` trägt sieben Werte, `Bereich` und `Fokus` je fünf; `Kommando` trägt keine Git-Variante; `ls fusion-workbench/circles/*/_a_circle.md` liefert genau einen Circle, den Web-Betrachter. **Warum Kandidat:** Es gibt nichts zu berichtigen. Der Stempel altert zwar, aber ein Stempel ist keine falsche Behauptung, und ein Umschreiben allein aus dem Wiederlesen des Texts wäre genau die Änderung, die meine eigene Regel verbietet.

**K03 — die Zusage über die drei Prüfordner-Fassungen.**
`CLAUDE.md:114` sagt „genau drei Fassungen, eine je Kiste". Nachgeprüft: `crates/krk-core/tests/gemeinsam/mod.rs`, `crates/krk-ui/src/pruefordner.rs`, `crates/krk-bench/src/wegwerfordner.rs` — drei, und `crates/krk-core/tests/baum.rs:116-118` hält die Liste als Probe fest. Der offene Defekt `circles/260813-0939-…/issues/260813-1110_o_eine-vierte-wegwerfordner-fassung-steht-in-xtask-…` behauptet eine vierte in `xtask`. **Warum Kandidat:** Ein offener Defekt widerspricht meinem Befund, und das ist nach `agents/curator.md` `### How the defect corpus is used` ein Halt. Nachgesehen habe ich: `grep -rn 'struct Wegwerfordner' xtask/src/` liefert keine Zeile, `xtask/src/release.rs:1068` nennt das Wort nur in einem Kommentar. **Inference:** Der Defekt ist wahrscheinlich behoben und nur nicht geschlossen worden — aber das ist eine Feststellung für einen Abgleich, nicht für mich, und `CLAUDE.md` ist in beiden Fällen unverändert richtig.

## 10. Arbeit außerhalb meiner Zuständigkeit

**Keine.** Kein Eintrag dieses Durchgangs verlangt Code, eine Probe, einen Helfer oder eine erzeugte Tabelle; alle dreizehn sind reine Textänderungen an `CLAUDE.md`. Es ist deshalb kein Defektdatensatz für einen anderen Ausführenden angelegt worden.

**Keine Testvorrichtung ist betroffen.** In fusions eigenem Repository pinnt `hooks/lib/__tests__/fixtures/rules-emission.golden` die Größen der Regeldateien; hier gibt es keine projekteigenen Regeldateien und keine Vorrichtung, die `CLAUDE.md` misst. Ein Anwendungsdurchgang lässt keine Probe rot werden.

## 11. Ergebnisse des Anwendungsdurchgangs

Gefahren am 260819 gegen den Baumstand `82c020a`, denselben, den die Erhebung gelesen hat. Zustimmung: `all`.

**Vor jedem Eintrag ist sein Vorher-Text von der Platte gelesen und gegen das Änderungsbuch gehalten worden.** Alle dreizehn stimmten byteweise überein; `CLAUDE.md` maß beim Beginn des Anwendungsdurchgangs unverändert 34274 Bytes und 178 Zeilen, und `git status` meldete die Datei sauber. Kein Eintrag ist deshalb als `stale` ausgefallen.

| Eintrag | Gruppe | Ergebnis |
|---|---|---|
| L01 | tier-1 | applied |
| L02 | tier-1 | applied |
| L03 | tier-1 | applied (zwei Teilersetzungen in derselben Zeile: der Versionssatz und die Rundenspanne) |
| L04 | tier-2 | applied |
| L05 | tier-1 | applied |
| L06 | tier-1 | applied |
| L07 | Streichung einer Vorschrift | applied |
| L08 | tier-1 | applied |
| L09 | tier-1 | applied |
| L10 | tier-1 | applied |
| L11 | tier-1 | applied |
| L12 | tier-1 | applied |
| L13 | tier-2 | applied |

**Dreizehn angewandt, null übersprungen, null veraltet, null gescheitert.** Kein Schreibvorgang ist von der Rechtekonfiguration abgewiesen worden.

**Die gestrichene Vorschrift, in einer durchsuchbaren Zeile:** L07 hat aus `CLAUDE.md` den Halbsatz „den Tag setzt der Nutzer, nicht das Werkzeug" entfernt — die Anweisung, vor einer Auslieferung den Tag `v<version>` von Hand auf HEAD zu setzen. Sie ist am 260813-1534 vom Nutzer zurückgenommen worden; seither taggt `cargo xtask release` selbst.

**Streichungen je Tier:** Tier 3: null. Tier 2: null (beide Einträge ergänzen). Tier 1: null (die Zahlen und Aufzählungen, die fallen, sind Messungen des Baums und keine Vorschriften). Streichung einer Vorschrift: eine, L07.

**Flächengrößen nach dem Durchgang.** `CLAUDE.md` steht bei 37896 Bytes und 181 Zeilen, gegen 34274 Bytes und 178 Zeilen vorher: 3622 Bytes und drei Zeilen mehr. Die Datei ist gewachsen, obwohl eine Vorschrift gefallen ist, weil zehn der dreizehn Einträge eine Zahl durch ein Kommando und seinen Grund ersetzen und das mehr Zeichen braucht als die Zahl. `git diff --stat` zählt 15 Einfügungen gegen 12 Streichungen. An den 163 Entscheidungsdatensätzen ist nichts geändert worden; projekteigene Regeldateien gibt es nicht.

**Rückweg:** `git checkout -- CLAUDE.md` stellt den Stand vor diesem Durchgang wieder her. Die Datei ist versioniert und war beim Beginn sauber, der Rückweg besteht also für alle dreizehn Einträge zusammen und für jeden einzeln über `git diff`.

**Nicht festgeschrieben.** Der Durchgang hinterlässt eine Änderung im Arbeitsbaum und hat nichts eingecheckt.

**Eine Folge, die ich nicht selbst behebe: die Zeilennummern in `CLAUDE.md` sind um drei verrutscht.** L02 hat drei Tabellenzeilen eingefügt, und alles darunter ist um drei nach hinten gewandert: `:106` → `:109`, `:176` → `:179`, `:178` → `:181`. Der offene Entscheidungsdatensatz, den die Erhebung angelegt hat (`shared/decisions/260819-1500_o_gilt-die-artefaktsprache-en-fuer-den-ganzen-bestand-oder-wird-die-deklaration-zurueckgenommen.md`, Zeile `**Cross-references:**`), zitiert noch `CLAUDE.md:4` gegen `CLAUDE.md:178`; gemeint ist jetzt `:181`. Das ist ein veralteter Verweis und damit Tier 1 — **aber an einem Entscheidungsdatensatz, und der ist eine bestehende Aussage auf einer meiner drei Flächen.** Sie zu ändern verlangt einen eigenen Gang durchs Gate, und dieser Eintrag stand nicht im zugestimmten Änderungsbuch. Er ist deshalb hier festgehalten und nicht angewandt; der nächste Kuratorenlauf findet ihn als Tier-1-Eintrag vor. Die Zeilennummern in **dieser** Datei bleiben unangetastet: sie schreiben auf, was die Erhebung gelesen hat, und fallen unter die Ortsregel aus `CLAUDE.md` `## Bindende Grundlage` — eine Aufzeichnung eines Standes behält ihren damaligen Stand.
