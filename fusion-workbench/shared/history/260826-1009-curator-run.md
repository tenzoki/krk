# Kuratorenlauf — 260826-1009

**Status:** Complete. Erhebung am 260826-1009, Anwendungsdurchgang am 260826-1014, beide gegen `2dce7d5`. Der eine freigegebene Eintrag L01 ist angewandt; sonst ist keine Datei angefasst.
**Datum:** 260826-1009 (Erhebung)
**Git HEAD beim Lesen:** `2dce7d5` (`docs(workbench): die Durchsicht der neun Aussagen und der Filtervorgabe, der Stand kann ausgeliefert werden`, 2026-08-26 09:31:13 +0200). `CLAUDE.md` ist im Arbeitsbaum unverändert gegenüber HEAD (`git status --porcelain -- CLAUDE.md` ist leer).
**Modus:** survey (260826-1009), danach apply (260826-1014). Der Anwendungsdurchgang kam mit `**Mode:** apply`, dem Pfad dieser Datei und der einen Kennung `L01`. Die Freigabe stammt aus der Wahl des Nutzers vom 260826, „den falschen Halbsatz noch richten" aus drei vorgelegten Möglichkeiten, und deckt genau diesen Gegenstand.
**Vorheriger Kuratorenlauf:** `shared/history/260826-0818-curator-run.md`, Baumstand `d08dbac`, neun Einträge, alle neun angewandt in `fb50fcd`. Davor `260821-2231`, `260820-2127`, `260820-1119`, `260819-1500` und `260814-1405`.

**Gegenstand und Grenze dieses Laufs.** Der Auftrag nennt einen einzigen Gegenstand: die Rundenzuschreibung, die der Eintrag L07 des vorigen Laufs neu in `CLAUDE.md` gesetzt hat. Der Lauf erhebt diesen einen Gegenstand und die eine Frage, ob dieselbe Zuschreibung an einer zweiten Stelle derselben Datei steht. Er erhebt nichts sonst. Die drei Kandidaten K01 bis K03 des vorigen Laufs bleiben unangetastet, und die Defektdatensätze bleiben unangetastet: ihre Marker gehören dem `reconciler`.

**Sprache dieser Datei.** Deutsch. `CLAUDE.md:3` deklariert `**Language:** de` als einzige Sprachzeile; nach der Fallback-Kette aus `$FUSION_PLUGIN_ROOT/rules/fusion-workbench-conventions.md` `## Project language` steuert sie beide Flächen. `bin/fusion-rules` gibt `stilwerk/chat-voice-de.yaml` und `stilwerk/default-voice-de.yaml` aus; beide sind gelesen.

---

## 1. Warum dieser Lauf überhaupt nötig ist

Der Lauf `260826-0818` hat mit seinem Eintrag L07 einen Absatz in `CLAUDE.md` neu gefasst und dabei geschrieben, am Melder des Ersthelferwechsels hingen „seit der Runde 14" zwei Empfänger. Die Zuschreibung stammt nicht aus einer eigenen Messung jenes Laufs, sondern wörtlich aus dem Datensatz, den er umgesetzt hat: `shared/issues/260823-1336_o_claude-md-nennt-einen-empfaenger-der-ersthelfermeldung-der-baum-traegt-seit-dem-260819-zwei.md` schreibt zweimal „Runde 14", einmal im Rumpf und einmal in seiner Belegzeile. Der Lauf hat den Datensatz geglaubt statt ihn nachzumessen.

Die Ironie gehört in die Begründung und nicht in die neue Fassung: derselbe Commit `fb50fcd` behebt an vier anderen Stellen genau diese Fehlerklasse, nämlich eine Arbeit, die einer Runde zugeschrieben wird, obwohl sie ohne aktiven Circle entstanden ist. L07 hat sie im selben Commit ein fünftes Mal gesetzt. Der Befund ist von der Durchsicht `shared/reviews/260826-0923-coderev-neun-aussagen-in-claude-md-und-die-vorgabe-des-dateifilters.md`, Befund 1, erhoben und als `shared/issues/260826-0923_o_claude-md-schreibt-den-zweiten-empfaenger-der-runde-14-zu-er-landete-ohne-aktiven-circle.md` abgelegt.

**Dieser Lauf glaubt auch diesen Datensatz nicht.** Jedes Kommando in Abschnitt 6 ist am 260826-1009 gegen `2dce7d5` selbst gefahren. Der Datensatz stimmt in jeder geprüften Zahl; das ist ein Ergebnis der Messung und nicht ihre Voraussetzung.

## 2. Die acht Belegquellen, und was in jeder gelesen wurde

Der Lauf ist auf einen Gegenstand verengt, und seine Quellenzählung sagt das offen. Wo eine Quelle nur durchsucht und nicht gelesen wurde, steht das dabei.

| # | Quelle | Gelesen |
|---|---|---|
| 1 | Circle-Datensätze (`circles/`) | **0 vollständig.** Der Bestand führt 19 Datensätze. Zwei sind über ihre Git-Historie befragt, nicht über ihren Inhalt: der Abschluss der Runde 13 und die Anlage der Runde 14. Für die Frage, wann ein Circle aktiv war, ist das Anlage- und Umbenennungsdatum die Auskunft, und der Inhalt trägt nichts dazu bei. |
| 2 | Entscheidungsdatensätze (`shared/decisions/` 41, `circles/*/decisions/` 145) | **0 vollständig**, 186 durchsucht (`grep -rl 'aktives_dem_ersthelfer_nachziehen'`). Kein Entscheidungsdatensatz außerhalb des Archivs nennt den zweiten Empfänger. |
| 3 | Sitzungsberichte (`shared/history/`, 140) | **1 gelesen**, der vorige Kuratorenlauf `260826-0818` (Kopf, Folgengruppen-Tabelle, Eintrag L07). Vier weitere sind als Treffer der Volltextsuche bekannt und nicht geöffnet. |
| 4 | Git-Historie | **`git log --follow -- CLAUDE.md`: 30 Commits**, davon die jüngsten fünf gelesen. Daneben vier gezielte Läufe über den Baum, alle in Abschnitt 6 mit Kommando und Ausgabe. Der Lauf hat die 30 Commits nicht einzeln geöffnet; für einen Gegenstand, dessen Entstehung ein `-S`-Lauf punktgenau datiert, wäre das Aufwand ohne Ertrag. |
| 5 | Durchsichten und Untersuchungen (`shared/reviews/` 21, `shared/analyses/` 4) | **1 gelesen**, `260826-0923-coderev-…` (Kopf, Zusammenfassung, Zahlen, Teil 1 und Befund 1). |
| 6 | `orchestrator-events.jsonl` | **1626 Zeilen durchsucht, 1 Treffer**, und der gilt einem anderen Commit (`fd361d7` vom 260825). Für `76ceb68` führt das Ereignisprotokoll **keinen** Eintrag. Das passt zum Befund, trägt ihn aber nicht: die Quelle ist ausdrücklich nur bestätigend, und eine Abwesenheit im Protokoll wäre auch für sich genommen kein Beleg. |
| 7 | Archiv (`archive/`, 80 Dateien) | **0 gelesen**, 80 durchsucht. Zwei Treffer, beide aus `archive/260819-1613-safe-cleanup-tier-1/shared/`, beide der Vorgang selbst und nicht seine Rundenzuschreibung. |
| 8 | `**Provenance:**`-Köpfe projekteigener Regeldateien | **0 — die Fläche fehlt.** Weder `./rules/` noch `.claude/rules/` liegt im Projekt (`ls` meldet beide als nicht vorhanden). |

**Zwei der drei Flächen sind in diesem Lauf gar nicht betroffen.** Die projekteigenen Regeldateien fehlen als Fläche vollständig. Die Entscheidungsdatensätze sind gelesen worden, tragen aber keinen Eintrag: der Gegenstand liegt allein in `CLAUDE.md`.

## 3. Flächengrößen

| Fläche | Vorher | Nachher (bei voller Freigabe) |
|---|---|---|
| `CLAUDE.md` | 49.793 Bytes, 195 Zeilen | 49.803 Bytes, 195 Zeilen |
| Entscheidungsdatensätze | 186 Dateien, unverändert | 186 Dateien, unverändert |
| Projekteigene Regeldateien | Fläche fehlt | Fläche fehlt |

Der einzige Eintrag tauscht „der Runde 14" gegen „dem 260819" und hängt den Commit in Klammern an. Die Zeilenzahl bleibt gleich, die Bytezahl steigt um 10: zwölf Zeichen kommen mit „ (`76ceb68`)" hinzu, zwei fallen mit der kürzeren Zeitangabe weg.

**Sprengweite: unauffällig.** Der Lauf löscht 12 Bytes von 49.793 und schreibt 22 zurück, also eine Löschung von 0,024 Prozent einer Fläche. Die Schwelle von 20 Prozent ist nicht in Sicht, und es gibt nichts vorab zu bestätigen.

## 4. Vergleichszählungen über die sechs Flächenpaare

**Die Auswahlregel dieses Laufs ist eine einzige, und sie ist eng:** eine Aussage kommt in den Vergleich, wenn sie eine Zeitangabe an die Empfänger des Ersthelfer-Melders oder an die eigenen Textflächen knüpft. Der Auftrag verengt den Lauf auf diesen Gegenstand, und eine breitere Regel wäre eine Erhebung, die hier niemand bestellt hat. Jedes Verdikt unten gilt den Paaren, die diese Regel erreicht hat, und nicht dem Bestand.

| Paar | Kandidaten nach der Regel | Gelesen | Verdikt |
|---|---|---|---|
| Datensatz gegen Datensatz | 2 (`260823-1336_o` gegen `260826-0923_o`) | 2 | Die beiden widersprechen sich in der Rundenzuschreibung und stimmen im Datum überein. Siehe Abschnitt 5. |
| Datensatz gegen Regeldatei | 0 | 0 | Die Fläche fehlt. |
| Datensatz gegen `CLAUDE.md` | 2 | 2 | `260826-0923_o` widerspricht `CLAUDE.md:141`; das ist der Eintrag L01. `260823-1336_o` stimmt mit `CLAUDE.md:141` in der falschen Zuschreibung überein, und beide sind falsch. |
| Regeldatei gegen Regeldatei | 0 | 0 | Die Fläche fehlt. |
| Regeldatei gegen `CLAUDE.md` | 0 | 0 | Die Fläche fehlt. |
| `CLAUDE.md` gegen sich selbst | 4 (`grep -n 'Runde 14' CLAUDE.md`) | 4 | Kein Widerspruch. Drei der vier Stellen halten; die vierte ist L01. Siehe Abschnitt 7. |

## 5. Der offene Datensatz, der in die Gegenrichtung zeigt

Die Regel verlangt, jeden Eintrag gegen die offenen Defektdatensätze zu halten und einen Eintrag herabzustufen, sobald ein offener Datensatz das Gegenteil behauptet. Hier stehen zwei offene Datensätze zum selben Gegenstand, und sie widersprechen einander:

- `shared/issues/260823-1336_o_…` sagt im Titel selbst „der Baum trägt **seit dem 260819** zwei", nennt im Rumpf und in der Belegzeile aber zusätzlich „Runde 14".
- `shared/issues/260826-0923_o_…` sagt, die Rundenzuschreibung sei falsch, und nennt das Datum.

**Der Widerspruch ist keiner in der Sache, sondern eine Etikettierung.** Beide Datensätze datieren den zweiten Empfänger auf den 260819. Sie unterscheiden sich allein darin, ob dieser Tag der Runde 14 zugeschlagen wird. Diese Frage ist am Baum entschieden und nicht durch Abwägung: Abschnitt 6 misst sie mit drei Kommandos. Ein Eintrag der Stufe 1 steht auf einer eigenen Messung, und keine der beiden Prosastellen hebt sie auf. Der Eintrag L01 bleibt deshalb ein Eintrag und wird nicht zum Kandidaten herabgestuft.

**Die zweite Stelle in `260823-1336_o` bleibt in diesem Lauf stehen.** Der Auftrag verengt den Lauf auf `CLAUDE.md`, und ein Defektdatensatz ist ohnehin keine der drei normativen Flächen. Dass die Zuschreibung dort mitzuberichtigen ist, steht bereits im Vorschlag von `260826-0923_o`; es ist abgelegt und braucht keinen neuen Datensatz. **Solange sie dort steht, kann sie ein späterer Lauf wieder nach `CLAUDE.md` tragen** — genau der Weg, den sie schon einmal genommen hat.

## 6. Die Messung, selbst gefahren am 260826-1009 gegen `2dce7d5`

Vier Kommandos, jedes mit seiner Ausgabe.

**Wann der zweite Empfänger in den Baum kam.**

```
git log --format='%h %ad %s' --date=iso -S 'aktives_dem_ersthelfer_nachziehen' \
    -- crates/krk-ui/src/appkit/anwendung.rs
  → f8304a7  2026-08-25 22:23:32 +0200  test(ui): aktives_setzen hat genau zwei Aufrufer, …
  → d3da6e3  2026-08-25 19:00:26 +0200  fix(ui): der Klick auf die Tableiste nimmt den Ersthelferrang mit
  → fd361d7  2026-08-25 18:32:43 +0200  fix(ui): der Tabbefehl nimmt den Ersthelferrang mit
  → 76ceb68  2026-08-19 11:20:18 +0200  fix(ui): ein Klick unter die letzte Zeile holt das
             Dateifenster als aktives
```

Der älteste Treffer ist die Einführung: **`76ceb68`, 2026-08-19 um 11:20**. Die Commit-Nachricht schreibt es selbst aus: „Der hatte einen Empfaenger und hat jetzt zwei, den neuen zuerst, damit die Anzeige schon mit dem neuen aktiv rechnet."

**Welcher Circle zu diesem Zeitpunkt aktiv war.**

```
git log --format='%h %ad %s' --date=iso \
    -- 'fusion-workbench/circles/260818-1615-ordner-angleichen-…/_c_circle.md'
  → c09ff3a  2026-08-19 08:12:00 +0200  die Runde 13 schliesst kohaerent, …

git log --reverse --diff-filter=A --format='%h %ad %s' --date=iso \
    -- 'fusion-workbench/circles/260819-2230-auswahl-und-kopieren-in-der-vorschau/*circle.md'
  → 258bd7c  2026-08-19 22:31:40 +0200  die Runde 14 ist geformt und aktiv, …
             (angelegte Datei: …/_t_circle.md)
```

Die Runde 13 war um **08:12** geschlossen, die Runde 14 wurde um **22:31** aktiv. `76ceb68` liegt um 11:20 dazwischen, gut elf Stunden vor dem Bestehen der Runde 14.

**Ob der Commit einer Runde zugehört.**

```
git show --name-only --format='' 76ceb68
  → crates/krk-ui/src/appkit/anwendung.rs
  → crates/krk-ui/src/appkit/tabelle.rs
  → fusion-workbench/shared/analyses/260819-1043-klick-holt-den-fokus-nicht.md
  → fusion-workbench/shared/decisions/260819-1043_a_welche-flaechen-holen-den-fokus-….md
  → fusion-workbench/shared/history/260819-1043-klick-holt-den-fokus-nicht.md
  → fusion-workbench/shared/history/260819-1210-klick-unter-die-letzte-zeile-….md
  → fusion-workbench/shared/issues/260819-0900_c_ein-klick-in-das-dateifenster-….md
  → fusion-workbench/shared/issues/260819-1043_c_ein-klick-unter-die-letzte-zeile-….md
```

Acht Dateien, davon sechs Werkbankartefakte, und **jedes einzelne liegt unter `shared/`**. Kein Circle-Verzeichnis ist berührt. Nach der Herkunftsregel ist das die Ablage einer Arbeit ohne aktiven Circle. Der Commit ist eine Defektbehebung auf eine Meldung des Nutzers vom selben Tag, zwischen zwei Runden gefahren.

**Was am Melder heute hängt.**

```
sed -n '1225,1230p' crates/krk-ui/src/appkit/anwendung.rs
  → fenster.melder_setzen(Box::new(move || {
  →     if let Some(selbst) = schwach.load() {
  →         selbst.aktives_dem_ersthelfer_nachziehen();
  →         selbst.fokusanzeige_nachziehen();
  →     }
  → }));
```

Zwei Empfänger, in der Reihenfolge, die der Absatz nennt. Die zweite Hälfte der L07-Fassung hält also unverändert; berichtigt wird allein ihre Zeitangabe.

**Der Quelltext selbst nennt das Datum und keine Runde.** `crates/krk-ui/src/appkit/anwendung.rs:8679`, der Doc-Kommentar der Probe `der_nachzug_der_anzeige_ruehrt_die_auslegung_nicht_an`: „**Seit dem 260819** traegt sie ihr Gewicht wirklich." Die neue Fassung übernimmt damit die Formulierung, die im Baum ohnehin steht.

## 7. Die zweite Stelle: sie meint eine andere Sache, und sie hält

`grep -n 'Runde 14' CLAUDE.md` liefert vier Zeilen. Drei davon sind nachgemessen, weil sie derselben Fehlerklasse angehören könnten.

**Zeile 137, der Absatz über den Ereignisabgriff: „Es sind seit der Runde 14 zwei".** Der Auftrag fragt, ob das dieselbe Sache meint. **Es ist eine andere.** Gezählt werden dort nicht die Empfänger des Melders, sondern KRKs eigene bedienbare Textflächen: die Textfläche des Editors und die Textanzeige der Vorschau, gehalten von `Anwendungsdelegierter::ist_eigene_textflaeche`. Der Gegenstand ist ein anderer, und die Zuschreibung trägt:

```
git log --format='%h %ad %s' --date=iso -S 'ist_eigene_textflaeche' \
    -- crates/krk-ui/src/appkit/anwendung.rs
  → 6531f38  2026-08-20 06:19:27 +0200  feat(ui): aus einer eigenen Textflaeche werden zwei,
             und die Tasten wirken wieder

git show --name-only --format='' 6531f38 | grep circles
  → fusion-workbench/circles/260819-2230-auswahl-und-kopieren-in-der-vorschau/history/
        260820-0617-coder-schritt-4-anmeldung-ereignisabgriff.md
  → fusion-workbench/circles/260819-2230-auswahl-und-kopieren-in-der-vorschau/planning/
        260819-2245_o_plan-auswahl-und-kopieren-in-der-vorschau.md
```

Der Commit liegt am 260820 um 06:19, also zwischen dem Aktivwerden der Runde 14 (260819-2231) und ihrem Abschluss (`2beb1de`, 260820-1051), und er legt seine Artefakte in **deren** Circle-Verzeichnis ab. Derselbe Commit trägt auch die Umbenennung von `ist_editorflaeche`, die der Absatz nennt, und die Anmeldung der Vorschau, die derselbe Absatz weiter unten ebenfalls der Runde 14 zuschreibt. **Alle drei Zuschreibungen der Zeile 137 halten. Kein Eintrag.**

**Zeile 79, die Hülle um `NSPasteboard`: „nimmt seit der Runde 14 zusätzlich eine hereingereichte Ablage entgegen".** Ebenfalls nachgemessen, weil sie dieselbe Form hat:

```
git log --format='%h %ad %s' --date=iso -S 'text_auf_ablage_schreiben' \
    -- crates/krk-ui/src/appkit/zwischenablage.rs
  → 9e10b94  2026-08-20 05:15:27 +0200  feat(ui): die eine Huelle um NSPasteboard nimmt
             jede Ablage entgegen
```

Auch dieser Commit liegt innerhalb der Runde 14. **Hält. Kein Eintrag.**

**Zeile 42** nennt die Runde 14 im Zusammenhang mit der Zusage L7 und trägt keine Datierung einer Codeänderung; sie fällt nicht unter die Auswahlregel und ist nicht geprüft.

**Damit ist die Antwort auf die Frage des Auftrags: nein.** Von den vier „Runde 14"-Stellen in `CLAUDE.md` ist genau eine falsch, und es ist die, die L07 gesetzt hat. Die drei anderen sind am Baum belegt.

## 8. Vorheriger Inhalt der Entscheidungsdatensätze

**Keiner.** Der Lauf ändert keinen Entscheidungsdatensatz. Die Pflicht, den vollständigen Inhalt jedes anzufassenden Datensatzes hier abzulegen, ist damit gegenstandslos und nicht etwa übergangen.

## 9. Die Änderungsliste

Ein Eintrag. Die Kennung `L01` ist hier vergeben und steht in dieser Datei, damit eine Freigabe einzelner Einträge ein Gate übersteht, das die Liste selbst nicht zeigt.

| Folgengruppe | Einträge | Zahl |
|---|---|---|
| Zusagenrücknahme | — | 0 |
| Stufe 3 | — | 0 |
| Stufe 2 | — | 0 |
| Stufe 1 | L01 | 1 |
| Verdichtung | — | 0 |
| Kandidaten (nicht zur Freigabe) | — | 0 |

### L01 — Der zweite Empfänger kam am 260819 und nicht mit der Runde 14

- **Fläche:** `CLAUDE.md`
- **Datei:** `CLAUDE.md:141`
- **Stufe:** 1
- **Beleg:** Selbst gefahren am 260826-1009 gegen `2dce7d5`, vier Kommandos mit Ausgabe in Abschnitt 6. `git log -S 'aktives_dem_ersthelfer_nachziehen' -- crates/krk-ui/src/appkit/anwendung.rs` datiert die Einführung auf `76ceb68`, **2026-08-19 11:20:18**. Die Runde 13 war um **08:12** geschlossen (`c09ff3a`), die Runde 14 wurde um **22:31** aktiv (`258bd7c`, legt `_t_circle.md` an). `git show --name-only 76ceb68` führt acht Dateien, sechs davon Werkbankartefakte, **alle unter `shared/`** und keine unter `circles/`: der Commit gehört zu keiner Runde. Bestätigend, aber nicht tragend: `shared/issues/260826-0923_o_…` und `shared/reviews/260826-0923-coderev-…` Befund 1 kommen mit denselben Kommandos zum selben Ergebnis; das Ereignisprotokoll führt für `76ceb68` keinen Eintrag. Der Quelltext nennt an der einen Stelle, an der er datiert, ebenfalls den Tag: `anwendung.rs:8679`, „Seit dem 260819 traegt sie ihr Gewicht wirklich."
- **Folgengruppe:** tier-1
- **Entfernte Zusage:** keine. Der Satz tauscht eine Zeitangabe gegen eine belastbare; der Rest des Absatzes, die Reihenfolge der zwei Empfänger und die auf `fokusanzeige_nachziehen` eingegrenzte Zusage bleiben Wort für Wort stehen.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Am Melder hängen seit der Runde 14 zwei Empfänger, und sie laufen in dieser Reihenfolge**

**Nachher:**

> **Am Melder hängen seit dem 260819 zwei Empfänger (`76ceb68`), und sie laufen in dieser Reihenfolge**

**Warum das Datum und der Commit und nicht eine andere Runde.** Eine Rundennummer wäre hier in jeder Fassung falsch, denn der Commit liegt zwischen zwei Runden und gehört keiner. Das Datum trägt, weil es die Git-Historie punktgenau hergibt, und der Commit daneben macht die Aussage in einem Kommando nachprüfbar. `CLAUDE.md` nennt an drei anderen Stellen einen Commit in genau dieser Klammerform (`3596e16`, `8807844`, `b8e198e`); die Fassung erfindet keine neue Schreibweise. **Der Commit ist zugleich die Sperre gegen den Rückweg des Fehlers:** wer die Zuschreibung ein drittes Mal in eine Runde umschreiben will, hat den Beleg dagegen im selben Satz stehen.

**Kein Kandidat, kein zweiter Eintrag, keine Coderarbeit.** Der Lauf hat außer L01 nichts gefunden, das er hätte vorlegen dürfen. Die drei Stellen aus Abschnitt 7 halten, und die Berichtigung in `260823-1336_o` ist bereits in `260826-0923_o` abgelegt.

## 10. Was dieser Lauf nicht getan hat

- Keinen Defektdatensatz angefasst. Der Marker von `260826-0923_o` und die zweite Fundstelle in `260823-1336_o` gehören dem `reconciler` und der ablegenden Instanz.
- Die Kandidaten K01 bis K03 des Laufs `260826-0818` nicht wieder aufgenommen.
- Nichts committet und nichts gestaget.
- Keine neue Erhebung über `CLAUDE.md` gefahren. Die 30 Commits aus `git log --follow -- CLAUDE.md` sind nicht einzeln geöffnet, und außerhalb der Auswahlregel aus Abschnitt 4 ist keine Aussage der Datei geprüft. Wer aus diesem Lauf liest, `CLAUDE.md` stehe im Übrigen auf dem Stand des Baums, liest mehr, als hier gemessen ist.

## 11. Ausgang je Eintrag

**L01 — `applied`** am 260826-1014 gegen `2dce7d5`.

**Die Vorprüfung am Datenträger.** Vor dem Schreiben ist die Vorher-Fassung aus Abschnitt 9 byteweise in `CLAUDE.md` gesucht worden und genau einmal gefunden, in Zeile 141. `git status --porcelain -- CLAUDE.md` war leer, der Baumstand derselbe wie bei der Erhebung. Datenträger und Liste stimmten überein; kein Eintrag war `stale`.

**Die Nachprüfung am geschriebenen Bereich.** Nach dem Schreiben ist die Nachher-Fassung aus Abschnitt 9 byteweise gesucht und genau einmal gefunden, die Vorher-Fassung keinmal. Der Wortdiff führt zwei entfernte und zwei eingefügte Stellen und sonst nichts:

```
git diff --word-diff=plain -- CLAUDE.md
  → [-der Runde 14-]   {+dem 260819+}
  → [-Empfänger,-]     {+Empfänger (`76ceb68`),+}
```

Verdoppelte Satzzeichen, die der Vorprüfung entgehen könnten, sind ausdrücklich mitgeprüft und nicht vorhanden.

**Dass genau eine Fundstelle gewichen ist.** Die Zeichenfolge „Runde 14" kommt in `git show HEAD:CLAUDE.md` fünfmal vor und im Arbeitsbaum viermal. Die verbliebenen vier verteilen sich auf die Zeilen 42, 79 und 137, also auf die drei Stellen, die Abschnitt 7 am Baum als tragend gemessen hat. Zeile 141 führt die Zuschreibung nicht mehr.

**Flächengrößen, gemessen statt vorhergesagt.**

| Fläche | Vorher | Nachher |
|---|---|---|
| `CLAUDE.md` | 49.793 Bytes, 195 Zeilen | 49.803 Bytes, 195 Zeilen |
| Entscheidungsdatensätze | 186 Dateien | 186 Dateien, keine angefasst |
| Projekteigene Regeldateien | Fläche fehlt | Fläche fehlt |

Die Schätzung aus Abschnitt 3 trifft auf das Byte: zehn Bytes mehr, die Zeilenzahl unverändert.

**Rückweg:** `git checkout -- CLAUDE.md`. Die Datei ist versioniert, der Rückweg steht also offen und ist nicht bloß behauptet.

**Was der Anwendungsdurchgang nicht getan hat.** Keine zweite Datei angefasst und keine zweite Stelle in `CLAUDE.md`. Nichts committet und nichts gestaget: `git diff --cached` ist leer, das Einchecken bleibt beim Nutzer. Der Vorschlag aus `260826-0923_o`, dieselbe Zuschreibung auch in `shared/issues/260823-1336_o_…` zu berichtigen, bleibt offen; ein Defektdatensatz ist keine der drei normativen Flächen, und sein Marker gehört dem `reconciler`.

**Kein Prüfstand hängt an dieser Änderung.** `CLAUDE.md` ist in diesem Projekt durch keine Probe und keine Fixture in ihrer Größe festgeschrieben; die Größenbindung aus dem Fusion-Plugin selbst (`hooks/lib/__tests__/fixtures/rules-emission.golden`) gilt dessen eigenen Regeldateien und nicht diesem Baum. Es entsteht damit keine Coderarbeit.
