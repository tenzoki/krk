# Kuratorenlauf — 260820-1119

**Status:** Complete — die Erhebung ist gefahren, das Gate ist mit „alle“ beantwortet, alle fünf Einträge sind angewandt.
**Datum:** 260820-1119 (Erhebung)
**Git HEAD beim Lesen:** `2beb1de` (`docs(workbench): die Runde 14 schliesst kohaerent, und das Portfolio zieht nach`)
**Modus:** survey (260820-1119) und apply (260820-1132, `**Approved:** all`)
**Vorheriger Kuratorenlauf:** `shared/history/260819-1500-curator-run.md`, Baumstand `82c020a`, dreizehn Einträge, alle angewandt. Davor `shared/history/260814-1405-curator-run.md` bei `43dfe90`.

**Sprache dieser Datei.** Deutsch. Der Streit, den der vorige Lauf an dieser Stelle benennen musste, ist am 260819-2032 vom Nutzer entschieden: die Zeile `**Artifact language:** en` ist aus `CLAUDE.md` entfernt, `**Language:** de` steuert wieder beide Flächen (`shared/decisions/260819-1500_i_gilt-die-artefaktsprache-…`). `bin/fusion-rules` gibt für diesen Lauf `stilwerk/chat-voice-de.yaml` und `stilwerk/default-voice-de.yaml` aus; beide sind gelesen.

---

## 1. Was sich seit dem vorigen Lauf geändert hat

Der vorige Lauf las den Baum bei `82c020a` am 260819. Seither sind **20 Commits** gelandet (`git log --oneline 82c020a..HEAD | wc -l`). Drei Dinge davon tragen Befunde dieses Laufs:

- **Die Runde 14 ist gefahren und kohärent geschlossen** (`circles/260819-2230-auswahl-und-kopieren-in-der-vorschau/_c_circle.md`, Schließung am 260820-1045). Die Vorschaufläche ist auswählbar geworden.
- **Die Artefaktsprachen-Deklaration ist zurückgenommen** (`6be1e81`), damit ist der eine unauflösbare Widerspruch des vorigen Laufs erledigt.
- **70 terminale Datensätze sind ins Archiv gewandert** (`8cc32d5`, Archivlauf `260819-1613-safe-cleanup-tier-1`). Der vorige Lauf fand das Archiv noch leer.

Die Erhebung ist trotzdem nicht auf dieses Intervall beschränkt: `git log --follow` ist über die ganze Historie von `CLAUDE.md` gelesen (25 Commits), nicht über ein Fenster ab dem 260819.

## 2. Die drei normativen Flächen

| # | Fläche | Befund |
|---|---|---|
| 1 | Entscheidungsdatensätze unter `$SCAN_DECISIONS` = `shared/decisions/` und `circles/*/decisions/` | vorhanden, **158 Dateien**, alle lesbar. Marker: 33 offen, 14 beantwortet, 101 umgesetzt, 8 überholt, 2 zurückgestellt |
| 2 | Projekteigene Regeldateien `./rules/` und `.claude/rules/` | **abwesend**. `ls ./rules` und `ls ./.claude/rules` melden beide „No such file or directory". Es gibt in diesem Projekt keine projekteigenen Regeldateien |
| 3 | `CLAUDE.md` im Projektwurzelverzeichnis | vorhanden, 182 Zeilen, 38020 Bytes, lesbar |

Keine Fläche war unlesbar. Für Fläche 2 gilt der Fall „abwesend" aus `agents/curator.md` `### Readable, absent, unreadable`: null gemeldet, die beiden anderen Flächen vollständig gefahren.

**Der Bestand ist gegen den vorigen Lauf um fünf Entscheidungsdatensätze geschrumpft** (163 → 158), obwohl neun neue hinzugekommen sind: vierzehn sind mit dem Archivlauf weggezogen.

## 3. Evidenzquellen — was gelesen wurde

| # | Quelle | Bestand | Gelesen |
|---|---|---|---|
| 1 | Circle-Datensätze (`circles/`) | 16 | 16 Markerzustände; 1 vollständig (`260819-2230-auswahl-und-kopieren-in-der-vorschau/_c_circle.md`, Directive, Grounding, Schließungsnotiz) |
| 2 | Entscheidungsdatensätze | 158 | 158 Marker; 2 vollständig (`260819-1500_i_gilt-die-artefaktsprache-…`, `260819-2216_a_schuldet-diese-runde-einen-abnahmelauf-gegen-die-zusage-l7`); die Zeilen `Superseded by:` aller 8 überholten; die Zeilen `Answered:`/`Implemented:` von 4 weiteren |
| 3 | Sitzungsprotokolle (`history/`) | 459 | 3 vollständig oder in den tragenden Abschnitten: `circles/260819-2230-…/history/260820-0834-reconciliation.md` (Abschnitt „Was `CLAUDE.md` jetzt falsch sagt"), `shared/history/260819-2040-artefaktsprache-deklaration-zurueckgenommen.md`, `shared/history/260819-1500-curator-run.md` (Kopf, Abschnitte 8 bis 11) |
| 4 | `git log --follow` | — | `CLAUDE.md`: 25 Commits, ganze Historie. Projekteigene Regeldateien: entfällt, es gibt keine. Dazu `git log 82c020a..HEAD` (20), `git tag -l --sort=creatordate` (13 Tags) |
| 5 | Durchsichten und Analysen | 60 + 3 | **0 vollständig.** Beide Bestände aufgelistet und nach Datum geprüft; die jüngste Durchsicht (`circles/260819-2230-…/reviews/260820-0745-coderev-…`) ist über ihre Kopfzeile `Not-opened:` gelesen und sonst nicht. **Das ist die Lücke dieses Durchgangs**, unten unter „Was nicht gelesen wurde" benannt |
| 6 | `orchestrator-events.jsonl` | 1267 Zeilen | 0. Die Quelle ist nur bestätigend und trägt keinen Befund dieses Laufs |
| 7 | Archivspeicher `$WORKBENCH/archive` | **71 Dateien** in einem Lauf (`260819-1613-safe-cleanup-tier-1`) | vollständiger Dateibestand gelesen und gegen den lebenden Baum aufgelöst, siehe K01. Keine Datei im Volltext |
| 8 | `**Provenance:**`-Kopfzeilen auf Regeldateien | **0** — es gibt keine projekteigenen Regeldateien, also auch keine Kopfzeilen | — |
| — | Defektkorpus (Gegenprobe, keine vierte Fläche) | 550, davon 145 offen | 145 offene Namen; 7 vollständig, davon 5 mit `CLAUDE.md` als Gegenstand |

**Was nicht gelesen wurde.** Von den 60 Durchsichten und 3 Analysen ist keine im Volltext gelesen. Von den 459 Sitzungsprotokollen sind drei gelesen. Ein Befund, der allein in einer ungelesenen Durchsicht oder einem ungelesenen Protokoll stünde, hätte dieser Durchgang nicht gefunden. Die Durchsicht der Runde 14 ist der wahrscheinlichste Ort einer solchen Lücke; ihre Befunde sind über die neun Defektdatensätze jener Runde erreicht worden, die sie hervorgebracht hat, und nicht über das Dokument selbst.

## 4. Flächengrößen

| Fläche | Bytes vorher | Zeilen vorher | Bytes nachher | Zeilen nachher |
|---|---|---|---|---|
| Entscheidungsdatensätze (158 Dateien zusammen) | 974910 | 10981 | unverändert | unverändert |
| Projekteigene Regeldateien | 0 | 0 | 0 | 0 |
| `CLAUDE.md` | 38020 | 182 | 39019 | 183 |

**Blast-Radius.** Kein Eintrag dieses Durchgangs streicht eine Vorschrift, und die zusammen entfernten Zeichen liegen unter 400 Bytes, also **unter einem Prozent** von `CLAUDE.md`. Aus den beiden anderen Flächen wird nichts entfernt. Die Schwelle von 20 Prozent je Fläche ist bei weitem nicht erreicht; es gibt keine gesonderte Rückfrage zum Ausmaß.

## 5. Vergleichszählungen je Flächenpaar

**Die Auswahlregel steht bei jedem Paar dabei, und das Verdikt gilt für die Paare, die die Regel erreicht hat, nicht für den Korpus.**

| Paar | Auswahlregel | Paare erzeugt | Gelesen | Verdikt |
|---|---|---|---|---|
| 1. Entscheidung gegen Entscheidung | (a) der Überholungsgraph: jeder Datensatz mit einer Zeile `Superseded by:` gegen den dort genannten Nachfolger; (b) die Slug-Token-Regel des vorigen Laufs, gefahren über die **9 seit `82c020a` neuen** Datensätze gegen alle 115 bindenden | **8** (a) + **2** (b) = 10 von 6555 möglichen | 10 | **kein Widerspruch.** Die zwei Treffer aus (b) sind ergänzende Fragen zu derselben Fläche, keine konkurrierenden Antworten; einer der beiden ist zudem `_o_` und überholt damit nichts. Zwei Formfehler und ein toter Marker in einer Zitatzeile stehen als Kandidaten unten |
| 2. Entscheidung gegen Regeldatei | entfällt — es gibt keine projekteigenen Regeldateien | 0 | 0 | — |
| 3. Entscheidung gegen `CLAUDE.md` | jeder Entscheidungsdatensatz, den `CLAUDE.md` zitiert (10), plus jeder seit `82c020a` neue Datensatz (9) | **19** | 19 (10 auf Markerebene, 9 auf Titelebene, 2 davon vollständig) | **ein Befund:** `shared/decisions/260819-2216_a_schuldet-diese-runde-…-l7.md` gegen `CLAUDE.md:37` (Eintrag L05). Die zehn zitierten Datensätze tragen alle den Marker, den `CLAUDE.md` ihnen zuschreibt |
| 4. Regeldatei gegen Regeldatei | entfällt | 0 | 0 | — |
| 5. Regeldatei gegen `CLAUDE.md` | entfällt | 0 | 0 | — |
| 6. `CLAUDE.md` gegen sich selbst | alle 182 Zeilen gegeneinander gelesen, mit besonderer Aufmerksamkeit für Kopfdeklarationen gegen Fließtext | ganze Datei | ganze Datei | **kein Widerspruch.** Der eine des vorigen Laufs (`:4` gegen `:178`) ist mit der Antwort vom 260819-2032 erledigt; die Kopfzeile ist entfernt und der Abschnitt `## Sprache` nachgezogen |

**Die bekannte Lücke der Regel (b)** bleibt dieselbe wie beim vorigen Lauf: sie findet Datensätze, die über **dieselbe Sache** sprechen, und sie findet **nicht** zwei Datensätze, die dieselbe Sache mit verschiedenen Wörtern benennen.

**Und eine Lücke, die neu ist:** die 115 bindenden Datensätze sind in diesem Durchgang **nicht** vollständig gegeneinander gefahren worden. Gefahren ist nur der Ausschnitt, den die neun neuen Datensätze berühren. Wer ein Verdikt über den ganzen Korpus braucht, hat es zuletzt vom Lauf `260819-1500` bei `82c020a`, und dort über 11 von 7381 möglichen Paaren.

## 6. Vorabinhalt geänderter Entscheidungsdatensätze

**Keiner.** Dieser Durchgang schlägt an keinem der 158 Entscheidungsdatensätze eine Änderung vor, weder am Rumpf noch am Marker noch am Dateinamen. Es ist deshalb kein Vorabinhalt festzuhalten. Alle fünf Einträge liegen auf `CLAUDE.md`.

**Das ist eine Abkehr von dem, was der vorige Lauf angekündigt hat**, und die Begründung steht unter K02.

---

## 7. Das Änderungsbuch

Fünf Einträge, alle auf `CLAUDE.md`. Gruppen in der Reihenfolge, in der sie am Gate erscheinen: Streichung einer Vorschrift (keine), Tier 3 (keine), Tier 2 (zwei), Tier 1 (drei), Zusammenführungen (keine).

### Streichung einer Vorschrift (0 Einträge)

Keiner. Kein Eintrag dieses Durchgangs entfernt eine Vorschrift; alle fünf berichtigen oder ergänzen eine Aussage über den Baum.

### Tier 3 (0 Einträge)

Keiner. Kein Befund ruht auf einer Bahn statt auf einem Datensatz oder einer Messung.

### Tier 2 (2 Einträge)

### L01 — der Ereignisabgriff hat seit der Runde 14 zwei angemeldete Flächen und nicht eine

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 124
- **Tier:** 2
- **Zitat:** `circles/260819-2230-auswahl-und-kopieren-in-der-vorschau/_c_circle.md`, Abschnitt `## Was offen bleibt, und warum`: „**`CLAUDE.md` trägt seit `6531f38` eine falsche Aussage** — die Textfläche des Editors sei die eine Ausnahme im Ereignisabgriff; es sind zwei". Ausführlich in `circles/260819-2230-…/history/260820-0834-reconciliation.md`, Abschnitt `## Was `CLAUDE.md` jetzt falsch sagt`, der beide überholten Sätze wörtlich zitiert: „die Textfläche des Editors ist die **eine Ausnahme** davon" und „**so der Editor**". **Am Baum nachgeprüft:** `Anwendungsdelegierter::ist_eigene_textflaeche` (`crates/krk-ui/src/appkit/anwendung.rs:2402`) hält den Ersthelfer über `isEqual` gegen `editor.textflaeche()` **und** gegen `vorschau.textflaeche()` und gibt `editorflaeche || vorschauflaeche` zurück. Der Doc-Kommentar an `Lage::ersthelfer_gehoert_appkit` (`crates/krk-ui/src/kommandos/zulaessigkeit.rs:138-142`) schreibt es aus: „Es sind seit der Runde 14 zwei, die Textflaeche des eingebauten Editors und die Textanzeige der Vorschau".
- **Konsequenzgruppe:** tier-2
- **Gestrichene Vorschrift:** keine. Der Satz über die Fläche eines Blattes, der die Warnung überhaupt erst brauchbar macht, bleibt wörtlich stehen
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher (zwei Teilersetzungen in derselben Zeile):**

Teil a:
> die Textfläche des Editors ist die eine Ausnahme davon, und sie wird über die Objektgleichheit erkannt, nicht über die Klasse.

Teil b:
> Ein Bereich der Fensterzeile wird angemeldet, sonst gehören seine Tasten AppKit und kein Befehl von KRK wirkt darin; so der Editor.

**Nachher:**

Teil a:
> KRKs eigene Textflächen sind die Ausnahme davon, und sie werden über die Objektgleichheit erkannt, nicht über die Klasse. **Es sind seit der Runde 14 zwei**: die Textfläche des eingebauten Editors und die Textanzeige der Vorschau. Beide hält `Anwendungsdelegierter::ist_eigene_textflaeche` (`krk-ui/src/appkit/anwendung.rs`), das bis zu jener Runde `ist_editorflaeche` hieß.

Teil b:
> Ein Bereich der Fensterzeile wird angemeldet, sonst gehören seine Tasten AppKit und kein Befehl von KRK wirkt darin; so der Editor, und seit der Runde 14 auch die Vorschau.

*Anmerkung zur Vorhalteliste:* Der Absatz ist ein nicht offensichtlicher Fehlerfall im Sinne der Vorhalteliste („wer die Warnung ohne diese Fallunterscheidung liest, meldet die falsche Fläche an"). Es wird nichts entfernt, sondern die Menge von eins auf zwei gesetzt; die Warnung selbst wird dadurch schärfer, nicht schwächer.

### L02 — die Zusage L7 steht wieder auf den Gegenständen der späteren Messrunde

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 37
- **Tier:** 2
- **Zitat:** `shared/decisions/260819-2216_a_schuldet-diese-runde-einen-abnahmelauf-gegen-die-zusage-l7.md`, Abschnitt `## Antwort 260819-2242`: „**Möglichkeit a.** Kein Abnahmelauf in dieser Runde. … **L7 kommt auf die Gegenstände der späteren Messrunde, neben die Geschwindigkeit der Syntaxhervorhebung aus C3 der Runde 2.**" Der Satz in `CLAUDE.md`, den er berührt, ist „**Drei davon sind mit dem Lauf vom 260810 erledigt** (L1, L4 und L7 halten). Offen bleibt der vierte, die Geschwindigkeit der Syntaxhervorhebung aus C3" — er nennt genau den Gegenstand, neben den der Datensatz L7 zurückstellt. Der Datensatz trägt den Marker `_a_` (beantwortet), also eine aufgezeichnete Antwort und keine offene Frage.
- **Konsequenzgruppe:** tier-2
- **Gestrichene Vorschrift:** keine — der Eintrag ergänzt einen Satz und ändert keinen bestehenden
- **Rückweg:** `git checkout -- CLAUDE.md`
- **Vertrauensgrad:** `inference:` — dass der Datensatz die `CLAUDE.md`-Zeile berührt, ist meine Lesart zweier Prosastellen und von keinem Kommando geprüft. Was geprüft ist: der Wortlaut beider Stellen und der Marker des Datensatzes.

**Vorher:**
> Offen bleibt der vierte, die Geschwindigkeit der Syntaxhervorhebung aus C3 — sie gehört zu keiner der zehn Zusagen, kommt aus einer fremden Kiste und ist auf dem Referenzgerät weiterhin ungemessen.

**Nachher:**
> Offen bleibt der vierte, die Geschwindigkeit der Syntaxhervorhebung aus C3 — sie gehört zu keiner der zehn Zusagen, kommt aus einer fremden Kiste und ist auf dem Referenzgerät weiterhin ungemessen. **L7 steht seit dem 260819-2242 wieder daneben:** die Runde 14 legt Arbeit innerhalb seiner Endbedingung nach und verzichtet dafür auf einen eigenen Abnahmelauf, also kommt die Zusage auf die Gegenstände der späteren Messrunde zurück (`shared/decisions/260819-2216_*_schuldet-diese-runde-einen-abnahmelauf-gegen-die-zusage-l7.md`).

### Tier 1 (3 Einträge)

### L03 — die Rundentabelle führt dreizehn Zeilen, gefahren sind vierzehn Runden

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 27 (Tabellenende)
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe. `ls fusion-workbench/circles/*/_*_circle.md | sed -E 's#.*/_([a-z])_circle.md#\1#' | sort | uniq -c` liefert `1 a`, `10 b`, `4 c`, `1 d`. Nach der Regel unmittelbar über der Tabelle zählen `_b_` und `_c_` als gefahren, also **14**. Die fehlende ist `260819-2230-auswahl-und-kopieren-in-der-vorschau`, geschlossen am 260820-1045. Der Satz, den die Lücke falsch macht, steht in Zeile 41: „**Was die Runden ab der zweiten hinzugefügt haben, steht in der Tabelle oben und wird hier nicht wiederholt**." Corroborierend, nicht tragend: `circles/260819-2230-…/history/260820-0834-reconciliation.md`, „**Unvollständig — die Tabelle der Runden (Zeilen 12 bis 27).** Sie führt dreizehn Runden; die vierzehnte fehlt."
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> | 13 | `260818-1615-ordner-angleichen-und-abwurf-aus-fremden-apps` | `opt+cmd+s` gleicht die Ordner der zwei Dateifenster an, Abwurf aus fremden Anwendungen |

**Nachher:**
> | 13 | `260818-1615-ordner-angleichen-und-abwurf-aus-fremden-apps` | `opt+cmd+s` gleicht die Ordner der zwei Dateifenster an, Abwurf aus fremden Anwendungen |
> | 14 | `260819-2230-auswahl-und-kopieren-in-der-vorschau` | die Vorschaufläche wird auswählbar, kopiert wird der Quelltext |

### L04 — drei kohärent geschlossene Runden mit gefahrenem Abnahmelauf, nicht zwei

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 66
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe. `ls -d fusion-workbench/circles/*/_c_circle.md | xargs -n1 dirname | xargs -n1 basename` liefert vier Namen: `260813-0939-titelleiste-fuehrt-version-und-semantische-tags`, `260817-0833-jeder-loeschweg-mit-rueckfrage-und-nur-noch-papierkorb`, `260818-1615-ordner-angleichen-und-abwurf-aus-fremden-apps`, `260819-2230-auswahl-und-kopieren-in-der-vorschau`. Der Satz spricht von „zwei" und „der dritten"; es sind vier. Dass in der vierten der Nutzer den Lauf gefahren hat, steht in `circles/260819-2230-…/_c_circle.md`, `## Closure note`: „**Die Directive ist erreicht, und der Nutzer hat sie am laufenden Bündel abgenommen.** Der Bündeldurchgang lief am 260820-1030 an `KRK.app` 0.5.4 aus `05cb614`".
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> In zwei der kohärent geschlossenen Runden hat der Nutzer den Abnahmelauf gefahren (`260813-0939-titelleiste-fuehrt-version-und-semantische-tags` und `260818-1615-ordner-angleichen-und-abwurf-aus-fremden-apps`); in der dritten (`260817-0833-jeder-loeschweg-mit-rueckfrage-und-nur-noch-papierkorb`) ist er **nicht** gefahren,

**Nachher:**
> In dreien der kohärent geschlossenen Runden hat der Nutzer den Abnahmelauf gefahren (`260813-0939-titelleiste-fuehrt-version-und-semantische-tags`, `260818-1615-ordner-angleichen-und-abwurf-aus-fremden-apps` und `260819-2230-auswahl-und-kopieren-in-der-vorschau`); in der vierten (`260817-0833-jeder-loeschweg-mit-rueckfrage-und-nur-noch-papierkorb`) ist er **nicht** gefahren,

### L05 — die eine Hülle um `NSPasteboard` nimmt seit der Runde 14 auch eine fremde Ablage entgegen

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 74
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe. `grep -n '^pub fn' crates/krk-ui/src/appkit/zwischenablage.rs` liefert unter anderem `259: pub fn text_auf_ablage_schreiben(ablage: &NSPasteboard, text: &str) -> bool` und `270: pub fn text_schreiben(text: &str) -> bool`. Der Doc-Kommentar darüber (`:255-257`) sagt: „Die Ablage kommt herein und wird nicht beschafft, wie bei [`dateiverweise`]: welche es ist, entscheidet der Rufer, nicht diese Huelle." Die Zusage „genau eine Hülle" hält unverändert — es kommt keine zweite Datei hinzu, sondern eine zweite Eintrittsart in dieselbe. Corroborierend, nicht tragend: `circles/260819-2230-…/history/260820-0834-reconciliation.md`, „**Unvollständig — Zeile 74, die eine Hülle um `NSPasteboard`.**"
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine — der Satz „Eine zweite daneben wäre der Fehler, den die Datei ausdrücklich vermeidet" bleibt wörtlich stehen
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**
> **Es gibt genau eine Hülle um `NSPasteboard`**, `crates/krk-ui/src/appkit/zwischenablage.rs`; sie ist seit der Runde 4 auch Ziel und nicht mehr nur Quelle. Eine zweite daneben wäre der Fehler, den die Datei ausdrücklich vermeidet.

**Nachher:**
> **Es gibt genau eine Hülle um `NSPasteboard`**, `crates/krk-ui/src/appkit/zwischenablage.rs`; sie ist seit der Runde 4 auch Ziel und nicht mehr nur Quelle und nimmt seit der Runde 14 zusätzlich eine hereingereichte Ablage entgegen (`text_auf_ablage_schreiben`), damit die Abfangstelle der Vorschau denselben Weg nimmt wie die zwei Pfadkopierer. Eine zweite daneben wäre der Fehler, den die Datei ausdrücklich vermeidet.

### Zusammenführungen (0 Einträge)

Keine. Kein Absatz dieses Durchgangs ist allein aus dem Wiederlesen des Texts umgeschrieben worden.

---

## 8. Widersprüche, die ich nicht auflösen darf

**Keiner.** Der eine des vorigen Laufs — `CLAUDE.md:4` gegen `CLAUDE.md:178`, die Artefaktsprache gegen den Schlusssatz des Abschnitts `## Sprache` — ist am 260819-2032 vom Nutzer mit Möglichkeit 2 entschieden und am selben Tag umgesetzt (`shared/decisions/260819-1500_i_gilt-die-artefaktsprache-…`, Zeile `Implemented:`; Protokoll `shared/history/260819-2040-artefaktsprache-deklaration-zurueckgenommen.md`). Dieser Durchgang legt deshalb keinen neuen offenen Entscheidungsdatensatz an.

## 9. Kandidaten — nicht angeboten und nicht anwendbar

**K01 — zweiundzwanzig Zitate in lebenden Entscheidungsdatensätzen zeigen auf Datensätze, die am 260819-1613 ins Archiv gewandert sind.**
Gemessen: der ganze Archivbestand (70 Datensätze) gegen jedes Zitat der Form `YYMMDD-HHMM_x_<slug>` in `shared/decisions/`, `circles/*/decisions/` und `CLAUDE.md`. **`CLAUDE.md` ist nicht betroffen** — der Archivschritt schließt in `CLAUDE.md` zitierte Dateien ausdrücklich aus (`skills/archive/SKILL.md:106`, `:276`). Betroffen sind **20 Entscheidungsdatensätze mit zusammen 22 Zitaten**, darunter `shared/decisions/260810-1850_i_…` (zwei), `shared/decisions/260815-1812_o_…` (zwei) und `circles/260802-0842-…/decisions/260806-0014_s_…` (zwei).
**Warum Kandidat:** Zwei Gründe, und jeder allein trägt.
Erstens sagt keine Regel, welche Gestalt das berichtigte Zitat hätte. `skills/archive/SKILL.md` schützt allein die Zitate aus `CLAUDE.md` und schweigt zu allen übrigen; ein Zitat auf den Archivpfad umzuschreiben, wäre das Erfinden einer Regel, die fusion nicht hat, und der Archivpfad trägt das Datum seines Laufs im Namen.
Zweitens gilt in diesem Projekt die Ortsregel aus `CLAUDE.md:170`: „**Aufzeichnungen eines Standes behalten ihren damaligen Marker**, und die Ausnahme gilt je Datei nach ihrem Ort, nicht je Absatz: `history/`, `reviews/`, `analyses/`, `issues/`, `decisions/`, `messungen/` und `spikes/`." Der Abgleich vom 260818-0807 hat sie so angewandt: seine Erhebung über 1465 Zitate schließt `decisions/` als eingefrorenen Speicher ausdrücklich aus (`shared/issues/260818-0807_o_vierzehn-tote-zeiger-…`, Abschnitt „Gemessen, an `9ac41ea`"). Ein Kuratorenlauf, der dieselben Zitate berichtigt, die der Abgleich nach der Regel des Projekts unangetastet lässt, führte zwei Wahrheiten über denselben Speicher ein.

**K02 — die Zeile `**Cross-references:**` von `shared/decisions/260819-1500_i_gilt-die-artefaktsprache-…` zitiert `CLAUDE.md:4` gegen `CLAUDE.md:178`.**
Beide Zeilennummern sind heute falsch, und die erste schlimmer als vorhergesagt: die Zeile 4 trägt nicht mehr eine verrutschte Deklaration, sondern gar keine — sie ist mit der Antwort auf genau diesen Datensatz entfernt worden.
**Warum Kandidat, obwohl der vorige Lauf ihn als Tier-1-Eintrag angekündigt hat** (`shared/history/260819-1500-curator-run.md`, Abschnitt 11, letzter Absatz): dieselbe Ortsregel wie unter K01. Ich kann `decisions/` nicht in einem Atemzug als eingefrorenen Speicher behandeln, wenn 22 Zitate betroffen sind, und als lebenden, wenn eines betroffen ist. Der Datensatz trägt zudem seit dem 260819-2032 den Marker `_i_` und ist damit terminal; seine Kopfzeile beschreibt richtig, welche zwei Stellen sich zum Zeitpunkt der Fragestellung widersprachen. **Das ist eine Abweichung von der Ankündigung des vorigen Laufs, und sie ist hier benannt und nicht stillschweigend genommen.** Wer sie anders sieht, hat mit einem Satz eine anwendbare Änderung: die Zeile bekommt den Zusatz „(Zeilennummern vom 260819-1500)".

**K03 — `CLAUDE.md` sagt nirgends, dass die Vorschaufläche auswählbar ist.**
Der Abgleich der Runde 14 nennt es ausdrücklich: „**Unvollständig — es fehlt ein Absatz über die Vorschau.** `CLAUDE.md` sagt heute nirgends, dass die Vorschaufläche auswählbar ist, dass bei gerendertem Markdown der **Quelltext** kopiert wird und dass ein `Quellbezug` neben dem gerenderten Text steht. Die Datei hat nie behauptet, die Fläche sei unauswählbar, also ist keine Aussage falsch geworden; es fehlt eine."
**Warum Kandidat:** Eine Auslassung ist keiner meiner zwei Änderungsgründe. Ich ändere eine Aussage bei einem Widerspruch zwischen Flächen oder bei einer geschichtlich begründeten Überholung; „hier könnte noch etwas stehen" ist beides nicht, und ein Absatz, den ich aus eigenem Ermessen hinzufüge, hat keinen Beleg, den ein späterer Lauf prüfen könnte. **Die tragende Hälfte der Lücke schließt L01 ohnehin:** dass die Vorschau die zweite angemeldete Textfläche ist, steht danach in `CLAUDE.md`. Wer den vollen Absatz will — Quelltext statt gerendertem Text in der Ablage, der `Quellbezug` neben dem Rendern —, sagt es, und er ist in einem Zug geschrieben.

**K04 — zwei überholte Entscheidungsdatensätze tragen die Zeile `Superseded by:` zweimal.**
`circles/260811-1304-statusleiste-mit-bereichsschaltern/decisions/260811-1305_s_ist-die-neue-leiste-die-statuszeile-aus-c1-oder-eine-zweite-flaeche.md` (Zeilen 73 und 74) und `circles/260813-0939-titelleiste-fuehrt-version-und-semantische-tags/decisions/260813-0939_s_wer-setzt-den-ersten-tag-v0-1-0-und-wann.md` (Zeilen 41 und 47) führen je eine leere Vorlagenzeile und darunter die gefüllte. Ein `grep -m1 'Superseded by:'` liest die leere und meldet „nicht überholt".
**Warum Kandidat:** unverändert der Grund des vorigen Laufs (dort K01). Das ist ein Formfehler und weder ein Widerspruch zwischen bindenden Aussagen noch eine geschichtlich begründete Überholung; keiner meiner zwei Änderungsgründe deckt ihn. Er gehört in einen Abgleich. **Er steht zum zweiten Mal hier**, und das ist der Hinweis wert: ein Kandidat, den zwei Läufe hintereinander finden, wird von niemandem aufgenommen.

**K05 — ein Zitat nennt einen Marker, den sein Ziel nicht mehr trägt.**
`circles/260811-1304-…/decisions/260811-1305_s_…:74` zitiert `…/decisions/260812-1105_a_die-statuszeile-zieht-ueber-die-volle-fensterbreite-und-laesst-sich-blaettern.md`; die Datei trägt heute `_s_`.
**Warum Kandidat:** Der offene Defekt `shared/issues/260817-1130_o_die-sternform-fuer-zitate-gilt-seit-dem-260815-und-drei-runden-schreiben-den-marker-aus.md` hat die ganze Gestalt schon gefilt, und die zugehörige Frage nach der Zitierform ist offen (`shared/decisions/260818-0201_o_does-a-cross-references-line-between-records-write-the-marker-in-the-star-form.md`). Eine offene Frage entscheidet man nicht durch eine Änderung, die ihre eine Antwort vorwegnimmt.

**K06 — die Zusage über die drei Prüfordner-Fassungen.**
`CLAUDE.md:116` sagt „genau drei Fassungen, eine je Kiste". Nachgeprüft: die drei genannten Dateien bestehen. Der offene Defekt `circles/260813-0939-…/issues/260813-1110_o_eine-vierte-wegwerfordner-fassung-steht-in-xtask-…` behauptet eine vierte in `xtask`.
**Warum Kandidat:** unverändert der Grund des vorigen Laufs (dort K03). Ein offener Defekt widerspricht meinem Befund, und das ist ein Halt. `CLAUDE.md` ist in beiden Lesarten unverändert richtig; es ist nichts zu ändern.

## 10. Arbeit außerhalb meiner Zuständigkeit

**Fünf offene Defektdatensätze beschreiben Aussagen in `CLAUDE.md`, die zwei Kuratorenläufe schon berichtigt haben.** Nachgeprüft, jede einzeln mit einem `grep` gegen den heutigen Stand der Datei:

| Datensatz | Behauptet | Heutiger Stand von `CLAUDE.md` |
|---|---|---|
| `shared/issues/260816-2138_o_claude-md-nennt-zehn-gefahrene-runden-es-sind-elf.md` | die Datei nennt eine Rundenzahl als Zahl | nennt keine Zahl mehr, sondern das Kommando `ls fusion-workbench/circles/*/_*_circle.md` (Lauf `260819-1500`, Eintrag L01) |
| `shared/issues/260816-1232_o_claude-md-sagt-den-tag-setze-der-nutzer-seit-dem-260813-setzt-ihn-das-werkzeug.md` | die Datei sagt „den Tag setzt der Nutzer" | `grep -c 'den Tag setzt der Nutzer, nicht das Werkzeug' CLAUDE.md` → 0 (Lauf `260819-1500`, Eintrag L07) |
| `shared/issues/260818-0028_o_claude-md-says-the-bundle-ships-as-v0-4-1-and-four-tags-have-been-set-since.md` | die Datei nennt `v0.4.1` | `grep -c 'v0\.4\.1' CLAUDE.md` → 0 (Lauf `260819-1500`, Eintrag L03) |
| `shared/issues/260818-1635_o_claude-md-nennt-zwei-nachzuziehende-stellen-je-kommando-die-dritte-haelt-kein-uebersetzer.md` | die Datei nennt die dritte Stelle nicht | Zeile 122 nennt sie seit dem Lauf `260819-1500`, Eintrag L09 |
| `circles/260816-1321-inhaltsfilter-mit-ankreuzfeld-content/issues/260816-1935_o_claude-md-nennt-zwei-filterregeln-und-eine-huelle-in-krk-ui-beides-hat-die-elfte-runde-abgeloest.md` | die Datei nennt zwei Filterregeln und eine Hülle in `krk-ui` | Zeile 130 nennt drei Regeln; `grep -c 'krk-ui/src/vorschau' CLAUDE.md` → 0 (Lauf `260819-1500`, Einträge L10 und L11) |

**Ich schließe keinen davon.** Einen Marker auf eine Prüfung am Baum hin weiterzustellen ist Sache des `reconciler` (Ausschluss 1 meines Auftrags). Gefilt als `shared/issues/260820-1119_o_fuenf-offene-defektdatensaetze-beschreiben-claude-md-aussagen-die-zwei-kuratorenlaeufe-berichtigt-haben.md`.

**Keine Codearbeit.** Kein Eintrag dieses Durchgangs verlangt einen Helfer, eine Probe oder eine erzeugte Tabelle; alle fünf sind reine Textänderungen an `CLAUDE.md`.

**Keine Testvorrichtung ist betroffen.** In fusions eigenem Repository pinnt `hooks/lib/__tests__/fixtures/rules-emission.golden` die Größen der Regeldateien; hier gibt es keine projekteigenen Regeldateien und keine Vorrichtung, die `CLAUDE.md` misst. Ein Anwendungsdurchgang lässt keine Probe rot werden.

## 11. Ergebnisse des Anwendungsdurchgangs

**Gefahren am 260820-1132**, Modus `apply`, `**Approved:** all`, Baumstand unverändert `2beb1de` — derselbe HEAD, den die Erhebung gelesen hat.

**Vorabprüfung je Eintrag.** Der Vorher-Text jedes Eintrags ist vor dem Schreiben frisch von der Platte gelesen und gegen das Änderungsbuch gehalten (`grep -o -F -f`). Alle sechs Textstellen — L01 trägt zwei Teilersetzungen — standen **genau einmal** und wörtlich so in `CLAUDE.md`. Kein Eintrag ist `stale`.

| Eintrag | Gruppe | Zeile | Ergebnis |
|---|---|---|---|
| L01 | tier-2 | 124 (jetzt 125) | **applied** — beide Teilersetzungen |
| L02 | tier-2 | 37 (jetzt 38) | **applied** |
| L03 | tier-1 | 27 (Tabellenende) | **applied** — die Zeile 14 steht |
| L04 | tier-1 | 66 (jetzt 67) | **applied** |
| L05 | tier-1 | 74 (jetzt 75) | **applied** |

**Fünf von fünf angewandt, kein `skipped`, kein `stale`, kein `failed`.** Keine Schreibsperre der Wächterkonfiguration hat gegriffen.

**Streichungen je Tier: null.** Kein Eintrag hat eine Vorschrift entfernt; die Zählung nach Tier lautet Tier 1 -> 0 Streichungen, Tier 2 -> 0, Tier 3 -> 0. Die Datei ist um 999 Bytes und eine Zeile **gewachsen**.

**Die Kandidaten K01 bis K06 sind nicht angewandt** und waren am Gate nicht angeboten. Sie stehen unverändert unter Abschnitt 9 und warten auf den Speicher, der sie aufnimmt — K04 zum zweiten Mal, K06 zum zweiten Mal.

**An den 158 Entscheidungsdatensätzen ist nichts geändert**, wie unter Abschnitt 6 angekündigt. Es gibt an dieser Fläche keinen Vorabinhalt und nichts zurückzunehmen.

**Rückweg.** `git checkout -- CLAUDE.md` stellt den Stand vor diesem Durchgang her; die Datei ist versioniert, ein Rückweg besteht also für jeden der fünf Einträge. Ein Commit ist **nicht** gefahren — der Arbeitsbaum trägt die Änderung, das Festschreiben ist Sache des Nutzers oder des Orchestrators.

**Nachgeprüft nach dem Schreiben:** `git diff --stat CLAUDE.md` liefert `1 file changed, 5 insertions(+), 4 deletions(-)`; `wc -c -l CLAUDE.md` liefert 183 Zeilen und 39019 Bytes. Keine andere Datei des Baums ist berührt.
