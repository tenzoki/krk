# Kuratorenlauf — 260814-1405

**Status:** Complete
**Datum:** 260814-1405 (Erhebung), 260814 (Anwendungsdurchgang)
**Git HEAD beim Lesen:** `43dfe90`
**Git HEAD beim Anwenden:** `43dfe90` — unverändert seit der Erhebung, der Baum stand beim Anwenden auf demselben Commit.
**Modus:** survey, danach apply. Der Anwendungsdurchgang hat die Zustimmung `all` erhalten und alle zwölf Einträge geschrieben; keiner war `stale`.
**Vorheriger Kuratorenlauf:** keiner. `find fusion-workbench -name '*curator*'` liefert
nichts; dies ist der erste Lauf in diesem Projekt. Es gibt deshalb kein Intervall, über das
dieser Bericht sagen könnte, was sich seither geändert hat.

---

## 1. Die drei normativen Flächen in diesem Projekt

| # | Fläche | Befund |
|---|---|---|
| 1 | Entscheidungsdatensätze unter `$SCAN_DECISIONS` = `fusion-workbench/shared/decisions/` | vorhanden, 15 Dateien, lesbar |
| 2 | Projekteigene Regeldateien `./rules/` und `.claude/rules/` | **abwesend**. `ls ./rules` und `ls .claude/rules` melden beide „No such file or directory". `.claude/` trägt allein `settings.local.json`. Es gibt in diesem Projekt keine projekteigenen Regeldateien. |
| 3 | `CLAUDE.md` im Projektwurzelverzeichnis | vorhanden, 164 Zeilen, 27573 Bytes, lesbar |

Keine Fläche war unlesbar. Fläche 2 ist abwesend, nicht unlesbar — der Unterschied zählt,
weil über eine abwesende Fläche kein Widerspruchsbefund möglich und keiner unterdrückt ist.

**Der Auflöser gibt für `$SCAN_DECISIONS` allein `shared/decisions` aus**, weil kein Circle
aktiv ist. Die 106 Entscheidungsdatensätze in den zehn Circle-Speichern sind damit **nicht**
Teil der von diesem Lauf bearbeitbaren Fläche. Sie sind als Beleg gelesen worden, nicht als
Gegenstand. Wer sie kuratieren will, aktiviert den betreffenden Circle oder erweitert den
Auflöser.

---

## 2. Belegquellen — was gelesen wurde, und was leer war

| # | Quelle | Ort | Gelesen |
|---|---|---|---|
| 1 | Circle-Datensätze | `circles/` | **10 von 10.** Kopf und Directive aller zehn, `## Closure note` aller neun geschlossenen, `## Grounding snapshot` der fünf jüngsten (Runden 5 bis 9) |
| 2 | Entscheidungsdatensätze | `shared/decisions/` | **15 von 15.** Titel, Statuszeile und die vier Zustandszeilen jeder Datei; die sieben offenen zusätzlich im Abschnitt `## Frage` |
| 3 | Sitzungshistorien | `shared/history/`, `circles/*/history/` | **1 vollständig** (`shared/history/260814-1357-orchestrator-session.md`, die Sitzung, die diesen Lauf ausgelöst hat), dazu die Verzeichnislisten beider Speicher (46 + 272 Dateien). Die 317 übrigen sind **nicht** gelesen. |
| 4 | `git log --follow` | Repository | **1 von 1 möglichen** — `CLAUDE.md`, alle 15 Commits. Für Fläche 2 entfällt die Quelle, weil es keine Datei gibt. `git blame` nicht benutzt: kein Befund hing an der Urheberschaft eines einzelnen Absatzes. |
| 5 | Durchsichten und Analysen | `shared/reviews/`, `shared/analyses/` | **1 Durchsicht gelistet, keine gelesen. Analysen: 0 — das Verzeichnis ist leer.** Die 40 Durchsichten und die eine Analyse in den Circle-Speichern sind nicht gelesen. |
| 6 | `orchestrator-events.jsonl` | Werkbankwurzel | **730 Zeilen, gefiltert gelesen** auf Vorkommen von `CLAUDE.md`. Nur bestätigend verwendet, wie die Regel es verlangt; kein Befund dieses Laufs stützt sich allein darauf. |
| 7 | Archivspeicher | `fusion-workbench/archive/` | **0. Das Verzeichnis ist leer** (`ls -1a` liefert allein `.` und `..`). `/fusion:archive` ist in diesem Projekt nie gelaufen. |
| 8 | `**Provenance:**`-Köpfe auf Regeldateien | — | **0, weil es keine projekteigenen Regeldateien gibt.** Die Quelle ist für dieses Projekt gegenstandslos, nicht übersprungen. |

**Zusätzlich gelesen, außerhalb der acht Quellen:** die 35 Defektdatensätze in
`shared/issues/` als Gegenprobe auf die eigenen Behauptungen dieses Laufs (Titelliste
vollständig, vier davon im Volltext), sowie 14 Quelldateien und 4 Baudateien im Baum für die
Prüfungen der Stufe 1.

**Die dünne Stelle dieses Laufs ist Quelle 3.** 317 von 318 Sitzungshistorien sind nicht
gelesen. Kein Eintrag dieses Ledgers stützt sich auf eine Sitzungshistorie; die Belege
stammen aus Circle-Abschlussnotizen, aus dem Baum und aus `git log`. Ein Befund, der allein
in einer ungelesenen Sitzungshistorie steht, ist diesem Lauf entgangen.

---

## 3. Flächengrößen

| Fläche | Bytes vorher | Zeilen vorher | Bytes nachher | Zeilen nachher |
|---|---|---|---|---|
| `CLAUDE.md` | 27573 | 164 | 29765 | 164 |
| `shared/decisions/` (15 Dateien) | 79791 | 1009 | unverändert — dieser Lauf schlägt keine Änderung an einem Entscheidungsdatensatz vor | unverändert |
| projekteigene Regeldateien | 0 | 0 | 0 | 0 |

**Nach dem Anwendungsdurchgang gemessen** mit `wc -c < CLAUDE.md` (29765), `grep -c '' CLAUDE.md`
(164) und `cat fusion-workbench/shared/decisions/*.md | wc -l -c` (1009 Zeilen, 79791 Bytes,
unverändert). `CLAUDE.md` ist um 2192 Bytes gewachsen und hat dieselbe Zeilenzahl: alle zwölf
Änderungen ersetzen Absätze in sich, keine fügt eine Zeile hinzu oder nimmt eine weg.
**Streichungen: null in allen drei Stufen.** Es gibt deshalb keine entfernte Vorschrift, nach der
ein späterer Suchlauf fahnden müsste.

**Sprengweite:** Die zwölf vorgeschlagenen Änderungen sind Umschreibungen, keine Streichungen.
Der größte Einzelabtrag ist L07 mit rund 60 Bytes; die Summe aller entfallenden Zeichen liegt
unter 400 Bytes, also unter 1,5 Prozent von `CLAUDE.md`. **Die Schwelle von 20 Prozent je
Fläche ist nicht erreicht**, ein vorgezogener Halt ist nicht nötig.

---

## 4. Vergleichszählungen je Flächenpaar

Die Grundmengen sind mit Kommandos erhoben, nicht geschätzt:

```sh
ls -1 fusion-workbench/shared/decisions/*.md | wc -l   # 15
ls ./rules .claude/rules 2>&1                          # beide: No such file or directory
grep -c '' CLAUDE.md                                   # 164
```

| # | Paar | Auswahlregel | Paare erzeugt | Paare gelesen |
|---|---|---|---|---|
| 1 | Entscheidung × Entscheidung | Alle 105 ungeordneten Paare aus den 15 Datensätzen, gefiltert auf Themenüberschneidung nach Titel und Kreuzverweiszeile | 105 erzeugt, **3 mit Überschneidung** (die zwei L9-Datensätze `260807-1904_i_` und `260810-2132_d_`; `260813-0053_o_schluckt-der-abgriff…` gegen `260813-0053_o_wie-viele-obermenues…`) | 3 |
| 2 | Entscheidung × Regeldatei | entfällt, Fläche 2 abwesend | 0 | 0 |
| 3 | Entscheidung × `CLAUDE.md` | Jeder Entscheidungsdatensatz, den `CLAUDE.md` mit Pfad zitiert, gegen den zitierenden Absatz | 8 | 8 |
| 4 | Regeldatei × Regeldatei | entfällt, Fläche 2 abwesend | 0 | 0 |
| 5 | Regeldatei × `CLAUDE.md` | entfällt, Fläche 2 abwesend | 0 | 0 |
| 6 | `CLAUDE.md` × `CLAUDE.md` | Jeder der 11 fettgesetzten Leitsätze gegen jeden anderen, plus jede Zahlenangabe gegen den Absatz in Zeile 102, der Zahlenangaben in dieser Datei ablehnt | 55 + 5 = 60 | 60 |

**Was die Urteile decken.** Für Paar 1 gilt: *unter den drei Paaren, die die Themenfilterung
erreicht hat, überholt kein lebender Datensatz einen anderen.* Die zwei L9-Datensätze stehen
bereits in einer annotierten Kette (`260807-1904_i_` trägt `Superseded by:` auf sich selbst
bezogen und ist umgesetzt; `260810-2132_d_` ist zurückgestellt), und die zwei aus dem
260813-0053 betreffen verschiedene Bestandteile derselben Runde. Über die 102 Paare, die die
Filterung nicht erreicht hat, sagt dieser Lauf nichts.

Für Paar 6 gilt ein **positiver Befund**: `CLAUDE.md` widerspricht sich in einem Punkt selbst.
Zeile 102 stellt die Regel auf, eine Zahl, die mit jeder Runde wächst, gehöre nicht in diese
Datei — „eine Zahl davor veraltet, wie es die Aufstellungen getan haben, die diese Datei aus
demselben Grund abgelegt hat". Die Zeilen 66 und 136 führen genau zwei solche Zahlen, und
beide sind falsch. Das ist der Grund, aus dem L07 und L08 die Ableitung vorschlagen und nicht
die berichtigte Zahl: die Datei hat die Regel schon, sie hält sie an zwei Stellen nicht.

---

## 5. Vorheriger Inhalt der Entscheidungsdatensätze

**Entfällt: dieser Lauf schlägt an keinem Entscheidungsdatensatz eine Änderung vor.** Die
Pflicht, den vollständigen Vorzustand jedes zu ändernden Entscheidungsdatensatzes hier
abzulegen, greift damit nicht. Sie ist nicht übersprungen, sondern gegenstandslos.

---

## 6. Das Ledger

Zwölf Einträge, alle auf `CLAUDE.md`. Kein Eintrag entfernt eine Vorschrift; kein Eintrag steht
auf der Vorhalteliste. Die Reihenfolge folgt der Folgengruppe, die schwerste zuerst.

---

### L01 — Zeile 11: „Vier Runden sind gefahren" — es sind neun

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `ls -d fusion-workbench/circles/*/` liefert 10 Verzeichnisse;
  `ls fusion-workbench/circles/*/_*_circle.md` liefert 8 mit `_b_`, 1 mit `_c_`
  (`260813-0939-titelleiste-fuehrt-version-und-semantische-tags/_c_circle.md`) und 1 mit `_a_`
  (`260804-0933-eingebauter-web-betrachter-im-vorschaufenster/_a_circle.md`). Neun Runden sind
  gefahren, nicht vier, und nicht alle neun schließen beschränkt. Bestätigt durch
  `shared/history/260814-1357-orchestrator-session.md`, Abschnitt `## Auffaelligkeit`.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Vier Runden sind gefahren**, alle vier als beschränkter Abschluss (`_b_`) und alle vier aus demselben Grund — siehe „Projektstand". Ihre Circles liegen unter `fusion-workbench/circles/`:

**Nachher:**

> **Neun Runden sind gefahren.** Wie viele es sind und wie jede geschlossen hat, sagt der Dateibestand und nicht diese Zeile: `ls fusion-workbench/circles/*/_*_circle.md`. Die Tabelle darunter führt die vier Runden, deren Datensätze diese Datei zitiert; sie ist ein Verweisregister und keine Aufstellung aller gefahrenen Runden.

---

### L02 — Zeile 54: „Alle vier Runden sind als beschränkter Abschluss geschlossen" — eine schließt kohärent

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 2
- **Beleg:** `circles/260813-0939-titelleiste-fuehrt-version-und-semantische-tags/_c_circle.md`,
  Abschnitt `## Closure note`, erster Satz: „**Geschlossen als kohärenter Abschluss (`_c_`) am
  260813-1415.** Die erste Runde dieses Projekts, die nicht beschränkt endet." Und zweiter
  Absatz: „Die sieben Runden davor endeten alle aus demselben Grund beschränkt: der Abnahmelauf
  verlangt KRK im Vordergrund und ist damit Nutzerarbeit, die kein Agent fahren kann. In dieser
  Runde hat der Nutzer sie gefahren." Der Datensatz nennt damit beides: den Satz in `CLAUDE.md`,
  den er überholt, und die ersetzende Lage.
- **Folgengruppe:** tier-2
- **Entfernte Vorschrift:** keine. Die tragende Aussage — der Abnahmelauf ist Nutzerarbeit, kein Agent kann ihn fahren — bleibt vollständig erhalten und behält ihren Verweis nach unten.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Alle vier Runden sind als beschränkter Abschluss geschlossen, und immer aus demselben Grund: der Abnahmelauf verlangt KRK im Vordergrund und ist damit Nutzerarbeit.** Kein Agent kann ihn fahren; warum, steht unten unter „Was man nicht sieht".

**Nachher:**

> **Fast jede gefahrene Runde ist als beschränkter Abschluss (`_b_`) geschlossen, und immer aus demselben Grund: der Abnahmelauf verlangt KRK im Vordergrund und ist damit Nutzerarbeit.** Welche das sind, sagt `ls fusion-workbench/circles/*/_b_circle.md`. Die eine Ausnahme ist die Runde `260813-0939-titelleiste-fuehrt-version-und-semantische-tags`, in der der Nutzer den Lauf selbst gefahren hat; sie schließt als bisher einzige kohärent (`_c_`). Kein Agent kann ihn fahren; warum, steht unten unter „Was man nicht sieht".

---

### L03 — Zeile 56: die Rangheuristik bekommt inzwischen sehr wohl eine Auskunft

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 2
- **Beleg:** derselbe Datensatz wie L02. Seit dem 260813-1415 trägt genau ein Circle `_c_`. Eine
  Heuristik, die allein `_c_` zählt, bekommt jetzt eine Auskunft — nur eine irreführende: sie
  setzt die eine abgenommene Runde über acht gleich weit gebaute. Der überholte Satz ist „bekommt
  hier für jeden Kandidaten dasselbe Kennzeichen und damit keine Auskunft".
- **Folgengruppe:** tier-2
- **Entfernte Vorschrift:** keine. Die Warnung an eine Rangheuristik bleibt und wird schärfer.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Das ist eine Eigenschaft dieses Projekts und keine Häufung von Fehlschlägen.** Wer eine Rangheuristik über die Circles legt, die allein `_c_` als erfüllte Vorbedingung zählt, bekommt hier für jeden Kandidaten dasselbe Kennzeichen und damit keine Auskunft.

**Nachher:**

> **Das ist eine Eigenschaft dieses Projekts und keine Häufung von Fehlschlägen.** Wer eine Rangheuristik über die Circles legt, die allein `_c_` als erfüllte Vorbedingung zählt, bekommt hier eine irreführende Auskunft: sie trennt die eine Runde, deren Abnahmelauf der Nutzer gefahren hat, von acht Runden, die genauso weit gebaut und nur nicht abgenommen sind. Der Marker misst hier die Verfügbarkeit des Nutzers und nicht die Reife der Runde.

---

### L04 — Zeile 158: „Zwei Circles sind vorgesehen und nicht gefahren" — es ist einer

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `ls fusion-workbench/circles/*/_a_circle.md` liefert genau eine Datei,
  `260804-0933-eingebauter-web-betrachter-im-vorschaufenster/_a_circle.md`. Die Statusleiste
  liegt als `260811-1304-statusleiste-mit-bereichsschaltern/_b_circle.md` vor; ihre
  Abschlussnotiz datiert den beschränkten Abschluss auf 260812-0820.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Zwei Circles sind vorgesehen und nicht gefahren** (Marker `_a_`): die Statusleiste mit Bereichsschaltern (`260811-1304-…`) und der Web-Betrachter im Vorschaufenster (`260804-0933-…`). Die Statusleiste steht auf Rang 1, seit die Runde 4 ihre harte Vorbedingung beseitigt hat; ihr Umfang ist durch einen Nachtrag zu Spaltenschaltern gewachsen, der vor der Aktivierung zu klären ist.

**Nachher:**

> **Welche Circles vorgesehen und nicht gefahren sind, sagt `ls fusion-workbench/circles/*/_a_circle.md` und nicht diese Zeile.** Am 260814 ist es einer: der Web-Betrachter im Vorschaufenster (`260804-0933-…`). Die Statusleiste mit Bereichsschaltern (`260811-1304-…`) ist als fünfte Runde gefahren und am 260812-0820 beschränkt geschlossen.

---

### L05 — Zeile 20: „Alle vier Speicher binden weiter" — es sind zehn Circle-Speicher und der gemeinsame

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `ls -d fusion-workbench/circles/*/decisions` liefert 10 Verzeichnisse, dazu
  `fusion-workbench/shared/decisions`. Die Zahl „vier" ist falsch; die Aussage, dass alle binden,
  steht unverändert auch in Zeile 142.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine. Die Bindungsaussage bleibt, nur die Zahl entfällt.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> Alle vier Speicher binden weiter.

**Nachher:**

> Alle Speicher binden weiter, auch die der Runden, die diese Tabelle nicht führt.

---

### L06 — Zeile 28: „Keine der Runden 2 bis 4" — der Geltungsbereich endet vier Runden zu früh

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `grep -oE '"L[0-9]+"' crates/krk-bench/src/messen.rs | sort -u` liefert genau
  `L1 L2 L3 L4 L5 L6 L7 L8 L9 L10` — zehn Zusagen, keine elfte. Die Runden 6 und 7 sagen es
  zusätzlich in ihren Circle-Datensätzen wörtlich zu: „Diese Runde setzt keine elfte Zeitzusage
  und fasst keine der zehn an"
  (`circles/260812-1000-teilen-ordnersprung-ablage-sichern-vorschau-rendern/_b_circle.md`,
  `circles/260813-0100-suche-in-der-belegung-vollstaendiges-menue-weitere-instanz/_b_circle.md`).
  Für die Runden 5, 8 und 9 trägt der Baum den Beleg, nicht der Datensatz.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Keine der Runden 2 bis 4 hat eine elfte Zahl gesetzt oder eine der zehn angefasst.**

**Nachher:**

> **Keine Runde nach der ersten hat eine elfte Zahl gesetzt oder eine der zehn angefasst**; nachzuzählen mit `grep -oE '"L[0-9]+"' crates/krk-bench/src/messen.rs | sort -u`.

---

### L07 — Zeile 66: „`Kommando` … 68 Varianten" — es sind 77, und die Zahl gehört nicht hierher

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `awk '/^pub enum Kommando/,/^}/' crates/krk-core/src/tasten/belegung.rs |
  grep -cE '^\s{4}[A-ZÄÖÜ][A-Za-zÄÖÜäöüß0-9]*,?$'` liefert **77**. Die drei anderen Zahlen des
  Absatzes halten: `Wirkungsbereich` 7, `Bereich` 5, `Fokus` 5, mit derselben Zählung erhoben.
  Der offene Defekt `shared/issues/260812-2253_o_claude-md-nennt-fuer-kommando-68-varianten-der-baum-traegt-75.md`
  führt vier Stände in vier Tagen (68 zitiert, 75 beim Ablegen, 76 in den Runden 7 und 8, 77 heute)
  und empfiehlt ausdrücklich, die Zahl durch etwas zu ersetzen, das nicht mitwächst.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine. Die tragende Aussage — vier Aufzählungen ohne Auffangzweig, jede hält den Bau an — bleibt Wort für Wort. Entfernt wird eine Messung, die durch ihre Ableitung ersetzt wird.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> Am 260811 nachgezählt: `Wirkungsbereich` (`krk-core/src/tasten/belegung.rs`) trägt sieben Werte, `Kommando` in derselben Datei 68 Varianten, `Bereich` (`krk-ui/src/fenstermodell.rs`) fünf und `Fokus` (`krk-ui/src/kommandos/fokus.rs`) fünf.

**Nachher:**

> Am 260814 nachgezählt: `Wirkungsbereich` (`krk-core/src/tasten/belegung.rs`) trägt sieben Werte, `Bereich` (`krk-ui/src/fenstermodell.rs`) fünf und `Fokus` (`krk-ui/src/kommandos/fokus.rs`) fünf. Für `Kommando` in derselben Datei steht hier keine Zahl: sie wächst mit fast jeder Runde und ist in dieser Datei viermal in vier Tagen falsch geworden (`shared/issues/260812-2253_*_claude-md-nennt-fuer-kommando-68-varianten-der-baum-traegt-75.md`). Wer sie braucht, zählt sie mit `awk '/^pub enum Kommando/,/^}/' crates/krk-core/src/tasten/belegung.rs`.

---

### L08 — Zeile 136: „31 von 33 Dateien" — es sind 38 von 40, und die Quote gehört nicht hierher

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `ls crates/krk-ui/src/appkit/*.rs crates/krk-ui/src/appkit/*/*.rs | wc -l` liefert
  **40**; `grep -l 'Ab welchem macOS die angesprochenen Klassen stehen'` über dieselbe Menge
  liefert **38**. Ohne den Abschnitt sind weiterhin genau `koordinaten.rs` und `mod.rs`.
  Der offene Defekt `shared/issues/260812-1438_o_claude-md-nennt-31-von-33-dateien-mit-untergrenzen-abschnitt-es-sind-33-von-35.md`
  führt vier Stände in vier Tagen und empfiehlt in seinem eigenen Text: „die zwei Ausnahmen nennen
  statt der Quote". Genau das schlägt dieser Eintrag vor.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine. Die Gewohnheit, ihr Grund und die beiden begründeten Ausnahmen bleiben. Entfernt wird eine Quote, die durch eine Aussage ersetzt wird, die nicht mitwächst.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> der Abschnitt `# Ab welchem macOS die angesprochenen Klassen stehen` im Modulkopf steht am 260811 in **31 von 33** Dateien unter `crates/krk-ui/src/appkit/` — ohne ihn sind `koordinaten.rs` und `mod.rs`, beide begründet.

**Nachher:**

> der Abschnitt `# Ab welchem macOS die angesprochenen Klassen stehen` im Modulkopf steht in **jeder** Datei unter `crates/krk-ui/src/appkit/` außer zweien: `koordinaten.rs` und `mod.rs`, beide begründet. Eine Quote steht hier nicht — sie ist zwischen dem 260811 und dem 260814 viermal falsch geworden, während die zwei Ausnahmen dieselben blieben (`shared/issues/260812-1438_*_claude-md-nennt-31-von-33-dateien-mit-untergrenzen-abschnitt-es-sind-33-von-35.md`).

---

### L09 — Zeile 116: der Fokusvorbehalt steht seit der Runde 7 nicht mehr in `ereignisse.rs`

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 2
- **Beleg:** `crates/krk-ui/src/kommandos/zulaessigkeit.rs`, Modulkopf, Abschnitt „Eine Frage, zwei
  Frager": „Beide fragen **dieselbe** Funktion auf **derselben** `Lage`, und ihre Antworten
  koennen deshalb nicht auseinanderlaufen. Genau das ist der Grund, aus dem die Regel eine
  Funktion ist und nicht zwei Abfragen an zwei Stellen." Und
  `crates/krk-ui/src/appkit/ereignisse.rs:84-96`: „**Seit der Runde 7 ist der Vorbehalt keine
  Station dieses Abgriffs mehr, sondern der Bestandteil (2) der Zulaessigkeitsregel am
  Delegierten.** … Zwei Stellen, die dieselbe Frage stellen, gibt es damit nicht mehr."
  Der überholte Satz in `CLAUDE.md` ist „**Zwei Stellen mit zwei verschiedenen Fragen**".
  Bestätigt durch die Abschlussnotiz der Runde 7
  (`circles/260813-0100-suche-in-der-belegung-vollstaendiges-menue-weitere-instanz/_b_circle.md`):
  „nebenbei kennt der Ereignisabgriff den Editor nicht mehr — die Kopplung, vor der `CLAUDE.md`
  seit Runden warnt."
- **Folgengruppe:** tier-2
- **Entfernte Vorschrift:** keine. Die Warnung — wer nur `ereignisse.rs` liest, hält den Vorbehalt für die einzige Sperre und erfindet einen Defekt — bleibt samt ihrem Beleg. Sie wird richtiger, weil sie jetzt auf die Datei zeigt, in der die Regel tatsächlich steht.
- **Vorhalteliste:** Dieser Eintrag berührt eine versteckte Kopplung, also einen Gegenstand der Vorhalteliste. Er ist zulässig, weil er eine Änderung der Stufe 2 mit ausdrücklichem überholendem Datensatz ist und weil er die Kopplung nicht entfernt, sondern richtigstellt.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Zwei Stellen mit zwei verschiedenen Fragen**: der Vorbehalt in `appkit/ereignisse.rs` fragt, wem die Taste gehört, der Delegierte fragt, welcher Befehl jetzt zulässig ist.

**Nachher:**

> **Seit der Runde 7 steht die Frage an einer Stelle und hat zwei Frager.** `krk-ui/src/kommandos/zulaessigkeit.rs` beantwortet sie aus vier Bestandteilen: es steht kein Blatt (oder der Befehl ist während eines Blattes erlaubt), der Ersthelfer des Schlüsselfensters gehört nicht AppKit, `fokus::wirkt` sagt ja, und das Schlüsselfenster gehört KRK. Der Ereignisabgriff über `kommando_ausfuehren` und das Hauptmenü über `validateMenuItem:` rufen dieselbe Funktion auf derselben `Lage`; der Fokusvorbehalt ist keine Station in `appkit/ereignisse.rs` mehr.

---

### L10 — Zeile 108: der Verweis auf `decisions/260810-1850_*` zeigt in den falschen Speicher

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `find fusion-workbench -name '260810-1850*'` liefert genau
  `fusion-workbench/shared/decisions/260810-1850_i_wie-kommt-der-messplan-bei-strg-c-weg-ohne-die-zusage-der-sitzungssicherung-zu-brechen.md`.
  Zeile 20 derselben Datei bestimmt, dass ein Pfad der Form `decisions/…` ohne Nennung eines
  Circles als Pfad in der Runde 2 zu lesen ist;
  `ls circles/260807-2116-eingebauter-editor-mit-textmarken/decisions/` führt keine Datei mit
  diesem Zeitstempel. Der Verweis in der Kurzform ist damit nicht auflösbar.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> gewählt hat das der Nutzer als Option 4 von `decisions/260810-1850_*_wie-kommt-der-messplan-bei-strg-c-weg-ohne-die-zusage-der-sitzungssicherung-zu-brechen.md`.

**Nachher:**

> gewählt hat das der Nutzer als Option 4 von `shared/decisions/260810-1850_*_wie-kommt-der-messplan-bei-strg-c-weg-ohne-die-zusage-der-sitzungssicherung-zu-brechen.md`.

---

### L11 — Zeile 108: der Verweis auf `issues/260810-1925_*` zeigt in den falschen Speicher

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 1
- **Beleg:** `find fusion-workbench -name '260810-1925*'` liefert genau
  `fusion-workbench/shared/issues/260810-1925_c_eine-probe-schreibt-ins-echte-temporaerverzeichnis-und-raeumt-dort-jetzt-fremde-messplaene-ab.md`.
  Nach der Regel in Zeile 20 löst der Verweis in der Kurzform gegen die Runde 2 auf, wo keine
  Datei mit diesem Zeitstempel liegt. Dieselbe Zeile 152 zitiert einen Nachbarn korrekt mit
  `shared/issues/…`, was den Befund stützt.
- **Folgengruppe:** tier-1
- **Entfernte Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> also räumt auch `cargo test` ab (`issues/260810-1925_*_eine-probe-schreibt-ins-echte-temporaerverzeichnis-und-raeumt-dort-jetzt-fremde-messplaene-ab.md`).

**Nachher:**

> also räumt auch `cargo test` ab (`shared/issues/260810-1925_*_eine-probe-schreibt-ins-echte-temporaerverzeichnis-und-raeumt-dort-jetzt-fremde-messplaene-ab.md`).

---

### L12 — Zeile 114: die Regel für eine zweite bedienbare Textfläche zeigt in einem Fall in die falsche Richtung

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`
- **Stufe:** 2
- **Beleg:** `circles/260813-2332-notizzettel-als-blatt-mit-zwei-zetteln/_b_circle.md:42`,
  Abschnitt `### Zwei Vorkehrungen der bestehenden Blätter, und eine davon gilt hier anders`:
  „**Der Ersthelfervorbehalt zeigt für den Zettel in die andere Richtung als für den Editor.**
  `CLAUDE.md` warnt: wer eine zweite bedienbare Textfläche baut, meldet sie in
  `ersthelfer_gehoert_appkit` (`appkit/ereignisse.rs`) an, sonst gehören ihre Tasten AppKit. Für
  die Textfläche eines Blattes ist genau das erwünscht … und der Grund gehört in ihren Modulkopf,
  **weil die stehende Warnung sonst zum Gegenteil verleitet**." Der überholte Satz in `CLAUDE.md`
  ist wörtlich „**Wer eine zweite bedienbare Textfläche baut, meldet sie dort an**, sonst gehören
  ihre Tasten AppKit und kein Befehl von KRK wirkt darin." Umgesetzt und im Baum belegt:
  `crates/krk-ui/src/appkit/blaetter/zettel.rs:15-37` trägt den Abschnitt „Warum die Textflaeche
  in `ersthelfer_gehoert_appkit` **nicht** angemeldet wird" und führt die Kette aus, an der `Esc`
  hängt. Zweiter unabhängiger Beleg:
  `circles/260813-2332-notizzettel-als-blatt-mit-zwei-zetteln/planning/260813-2348_o_spec-notizzettel-als-blatt-mit-zwei-zetteln.md:37`.
- **Folgengruppe:** tier-2
- **Entfernte Vorschrift:** keine. Die Anmeldepflicht bleibt für den Fall, für den sie gilt — eine bedienbare Fläche, die ein Bereich der Fensterzeile ist. Ergänzt wird der Fall, in dem sie sich umkehrt.
- **Vorhalteliste:** Dieser Eintrag berührt eine versteckte Kopplung und eine nicht offensichtliche Fehlerquelle, also zwei Gegenstände der Vorhalteliste. Er ist zulässig, weil er eine Änderung der Stufe 2 mit ausdrücklichem überholendem Datensatz ist und weil er nichts entfernt.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Wer eine zweite bedienbare Textfläche baut, meldet sie dort an**, sonst gehören ihre Tasten AppKit und kein Befehl von KRK wirkt darin.

**Nachher:**

> **Wer eine zweite bedienbare Textfläche baut, entscheidet bewusst, ob er sie dort anmeldet — und die Antwort hängt davon ab, was die Fläche ist.** Ein Bereich der Fensterzeile wird angemeldet, sonst gehören seine Tasten AppKit und kein Befehl von KRK wirkt darin; so der Editor. Die Fläche eines Blattes wird **nicht** angemeldet, denn dort ist genau das erwünscht: `Esc` schließt den Notizzettel nur, weil sein Ersthelfer AppKit gehört und `Abbrechen` damit unzulässig bleibt (`appkit/blaetter/zettel.rs`, Modulkopf; `circles/260813-2332-notizzettel-als-blatt-mit-zwei-zetteln/_b_circle.md`). Wer die Warnung ohne diese Fallunterscheidung liest, meldet die falsche Fläche an.

---

## 7. Kandidaten — nicht zur Abnahme gestellt, nicht anwendbar

Vier Befunde, die dieser Lauf gefunden, aber nicht als Änderung vorgelegt hat. Sie stehen hier,
damit sie nicht verloren gehen.

### K01 — Zeile 156: „Außerhalb der vier gefahrenen Runden"

Derselbe veraltete Geltungsbereich wie L01. Warum Kandidat und kein Eintrag: der Satz zählt
danach auf, was außerhalb liegt (KI-Anbindung, Browser, Dateivergleich, Suchen und Ersetzen über
mehrere Dateien, Server-Protokolle, Git jenseits der vier Operationen). Diese Liste habe ich
**nicht** vollständig gegen die Runden 5 bis 9 geprüft; die Runde 6 hat Markdown gerendert und
dabei eine Web-Ansicht ausdrücklich verworfen, was den Browser-Punkt stützt, für die übrigen fünf
Punkte fehlt mir der Beleg. Eine Änderung, die nur „vier" streicht, wäre richtig, aber sie
verlängert die Lebensdauer einer Aufzählung, deren Gültigkeit dieser Lauf nicht belegt hat.

### K02 — Zeile 32: der Projektstand ist drei Tage alt und seine Fähigkeitsliste unvollständig

„Geprüft am 260811-2230" ist als Aussage **wahr** — die Datei sagt ehrlich, wann sie zuletzt
nachgeführt wurde, und der Stempel ist kein falsifizierter Anspruch. Falsch ist nichts daran;
unvollständig ist die Liste dahinter, die Statusleiste, Teilen, Ordnersprung, Notizzettel,
weitere Instanz, vollständiges Hauptmenü, Suche in der Belegung und die Version in der
Titelleiste nicht führt. **Das Ergänzen dieser Liste ist der Durchgang „add" aus
`/fusion:revise-claude-md` und nicht Kuratorenarbeit.** Ein Kurator, der den Stempel auf heute
setzt, ohne die Liste zu ergänzen, würde eine unvollständige Aufstellung als aktuell ausgeben.
Der Stempel bleibt deshalb stehen.

### K03 — vier offene Entscheidungsdatensätze, die die Runde 7 beantwortet zu haben scheint

`shared/decisions/260813-0053_o_wie-viele-obermenues-traegt-die-menueleiste-fuer-81-funktionen.md`
fragt nach der Zahl der Obermenüs; die Abschlussnotiz der Runde 7 sagt „Alle 82 Funktionen stehen
im Menue, in neun Obermenues". Ebenso stehen die drei Geschwister aus dem 260813-0053 auf `_o_`,
während die Runde gebaut und geschlossen ist. **Das Vorrücken eines Markers auf Grund einer
Prüfung gegen den Baum gehört dem `reconciler` und nicht mir** (Ausschluss 1 meines Auftrags).
Empfehlung: einen `reconciler`-Lauf über `shared/decisions/` ansetzen.

### K04 — Zahlen in den Entscheidungsdatensätzen selbst

Mehrere Datensätze führen im Titel oder in der Frage eine Zahl, die inzwischen falsch ist
(„81 Funktionen", „68 Varianten", „31 von 33"). **Diese Zahlen sind nicht zu berichtigen.**
`CLAUDE.md` Zeile 154 bestimmt: „Aufzeichnungen eines Standes behalten ihren damaligen Marker",
und die Ortsregel nennt `decisions/` und `issues/` ausdrücklich. Der Defektdatensatz
`260812-2253_o_…` sagt dasselbe über sich selbst: „Der Titel bleibt, wie er ist: er benennt den
Fehler, nicht den Tagesstand." Dieser Lauf folgt der Regel, statt sie zu brechen, und trägt sie
hier ein, damit ein späterer Lauf die Frage nicht neu aufwirft.

---

## 8. Arbeit außerhalb meines Auftrags

Kein Eintrag dieses Laufs verlangt Ableitungscode, eine Hilfsfunktion oder eine Probe. L07 und
L08 nennen Kommandos, die es schon gibt (`awk`, `grep`, `ls`); es entsteht keine Coder-Arbeit.

**Es gibt in diesem Baum keinen Test, der die Größe oder den Zeilenbestand von `CLAUDE.md`
festnagelt.** Geprüft mit `grep -rn 'CLAUDE' crates xtask`: **fünf Treffer**, sämtlich
Kommentarzeilen (`markdown.rs:514`, `appkit/textautomatik.rs:169`,
`appkit/blaetter/zettel.rs:17`, `appkit/anwendung.rs:762`, `krk-bench/src/messen.rs:1330`).
Alle fünf nennen die Datei in Prosa und prüfen nichts an ihr. Der Anwendungsdurchgang bricht
damit keine Probe.

**Eine Korrektur an diesem Bericht selbst.** Die Größe von `shared/decisions/` in Abschnitt 3
stand in einem ersten Entwurf dieses Laufs auf 100352 Bytes und 1717 Zeilen. Diese Zahl war
nicht gemessen. Gemessen mit `cat fusion-workbench/shared/decisions/*.md | wc -l -c` sind es
**79791 Bytes und 1009 Zeilen**; der Bericht führt jetzt die gemessene Zahl. Der Fehler ist hier
festgehalten statt stillschweigend berichtigt, weil dieser Lauf anderen Dateien vorwirft,
ungemessene Zahlen zu führen.

**Kein Defektdatensatz ist in diesem Lauf angelegt worden.** Die drei Befunde, die einen
verdient hätten, haben bereits einen: `shared/issues/260812-1438_o_…`,
`shared/issues/260812-2253_o_…` und `shared/issues/260813-1345_o_…`. Ein vierter daneben wäre
ein Doppel.

**Kein Entscheidungsdatensatz ist in diesem Lauf angelegt worden.** Es ist kein Widerspruch
gefunden worden, bei dem zwei lebende Aussagen beide verteidigbar wären; alle zwölf Einträge haben
eine falsifizierte oder überholte Seite.

---

## 9. Prüfung der Vorher-Texte

Vor dem Gate maschinell geprüft: **alle zwölf Vorher-Texte stimmen zeichengenau mit
`CLAUDE.md` am Stand `43dfe90` überein, und jeder kommt genau einmal vor.** Der
Anwendungsdurchgang liest jeden Text vor der Änderung erneut von der Platte; weicht er ab,
wird der Eintrag als `stale` vermerkt und nichts für ihn geschrieben.

## 10. Ergebnisse je Eintrag

Zustimmung am Gate: `all`, also alle zwölf Einträge. Vor jeder Änderung ist der Vorher-Text
erneut von der Platte gelesen worden; **alle zwölf standen zeichengenau und genau einmal in
`CLAUDE.md`**, kein Eintrag ist deshalb als `stale` vermerkt. Kein Schreibvorgang ist von der
Wächterkonfiguration abgewiesen worden.

| Eintrag | Gegenstand | Stufe | Ergebnis |
|---|---|---|---|
| L01 | „Vier Runden sind gefahren" → neun, mit Ableitung | 1 | applied |
| L02 | „Alle vier Runden beschränkt geschlossen" → eine schließt kohärent | 2 | applied |
| L03 | Rangheuristik bekommt jetzt eine irreführende Auskunft | 2 | applied |
| L04 | „Zwei Circles vorgesehen" → einer, mit Ableitung | 1 | applied |
| L05 | „Alle vier Speicher binden weiter" → alle Speicher | 1 | applied |
| L06 | „Runden 2 bis 4" → jede Runde nach der ersten | 1 | applied |
| L07 | `Kommando` 68 Varianten → Zählkommando statt Zahl | 1 | applied |
| L08 | „31 von 33 Dateien" → die zwei Ausnahmen statt der Quote | 1 | applied |
| L09 | Fokusvorbehalt steht seit Runde 7 in `zulaessigkeit.rs` | 2 | applied |
| L10 | Verweis `decisions/260810-1850_*` → `shared/decisions/…` | 1 | applied |
| L11 | Verweis `issues/260810-1925_*` → `shared/issues/…` | 1 | applied |
| L12 | Anmeldepflicht für eine zweite Textfläche: Fallunterscheidung | 2 | applied |

**Zusammenfassung:** 12 angewandt, 0 übersprungen, 0 veraltet, 0 fehlgeschlagen. Acht Einträge
der Stufe 1, vier der Stufe 2, keiner der Stufe 3, keine Zusammenlegung, **keine Streichung einer
Vorschrift**.

**Rückweg für den gesamten Lauf:** `git checkout -- CLAUDE.md`. Es ist die einzige berührte Datei,
sie steht unter Versionsverwaltung, und der Stand davor ist `43dfe90`.

**Nicht festgenagelt:** Der Anwendungsdurchgang hat keine Probe und keine Fixture verletzt. Der
Erhebungsdurchgang hat das in Abschnitt 8 mit `grep -rn 'CLAUDE' crates xtask` geprüft; alle fünf
Treffer sind Kommentarzeilen. Es entsteht keine Coder-Arbeit aus diesem Lauf.

**Offen geblieben und an andere übergeben:** die vier Kandidaten aus Abschnitt 7. K03 verlangt
einen `reconciler`-Lauf über `shared/decisions/`, K01 und K02 gehören dem Durchgang „add" aus
`/fusion:revise-claude-md`, K04 ist ausdrücklich nicht zu ändern.
