# Kuratorenlauf — 260826-1100

**Status:** Complete. Erhebung am 260826-1100 gegen `7af4c36`, Anwendung am 260826-1112 gegen denselben Baumstand. Freigegeben und angewandt ist der eine Eintrag L01, und er hat allein `CLAUDE.md` angefasst.
**Datum:** 260826-1100 (Erhebung)
**Git HEAD beim Lesen:** `7af4c36` (`docs(workbench): der Schlussabgleich der wiederaufgenommenen Sitzung`, 2026-08-26 10:32:27 +0200). `CLAUDE.md` ist im Arbeitsbaum unverändert gegenüber HEAD (`git status --porcelain -- CLAUDE.md` ist leer).
**Modus:** survey am 260826-1100, danach apply am 260826-1112. Der Erhebungsauftrag nannte keine `**Mode:**`-Zeile, und die Vorgabe ist der Durchgang, der nichts schreibt. Der zweite Auftrag nennt `**Mode:** apply`, diese Datei als `**Ledger:**` und `L01` als `**Approved:**`; beide Pflichtangaben liegen vor, und die Freigabe deckt genau den einen Eintrag.
**Vorheriger Kuratorenlauf:** `shared/history/260826-1009-curator-run.md`, Baumstand `2dce7d5`, ein Eintrag, angewandt. Davor `260826-0818` (`d08dbac`, neun Einträge, alle angewandt in `fb50fcd`), `260821-2231`, `260820-2127`, `260820-1119`, `260819-1500` und `260814-1405`.

**Was seit dem vorigen Lauf dazugekommen ist.** Zwischen `2dce7d5` und `7af4c36` liegen zwei Commits. `c95f28b` hat den Eintrag L01 des vorigen Laufs angewandt, `7af4c36` den Schlussabgleich der Sitzung abgelegt, und dieser Abgleich hat den Datensatz gefunden, der diesen Lauf auslöst.

**Gegenstand und Grenze dieses Laufs.** Der Auftrag nennt einen einzigen Gegenstand: dass `CLAUDE.md` nichts darüber sagt, dass die tiefe Suche des Dateifilters seit `20c9833` ab Werk eingeschaltet ist. Der Lauf erhebt diesen Gegenstand und die zwei Fragen, die der Auftrag daran hängt: ob die Vorgabe wirklich an einer Stelle steht, und ob der neue Satz die offene Frage nach der Schwelle des Inhaltsfilters stillschweigend vorentscheidet. Er erhebt nichts sonst. Die Defektdatensätze und die Entscheidungsdatensätze bleiben unangetastet; ihre Marker gehören dem `reconciler`.

**Sprache dieser Datei.** Deutsch. `CLAUDE.md:3` deklariert `**Language:** de` als einzige Sprachzeile; nach der Fallback-Kette aus `$FUSION_PLUGIN_ROOT/rules/fusion-workbench-conventions.md` `## Project language` steuert sie beide Flächen. `bin/fusion-rules` gibt `stilwerk/chat-voice-de.yaml` und `stilwerk/default-voice-de.yaml` aus; beide sind gelesen.

---

## 1. Die acht Belegquellen, und was in jeder gelesen wurde

Der Lauf ist auf einen Gegenstand verengt, und seine Quellenzählung sagt das offen. Wo eine Quelle nur durchsucht und nicht gelesen wurde, steht das dabei.

| # | Quelle | Gelesen |
|---|---|---|
| 1 | Circle-Datensätze (`circles/`, 19 Verzeichnisse, 19 Datensätze) | **0 gelesen.** Der Gegenstand ist nach der Runde 17 und ohne aktiven Circle entstanden; kein Circle-Datensatz kann ihn führen. Die Runde 14, die den Filter gebaut hat, ist über ihren Entscheidungsspeicher erreicht und nicht über ihren Datensatz. |
| 2 | Entscheidungsdatensätze (`shared/decisions/` 41, `circles/*/decisions/` 145, zusammen 186) | **3 vollständig gelesen**, 186 durchsucht. Gelesen: `shared/decisions/260826-0859_o_…` (die offene Frage zur Schwelle), `circles/260814-1551-…/decisions/260814-1830_o_gilt-das-ankreuzfeld-deep-je-tab-oder-je-fenster.md` (die zweite offene Frage, die denselben Wert zum Gegenstand hat) und der Kopf von `260826-0923_o_…`. Die Suchen sind in Abschnitt 5 mit Kommando und Ausgabe geführt. |
| 3 | Sitzungsberichte (`shared/history/` 142, `circles/*/history/` 415, zusammen 557) | **2 gelesen**: der Schlussabgleich `260826-1024-reconciliation.md`, der den Befund erhoben hat, und der vorige Kuratorenlauf `260826-1009-curator-run.md` (Kopf, Quellentabelle, ein Eintrag als Formvorlage). Vom Lauf `260826-0818` sind die Einträge L01 und L02 gelesen, weil L02 der Vorfall ist, an dem dieser Lauf seine Stufenzuordnung misst. |
| 4 | Git-Historie | **`git show 20c9833` vollständig gelesen** (Botschaft und Diff, 3 Dateien). `git log --oneline -3` und `git status --porcelain -- CLAUDE.md` gefahren. `git log --follow -- CLAUDE.md` ist **nicht** einzeln geöffnet worden: der Gegenstand ist eine Lücke in `CLAUDE.md` und keine Aussage mit einer Entstehungsgeschichte, und der Commit, der die Lücke geschaffen hat, ist bekannt. |
| 5 | Durchsichten und Untersuchungen (`shared/reviews/` 21, `shared/analyses/` 4) | **0 vollständig gelesen.** `260826-0923-coderev-neun-aussagen-in-claude-md-und-die-vorgabe-des-dateifilters.md` ist über die Zitate des Defektdatensatzes erreicht und nicht selbst geöffnet; sein Befund („`CLAUDE.md` wird durch die Deep-Änderung nicht falsch") ist in diesem Lauf am Baum nachgeprüft und nicht geglaubt. |
| 6 | `orchestrator-events.jsonl` (1631 Zeilen) | **Durchsucht, 1 Treffer.** Die Quelle ist ausdrücklich nur bestätigend und trägt hier keinen Eintrag. |
| 7 | Archiv (`archive/`, 82 Dateien) | **0 gelesen**, 82 durchsucht. Ein Treffer (`archive/260819-1613-safe-cleanup-tier-1/shared/planning/260817-0536_c_spec-absicherung-jedes-loeschwegs.md`), und er gilt dem Wort „Vorbelegung" in einem anderen Zusammenhang, dem Löschweg. |
| 8 | `**Provenance:**`-Köpfe projekteigener Regeldateien | **0 — die Fläche fehlt.** Weder `./rules/` noch `.claude/rules/` liegt im Projekt; `ls` meldet beide als nicht vorhanden. |

**Zwei der drei Flächen sind in diesem Lauf nicht betroffen.** Die projekteigenen Regeldateien fehlen als Fläche vollständig. Die Entscheidungsdatensätze sind gelesen und tragen keinen Eintrag: der Gegenstand liegt allein in `CLAUDE.md`, und die zwei offenen Fragen, die daran hängen, sind Grund für eine engere Formulierung und nicht für eine Änderung an ihnen.

## 2. Flächengrößen

| Fläche | Vorher | Nachher (bei voller Freigabe) |
|---|---|---|
| `CLAUDE.md` | 49.803 Bytes, 195 Zeilen | 50.484 Bytes, 195 Zeilen |
| Entscheidungsdatensätze | 186 Dateien, unverändert | 186 Dateien, unverändert |
| Projekteigene Regeldateien | Fläche fehlt | Fläche fehlt |

**Streichungen nach Stufe:** keine. Der eine Eintrag hängt 681 Bytes an einen bestehenden Absatz an und löscht kein Zeichen.

**Sprengweite: unauffällig.** Null gelöschte Bytes von 49.803. Die Schwelle von 20 Prozent ist nicht in Sicht, und es gibt nichts vorab zu bestätigen.

**Keine Probe im Baum ist betroffen.** `grep -rn "CLAUDE.md" crates/ xtask/ Makefile` liefert zehn Treffer, alle in Doc-Kommentaren, die auf die Datei verweisen; keiner hält ihre Größe, ihre Zeilenzahl oder ihren Wortlaut. Die Fixtur-Frage stellt sich in diesem Projekt nicht: die Größen der Regeldateien sind in fusions eigenem Baum festgeschrieben, und dieses Projekt hat keine.

## 3. Vergleichszählungen über die sechs Flächenpaare

**Die Auswahlregel dieses Laufs ist eine einzige, und sie ist eng:** eine Aussage kommt in den Vergleich, wenn sie den Anfangsstand eines Filterschalters nennt, oder wenn sie sagt, was den Durchlauf über den Unterbaum auslöst. Der Auftrag verengt den Lauf auf diesen Gegenstand. Jedes Verdikt unten gilt den Paaren, die diese Regel erreicht hat, und nicht dem Bestand.

Die Grundgesamtheit ist mit Kommando erhoben und nicht geschätzt: `ls fusion-workbench/shared/decisions/*.md | wc -l` → 41, `ls fusion-workbench/circles/*/decisions/*.md | wc -l` → 145, zusammen 186 Datensätze. Die Kandidaten daraus liefert `grep -rli "ankreuzfeld .deep\|tiefe suche"` über dieselben zwei Speicher, Ausgabe in Abschnitt 5.

| Paar | Kandidaten nach der Regel | Gelesen | Verdikt |
|---|---|---|---|
| Datensatz gegen Datensatz | 2 (`260826-0859_o_…` gegen `260814-1830_o_gilt-das-ankreuzfeld-deep-je-tab-oder-je-fenster.md`) | 2 | Kein Widerspruch. Die beiden fragen verschiedene Fragen an denselben Wert: die eine nach der Schwelle, die andere nach dem Gültigkeitsbereich. Beide sind offen, und eine offene Frage überholt nichts. |
| Datensatz gegen Regeldatei | 0 | 0 | Die Fläche fehlt. |
| Datensatz gegen `CLAUDE.md` | 2 | 2 | Kein Widerspruch, und das ist der Befund dieses Laufs: `CLAUDE.md` sagt zu dem, was beide Datensätze zum Gegenstand haben, nichts. Siehe Abschnitt 4. |
| Regeldatei gegen Regeldatei | 0 | 0 | Die Fläche fehlt. |
| Regeldatei gegen `CLAUDE.md` | 0 | 0 | Die Fläche fehlt. |
| `CLAUDE.md` gegen sich selbst | 3 (Zeile 24, Zeile 143, Zeile 147) | 3 | Kein Widerspruch. Zeile 24 beschreibt das Ankreuzfeld als Gegenstand der Runde 10, Zeile 143 die drei Filterregeln, Zeile 147 die Kosten des Durchlaufs. Keine der drei nennt einen Anfangsstand, und keine sagt, was den Durchlauf auslöst. |

## 4. Der Befund, und was ihn trägt

**`CLAUDE.md` enthält keine falsche Aussage über die tiefe Suche; es fehlt eine.** Selbst gefahren am 260826-1100 gegen `7af4c36`:

```
grep -n "Tippen im Dateifenster\|Durchlauf über den Unterbaum\|inhaltsschwelle\|Deep\|Content" CLAUDE.md
  → 24, 25   (die Tabellenzeilen der Runden 10 und 11, die die zwei Ankreuzfelder benennen)
  → 117      (die Auslieferungskette, ein Treffer auf „Content" in `CFBundleShortVersionString`)
  → 143      (der Filterabsatz: die drei Regeln, `inhaltsschwelle` ohne Zahl, die Ordnerwechsel-Regel)
  → 147      (der Absatz über den Durchlauf: seine Deskriptorbilanz)
```

Keine dieser vier Stellen nennt einen Anfangsstand. Sie gelten nach `20c9833` unverändert.

**Der Baum sagt es an genau einer Stelle.** Selbst gemessen, dieselbe Stunde:

```
grep -rn "tief: *\(true\|false\)" crates/ --include="*.rs"
  → crates/krk-core/src/verzeichnis/modell.rs:374:            tief: true,
```

Ein Treffer im ganzen Baum. Die Behauptung des Auftrags, die Vorgabe stehe an einer Stelle, hält der Messung stand.

**Und der erste Anschlag genügt.** `Tabliste::durchlauf_nachziehen_an` (`crates/krk-ui/src/tabs.rs:897`) kehrt um, solange `!tab.modell.filter_steht() || (!tab.modell.tief() && !tab.modell.inhalt_wirkt())` gilt. `filter_steht` ist `!self.filtertext.is_empty()` (`crates/krk-core/src/verzeichnis/modell.rs:936`), also ein Zeichen. Steht die tiefe Suche ab Werk auf `true`, ist die zweite Hälfte der Bedingung ab dem ersten Anschlag erfüllt. Vor `20c9833` verlangte derselbe Weg einen Klick des Nutzers auf das Ankreuzfeld.

**Die zwei Nebenfragen des Auftrags, beide selbst nachgemessen und beide mit einer Folge für die Formulierung.**

`krk_core::ablage::sitzung::Tab` trägt fünf Felder (`ordner`, `auswahl`, `verstecke_ausgeblendet`, `sortierung`, `bildlauf`, `crates/krk-core/src/ablage/sitzung.rs:82-113`), keines davon den Stand der tiefen Suche oder des Inhaltsfilters. `Tabinhalt::aus_zustand` (`crates/krk-ui/src/tabs.rs:100-115`) ist der eine Weg, auf dem ein Tab entsteht, ruft `Ordnermodell::neu` und setzt danach allein `verstecke_ausblenden_setzen` und `sortierung_setzen`; ein neuer Tab erbt den Stand also nicht vom Geschwistertab. Beides stimmt mit der Commit-Botschaft von `20c9833` überein und ist hier unabhängig davon erhoben.

**Genau diese zwei Halbsätze gehören trotzdem nicht in den neuen Text.** Ob der Stand die Sitzung übersteht und ob er je Tab oder je Fenster gilt, ist der Gegenstand von `circles/260814-1551-tippen-filtert-dateiliste-flach-und-tief/decisions/260814-1830_*_gilt-das-ankreuzfeld-deep-je-tab-oder-je-fenster.md`, offen seit dem 260814. Der letzte Punkt seiner Constraints sagt es wörtlich: „Ob der Stand die Sitzung übersteht, gehört zur Antwort." Der Baum selbst schreibt seine Antwort ausdrücklich als vorläufig aus: die Probe `ein_neuer_tab_traegt_die_vorbelegung_der_tiefen_suche` (`crates/krk-ui/src/tabs.rs:1597-1616`) trägt im Kommentar „Das ist die Antwort des Baumes und keine Antwort auf … die offen bleibt". Wer den heutigen Stand in `CLAUDE.md` schreibt, macht aus einer vorläufigen Antwort eine verbindliche, und das ist die Vorentscheidung, nach der der Auftrag gefragt hat — an einer zweiten Stelle, die der Auftrag nicht genannt hat.

**Die Schwelle des Inhaltsfilters ist der Fall, den der Auftrag genannt hat, und er ist mit einem Zitat gelöst.** `inhaltsschwelle(tief)` gibt fünf bei eingeschalteter tiefer Suche und drei sonst (`crates/krk-core/src/verzeichnis/filter.rs:157-159`), und ob die Kopplung so bleibt, ist offen (`shared/decisions/260826-0859_*_…`). Der Eintrag unten nennt die Kopplung, nennt keine der zwei Zahlen und zitiert die offene Frage. Damit steht in `CLAUDE.md` die Auskunft „hier hängt etwas daran, und die Frage läuft" statt einer Antwort auf sie.

**Wohin der Satz gehört, und warum nicht an die andere der zwei Stellen.** Der Defektdatensatz lässt beide zu, den Filterabsatz (Zeile 143) und den Absatz über den Durchlauf (Zeile 147). Der Eintrag setzt ihn an das Ende von Zeile 143. Der Absatz führt das Ankreuzfeld „Deep" und `inhaltsschwelle` schon als Begriffe, der neue Satz knüpft dort an eingeführte Namen an und muss keinen zweiten Ort für dieselbe Sache eröffnen. Zeile 147 hat den stärkeren Anspruch aus der Lesersicht des Datensatzes, weil dort die Kosten stehen; sie hat den schwächeren aus der Sicht dessen, der wissen will, womit der Filter beginnt, und dieser Leser sucht bei den Filterregeln. Eine zweite Nennung an beiden Stellen fällt aus, solange `CLAUDE.md` eine Aussage einmal führt.

## 5. Die gefahrenen Suchen, mit Ausgabe

```
ls fusion-workbench/shared/decisions/*.md | wc -l                 → 41
ls fusion-workbench/circles/*/decisions/*.md | wc -l               → 145
grep -rli "ankreuzfeld .deep\|tiefe suche" <beide Speicher>        → 8 Dateien
  shared/decisions/260826-0859_o_die-vorgabe-der-tiefen-suche-…
  circles/260814-1551-…/decisions/260814-1830_i_bleibt-der-filtertext-…
  circles/260814-1551-…/decisions/260814-1552_a_wie-kommt-der-nutzer-von-einem-tiefen-treffer-…
  circles/260814-1551-…/decisions/260814-1830_o_gilt-das-ankreuzfeld-deep-je-tab-oder-je-fenster.md
  circles/260814-1551-…/decisions/260814-1552_i_steigt-die-tiefe-suche-in-symbolische-verknuepfungen-hinab.md
  circles/260814-1551-…/decisions/260814-1552_i_welche-tastenkombination-schaltet-die-tiefe-suche.md
  circles/260814-1551-…/decisions/260814-1552_i_passt-der-filter-auf-den-namensanfang-…
  circles/260814-1551-…/decisions/260814-1830_o_an-welcher-stelle-der-bedeutungen-von-esc-…
grep -rli "vorbelegung\|inhaltsschwelle" fusion-workbench/archive  → 1 Datei, anderer Gegenstand
find fusion-workbench/archive -type f | wc -l                      → 82
wc -l < fusion-workbench/orchestrator-events.jsonl                 → 1631
ls ./rules ./.claude/rules                                         → beide nicht vorhanden
wc -c -l CLAUDE.md                                                 → 195 Zeilen, 49.803 Bytes
git status --porcelain -- CLAUDE.md                                → leer
```

**Von den acht Kandidaten des zweiten Laufs sind zwei gelesen.** Die sechs übrigen stammen sämtlich aus der Runde 14 und beantworten Fragen des Filterbaus, die den Anfangsstand nicht berühren; ihre Titel sagen das, und die Auswahlregel dieses Laufs erreicht sie nicht. Das ist eine Aussage über die Regel und nicht über den Bestand.

## 6. Die Änderungsliste

Ein Eintrag, auf `CLAUDE.md`. Nach Folgengruppe, in der Reihenfolge, in der das Gate sie zeigt:

| Folgengruppe | Kennungen | Zahl |
|---|---|---|
| Zusagenrücknahme | — | 0 |
| Stufe 3 | — | 0 |
| Stufe 2 | — | 0 |
| Stufe 1 | L01 | 1 |
| Verdichtung | — | 0 |

Kandidaten: **0.** Es liegt kein Befund ohne den Beleg vor, den seine Stufe verlangt.

### L01 — Der Filterabsatz sagt jetzt, dass die tiefe Suche ab Werk steht und ein Anschlag den Unterbaum anläuft

- **Fläche:** `CLAUDE.md`
- **Datei:** `CLAUDE.md:143` (der Satz tritt an das Ende des Absatzes)
- **Stufe:** 1
- **Beleg:** Selbst gefahren am 260826-1100 gegen `7af4c36`. `grep -rn "tief: *\(true\|false\)" crates/ --include="*.rs"` → ein Treffer, `crates/krk-core/src/verzeichnis/modell.rs:374: tief: true,`. Die Bedingung des Durchlaufs steht in `crates/krk-ui/src/tabs.rs:897`, `filter_steht` in `crates/krk-core/src/verzeichnis/modell.rs:936`, die Staffelung in `crates/krk-core/src/verzeichnis/filter.rs:157-159`. Der Commit ist `20c9833` (`feat(core): die Vorgabe des Dateifilters steht jetzt auf tiefer Suche`, 2026-08-26 09:07:31), Diff vollständig gelesen. Abgelegt als `shared/issues/260826-1024_*_claude-md-sagt-nicht-dass-die-tiefe-suche-ab-werk-steht-und-ein-anschlag-den-unterbaum-ablaeuft.md`, gefunden im Schlussabgleich `shared/history/260826-1024-reconciliation.md`.
- **Zur Stufe, offen gesagt:** `CLAUDE.md` trägt zu diesem Gegenstand keinen falschen Satz, sondern gar keinen. Falsch ist der Schluss, den ein Leser der Zeilen 143 und 147 heute zieht, nämlich dass der Durchlauf über den Unterbaum einen Klick verlangt. Der Eintrag ist damit eine Ergänzung mit Stufe-1-Beleg, also einem selbst gefahrenen Kommando mit Ausgabe, und nicht die Berichtigung einer Zeile. Dieselbe Zuordnung hat der Lauf `260826-0818` mit seinem Eintrag L02 getroffen, den der Nutzer freigegeben hat. `inference:` — dass diese Zuordnung die richtige ist, ist eine Auslegung der Stufentabelle und keine Messung; das Gate steht dahinter.
- **Folgengruppe:** tier-1
- **Entfernte Zusage:** keine. Der Satz kommt hinzu, kein Zeichen des Absatzes ändert sich.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher** (das Ende des Absatzes; davor bleibt der Absatz unverändert):

> … und fällt nur durch `Esc`, durch das Zurücknehmen seines letzten Zeichens, mit dem Tab, der ihn hält, oder mit der Sitzung (`circles/260814-1551-tippen-filtert-dateiliste-flach-und-tief/decisions/260814-1830_*_bleibt-der-filtertext-bei-einem-ordnerwechsel-stehen-wenn-deep-aus-ist.md`).

**Nachher:**

> … und fällt nur durch `Esc`, durch das Zurücknehmen seines letzten Zeichens, mit dem Tab, der ihn hält, oder mit der Sitzung (`circles/260814-1551-tippen-filtert-dateiliste-flach-und-tief/decisions/260814-1830_*_bleibt-der-filtertext-bei-einem-ordnerwechsel-stehen-wenn-deep-aus-ist.md`). **Seit dem 260826 steht das Ankreuzfeld „Deep“ ab Werk auf ein, und damit stößt schon der erste Anschlag im Dateifenster den Durchlauf über den Unterbaum an**; bis dahin verlangte derselbe Weg einen Klick des Nutzers. Die Vorbelegung steht an genau einer Stelle, `Ordnermodell::neu` in `crates/krk-core/src/verzeichnis/modell.rs`, und ihr Kommentar dort ist die Auskunft, nicht diese Zeile. An demselben Wert hängt die Schwelle des Inhaltsfilters, die `inhaltsschwelle` nach dem Stand der tiefen Suche staffelt; ob die Kopplung so bleibt, ist offen (`shared/decisions/260826-0859_*_die-vorgabe-der-tiefen-suche-hebt-die-schwelle-des-inhaltsfilters-von-drei-auf-fuenf.md`).

## 7. Was dieser Lauf nicht getan hat

- **Keine Zahl in die Prosa.** Weder die Drei noch die Fünf der Staffelung steht im neuen Text, und die Vorbelegung selbst steht als Ort und nicht als Wert. `CLAUDE.md` verlangt diese Form von sich selbst, und beide gefragten Werte sind mit einem Kommando aus dem Baum zu holen.
- **Keine Antwort auf die zwei offenen Fragen.** Die Frage nach der Schwelle (`260826-0859_o_…`) ist zitiert und nicht beantwortet. Die Frage nach dem Gültigkeitsbereich (`260814-1830_o_gilt-das-ankreuzfeld-deep-je-tab-oder-je-fenster.md`) ist im neuen Text gar nicht berührt, und Abschnitt 4 sagt, warum das eine bewusste Auslassung ist.
- **Kein Marker bewegt.** Der Defektdatensatz `260826-1024_o_…` bleibt offen; das Schließen ist eine Bewegung gegen den Baumstand und gehört `agents/reconciler.md`. Dasselbe gilt für die zwei Entscheidungsdatensätze.
- **Nichts committet und nichts gestagt.**

## 8. Ausgänge

**L01 — angewandt.** Gefahren am 260826-1112 gegen `7af4c36`.

Der Vortext ist vor dem Schreiben von der Platte gelesen und stimmte zeichengenau mit dem Eintrag überein; der Anker `…_bleibt-der-filtertext-bei-einem-ordnerwechsel-stehen-wenn-deep-aus-ist.md`).` kommt in `CLAUDE.md` genau einmal vor. Nach dem Schreiben ist die Zeile 143 erneut gelesen und gegen den Nachher-Block des Eintrags Byte für Byte verglichen worden: sie endet auf genau die freigegebenen Zeichen. Der Satz ist angehängt, kein Zeichen des Absatzes ist ersetzt.

| | Vorher | Nachher |
|---|---|---|
| `CLAUDE.md`, Bytes | 49.803 | 50.484 |
| `CLAUDE.md`, Zeilen | 195 | 195 |

Die Zunahme beträgt 681 Bytes und trifft die Vorhersage aus Abschnitt 2 genau. `git diff --stat -- CLAUDE.md` meldet eine geänderte Zeile, eine Einfügung und eine Löschung, wie es bei einer Zeile, an die angehängt wird, zu erwarten ist.

**Die zwei anderen Flächen sind unangetastet.** Die 186 Entscheidungsdatensätze tragen keinen Eintrag dieses Laufs, und projekteigene Regeldateien liegen im Projekt nicht.

**Nichts committet, nichts gestagt.** `git diff --cached --name-only` ist leer; der Arbeitsbaum steht, wie der Auftrag es verlangt. Der Rückweg für die Änderung ist `git checkout -- CLAUDE.md`.

**Keine Probe im Baum ist nachzuziehen.** Die zehn Erwähnungen von `CLAUDE.md` unter `crates/`, `xtask/` und im `Makefile` sind Verweise in Doc-Kommentaren und halten weder Größe noch Wortlaut der Datei.

**Offen bleibt, was Abschnitt 7 als offen ausweist.** Der Defektdatensatz `shared/issues/260826-1024_o_…` trägt weiter seinen Marker: ihn zu bewegen, ist ein Abgleich gegen den Baumstand und gehört `agents/reconciler.md`.
