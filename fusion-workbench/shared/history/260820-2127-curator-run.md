# Kuratorenlauf — 260820-2127

**Status:** Complete — die Erhebung ist gefahren, das Gate ist mit „alle sechs Einträge“ beantwortet, und der Anwendungsdurchgang hat alle sechs angewandt.
**Datum:** 260820-2127 (Erhebung)
**Git HEAD beim Lesen:** `84a9176` (`chore(workbench): neun terminale Datensaetze ins Archiv, das Waechterprotokoll gerollt`), Arbeitsbaum sauber
**Modus:** survey (Erhebung), danach apply (Anwendungsdurchgang, Freigabe `all`)
**Vorheriger Kuratorenlauf:** `shared/history/260820-1119-curator-run.md`, Baumstand `2beb1de`, fünf Einträge, alle angewandt. Davor `shared/history/260819-1500-curator-run.md` bei `82c020a` und `shared/history/260814-1405-curator-run.md` bei `43dfe90`.

**Sprache dieser Datei.** Deutsch. `CLAUDE.md:179` deklariert `**Language:** de` als einzige Sprachzeile; nach der Fallback-Kette aus `$FUSION_PLUGIN_ROOT/rules/fusion-workbench-conventions.md` `## Project language` steuert sie beide Flächen. `bin/fusion-rules` gibt `stilwerk/chat-voice-de.yaml` und `stilwerk/default-voice-de.yaml` aus; beide sind gelesen.

---

## 1. Was sich seit dem vorigen Lauf geändert hat

Der vorige Lauf las den Baum bei `2beb1de`, gut zehn Stunden vor diesem. Seither sind **sechs Commits** gelandet (`git log --oneline 2beb1de..HEAD | wc -l` → 6). Drei davon tragen Befunde dieses Laufs:

- **`f5300f4` legt einen zweiten Auslieferungsweg an** (`xtask/src/beglaubigung.rs`, 665 Zeilen, dazu `certify-only.sh` und ein `make`-Ziel). Der Commit sagt in seinem eigenen Rumpf: „CLAUDE.md kennt den zweiten Weg noch nicht; das ist Kuratorarbeit und nicht in diesem Commit."
- **`b0379bc` bringt den breiten Abgleich zum Sitzungsabschluss** (`shared/history/260820-2056-reconciliation.md`, Verdikt `review-needed`). Er filt unter anderem `shared/issues/260820-2056_o_claude-md-nennt-eine-zaehlprobe-unter-einem-namen-den-der-baum-nicht-traegt.md` und schreibt ausdrücklich „Kuratorarbeit" darüber.
- **`84a9176` archiviert neun terminale Datensätze** (Archivlauf `260820-2115-safe-cleanup-tier-1`), darunter sieben Defekte über `CLAUDE.md`, die zwei Kuratorenläufe berichtigt und der Abgleich vom 260820-2056 geschlossen hat.

Die Erhebung ist nicht auf dieses Intervall beschränkt: `git log --follow` ist über die ganze Historie von `CLAUDE.md` gelesen, und zwei Befunde dieses Laufs (L02 und L05) sind älter als das Intervall — einer seit dem 260814, einer mindestens seit der Runde 11.

## 2. Die drei normativen Flächen

| # | Fläche | Befund |
|---|---|---|
| 1 | Entscheidungsdatensätze unter `$SCAN_DECISIONS` = `shared/decisions/`, dazu die Circle-Speicher `circles/*/decisions/` von Hand hinzugelistet | vorhanden, **156 Dateien**, alle lesbar. Marker: 33 offen, 6 beantwortet, 107 umgesetzt, 8 überholt, 2 zurückgestellt |
| 2 | Projekteigene Regeldateien `./rules/` und `.claude/rules/` | **abwesend**. `ls rules` und `ls .claude/rules` melden beide „No such file or directory". Es gibt in diesem Projekt keine projekteigenen Regeldateien und damit auch keine `**Provenance:**`-Kopfzeilen |
| 3 | `CLAUDE.md` im Projektwurzelverzeichnis | vorhanden, 183 Zeilen, 39019 Bytes, lesbar |

Keine Fläche war unlesbar. Für Fläche 2 gilt der Fall „abwesend": null gemeldet, die beiden anderen Flächen vollständig gefahren.

**`bin/fusion-paths curator` gibt für `SCAN_DECISIONS` allein `shared/decisions` aus**, weil kein Circle aktiv ist. Die Circle-Speicher sind deshalb von Hand dazugelistet — genau der Handgriff, den der Abgleich vom 260820-2056 unter „Für den nächsten Durchgang" verlangt. Ohne ihn fielen 79 der 156 Datensätze aus dem Suchbereich.

## 3. Evidenzquellen — was gelesen wurde

| # | Quelle | Bestand | Gelesen |
|---|---|---|---|
| 1 | Circle-Datensätze (`circles/`) | 16 | 16 Markerzustände; die Kopffelder `Active spec/plan:` und `Active session history:` aller 16; die Schließungsnotiz der Runde 14 über den vorigen Lauf |
| 2 | Entscheidungsdatensätze | 156 | 156 Marker; die 8 Zeilen `Superseded by:` aller überholten samt ihrer Nachfolger; die Zeilen `Answered:`/`Implemented:` von 4 weiteren; die 11 im Intervall geänderten im Diff; 19 auf Markerebene gegen die Zitate in `CLAUDE.md` |
| 3 | Sitzungsprotokolle (`history/`) | 462 | 3 vollständig: `shared/history/260820-2056-reconciliation.md`, `shared/history/260820-1624-coder-nur-beglaubigungsweg-fuer-ein-fertiges-buendel.md`, `shared/history/260820-1119-curator-run.md`. Dazu `shared/history/260819-2040-artefaktsprache-deklaration-zurueckgenommen.md` in der tragenden Zeile |
| 4 | `git log --follow` | — | `CLAUDE.md`: ganze Historie, dazu `git log -S` auf vier einzelne Formulierungen und `git blame` auf drei Zeilen. Projekteigene Regeldateien: entfällt. Dazu `git log --oneline 2beb1de..HEAD` (6), `git show --stat f5300f4`, `git tag -l --sort=creatordate` (14 Tags) |
| 5 | Durchsichten und Analysen | 60 + 3 | **Der Volltext von zweien**, plus alle 39 Fundstellen von `CLAUDE.md` über den ganzen Bestand mit Kontext. Gelesen: `circles/260817-0833-…/reviews/260818-0754-coderev-zeigerreparatur-buendel-g.md` (Befundteil) und `circles/260817-0833-…/reviews/260818-0024-coderev-bundle-e-the-prose-and-the-records.md` (Befundteil). **Das schließt die benannte Lücke des vorigen Laufs teilweise**, siehe „Was nicht gelesen wurde" |
| 6 | `orchestrator-events.jsonl` | 1269 Zeilen | die letzten 3. Corroborierend für L06 (`session_start … certify-only auf Nutzerwunsch`), tragend für keinen Befund |
| 7 | Archivspeicher `$WORKBENCH/archive` | **82 Dateien in zwei Läufen** (`260819-1613`, `260820-2115`) | vollständiger Dateibestand aufgelöst und gegen jedes Zitat der lebenden Entscheidungsspeicher und `CLAUDE.md` gehalten, siehe K01. Der Manifest-Kopf des neuen Laufs im Volltext |
| 8 | `**Provenance:**`-Kopfzeilen auf Regeldateien | **0** — es gibt keine projekteigenen Regeldateien | — |
| — | Defektkorpus (Gegenprobe, keine vierte Fläche) | 547, davon 143 offen | 143 offene Namen; 2 vollständig (`260820-2056_o_claude-md-nennt-eine-zaehlprobe-…`, `260818-0753_o_die-ausnahme-fuer-maschinell-gelesene-kopffelder-…`) |

**Was nicht gelesen wurde.** Von den 60 Durchsichten sind zwei im Befundteil gelesen; die übrigen 58 nur über ihre `CLAUDE.md`-Fundstellen mit Kontext. Von den 462 Sitzungsprotokollen sind drei gelesen. Von den 143 offenen Defekten sind zwei im Volltext gelesen. Ein Befund, der in einer ungelesenen Durchsicht ohne das Wort `CLAUDE.md` stünde, hätte dieser Durchgang nicht gefunden.

## 4. Flächengrößen

| Fläche | Bytes vorher | Zeilen vorher | Bytes nachher | Zeilen nachher |
|---|---|---|---|---|
| Entscheidungsdatensätze (156 Dateien zusammen) | 970179 | 10981 | **970179** | **10981** |
| Projekteigene Regeldateien | 0 | 0 | 0 | 0 |
| `CLAUDE.md` | 39019 | 183 | **40473** | **185** |

**Blast-Radius.** Kein Eintrag dieses Durchgangs streicht eine Vorschrift. Zusammen entfernt werden weniger als 500 Zeichen aus `CLAUDE.md`, also **weit unter zwei Prozent** der 39019 Bytes; die Einträge fügen mehr hinzu, als sie wegnehmen. Aus den beiden anderen Flächen wird nichts entfernt. Die Schwelle von 20 Prozent je Fläche ist bei weitem nicht erreicht; **es gibt keine gesonderte Rückfrage zum Ausmaß.**

## 5. Vergleichszählungen je Flächenpaar

**Die Auswahlregel steht bei jedem Paar dabei, und das Verdikt gilt für die Paare, die die Regel erreicht hat, nicht für den Korpus.** Die Korpuszahlen sind mit `find … | wc -l` erhoben, nicht geschätzt.

| Paar | Auswahlregel | Paare erzeugt | Gelesen | Verdikt |
|---|---|---|---|---|
| 1. Entscheidung gegen Entscheidung | (a) der Überholungsgraph: jeder Datensatz mit `Superseded by:` gegen den dort genannten Nachfolger; (b) die **11 im Intervall geänderten** Datensätze (8 Umbenennungen auf `_i_`, 3 Rumpfergänzungen) gegen die 39 aktiven | **8** (a) + **11** (b) = 19 von 12090 möglichen | 19 | **kein Widerspruch.** Die acht Überholungen nennen jede ausdrücklich, welche Hälfte fällt und welche fortgilt. Die elf Änderungen sind Markerhebungen und Abgleichsvermerke, keine konkurrierenden Antworten. Zwei Formfehler stehen als Kandidaten unten |
| 2. Entscheidung gegen Regeldatei | entfällt — es gibt keine projekteigenen Regeldateien | 0 | 0 | — |
| 3. Entscheidung gegen `CLAUDE.md` | jeder Datensatz, den `CLAUDE.md` zitiert (**19**, alle mit `grep -oE` aus der Datei gezogen und einzeln gegen den Bestand aufgelöst), plus die 11 im Intervall geänderten | **30** | 30 (19 auf Markerebene, davon 4 im Rumpf; 11 im Diff) | **kein Widerspruch.** Alle 19 Zitate lösen auf, keines zeigt ins Archiv, und jeder Marker stimmt mit dem überein, was `CLAUDE.md` ihm zuschreibt (offen, zurückgestellt, geschlossen, umgesetzt) |
| 4. Regeldatei gegen Regeldatei | entfällt | 0 | 0 | — |
| 5. Regeldatei gegen `CLAUDE.md` | entfällt | 0 | 0 | — |
| 6. `CLAUDE.md` gegen sich selbst | alle 183 Zeilen gegeneinander gelesen, mit besonderer Aufmerksamkeit für Rückverweise innerhalb eines Absatzes | ganze Datei | ganze Datei | **ein Befund:** Zeile 77 sagt „`Kommando` in derselben Datei" und meint damit die zuletzt genannte, `krk-ui/src/kommandos/fokus.rs`; derselbe Satz nennt zwei Sätze später `crates/krk-core/src/tasten/belegung.rs`. Eintrag L05 |

**Die Lücke der Regel (b)** ist dieselbe wie in beiden Vorläufen: sie erreicht nur den Ausschnitt, den die geänderten Datensätze berühren, und sie findet nicht zwei Datensätze, die dieselbe Sache mit verschiedenen Wörtern benennen. **Ein Verdikt über alle 39 aktiven Datensätze gegeneinander hat dieser Durchgang nicht**; zuletzt gefahren hat einen Ausschnitt davon der Lauf `260819-1500` über 11 von 7381 möglichen Paaren.

## 6. Vorabinhalt geänderter Entscheidungsdatensätze

**Keiner.** Dieser Durchgang schlägt an keinem der 156 Entscheidungsdatensätze eine Änderung vor, weder am Rumpf noch am Marker noch am Dateinamen. Alle sechs Einträge liegen auf `CLAUDE.md`. Es ist deshalb kein Vorabinhalt festzuhalten.

---

## 7. Das Änderungsbuch

Sechs Einträge, alle auf `CLAUDE.md`. Gruppen in der Reihenfolge, in der sie am Gate erscheinen: Streichung einer Vorschrift (keine), Tier 3 (keine), Tier 2 (einer), Tier 1 (fünf), Zusammenführungen (keine).

### Streichung einer Vorschrift (0 Einträge)

Keiner. Kein Eintrag entfernt eine Vorschrift. Drei Einträge (L02, L03, L04) entfernen eine **Zahl oder eine Zeilennummer** und setzen an ihre Stelle das Kommando, das sie erzeugt; die Aussage darüber bleibt in allen dreien wörtlich stehen.

### Tier 3 (0 Einträge)

Keiner. Kein Befund ruht auf einer Bahn statt auf einem Kommando oder einem Datensatz.

### Tier 2 (1 Eintrag)

### L06 — seit dem 260820 gibt es einen zweiten Auslieferungsweg, und `CLAUDE.md` kennt nur den ersten

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 109 (Ende des Absatzes)
- **Tier:** 2
- **Zitat:** `shared/history/260820-1624-coder-nur-beglaubigungsweg-fuer-ein-fertiges-buendel.md`, Abschnitt `## Offen`, wörtlich: „`CLAUDE.md` nennt unter „Bauen und prüfen" die vier Schichten der Auslieferungskette und kennt den zweiten Weg noch nicht. Nachzuziehen ist dort ein Satz; der Kurator entscheidet die Form." Derselbe Satz steht im Rumpf des Commits `f5300f4`. Der Satz in `CLAUDE.md`, den er berührt, ist „**Ausgeliefert wird über `./release.sh <version>`, und die Kette darunter hat vier Schichten, von denen jede genau eine Sache hinzufügt:**". **Am Baum nachgeprüft:** `xtask/src/main.rs:191` führt den Unterbefehl `"beglaubigen" => beglaubigung::ausfuehren(…)`, `Makefile:144` das Ziel `beglaubigen`, und `certify-only.sh` liegt im Wurzelverzeichnis und ruft `make -C … beglaubigen VERSION="$1"`. `README.md:360` trägt den Abschnitt `### Nur beglaubigen`. Corroborierend, nicht tragend: `orchestrator-events.jsonl`, letzter Eintrag, `session_start … "Fortsetzung nach Sitzungsabschluss: certify-only auf Nutzerwunsch"`.
- **Konsequenzgruppe:** tier-2
- **Gestrichene Vorschrift:** keine. Der ganze bestehende Absatz bleibt wörtlich stehen; der Eintrag hängt einen zweiten an
- **Rückweg:** `git checkout -- CLAUDE.md`
- **Vertrauensgrad:** `verified:` für alles, was am Baum steht (die Kette, die Ziele, der Abschnitt in `README.md`). `inference:` für die Einordnung als Überholung: dass der bestehende Satz durch den zweiten Weg zu eng geworden ist, ist meine Lesart zweier Prosastellen und von keinem Kommando geprüft. Der Coder und der Commit nennen dieselbe Stelle und dieselbe Abhilfe, und beide weisen sie ausdrücklich dem Kurator zu

**Vorher** (Ankerzeile, das Ende des Absatzes; nichts daran wird geändert):

> Wann Major, Minor oder Patch steigt, steht in `README.md` unter `### Versionsstufen`.

**Nachher** (dieselbe Zeile, gefolgt von einem neuen Absatz):

> Wann Major, Minor oder Patch steigt, steht in `README.md` unter `### Versionsstufen`.
>
> **Seit dem 260820 steht daneben ein zweiter Weg mit denselben vier Schichten, der nur beglaubigt:** `./certify-only.sh <version>` → `make beglaubigen VERSION=…` → `cargo xtask beglaubigen <version>`. Er ist für den Lauf gedacht, der erst an der Beglaubigung gescheitert ist — etwa am Zeitüberlauf des Uploads zu Apple —, während das fertige, signierte Bündel unter `target/KRK.app` liegt und allein das Ticket fehlt; ein zweites `./release.sh <zahl>` bräche in dieser Lage an Station 1 ab, weil der Tag nach dem ersten Lauf nicht mehr allein auf HEAD steht. **Er prüft weder Tag noch Arbeitsbaum, und das ist der Zweck und keine Nachlässigkeit:** ein so beglaubigtes Bündel ist nicht durch die Vorprüfungen der Auslieferungskette gegangen. Gebaut wird nichts; die Zahl im Argument hält `xtask` gegen `CFBundleShortVersionString` der `Info.plist` **im gebauten Bündel** und nicht gegen die `Cargo.toml`, damit kein Bündel von vorgestern bei Apple landet. Im Einzelnen steht der Weg in `README.md` unter `### Nur beglaubigen`.

### Tier 1 (5 Einträge)

### L01 — `CLAUDE.md` schickt den Leser an eine Zählprobe, die es unter diesem Namen nicht gibt

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 131
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe.
  ```
  $ grep -rn 'die_zeichenregel_und_der_vergleich_stehen_je_einmal' --include='*.rs' crates/
  (keine Ausgabe, Rückgabewert 1)
  $ grep -n 'die_zeichenregel' crates/krk-core/tests/verzeichnis.rs
  3095:fn die_zeichenregel_hat_zwei_rufer_und_der_vergleich_drei() {
  ```
  Der Defektdatensatz `shared/issues/260820-2056_o_claude-md-nennt-eine-zaehlprobe-unter-einem-namen-den-der-baum-nicht-traegt.md` beschreibt denselben Befund, misst ihn an `f5300f4` und schreibt unter `## Was zu tun ist`: „Das ist Kuratorarbeit an einer der drei normativen Flächen, nicht Codearbeit." Der Datensatz hält daneben fest, dass der Spec der Runde 11 denselben alten Namen führt und dort **stehen bleibt** — dieser Eintrag fasst ihn nicht an.
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine. Der Satz behält seine Aussage („die Zahl steht in der Probe und nicht in dieser Datei") und bekommt nur den Namen, unter dem die Probe wirklich steht
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> Wie viele Rufer jede hat, sagt die Zählprobe `die_zeichenregel_und_der_vergleich_stehen_je_einmal_…` in `crates/krk-core/tests/verzeichnis.rs` und nicht diese Zeile

**Nachher:**

> Wie viele Rufer jede hat, sagt die Zählprobe `die_zeichenregel_hat_zwei_rufer_und_der_vergleich_drei` in `crates/krk-core/tests/verzeichnis.rs` und nicht diese Zeile

*Zur Form:* der Name steht ausgeschrieben und ohne Auslassungszeichen. Der Defektdatensatz begründet es: die Kürzung mit `…` liest sich als Präfix, und `die_zeichenregel_und_der_vergleich` trifft nichts mehr. Das ist dieselbe Gestalt, vor der `CLAUDE.md:169` für Datensatzverweise warnt.

### L02 — der genannte Aufrufer der Hülle ist `lesen` und nicht `oeffnen`

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 139
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe.
  ```
  $ grep -n 'sys::ohne_warten_oeffnen(' crates/krk-core/src/text/datei.rs
  421:    let mut datei = match crate::verzeichnis::sys::ohne_warten_oeffnen(pfad) {
  606:    let datei = match crate::verzeichnis::sys::ohne_warten_oeffnen(pfad) {
  $ grep -n '^pub fn ' crates/krk-core/src/text/datei.rs | awk -F: '$1<=421' | tail -1
  418:pub fn lesen(pfad: &Path) -> Textstand {
  ```
  Die Zeile 421 liegt in `pub fn lesen` (ab `:418`), nicht in `oeffnen`. `pub fn oeffnen` steht bei `:501` und ruft die Hülle **nicht**, sondern `lesen`; sein Doc-Kommentar (`:490-495`) schreibt es aus: „der Editor sieht von der Zerlegung in [`lesen`] nichts". Die Zusage „zwei Aufrufer, beide in `datei.rs`" hält unverändert — falsch ist der Name des einen und die Zeilennummer des anderen (`:605` ist die `fn`-Zeile, der Aufruf steht in `:606`).
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine. „Die Hülle hat zwei Aufrufer, und beide liegen seit der Runde 11 in `krk-core/src/text/datei.rs`" bleibt wörtlich stehen, und die Zuordnung „für den Editor" / „für die Vorschau und den Inhaltsfilter" ebenso
- **Rückweg:** `git checkout -- CLAUDE.md`
- **Zur Form — Ableiten statt Berichtigen:** die zwei Zeilennummern sind eine Messung des Baums, die ein Kommando erzeugt, also nennt der Eintrag das Kommando und nicht die berichtigten Zahlen. `CLAUDE.md` führt diese Form an fünf weiteren Stellen (Zeilen 8, 42, 73, 77, 121)

**Vorher:**

> `oeffnen` für den Editor (`:421`) und `bis_zur_grenze_lesen` (`:605`) für die Vorschau und den Inhaltsfilter

**Nachher:**

> `lesen`, auf dem `oeffnen` für den Editor aufsetzt, und `bis_zur_grenze_lesen` für die Vorschau und den Inhaltsfilter; welche Zeilen, sagt `grep -n 'sys::ohne_warten_oeffnen(' crates/krk-core/src/text/datei.rs`

### L03 — die Zahl der englischen Defektdatensätze stimmt unter keiner Lesart mehr

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 183
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe. Die Zeile wurde am 260819 in `6be1e81` geschrieben (`git blame -L 183,183 -- CLAUDE.md`); die Deklaration `**Artifact language:** en` stand von `c75972c` (260817) bis `6be1e81` (260819). Zwei Lesarten kommen für „elf" in Frage, und **keine liefert heute elf**:
  ```
  # (a) offene englische Defektdatensätze aus dem Fenster, heute
  → 9   (zwei sind seit dem 260819 geschlossen und ins Archiv gewandert:
         260818-0028 und 260817-1610, beide in archive/260820-2115-safe-cleanup-tier-1/)
  # (b) englische Defektdatensätze im Baum überhaupt
  → 45
  ```
  Die Lesart (a) traf am Tag des Schreibens zu — an `6be1e81` waren es zehn, mit einem Grenzfall elf. Der Archivlauf `260820-2115` hat zwei davon weggezogen.
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine. Die eigentliche Regel — „vorhandene Artefakte werden nach `## Project language` nicht übersetzt" — steht danach wörtlich unverändert da
- **Rückweg:** `git checkout -- CLAUDE.md`
- **Vertrauensgrad:** `verified:` dafür, dass 11 heute unter keiner der zwei Lesarten herauskommt. `inference:` dafür, welche der beiden Lesarten der Verfasser gemeint hat; der Befund hängt nicht daran, weil beide Lesarten scheitern
- **Zur Form — Ableiten statt Berichtigen:** eine berichtigte Zahl wäre am nächsten Archivlauf wieder falsch. Der Eintrag entfernt die Zahl und nennt den Grund, genau wie `CLAUDE.md` es bei der Untergrenzen-Quote (Zeile 155) und bei der Variantenzahl von `Kommando` (Zeile 77) schon getan hat

**Vorher:**

> Elf Defektdatensätze aus der Zeit der zurückgenommenen Artefaktsprachen-Deklaration sind englisch und bleiben als Bestand stehen; vorhandene Artefakte werden nach `## Project language` nicht übersetzt.

**Nachher:**

> Die Defektdatensätze aus der Zeit der zurückgenommenen Artefaktsprachen-Deklaration sind englisch und bleiben als Bestand stehen. **Eine Zahl steht hier nicht:** sie fällt mit jedem Archivlauf, und am 260820 sind zwei dieser Datensätze ins Archiv gewandert. Vorhandene Artefakte werden nach `## Project language` nicht übersetzt.

### L04 — „siebenmal gestiegen" ist seit zwei Auslieferungen falsch

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 42
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe.
  ```
  $ git tag -l --sort=creatordate --format='%(creatordate:short)' | awk '$1>="2026-08-15"' | uniq -c
     3 2026-08-15
     2 2026-08-16
     1 2026-08-17
     1 2026-08-18
     2 2026-08-19
     1 2026-08-20
  ```
  Zehn Tags tragen ein Datum ab dem 260815, also neun Anstiege nach `v0.4.0`. Die Zeile wurde am 260819 in `5886d04` geschrieben (`git blame -L 42,42 -- CLAUDE.md`), als es sieben waren; `v0.5.4` und `v0.5.5` sind seither dazugekommen. **An jedem Tag vom 260815 bis zum 260820 steht mindestens ein Tag**, wie die Ausgabe zeigt.
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine. Die Anweisung „Welche Version, sagt `grep '^version' Cargo.toml` und der jüngste Tag aus `git tag -l`, nicht diese Zeile" bleibt wörtlich stehen; der Eintrag ersetzt allein die Begründung dahinter
- **Rückweg:** `git checkout -- CLAUDE.md`
- **Zur Form — Ableiten statt Berichtigen:** „neunmal" wäre bei der nächsten Auslieferung wieder falsch, und die nächste Auslieferung war in den letzten sechs Tagen nie weiter weg als einen Tag

**Vorher:**

> die Zahl ist seit dem 260815 siebenmal gestiegen

**Nachher:**

> seit dem 260815 ist sie an jedem Tag mindestens einmal gestiegen, und wie oft insgesamt, sagt `git tag -l --sort=creatordate` und keine Zahl an dieser Stelle

### L05 — „in derselben Datei" zeigt auf die falsche Datei, und derselbe Satz nennt zwei Sätze später die richtige

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 77
- **Tier:** 1 (Flächenpaar 6, `CLAUDE.md` gegen sich selbst — ein toter Verweis im Sinne von „stale reference")
- **Zitat:** Kommando und Ausgabe.
  ```
  $ grep -rn '^pub enum Wirkungsbereich\|^pub enum Bereich\|^pub enum Fokus\|^pub enum Kommando' crates/
  crates/krk-core/src/tasten/belegung.rs:210:pub enum Wirkungsbereich {
  crates/krk-core/src/tasten/belegung.rs:329:pub enum Kommando {
  crates/krk-ui/src/fenstermodell.rs:103:pub enum Bereich {
  crates/krk-ui/src/kommandos/fokus.rs:75:pub enum Fokus {
  $ grep -n 'pub enum Kommando' crates/krk-ui/src/kommandos/fokus.rs
  (keine Ausgabe, Rückgabewert 1)
  ```
  Die zuletzt genannte Datei vor „in derselben Datei" ist `krk-ui/src/kommandos/fokus.rs` (für `Fokus`); `Kommando` liegt in `krk-core/src/tasten/belegung.rs`, was **derselbe Satz zwei Sätze später selbst schreibt** (`awk '/^pub enum Kommando/,/^}/' crates/krk-core/src/tasten/belegung.rs`). Der Fehler steht seit `a355347` (260814), wo der Absatz eingeführt wurde — `git log -S'Für \`Kommando\` in derselben Datei' -- CLAUDE.md` liefert genau diesen einen Commit. Die drei Zahlen des Satzes stimmen: `Wirkungsbereich` 7, `Bereich` 5, `Fokus` 5, alle drei nachgezählt.
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> Für `Kommando` in derselben Datei steht hier keine Zahl

**Nachher:**

> Für `Kommando` — dieselbe Datei wie `Wirkungsbereich`, also `krk-core/src/tasten/belegung.rs` — steht hier keine Zahl

### Zusammenführungen (0 Einträge)

Keine. Kein Absatz dieses Durchgangs ist allein aus dem Wiederlesen des Texts umgeschrieben worden.

---

## 8. Widersprüche, die ich nicht auflösen darf

**Keiner.** Von den sechs Flächenpaaren erzeugen vier keine Paare (es gibt keine projekteigenen Regeldateien), und die zwei übrigen liefern einen einzigen Befund, der kein Konflikt zweier vertretbarer Positionen ist, sondern ein toter Verweis (L05). **Dieser Durchgang legt deshalb keinen neuen offenen Entscheidungsdatensatz an.**

## 9. Kandidaten — nicht angeboten und nicht anwendbar

**K01 — einundzwanzig Zitate in lebenden Entscheidungsdatensätzen zeigen auf zwanzig Datensätze, die inzwischen im Archiv liegen.**
Gemessen: der ganze Archivbestand (80 Datensätze in zwei Läufen) gegen jedes Zitat der Form `YYMMDD-HHMM_x_<slug>` in `shared/decisions/`, `circles/*/decisions/` und `CLAUDE.md`. **`CLAUDE.md` ist unbetroffen** — alle 19 Zitate der Datei lösen im lebenden Baum auf, keines zeigt ins Archiv. Der Archivlauf vom 260820 hat die Lage nicht verschlechtert: keiner der neun neu archivierten Datensätze wird von einem lebenden Entscheidungsdatensatz zitiert.
**Warum Kandidat:** unverändert die zwei Gründe des vorigen Laufs. Erstens sagt keine Regel, welche Gestalt das berichtigte Zitat hätte. Zweitens gilt in diesem Projekt die Ortsregel aus `CLAUDE.md:171`, die `decisions/` als eingefrorenen Speicher führt; der Abgleich vom 260818-0807 hat sie so angewandt. **Er steht zum dritten Mal hier.**

**K02 — die Ausnahme für maschinell gelesene Kopffelder steht in keiner bindenden Regel.**
Die Durchsicht `circles/260817-0833-…/reviews/260818-0754-coderev-zeigerreparatur-buendel-g.md:106` schreibt ausdrücklich: „Befund 2 ist eine Zeile in `CLAUDE.md` und gehoert an das naechste Gate zur Pflege der normativen Flaechen." Der Datensatz ist `circles/260817-0833-…/issues/260818-0753_o_die-ausnahme-fuer-maschinell-gelesene-kopffelder-steht-nur-in-einem-geschlossenen-datensatz.md`; er empfiehlt einen Satz unter `## Bindende Grundlage`, wonach Kopffelder, deren Wert wörtlich als Pfad gelesen wird, den Buchstaben behalten.
**Warum Kandidat:** drei Gründe, und der dritte allein trägt.
Erstens ist der Datensatz ein **offener** Defekt, und die anschließende Frage nach der Zitierform ist als **offene** Entscheidung anhängig (`shared/decisions/260818-0201_o_does-a-cross-references-line-between-records-write-the-marker-in-the-star-form.md`). Eine offene Frage entscheidet man nicht durch eine Änderung, die ihre Antwort vorwegnimmt — derselbe Grund, aus dem der vorige Lauf K05 nicht angeboten hat.
Zweitens ist der bindende Gegendatensatz `260815-1145` seit dem 260819-1613 archiviert; ich stünde also mit einer Änderung an einer Regel, deren Grundlage ich nur noch im Archiv lesen kann.
Drittens, und entscheidend: **die eine lebende Fundstelle, auf die der Befund zeigt, gibt es nicht mehr.** Der Befund nennt `circles/260817-0833-…/_t_circle.md:7` als „heute genau eine solche Stelle im lebenden Text". Der Circle ist seit dem 260817 geschlossen, `ls circles/*/_t_circle.md` findet nichts, und alle 16 Circle-Datensätze führen `**Active spec/plan:**` heute in der Sternform. Die Lücke ist real und kann wiederkehren, sobald ein Circle aktiv wird; sie ist derzeit gegenstandslos.

**K03 — zwei überholte Entscheidungsdatensätze tragen die Zeile `Superseded by:` zweimal.**
`circles/260811-1304-…/decisions/260811-1305_s_…` (Zeilen 73 und 74) und `circles/260813-0939-…/decisions/260813-0939_s_…` (Zeilen 41 und 47).
**Warum Kandidat:** ein Formfehler, und keiner meiner zwei Änderungsgründe deckt ihn. **Neu gegenüber dem vorigen Lauf ist, dass er jetzt einen Besitzer hat:** der Abgleich vom 260820-2056 hat die Gestalt über alle 156 Datensätze erhoben und als `shared/issues/260820-2056_o_dreissig-entscheidungsdatensaetze-tragen-eine-leere-vorlagenzeile-vor-der-gefuellten.md` gefilt — 30 Dateien, 46 Schlüsselfälle. Der Kandidat, den zwei Kuratorenläufe hintereinander gefunden und niemand aufgenommen hatte, ist damit angekommen; er steht hier nur noch, um die Verbindung festzuhalten.

**K04 — ein Zitat in `CLAUDE.md` löst auf fünf Dateien auf.**
`CLAUDE.md:135` nennt „den Defekt `260815-0211` in seiner zweiten Gestalt". `find fusion-workbench -name '260815-0211_*'` liefert fünf Datensätze; gemeint ist `circles/260814-1551-…/issues/260815-0211_c_ein-deskriptormangel-des-prozesses-wird-zu-einem-dauerhaften-kein-treffer-darunter.md`.
**Warum Kandidat:** die Aussage des Satzes ist richtig und der Zeiger ist nicht tot, sondern mehrdeutig. Das ist weder ein Widerspruch noch eine Überholung; es ist die Gestalt, vor der `CLAUDE.md:169` selbst warnt, an einem eigenen Verweis. Ein Eintrag dafür wäre eine Verbesserung aus dem Wiederlesen des Texts, und die ist mir ausdrücklich verwehrt. **Wer den Slug ergänzt haben will, sagt es**; es ist eine Zeile.

**K05 — die Vorschau und der zweite Auslieferungsweg sind die zwei Stellen, an denen `CLAUDE.md` etwas nicht sagt.**
Der Absatz über die auswählbare Vorschaufläche fehlt weiter (K03 des vorigen Laufs, unverändert). L06 schließt die zweite Lücke; die erste bleibt.
**Warum Kandidat:** eine Auslassung ist keiner meiner zwei Änderungsgründe, und ein Absatz, den ich aus eigenem Ermessen hinzufüge, hat keinen Beleg, den ein späterer Lauf prüfen könnte. **L06 ist bewusst anders behandelt**, und der Unterschied ist benannt: dort nennen der Coder und der Commit die betroffene Stelle in `CLAUDE.md` wörtlich und weisen die Nacharbeit ausdrücklich dem Kurator zu; hier tut das niemand.

## 10. Arbeit außerhalb meiner Zuständigkeit

**Keine Codearbeit.** Kein Eintrag dieses Durchgangs verlangt einen Helfer, eine Probe oder eine erzeugte Tabelle; alle sechs sind reine Textänderungen an `CLAUDE.md`. Die zwei Ableitungen, die L02 und L04 einsetzen, sind fertige Kommandos aus Standardwerkzeugen.

**Keine Testvorrichtung ist betroffen.** In fusions eigenem Repository pinnt `hooks/lib/__tests__/fixtures/rules-emission.golden` die Größen der Regeldateien; hier gibt es keine projekteigenen Regeldateien und keine Vorrichtung, die `CLAUDE.md` misst. Ein Anwendungsdurchgang lässt keine Probe rot werden.

**Kein Defektdatensatz ist zu filen.** Die zwei Befunde, die andere gehören — der Formfehler der doppelten Vorlagenzeilen und die fehlende Kopffeld-Ausnahme —, sind beide bereits gefilt (`shared/issues/260820-2056_o_dreissig-…`, `circles/260817-0833-…/issues/260818-0753_o_…`). Ein dritter Datensatz wäre ein Doppel.

**Ein Datensatz bleibt für den `reconciler` liegen.** `shared/issues/260820-2056_o_claude-md-nennt-eine-zaehlprobe-unter-einem-namen-den-der-baum-nicht-traegt.md` ist mit L01 sachlich erledigt, sobald der Anwendungsdurchgang gelaufen ist. **Ich schließe ihn nicht**: einen Marker auf eine Prüfung am Baum hin weiterzustellen ist Sache des `reconciler` (Ausschluss 1 meines Auftrags).

## 11. Ergebnisse des Anwendungsdurchgangs

**Gefahren am 260820-2127**, Modus `apply`, Freigabe `all` (alle sechs Einträge). Baumstand beim Anwenden: `84a9176`, derselbe wie bei der Erhebung; der Arbeitsbaum trug allein diese unversionierte Laufdatei.

**Die Vorabprüfung ist für alle sechs Einträge gefahren und hat für alle sechs bestanden.** Der Vorher-Text jedes Eintrags wurde vor dem Anwenden erneut von der Platte gelesen und zeichengenau gegen das Änderungsbuch gehalten; `CLAUDE.md` maß dabei unverändert 39019 Bytes in 183 Zeilen, wie in Abschnitt 4 festgehalten. Kein Eintrag ist als `stale` ausgefallen.

| Eintrag | Gegenstand | Ergebnis |
|---|---|---|
| L01 | Name der Zählprobe in Zeile 131 | **applied** |
| L02 | Aufrufer der Hülle in Zeile 139 | **applied** |
| L03 | Zahl der englischen Defektdatensätze in Zeile 183 | **applied** |
| L04 | „siebenmal gestiegen" in Zeile 42 | **applied** |
| L05 | „in derselben Datei" in Zeile 77 | **applied** |
| L06 | zweiter Auslieferungsweg, neuer Absatz nach Zeile 109 | **applied** |

Sechs von sechs angewandt, null übersprungen, null veraltet, null gescheitert. **Keine Schreiboperation ist von der Wächterkonfiguration abgewiesen worden.**

**Was am Baum steht.** `CLAUDE.md` wächst von 39019 auf 40473 Bytes und von 183 auf 185 Zeilen; `git diff --stat` zählt 7 hinzugefügte und 5 entfernte Zeilen. Die zwei zusätzlichen Zeilen sind die Leerzeile und der neue Absatz aus L06 — die Ankerzeile 109 selbst bleibt unberührt, weshalb der Diff sie nicht als geändert führt. Die fünf übrigen Einträge ändern je genau eine Zeile an Ort und Stelle. **An den 156 Entscheidungsdatensätzen ist nichts geändert**: 970179 Bytes in 10981 Zeilen vor wie nach dem Durchgang, nachgezählt mit demselben `find … -exec cat {} + | wc -c -l`. Projekteigene Regeldateien gibt es nicht, also bleibt diese Fläche bei null.

**Nachgeprüft nach dem Anwenden.** Keine der fünf ersetzten Formulierungen steht noch im Text (`grep -c 'siebenmal gestiegen\|Elf Defektdatensätze\|stehen_je_einmal\|in derselben Datei steht hier keine Zahl' CLAUDE.md` → 0); der neue Probenname und der neue Absatz stehen je einmal da. **Die formgebundene Sprachzeile `**Language:** de` steht unverändert in Zeile 3** und ist von keinem Eintrag berührt. `CLAUDE.md` führt keinen Selbstverweis der Form `CLAUDE.md:<Zeile>`, also verschiebt der neue Absatz keine Zeilennummer, auf die die Datei selbst zeigt.

**Die Zeilennummern dieser Datei sind Stand der Erhebung und bleiben stehen.** Jede Zeilenangabe in den Abschnitten 1 bis 10 nennt die Stelle **vor** dem Anwenden, und genau das ist ihr Zweck: sie sagt, wo der Vorher-Text lag. Der neue Absatz aus L06 schiebt alles ab der alten Zeile 110 um zwei Zeilen nach hinten, also stehen die Angaben `CLAUDE.md:121`, `:131`, `:135`, `:139`, `:155`, `:169`, `:171`, `:179` und `:183` heute je zwei Zeilen tiefer. Unverschoben bleiben die Angaben `:8`, `:42`, `:73`, `:77` und `:109`. Die formgebundene Deklaration `**Language:** de` liegt unverändert in Zeile 3; die Kopfnotiz oben nennt mit `:179` den erklärenden Absatz unter `## Sprache`, der heute in Zeile 181 steht.

**Rückweg für den ganzen Durchgang:** `git checkout -- CLAUDE.md`. Das ist der einzige berührte Pfad, und er ist versioniert; ein Rückweg besteht also für jeden der sechs Einträge.

**Nichts ist eingecheckt.** Der Durchgang hinterlässt Änderungen im Arbeitsbaum; der Nutzer oder der Orchestrator committet.

**Was für andere liegen bleibt.** Unverändert gegenüber Abschnitt 10: `shared/issues/260820-2056_o_claude-md-nennt-eine-zaehlprobe-unter-einem-namen-den-der-baum-nicht-traegt.md` ist mit L01 sachlich erledigt, den Marker stellt aber der `reconciler` weiter und nicht ich. Die fünf Kandidaten aus Abschnitt 9 sind weder angeboten noch angewandt worden.

