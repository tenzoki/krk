# Kuratorenlauf — 260821-2231

**Status:** Complete — die Erhebung ist gefahren, das Gate hat alle fünf Einträge freigegeben, der Anwendungsdurchgang ist gefahren.
**Datum:** 260821-2231 (Erhebung)
**Git HEAD beim Lesen:** `1b974f9` (`docs(workbench): das Portfolio hat zum ersten Mal keine vorgesehene Arbeit`), Arbeitsbaum sauber
**Modus:** survey (Erhebung, 260821-2231) und apply (Anwendung, 260821-2246). Freigabe am Gate: `all` — alle fünf Einträge.
**Vorheriger Kuratorenlauf:** `shared/history/260820-2127-curator-run.md`, Baumstand `84a9176`, sechs Einträge, alle angewandt. Davor `shared/history/260820-1119-curator-run.md` bei `2beb1de`, `shared/history/260819-1500-curator-run.md` bei `82c020a` und `shared/history/260814-1405-curator-run.md` bei `43dfe90`.

**Sprache dieser Datei.** Deutsch. `CLAUDE.md:3` deklariert `**Language:** de` als einzige Sprachzeile; nach der Fallback-Kette aus `$FUSION_PLUGIN_ROOT/rules/fusion-workbench-conventions.md` `## Project language` steuert sie beide Flächen. `bin/fusion-rules` gibt `stilwerk/chat-voice-de.yaml` und `stilwerk/default-voice-de.yaml` aus; beide sind gelesen.

---

## 1. Was sich seit dem vorigen Lauf geändert hat

Der vorige Lauf las den Baum bei `84a9176`, rund fünfundzwanzig Stunden vor diesem. Seither sind **27 Commits** gelandet (`git log --oneline 84a9176..HEAD | wc -l` → 27). Das Intervall trägt eine ganze gefahrene Runde:

- **Die Runde 15 ist gebaut, ausgeliefert und kohärent geschlossen.** `cargo xtask veroeffentlichen` ist als achte Station der Auslieferungskette entstanden (`3175d76`, `72f7a5d`), `KRK 0.5.6` liegt seit dem 260821 um 20:24 öffentlich unter `https://github.com/tenzoki/krk/releases/tag/v0.5.6`, und der Circle `circles/260821-1644-veroeffentlichen-als-achte-station/_c_circle.md` ist nachgetragen und geschlossen.
- **Der vorgesehene Web-Betrachter ist abgesagt** (`ae6096f`). Der Circle `260804-0933-eingebauter-web-betrachter-im-vorschaufenster` trägt seit dem 260821-2202 den Marker `_d_`; der Datensatz dazu ist `shared/decisions/260821-2202_a_zeigt-krk-web-inhalt-selbst-an-oder-gibt-er-ihn-an-den-systembrowser-ab.md`.
- **Die `README.md` ist von 639 auf 407 Zeilen umgebaut** (`26212b1`) und führt jetzt den Nutzer zuerst, mit der Betriebsregel gegen den Datenverlust in den ersten dreißig Zeilen.
- **Das Portfolio benennt die Lücke ausdrücklich und weist sie diesem Agenten zu.** `portfolio.md:211` unter Punkt 8: „Ein Durchgang von `/fusion:curate` schließt beides."

Die Erhebung ist nicht auf dieses Intervall beschränkt: `git log --follow` ist über die ganze Historie von `CLAUDE.md` gelesen, und alle 19 Datensatzzitate der Datei sind einzeln gegen den Bestand aufgelöst.

## 2. Die drei normativen Flächen

| # | Fläche | Befund |
|---|---|---|
| 1 | Entscheidungsdatensätze unter `$SCAN_DECISIONS` = `shared/decisions/`, dazu die Circle-Speicher `circles/*/decisions/` von Hand hinzugelistet | vorhanden, **160 Dateien**, alle lesbar. Marker: 36 offen, 7 beantwortet, 107 umgesetzt, 8 überholt, 2 zurückgestellt |
| 2 | Projekteigene Regeldateien `./rules/` und `.claude/rules/` | **abwesend**. `ls rules` und `ls .claude/rules` melden beide „No such file or directory". Es gibt in diesem Projekt keine projekteigenen Regeldateien und damit auch keine `**Provenance:**`-Kopfzeilen |
| 3 | `CLAUDE.md` im Projektwurzelverzeichnis | vorhanden, 185 Zeilen, 40473 Bytes, lesbar |

Keine Fläche war unlesbar. Für Fläche 2 gilt der Fall „abwesend": null gemeldet, die beiden anderen Flächen vollständig gefahren.

**`bin/fusion-paths curator` gibt für `SCAN_DECISIONS` allein `shared/decisions` aus**, weil kein Circle aktiv ist. Die 129 Datensätze der Circle-Speicher sind deshalb von Hand dazugelistet; ohne diesen Handgriff fielen sie aus dem Suchbereich.

## 3. Evidenzquellen — was gelesen wurde

| # | Quelle | Bestand | Gelesen |
|---|---|---|---|
| 1 | Circle-Datensätze (`circles/`) | 17 | 17 Markerzustände; der Datensatz der Runde 15 im Volltext (Directive, Grounding, Dependencies, Turn log, Schließungsnotiz); der Kopf und die Directive des abgesagten Web-Betrachters |
| 2 | Entscheidungsdatensätze | 160 | 160 Marker; die 8 Zeilen `Superseded by:` aller überholten samt ihrer Nachfolger; die 4 im Intervall neu angelegten, davon 2 im Volltext; 19 auf Markerebene gegen die Zitate in `CLAUDE.md` |
| 3 | Sitzungsprotokolle (`history/`) | 478 | 3 in den tragenden Abschnitten: `shared/history/260821-2204-playmaker-direct-dispatch.md`, `shared/history/260821-2115-playmaker-orchestrator-phase4.md`, `shared/history/260820-2127-curator-run.md` (der vorige Lauf, im Volltext). Dazu `shared/history/260821-1532-reconciliation.md` über eine Volltextsuche nach `CLAUDE`/`Kurator`, ohne Treffer |
| 4 | `git log --follow` | — | `CLAUDE.md`: ganze Historie. Projekteigene Regeldateien: entfällt. Dazu `git log --oneline 84a9176..HEAD` (27), `git diff --name-status 84a9176..HEAD -- fusion-workbench/` (53 Dateien), `git tag -l --sort=creatordate` |
| 5 | Durchsichten und Analysen | 65 + 4 | alle Fundstellen von `CLAUDE.md` über den ganzen Bestand mit Kontext (22 Zeilen in 12 Dateien), dazu der Kopf und das Verdikt der Abnahmedurchsicht `circles/260821-1644-…/reviews/260821-2105-coderev-abnahme-der-fuenfzehn-nutzerkriterien.md`. Die Untersuchung `shared/analyses/260820-2242-lesezeichenverlust-nach-installation.md` ist über die Darstellung in der `README.md` und im Circle-Datensatz gelesen, nicht im Volltext |
| 6 | `orchestrator-events.jsonl` | 1292 Zeilen | die letzten 4. Corroborierend für L02 und L04 (`session_end … "3 Turns, 21 Commits, KRK 0.5.6 ausgeliefert, Runde 15 kohaerent geschlossen"`), tragend für keinen Befund |
| 7 | Archivspeicher `$WORKBENCH/archive` | **82 Dateien in zwei Läufen** (`260819-1613`, `260820-2115`), davon 79 Datensätze | vollständiger Dateibestand aufgelöst und gegen jedes Zitat der lebenden Entscheidungsspeicher und `CLAUDE.md` gehalten, siehe K01. Der Lauf hat im Intervall nichts hinzugefügt |
| 8 | `**Provenance:**`-Kopfzeilen auf Regeldateien | **0** — es gibt keine projekteigenen Regeldateien | — |
| — | Defektkorpus (Gegenprobe, keine vierte Fläche) | 566, davon **152 offen** | 152 offene Namen; 1 im Volltext (`shared/issues/260821-1532_o_zwei-fremde-werkzeuge-werden-seit-langem-ueber-den-suchpfad-gerufen-…`), weil er `CLAUDE.md` zitiert. Kein offener Defekt widerspricht einem der fünf Einträge |

**Was nicht gelesen wurde.** Von den 478 Sitzungsprotokollen sind vier angefasst und eines im Volltext gelesen. Von den 65 Durchsichten ist eine im Kopf gelesen, die übrigen nur über ihre `CLAUDE.md`-Fundstellen mit Kontext. Von den 152 offenen Defekten ist einer im Volltext gelesen. Die Untersuchung zum Lesezeichenverlust ist nicht im Volltext gelesen; L05 stützt sich auf ihre Wiedergabe in `README.md:23-43` und im Circle-Datensatz der Runde 15, beide zitiert. **Ein Befund, der in einer ungelesenen Durchsicht ohne das Wort `CLAUDE.md` stünde, hätte dieser Durchgang nicht gefunden.**

## 4. Flächengrößen

| Fläche | Bytes vorher | Zeilen vorher | Bytes nachher | Zeilen nachher |
|---|---|---|---|---|
| Entscheidungsdatensätze (160 Dateien zusammen) | 992836 | 11356 | 992836, unverändert (kein Eintrag berührt sie) | 11356, unverändert |
| Projekteigene Regeldateien | 0 | 0 | 0 | 0 |
| `CLAUDE.md` | 40473 | 185 | **43879** | **190** |

**Blast-Radius.** **Kein Eintrag dieses Durchgangs streicht eine Vorschrift, und kein Eintrag löscht einen Absatz.** Drei Einträge ersetzen je eine Zeile an Ort und Stelle, zwei hängen je einen Absatz an. Zusammen entfernt werden weniger als 400 Zeichen aus `CLAUDE.md`, also **unter einem Prozent** der 40473 Bytes; die Einträge fügen deutlich mehr hinzu, als sie wegnehmen. Aus den beiden anderen Flächen wird nichts entfernt. Die Schwelle von 20 Prozent je Fläche ist bei weitem nicht erreicht; **es gibt keine gesonderte Rückfrage zum Ausmaß.**

## 5. Vergleichszählungen je Flächenpaar

**Die Auswahlregel steht bei jedem Paar dabei, und das Verdikt gilt für die Paare, die die Regel erreicht hat, nicht für den Korpus.** Die Korpuszahlen sind mit `find … | wc -l` erhoben, nicht geschätzt.

| Paar | Auswahlregel | Paare erzeugt | Gelesen | Verdikt |
|---|---|---|---|---|
| 1. Entscheidung gegen Entscheidung | (a) der Überholungsgraph: jeder Datensatz mit `Superseded by:` gegen den dort genannten Nachfolger; (b) die **4 im Intervall neu angelegten** gegen die 43 aktiven (offen und beantwortet) | **8** (a) + **4** (b) = 12 von 12720 möglichen | 12 | **kein Widerspruch.** Die acht Überholungen nennen jede ausdrücklich, welche Hälfte fällt und welche fortgilt. Von den vier neuen sind drei offene Fragen ohne Antwort und einer die Absage an den Web-Betrachter, die keinem aktiven Datensatz widerspricht. Zwei Formfehler stehen als Kandidat K03 unten |
| 2. Entscheidung gegen Regeldatei | entfällt — es gibt keine projekteigenen Regeldateien | 0 | 0 | — |
| 3. Entscheidung gegen `CLAUDE.md` | jeder Datensatz, den `CLAUDE.md` zitiert (**19**, mit `grep -oE '[0-9]{6}-[0-9]{4}_\*_[a-z0-9-]+' CLAUDE.md` gezogen und einzeln über `find` aufgelöst), plus die 4 im Intervall neu angelegten | **23** | 23 (19 auf Markerebene, davon 6 im Rumpf; 4 neue, davon 2 im Volltext) | **ein Befund.** Alle 19 Zitate lösen auf, keines zeigt ins Archiv, und jeder Marker stimmt mit dem überein, was `CLAUDE.md` ihm zuschreibt. Der Befund kommt von einem der vier neuen: `260821-2202_a_` sagt den Web-Betrachter ab, den `CLAUDE.md:177` als vorgesehen führt. Eintrag L03 |
| 4. Regeldatei gegen Regeldatei | entfällt | 0 | 0 | — |
| 5. Regeldatei gegen `CLAUDE.md` | entfällt | 0 | 0 | — |
| 6. `CLAUDE.md` gegen sich selbst | alle 185 Zeilen gegeneinander gelesen, mit besonderer Aufmerksamkeit für Zählungen, die auf den Circle-Bestand zeigen | ganze Datei | ganze Datei | **ein Befund:** die Rundentabelle (Zeilen 13 bis 28) führt vierzehn Zeilen, und Zeile 67 zählt vier kohärent geschlossene Runden. Der Bestand trägt fünfzehn gefahrene Runden und fünf kohärent geschlossene. Einträge L01 und L02 |

**Die Lücke der Regel (b)** ist dieselbe wie in allen Vorläufen: sie erreicht nur den Ausschnitt, den die neuen Datensätze berühren, und sie findet nicht zwei Datensätze, die dieselbe Sache mit verschiedenen Wörtern benennen. **Ein Verdikt über alle 43 aktiven Datensätze gegeneinander hat dieser Durchgang nicht.**

## 6. Vorabinhalt geänderter Entscheidungsdatensätze

**Keiner.** Dieser Durchgang schlägt an keinem der 160 Entscheidungsdatensätze eine Änderung vor, weder am Rumpf noch am Marker noch am Dateinamen. Alle fünf Einträge liegen auf `CLAUDE.md`. Es ist deshalb kein Vorabinhalt festzuhalten.

---

## 7. Das Änderungsbuch

Fünf Einträge, alle auf `CLAUDE.md`. Gruppen in der Reihenfolge, in der sie am Gate erscheinen: Streichung einer Vorschrift (keine), Tier 3 (keine), Tier 2 (zwei), Tier 1 (drei), Zusammenführungen (keine).

### Streichung einer Vorschrift (0 Einträge)

Keiner. Kein Eintrag entfernt eine Vorschrift, und keiner löscht einen Absatz.

### Tier 3 (0 Einträge)

Keiner. Kein Befund ruht auf einer Bahn statt auf einem Kommando oder einem Datensatz.

### Tier 2 (2 Einträge)

### L04 — die Auslieferungskette endet seit der Runde 15 nicht mehr auf der Platte, und `CLAUDE.md` kennt nur die sieben Stationen davor

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 111 (Ende des Absatzes; die Zeile selbst bleibt unberührt)
- **Tier:** 2
- **Zitat:** `circles/260821-1644-veroeffentlichen-als-achte-station/_c_circle.md`, Abschnitt `## Was die Runde gebracht hat`, wörtlich: „`cargo xtask veroeffentlichen <zahl>` ist die achte Station der Auslieferungskette und hat zwei Rufer, dieselbe Gestalt wie die siebte seit `f5300f4`". Derselbe Datensatz, Abschnitt `## Grounding snapshot`: „**Die Auslieferungskette endete auf der Platte.** `cargo xtask release` fuhr sieben Stationen … Ein Schritt, der das Bündel weitergebbar macht … fehlte." Die Zuweisung an diesen Agenten steht in `portfolio.md:211` unter Punkt 8: die fünfzehnte Tabellenzeile fehle, „und mit ihr die achte Station der Auslieferungskette, das öffentliche Release und die Betriebsregel gegen den Datenverlust beim Austausch der App. Ein Durchgang von `/fusion:curate` schließt beides." **Am Baum nachgeprüft:**
  ```
  $ grep -n 'veroeffentlichen' xtask/src/main.rs
  137:  cargo xtask veroeffentlichen <zahl>
  235:        "veroeffentlichen" => veroeffentlichung::ausfuehren(&argumente[1..]),
  $ grep -n 'Station' xtask/src/release.rs | head -1
  3://! Der Weg in acht Stationen, jede scheitert mit einer benennenden Meldung.
  $ grep -n 'TICKETKENNUNG' xtask/src/veroeffentlichung.rs | head -1
  68:const TICKETKENNUNG: &[u8] = b"s8ch";
  $ grep -n '^### ' README.md | sed -n '6,8p'
  223:### Die acht Stationen
  269:### Nur beglaubigen
  299:### Nur veröffentlichen
  ```
  Es gibt für den dritten Weg weder ein Skript noch ein `make`-Ziel: `ls *.sh` liefert `certify-only.sh` und `release.sh`, und `grep -n '^veroeffentlichen:' Makefile` gibt nichts aus. Die offene Frage dazu ist `shared/decisions/260821-1115_o_bekommt-der-veroeffentlichungsbefehl-eine-eigene-huelle-wie-certify-only-sh.md`.
- **Konsequenzgruppe:** tier-2
- **Gestrichene Vorschrift:** keine. Der ganze bestehende Absatz bleibt wörtlich stehen; der Eintrag hängt einen zweiten an
- **Rückweg:** `git checkout -- CLAUDE.md`
- **Vertrauensgrad:** `verified:` für alles, was am Baum steht (der Unterbefehl, die acht Stationen, die Ticketkennung, die zwei Abschnitte der `README.md`, die fehlende Hülle). `inference:` für die Einordnung als Überholung: dass der bestehende Absatz mit der achten Station zu eng geworden ist, ist meine Lesart zweier Prosastellen und von keinem Kommando geprüft. Der Circle-Datensatz und das Portfolio nennen dieselbe Lücke, und das Portfolio weist sie ausdrücklich `/fusion:curate` zu

**Vorher** (Ankerzeile, das Ende des Absatzes in Zeile 111; nichts daran wird geändert):

> Im Einzelnen steht der Weg in `README.md` unter `### Nur beglaubigen`.

**Nachher** (dieselbe Zeile, gefolgt von einem neuen Absatz):

> Im Einzelnen steht der Weg in `README.md` unter `### Nur beglaubigen`.
>
> **Seit der Runde 15 endet die Kette nicht mehr auf der Platte:** `cargo xtask release` fährt acht Stationen, und die achte packt `target/KRK-<zahl>.zip`, schiebt HEAD und den einen Tag `v<zahl>` in **einem** `git push` zur Gegenseite und legt mit `gh release create` eine öffentliche Releaseseite an. Welche acht das sind, sagt `README.md` unter `### Die acht Stationen` und nicht diese Datei. **Station 8 ist die einzige Wirkung der ganzen Kette, die über dieses Gerät hinausgeht und sich nicht zurücknehmen lässt**; deshalb fragt schon Station 1 nach `gh`, obwohl erst Station 8 es braucht. Wie die siebte hat auch die achte einen zweiten Rufer für den Lauf, der weit gekommen und am Netz gescheitert ist: `cargo xtask veroeffentlichen <zahl>` fährt Station 8 allein. **Für diesen dritten Weg gibt es bewusst noch keine Hülle**, weder ein Skript noch ein Ziel im `Makefile`; ob er eine bekommt, ist offen (`shared/decisions/260821-1115_*_bekommt-der-veroeffentlichungsbefehl-eine-eigene-huelle-wie-certify-only-sh.md`), und solange nicht, trägt der Aufruf den vollen Pfad zu `cargo`. Ob das Ticket am Bündel hängt, fragt der Befehl an den ersten vier Bytes von `target/KRK.app/Contents/CodeResources` und nicht bei Apple: `xcrun stapler validate` bräuchte Netz, während gerade zu prüfen ist, ob das Bündel den Nachweis **mitbringt**.

### L05 — die Betriebsregel gegen den Datenverlust beim Austausch der App steht in keiner der drei normativen Flächen

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 115 (Ende des einleitenden Absatzes von „Was man nicht sieht, wenn man es nicht weiß"; die Zeile selbst bleibt unberührt)
- **Tier:** 2
- **Zitat:** `circles/260821-1644-veroeffentlichen-als-achte-station/_c_circle.md`, Abschnitt `## Grounding snapshot`, wörtlich: „**Der Anlass der Runde ist ein Datenverlust und keine Bequemlichkeit.** Am 17.08. um 19:13:48 hat KRK seinen Ablageordner `~/Library/Application Support/KRK/` neu angelegt … Die Betriebsregel daraus lautet, die neue Fassung über die alte zu kopieren und die alte nicht vorher zu löschen. Sie stand nirgends dort, wo der Nutzer sie beim Installieren liest". Die Zuweisung an diesen Agenten ist dieselbe wie bei L04: `portfolio.md:211` nennt „die Betriebsregel gegen den Datenverlust beim Austausch der App" als dritten Teil der Lücke und schreibt „Ein Durchgang von `/fusion:curate` schließt beides." **Am Baum nachgeprüft:**
  ```
  $ sed -n '24,29p' README.md
  **Die alte Fassung vorher nicht löschen.** Ein Überkopieren ist gefahrlos, ein
  Löschen ist es nicht. Werkzeuge, die eine App samt ihrer Stützdateien entfernen
  — der App Deleter von ForkLift ist eines —, nehmen dabei den Ordner
  `~/Library/Application Support/KRK/` mit. …
  $ grep -n 'RELEASETEXT\|der_releasetext_traegt_jede_seiner_aussagen' xtask/src/veroeffentlichung.rs
  551:const RELEASETEXT: &str = "\
  955:    fn der_releasetext_traegt_jede_seiner_aussagen() {
  $ grep -c 'Application Support/KRK' CLAUDE.md
  0
  ```
  Die Untersuchung liegt unter `shared/analyses/260820-2242-lesezeichenverlust-nach-installation.md`.
- **Konsequenzgruppe:** tier-2
- **Gestrichene Vorschrift:** keine. Der einleitende Absatz bleibt wörtlich stehen; der Eintrag hängt einen neuen an
- **Rückweg:** `git checkout -- CLAUDE.md`
- **Vertrauensgrad:** `verified:` für den Wortlaut der `README.md`, für die Konstante und ihre Probe und dafür, dass `CLAUDE.md` den Ablageordner heute nirgends nennt. `inference:` dafür, dass die Regel in `CLAUDE.md` gehört: sie ist eine Betriebsregel für den Nutzer, und diese Datei richtet sich an Agenten. Sie steht hier trotzdem, weil sie unter „Was man nicht sieht" fällt — eine Eigenschaft, die schon eine Sitzung gekostet hat — und weil jeder Agent, der an der Ablage oder an der Auslieferung arbeitet, wissen muss, dass der Ablageordner außerhalb des Bündels liegt und ein Löschwerkzeug ihn mitnimmt. `speculation:` wäre die Behauptung, das Portfolio meine mit „Betriebsregel" genau diesen Ort; es nennt sie als Teil der Lücke und nennt den Abschnitt nicht

**Vorher** (Ankerzeile, Zeile 115; nichts daran wird geändert):

> Eigenschaften, von denen jede schon einmal eine Sitzung gekostet hat. Gezählt sind sie nicht: die Liste wächst mit jeder Runde, und eine Zahl davor veraltet, wie es die Aufstellungen getan haben, die diese Datei aus demselben Grund abgelegt hat.

**Nachher** (dieselbe Zeile, gefolgt von einem neuen Absatz):

> Eigenschaften, von denen jede schon einmal eine Sitzung gekostet hat. Gezählt sind sie nicht: die Liste wächst mit jeder Runde, und eine Zahl davor veraltet, wie es die Aufstellungen getan haben, die diese Datei aus demselben Grund abgelegt hat.
>
> **KRKs Bestand liegt außerhalb des Bündels, und ein Löschwerkzeug nimmt ihn mit.** Alles, was sich die Anwendung merkt — die Lesezeichen, die gesicherte Sitzung, die abweichende Tastenbelegung und die zwei Notizzettel —, hält sie unter `~/Library/Application Support/KRK/`. Werkzeuge, die eine App samt ihrer Stützdateien entfernen, löschen diesen Ordner mit; am 17.08. ist genau das geschehen, drei Minuten und 46 Sekunden nach einem Auslieferungslauf (`shared/analyses/260820-2242-lesezeichenverlust-nach-installation.md`, dreizehn Beweisstücke, kein Schreiber im Baum fasst die Datei an). **Die Betriebsregel daraus lautet: die neue Fassung über die alte kopieren und die alte nicht vorher löschen.** Sie steht seit der Runde 15 an zwei Stellen, an denen der Nutzer sie beim Installieren liest, und beide sind für ihn und nicht für einen Agenten geschrieben: in den ersten dreißig Zeilen der `README.md` und im festen Text jeder Releaseseite, der als Konstante `RELEASETEXT` in `xtask/src/veroeffentlichung.rs` an genau einer Stelle steht und dessen Aussagen die Probe `der_releasetext_traegt_jede_seiner_aussagen` einzeln hält. Wer den Text ändert, ändert ihn dort und nicht in einer Kopie.

### Tier 1 (3 Einträge)

### L01 — die Rundentabelle endet bei vierzehn, und der Bestand trägt fünfzehn gefahrene Runden

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 28 (letzte Zeile der Tabelle)
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe.
  ```
  $ ls fusion-workbench/circles/*/_[bc]_circle.md | wc -l
  15
  $ ls fusion-workbench/circles/260821-1644-veroeffentlichen-als-achte-station/*_circle.md
  fusion-workbench/circles/260821-1644-veroeffentlichen-als-achte-station/_c_circle.md
  ```
  Zeile 11 sagt selbst: „Beschränkt (`_b_`) und kohärent (`_c_`) geschlossene Runden zählen als gefahren". Der Kopf des Datensatzes lautet „Veröffentlichen als achte Station der Auslieferungskette"; seine Schließungsnotiz sagt „**Kohärent geschlossen am 260821-2110.**". Corroborierend: `shared/history/260821-2115-playmaker-orchestrator-phase4.md:34` — „zehn der fünfzehn gefahrenen Runden tragen `_b_`". Tragend zugewiesen: `portfolio.md:212` — „Die Rundentabelle führt vierzehn Zeilen; die fünfzehnte fehlt".
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine. Die Tabelle bekommt eine Zeile und verliert keine
- **Rückweg:** `git checkout -- CLAUDE.md`
- **Zur Form:** eine Ableitung tritt hier nicht an die Stelle der Zeile. Die Tabelle ist ausdrücklich „ein Verweisregister für die Pfadregel im Absatz danach" (Zeile 11), und die Pfadregel braucht den Namen des Circles neben seiner Rundenzahl. Ein Kommando liefert den Gegenstand der Runde nicht

**Vorher** (Zeile 28, die letzte Tabellenzeile):

> | 14 | `260819-2230-auswahl-und-kopieren-in-der-vorschau` | die Vorschaufläche wird auswählbar, kopiert wird der Quelltext |

**Nachher** (dieselbe Zeile, gefolgt von einer neuen):

> | 14 | `260819-2230-auswahl-und-kopieren-in-der-vorschau` | die Vorschaufläche wird auswählbar, kopiert wird der Quelltext |
> | 15 | `260821-1644-veroeffentlichen-als-achte-station` | Veröffentlichen als achte Station der Auslieferungskette, öffentliche Releaseseite, Betriebsregel gegen den Datenverlust |

### L02 — „in der vierten" zählt eine Menge von vier kohärent geschlossenen Runden, es sind fünf

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 67
- **Tier:** 1
- **Zitat:** Kommando und Ausgabe.
  ```
  $ ls fusion-workbench/circles/*/_c_circle.md
  fusion-workbench/circles/260813-0939-titelleiste-fuehrt-version-und-semantische-tags/_c_circle.md
  fusion-workbench/circles/260817-0833-jeder-loeschweg-mit-rueckfrage-und-nur-noch-papierkorb/_c_circle.md
  fusion-workbench/circles/260818-1615-ordner-angleichen-und-abwurf-aus-fremden-apps/_c_circle.md
  fusion-workbench/circles/260819-2230-auswahl-und-kopieren-in-der-vorschau/_c_circle.md
  fusion-workbench/circles/260821-1644-veroeffentlichen-als-achte-station/_c_circle.md
  ```
  Fünf, nicht vier. Dass der Nutzer in der fünften den Abnahmelauf **gefahren** hat, steht in der Schließungsnotiz jenes Datensatzes: „**Der Marker `_c_` steht auf einer gefahrenen Abnahme.** … die 15 Kriterien sind am 260821-2105 gemessen worden." Und: „**Ein Kriterium ist nicht prüfbar und bleibt es.** C2.2 verlangt einen zweiten Mac ohne Netzverbindung". Die Abnahmedurchsicht `circles/260821-1644-…/reviews/260821-2105-coderev-abnahme-der-fuenfzehn-nutzerkriterien.md` verdiktiert „Fünfzehn geprüft. Vierzehn halten. Eines ist auf diesem Gerät nicht prüfbar. Keines hält nicht." Corroborierend: `portfolio.md:197-203`, Punkt 7 — „Die Runde 15 kommt als fünfte hinzu".
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine. Der tragende Satz — der Marker `_c_` heißt hier nicht „vom Nutzer abgenommen", und die eine Ausnahme steht auf kohärent, weil ihre Directive über die zehn Zeitzusagen nichts sagt — bleibt wörtlich stehen. Der Eintrag ersetzt allein die Zählung
- **Rückweg:** `git checkout -- CLAUDE.md`
- **Zur Form:** die Ordnungszahl „in der vierten" ist keine Messung, die ein Kommando erzeugt, sondern der Rest einer Aufzählung. Der Eintrag setzt „in einer" an ihre Stelle, damit die nächste kohärent geschlossene Runde diesen Satz nicht wieder falsch macht; die Aufzählung selbst bleibt, weil kein Kommando sagt, in welcher Runde der Nutzer gemessen hat

**Vorher:**

> In dreien der kohärent geschlossenen Runden hat der Nutzer den Abnahmelauf gefahren (`260813-0939-titelleiste-fuehrt-version-und-semantische-tags`, `260818-1615-ordner-angleichen-und-abwurf-aus-fremden-apps` und `260819-2230-auswahl-und-kopieren-in-der-vorschau`); in der vierten (`260817-0833-jeder-loeschweg-mit-rueckfrage-und-nur-noch-papierkorb`) ist er **nicht** gefahren

**Nachher:**

> In vieren der kohärent geschlossenen Runden hat der Nutzer den Abnahmelauf gefahren (`260813-0939-titelleiste-fuehrt-version-und-semantische-tags`, `260818-1615-ordner-angleichen-und-abwurf-aus-fremden-apps`, `260819-2230-auswahl-und-kopieren-in-der-vorschau` und `260821-1644-veroeffentlichen-als-achte-station`, deren Abnahme fünfzehn Nutzerkriterien geprüft hat und für eines mangels zweitem Mac ein Indiz statt einer Abnahme liefert); in einer (`260817-0833-jeder-loeschweg-mit-rueckfrage-und-nur-noch-papierkorb`) ist er **nicht** gefahren

### L03 — das Kommando für die vorgesehenen Circles gibt nichts mehr aus, und der genannte ist abgesagt

- **Fläche:** CLAUDE.md
- **Datei:** `/Users/k1/Projects/productive/krk/CLAUDE.md`, Zeile 177
- **Tier:** 1 (die Messung), gestützt auf Tier 2 (die Absage)
- **Zitat:** Kommando und Ausgabe.
  ```
  $ ls fusion-workbench/circles/*/_a_circle.md
  zsh: no matches found: fusion-workbench/circles/*/_a_circle.md
  $ ls fusion-workbench/circles/260804-0933-eingebauter-web-betrachter-im-vorschaufenster/*_circle.md
  fusion-workbench/circles/260804-0933-eingebauter-web-betrachter-im-vorschaufenster/_d_circle.md
  ```
  Der Datensatz `shared/decisions/260821-2202_a_zeigt-krk-web-inhalt-selbst-an-oder-gibt-er-ihn-an-den-systembrowser-ab.md` schreibt in seiner Zeile `Answered:`: „Der Nutzer hat am 260821 Option 2 gewählt: **das Abgeben an den Systembrowser genügt, ein eingebauter Betrachter löst kein echtes Problem.** Der Circle … ist daraufhin auf `_d_` gesetzt worden … Die Wahl ist eine Absage an die Zusage und keine Vertagung — der Marker `_d_` ist die nächstliegende Entsprechung, die das Circle-Vokabular kennt, und nicht die genaue." Zugewiesen in `portfolio.md:213-215`: „er sagt, `ls fusion-workbench/circles/*/_a_circle.md` liefere am 260815 einen Circle, den Web-Betrachter. Das Kommando liefert seit dem 260821-2202 nichts."
- **Konsequenzgruppe:** tier-1
- **Gestrichene Vorschrift:** keine. Der tragende erste Satz („was vorgesehen ist, sagt das Kommando und nicht diese Zeile") und der Nachsatz über die Statusleiste bleiben wörtlich stehen
- **Rückweg:** `git checkout -- CLAUDE.md`
- **Vertrauensgrad:** `verified:` für beide Kommandos und für den Wortlaut der `Answered:`-Zeile. Der Eintrag trägt die Absage in die Datei, statt nur die Zahl auf null zu setzen, weil „null vorgesehene Circles" allein den Leser die Zusage an anderer Stelle weiter für offen halten ließe

**Vorher:**

> **Welche Circles vorgesehen und nicht gefahren sind, sagt `ls fusion-workbench/circles/*/_a_circle.md` und nicht diese Zeile.** Am 260815 ist es einer: der Web-Betrachter im Vorschaufenster (`260804-0933-…`). Die Statusleiste mit Bereichsschaltern (`260811-1304-…`) ist als fünfte Runde gefahren und am 260812-0820 beschränkt geschlossen.

**Nachher:**

> **Welche Circles vorgesehen und nicht gefahren sind, sagt `ls fusion-workbench/circles/*/_a_circle.md` und nicht diese Zeile.** Seit dem 260821-2202 gibt das Kommando nichts aus. Der Web-Betrachter im Vorschaufenster (`260804-0933-…`) war der letzte vorgesehene, und der Nutzer hat ihn abgesagt: das Abgeben an den Systembrowser genügt (`shared/decisions/260821-2202_*_zeigt-krk-web-inhalt-selbst-an-oder-gibt-er-ihn-an-den-systembrowser-ab.md`, Möglichkeit 2). **Der Circle trägt seitdem `_d_`, und das ist die nächstliegende Entsprechung und nicht die genaue** — das Vokabular kennt für eine Absage keinen eigenen Marker, und wer `_d_` hier als „später" liest, liest es falsch. Die Statusleiste mit Bereichsschaltern (`260811-1304-…`) ist als fünfte Runde gefahren und am 260812-0820 beschränkt geschlossen.

### Zusammenführungen (0 Einträge)

Keine. Kein Absatz dieses Durchgangs ist allein aus dem Wiederlesen des Texts umgeschrieben worden.

---

## 8. Widersprüche, die ich nicht auflösen darf

**Keiner.** Von den sechs Flächenpaaren erzeugen vier keine Paare (es gibt keine projekteigenen Regeldateien), und die zwei übrigen liefern zwei Befunde, von denen keiner ein Konflikt zweier vertretbarer Positionen ist: beide sind veraltete Zählungen gegen einen gewachsenen Bestand. **Dieser Durchgang legt deshalb keinen neuen offenen Entscheidungsdatensatz an.**

## 9. Kandidaten — nicht angeboten und nicht anwendbar

**K01 — Zitate in lebenden Entscheidungsdatensätzen zeigen auf Datensätze, die inzwischen im Archiv liegen.**
Gemessen: alle 79 Datensätze der zwei Archivläufe gegen jeden Zeitstempelverweis in `shared/decisions/`, `circles/*/decisions/` und `CLAUDE.md`. **25 der 79 archivierten Datensätze werden von mindestens einer lebenden Stelle zitiert.** **`CLAUDE.md` ist unbetroffen** — alle 19 Zitate der Datei lösen im lebenden Baum auf, keines zeigt ins Archiv. Der Archivbestand ist im Intervall nicht gewachsen; die höhere Zahl gegenüber dem vorigen Lauf (dort 21 Zitate auf 20 Datensätze) kommt von einer anderen Zählweise, nicht von neuen Fällen.
**Warum Kandidat:** unverändert die zwei Gründe der Vorläufe. Erstens sagt keine Regel, welche Gestalt das berichtigte Zitat hätte. Zweitens gilt in diesem Projekt die Ortsregel aus `CLAUDE.md:173`, die `decisions/` als Speicher für Aufzeichnungen eines Standes führt. **Er steht zum vierten Mal hier.**

**K02 — die Ausnahme für maschinell gelesene Kopffelder steht in keiner bindenden Regel.**
Der Datensatz ist `circles/260817-0833-…/issues/260818-0753_o_die-ausnahme-fuer-maschinell-gelesene-kopffelder-steht-nur-in-einem-geschlossenen-datensatz.md`.
**Warum Kandidat:** unverändert gegenüber dem vorigen Lauf. Die anschließende Frage nach der Zitierform ist als **offene** Entscheidung anhängig (`shared/decisions/260818-0201_o_does-a-cross-references-line-between-records-write-the-marker-in-the-star-form.md`), und eine Änderung, die ihre Antwort vorwegnimmt, entschiede sie durch vollendete Tatsache. **Neu gemessen:** `ls circles/*/_t_circle.md` findet weiterhin nichts, und die Kopffelder aller 17 Circle-Datensätze stehen in der Sternform. Die Lücke bleibt gegenstandslos, solange kein Circle aktiv ist.

**K03 — zwei überholte Entscheidungsdatensätze tragen die Zeile `Superseded by:` zweimal.**
`circles/260811-1304-…/decisions/260811-1305_s_…` (Zeilen 73 und 74) und `circles/260813-0939-…/decisions/260813-0939_s_…` (Zeilen 41 und 47), beide unverändert.
**Warum Kandidat:** ein Formfehler, und keiner meiner zwei Änderungsgründe deckt ihn. Er hat seit dem 260820 einen Besitzer: `shared/issues/260820-2056_o_dreissig-entscheidungsdatensaetze-tragen-eine-leere-vorlagenzeile-vor-der-gefuellten.md`.

**K04 — ein Zitat in `CLAUDE.md` löst auf fünf Dateien auf.**
`CLAUDE.md:137` nennt „den Defekt `260815-0211` in seiner zweiten Gestalt"; `find fusion-workbench -name '260815-0211_*'` liefert fünf Datensätze.
**Warum Kandidat:** unverändert. Die Aussage des Satzes ist richtig und der Zeiger ist nicht tot, sondern mehrdeutig. Ein Eintrag dafür wäre eine Verbesserung aus dem Wiederlesen des Texts. **Wer den Slug ergänzt haben will, sagt es**; es ist eine Zeile.

**K05 — die auswählbare Vorschaufläche der Runde 14 hat weiterhin keinen eigenen Absatz.**
K03 des Laufs vom 260820-1119 und K05 des Laufs vom 260820-2127, unverändert.
**Warum Kandidat:** eine Auslassung ist keiner meiner zwei Änderungsgründe. **L04 und L05 sind bewusst anders behandelt**, und der Unterschied ist benannt: dort nennt `portfolio.md:211` die betroffene Lücke wörtlich und weist sie ausdrücklich `/fusion:curate` zu; hier tut das niemand.

**K06 — der Verzeichnisbaum in `CLAUDE.md` führt `certify-only.sh` nicht.**
Der Baum in den Zeilen 44 bis 65 nennt `release.sh` als „zweite Hülle" und lässt `certify-only.sh` aus, das seit dem 260820 im Wurzelverzeichnis liegt; er lässt daneben auch `Cargo.lock`, `rustfmt.toml`, `fusion.json`, `activity-log-k1.md` und die Verzeichnisse `fusion-workbench/` und `target/` aus.
**Warum Kandidat:** der Baum behauptet an keiner Stelle Vollständigkeit, also ist keine Aussage falsifiziert. Ein Eintrag wäre eine Verbesserung aus dem Wiederlesen des Texts, und die ist mir verwehrt. **Wer die Zeile ergänzt haben will, sagt es.**

**K07 — Zeile 175 führt „ein integrierter Browser" unter dem, was außerhalb der gefahrenen Runden liegt, und er ist seit dem 260821 abgesagt.**
Der Satz ist wörtlich weiter richtig: der Browser liegt außerhalb aller gefahrenen Runden. Er liest sich aber als Vorhaben, und `shared/decisions/260821-2202_a_` sagt genau das ab.
**Warum Kandidat:** kein Widerspruch im Sinne meines Auftrags — ein Agent, der dem einen Satz folgt, verletzt den anderen nicht. L03 trägt die Absage zwei Absätze später in die Datei, und ein zweiter Eintrag an derselben Sache wäre eine Auslegung und keine Berichtigung. **Wenn du willst, dass Zeile 175 die Absage mitnennt, sag es**; es ist ein Halbsatz.

## 10. Arbeit außerhalb meiner Zuständigkeit

**Keine Codearbeit.** Kein Eintrag dieses Durchgangs verlangt einen Helfer, eine Probe oder eine erzeugte Tabelle; alle fünf sind reine Textänderungen an `CLAUDE.md`. Die Ableitungen, auf die sie verweisen (`README.md`-Abschnitte, `git tag -l`, `ls`), sind fertige Kommandos aus Standardwerkzeugen.

**Keine Testvorrichtung ist betroffen.** In fusions eigenem Repository pinnt `hooks/lib/__tests__/fixtures/rules-emission.golden` die Größen der Regeldateien; hier gibt es keine projekteigenen Regeldateien und keine Vorrichtung, die `CLAUDE.md` misst. Ein Anwendungsdurchgang lässt keine Probe rot werden.

**Kein Defektdatensatz ist zu filen.** Die Befunde, die anderen gehören, sind bereits gefilt: die doppelten Vorlagenzeilen (`shared/issues/260820-2056_o_dreissig-…`), die fehlende Kopffeld-Ausnahme (`circles/260817-0833-…/issues/260818-0753_o_…`) und die zwei über den Suchpfad gerufenen Werkzeuge (`shared/issues/260821-1532_o_zwei-fremde-werkzeuge-…`). Ein weiterer Datensatz wäre ein Doppel.

**Zwei Datensätze bleiben für den `reconciler` liegen.**
`shared/issues/260820-2056_o_claude-md-nennt-eine-zaehlprobe-unter-einem-namen-den-der-baum-nicht-traegt.md` steht weiterhin auf offen, obwohl der vorige Kuratorenlauf ihn mit L01 sachlich erledigt hat (`CLAUDE.md:133` trägt heute `die_zeichenregel_hat_zwei_rufer_und_der_vergleich_drei`, am Baum bei `crates/krk-core/tests/verzeichnis.rs:3095` bestätigt). **Ich schließe ihn nicht:** einen Marker auf eine Prüfung am Baum hin weiterzustellen ist Sache des `reconciler` (Ausschluss 1 meines Auftrags).
`shared/decisions/260821-2202_a_zeigt-krk-web-inhalt-selbst-an-oder-gibt-er-ihn-an-den-systembrowser-ab.md` trägt „beantwortet", und die Antwort ist eine Absage, deren Umsetzung im Nichtstun besteht. Ob der Marker deshalb auf `_i_` gehört, ist eine Prüfung gegen den Baum und damit ebenfalls Sache des `reconciler`.

## 11. Ergebnisse des Anwendungsdurchgangs

Gefahren am **260821-2246** gegen den Baumstand `1b974f9` — denselben, den die Erhebung gelesen hat. Der Arbeitsbaum trug vor dem Durchgang keine Änderung an `CLAUDE.md`: `git status --porcelain` meldete allein die unversionierte Laufdatei selbst.

**Freigabe:** `**Approved:** all`, also alle fünf Einträge. Kein Eintrag ist zurückgestellt.

**Vorabprüfung je Eintrag.** Vor dem Anwenden ist der Vorher-Text jedes Eintrags einzeln von der Platte gelesen und Zeichen für Zeichen gegen das Änderungsbuch gehalten (`sed -n '28p;67p;111p;115p;177p' CLAUDE.md`). **Alle fünf stimmten überein**; kein Eintrag ist `stale`.

| Eintrag | Zeile vorher | Ergebnis |
|---|---|---|
| L01 — die fünfzehnte Tabellenzeile | 28 | **applied** — eine Zeile hinzugefügt, die vierzehnte unberührt |
| L02 — „in dreien"/„in der vierten" wird „in vieren"/„in einer" | 67 | **applied** — allein die Zählung ersetzt, der tragende Satz wörtlich stehen geblieben |
| L03 — das Kommando für die vorgesehenen Circles gibt nichts mehr aus | 177 | **applied** — Satz zwei ersetzt, Satz eins und der Nachsatz zur Statusleiste wörtlich stehen geblieben |
| L04 — die achte Station der Auslieferungskette | 111 (Anker) | **applied** — ein Absatz angehängt, die Ankerzeile unberührt |
| L05 — die Betriebsregel gegen den Datenverlust | 115 (Anker) | **applied** — ein Absatz angehängt, die Ankerzeile unberührt |

**Fünf angewandt, null übersprungen, null veraltet, null gescheitert.** Kein Schreibvorgang ist von der Wächterkonfiguration des Projekts abgewiesen worden.

**Was der Baum danach zeigt.** `git diff --stat CLAUDE.md` → `1 file changed, 7 insertions(+), 2 deletions(-)`; die zwei Löschungen sind die zwei ersetzten Zeilen der Einträge L02 und L03, nicht der Verlust eines Absatzes. `CLAUDE.md` wächst von 40473 auf 43879 Bytes und von 185 auf 190 Zeilen. **Keine Vorschrift ist entfernt worden**, weder gewollt noch nebenbei: das Änderungsbuch führt in allen fünf Einträgen „Gestrichene Vorschrift: keine", und der Diff bestätigt es.

**Eine Formbeobachtung zu L03, ohne Folge.** Der eingesetzte Text schreibt `wer „_d_" hier als „später" liest` mit dem schließenden Anführungszeichen U+201C. Die Datei führt beide Formen schon vorher — Zeile 138 schreibt `das Ankreuzfeld „Deep"` mit demselben Zeichen, die übrigen zwanzig Stellen schließen mit dem geraden `"`. Eine Vereinheitlichung wäre eine Änderung aus dem Wiederlesen des Texts und steht mir nicht zu; sie ist hier festgehalten, damit sie nicht als Fehler dieses Durchgangs gelesen wird.

**Rückweg.** `git checkout -- CLAUDE.md` stellt den Stand vor diesem Durchgang her; die Datei ist versioniert, und der Baumstand `1b974f9` ist sauber gewesen. Es ist **nicht** committet — das ist Sache des Nutzers oder des Orchestrators.

**Was liegen bleibt.** Die sieben Kandidaten aus Abschnitt 9 sind unverändert nicht angeboten und nicht angewandt. Die zwei Datensätze aus Abschnitt 10 bleiben für den `reconciler` liegen.
