# Kuratorenlauf — 260826-0818

**Status:** Complete — Erhebung und Anwendungsdurchgang sind beide gefahren. Der Nutzer hat am 260826 alle neun Einträge freigegeben; alle neun sind angewandt und in Abschnitt 11 einzeln nachgetragen. Die drei Kandidaten K01 bis K03 sind nicht freigegeben und nicht angewandt.
**Datum:** 260826-0818 (Erhebung)
**Git HEAD beim Lesen:** `d08dbac` (`docs(workbench): der Abgleich der Runde 18 und die sieben umgesetzten Entscheidungen`). Die Erhebung hat bei `e5ec81a` begonnen; die zwei Commits `180fc53` und `d08dbac` sind während des Lesens gelandet und haben `CLAUDE.md` nicht angefasst (`git diff --name-only e5ec81a..d08dbac -- CLAUDE.md` ist leer). Arbeitsbaum: eine geänderte und eine unverfolgte Werkbankdatei, keine der drei Flächen.
**Modus:** survey (260826-0818) und apply (260826-0837). Die Erhebung hat nichts geschrieben außer dieser Datei; der Anwendungsdurchgang hat allein `CLAUDE.md` angefasst und nichts gestaged.
**Baumstand beim Anwenden:** `d08dbac`, unverändert gegenüber der Erhebung. Die zwei Commits, die während der Erhebung gelandet sind, sind vor dem Schreiben einzeln gegen die neun Einträge gehalten: `180fc53` ändert `resources/default-readers.toml` und drei Werkbankdateien, `d08dbac` fünfzehn Werkbankdateien; keiner berührt eine Stelle, die ein Eintrag anfasst oder zitiert. `CLAUDE.md` stand beim Anwenden byteweise auf dem Stand der Erhebung (194 Zeilen, 47.809 Bytes, `git status` leer). Die Belege aller neun Einträge sind vor dem Schreiben ein zweites Mal gefahren und halten unverändert.
**Vorheriger Kuratorenlauf:** `shared/history/260821-2231-curator-run.md`, Baumstand `1b974f9`, fünf Einträge, alle angewandt. Davor `260820-2127`, `260820-1119`, `260819-1500` und `260814-1405`.

**Sprache dieser Datei.** Deutsch. `CLAUDE.md:3` deklariert `**Language:** de` als einzige Sprachzeile; nach der Fallback-Kette aus `$FUSION_PLUGIN_ROOT/rules/fusion-workbench-conventions.md` `## Project language` steuert sie beide Flächen. `bin/fusion-rules` gibt `stilwerk/chat-voice-de.yaml` und `stilwerk/default-voice-de.yaml` aus; beide sind gelesen.

---

## 1. Was sich seit dem vorigen Lauf geändert hat

Der vorige Kuratorenlauf las den Baum bei `1b974f9`, rund vier Tage vor diesem. Seither sind **100 Commits** gelandet (`git log --oneline 1b974f9..HEAD | wc -l` → 100), und das Intervall trägt drei gefahrene Runden: die 16 (Profil-Zusammenfassung in der Vorschau), die 17 (Kontextmenü mit Zip, Unzip und Finder) und die 18, die keinen Circle-Datensatz hat und der Anlass dieses Laufs ist.

`CLAUDE.md` ist in diesem Intervall genau einmal angefasst worden, und nicht von einem Kuratorenlauf: `8c111ea` vom 260825-1249 („CLAUDE.md steht auf dem Stand der Runde 17"). Die Datei hat damit vier Tage ohne Kuratierung hinter sich, und in dieser Zeit sind vier Defektdatensätze über ihre Aussagen offen geblieben.

**Die Erhebung ist nicht auf dieses Intervall beschränkt.** `git log --follow` ist über die ganze Historie von `CLAUDE.md` gelesen (29 Commits, `c0682ff` vom 260802 bis `8c111ea` vom 260825), und alle Datensatzzitate der Datei sind einzeln gegen den Bestand aufgelöst.

## 2. Die drei normativen Flächen

| # | Fläche | Befund |
|---|---|---|
| 1 | Entscheidungsdatensätze: `$SCAN_DECISIONS` = `shared/decisions/`, dazu die Circle-Speicher `circles/*/decisions/` von Hand hinzugelistet | vorhanden, **184 Dateien** (39 gemeinsam, 145 in Circles), alle lesbar. Marker: 35 offen, 7 beantwortet, 132 umgesetzt, 8 überholt, 2 zurückgestellt |
| 2 | Projekteigene Regeldateien `./rules/` und `.claude/rules/` | **abwesend.** `ls rules` und `ls .claude/rules` melden beide „No such file or directory". Es gibt in diesem Projekt keine projekteigenen Regeldateien und damit auch keine `**Provenance:**`-Kopfzeilen |
| 3 | `CLAUDE.md` im Projektwurzelverzeichnis | vorhanden, 194 Zeilen, 47.809 Bytes, lesbar |

Keine Fläche war unlesbar. Für Fläche 2 gilt der Fall „abwesend": null gemeldet, die zwei anderen Flächen vollständig gefahren. **Kein Widerspruchsbefund dieses Laufs betrifft eine Regeldatei**, weil es keine gibt.

`bin/fusion-paths curator` gibt für jeden `SCAN_*`-Schlüssel allein den gemeinsamen Speicher aus, weil kein Circle aktiv ist. Die Circle-Speicher sind deshalb von Hand dazugelistet; ohne diesen Handgriff fielen 145 Entscheidungs- und rund 570 Defektdatensätze aus dem Suchbereich.

## 3. Evidenzquellen — was gelesen wurde

| # | Quelle | Bestand | Gelesen |
|---|---|---|---|
| 1 | Circle-Datensätze (`$SCAN_CIRCLES` = `circles/`) | **19** (`ls circles/*/_*_circle.md \| wc -l`) | alle 19 Markerzustände; einer im Volltext (`260816-2255-befehle-absetzen-und-makros-speichern/_d_circle.md`, Directive, Grounding, Dependencies). Für die Runde 18 gibt es keinen; an seine Stelle traten ihr Plan und der Abgleich |
| 2 | Entscheidungsdatensätze | **184** | alle 184 Marker; die 42 aktiven (`_o_` und `_a_`) namentlich; vier im Volltext; 24 Zitatziele aus `CLAUDE.md` einzeln aufgelöst |
| 3 | Sitzungsprotokolle (`$SCAN_HISTORY` plus die Circle-Speicher) | **552** (137 gemeinsam, 415 in Circles) | zwei im Volltext: `shared/history/260826-0157-reconciliation.md` (der Abgleich dieser Sitzung) und der Kopf von `shared/history/260821-2231-curator-run.md`; fünf frühere Kuratorenläufe namentlich |
| 4 | `git log --follow` | — | über die ganze Historie von `CLAUDE.md` (29 Commits); über zwei Circle-Datensätze (`260804-0933`, `260816-2255`); `git for-each-ref` über alle Tags. Für Regeldateien entfällt die Quelle, es gibt keine |
| 5 | Durchsichten und Analysen | **82** Durchsichten, **5** Analysen | Bestände aufgelistet; acht Durchsichten, die `CLAUDE.md` nennen, ermittelt; die zwei jüngsten (`260826-0139-coderev`, `260826-0145-ontorev`) auf `CLAUDE.md`-Befunde geprüft — sie tragen keine. Keine Analyse im Volltext gelesen |
| 6 | `orchestrator-events.jsonl` | **1.606** Zeilen | nur bestätigend gelesen: die letzten vier Einträge und die drei Zeilen mit `curator`. **Kein Befund dieses Laufs ruht auf dieser Quelle** |
| 7 | Archivspeicher `$WORKBENCH/archive` | **82** Dateien in zwei Archivläufen (`260819-1613`, `260820-2115`) | Bestand aufgelistet; 20 Dateien ermittelt, die `CLAUDE.md` nennen; eine im Volltext (`260816-2138_c_claude-md-nennt-zehn-gefahrene-runden-es-sind-elf.md`), sie trägt den Beleg zu L01 |
| 8 | `**Provenance:**`-Kopfzeilen auf Regeldateien | **0** | ausdrücklich null: dieses Projekt hat keine eigenen Regeldateien |

**Was nicht gelesen wurde, und was das kostet.** Von den 552 Sitzungsprotokollen sind zwei im Volltext gelesen; die übrigen sind über die Kuratoren- und Abgleichsdateien und über `git log` erreicht worden, nicht einzeln. Von den 82 Durchsichten und fünf Analysen ist keine im Volltext gelesen. Ein Befund, der allein in einem ungelesenen Protokoll oder einer ungelesenen Analyse steht, ist von diesem Lauf nicht gefunden worden.

## 4. Flächengrößen, vorher und nachher

| Fläche | Zeilen vorher | Bytes vorher | Zeilen nachher | Bytes nachher |
|---|---|---|---|---|
| `CLAUDE.md` | 194 | 47.809 | 195 | 49.793 |
| Entscheidungsdatensätze (184 Dateien) | 13.311 | 1.138.890 | unverändert | unverändert |
| Projekteigene Regeldateien | 0 | 0 | 0 | 0 |

Die Nachher-Spalte trägt der Anwendungsdurchgang: `CLAUDE.md` wächst um eine Zeile (die Tabellenzeile der Runde 18 aus L02) und um 1.984 Bytes, also 4,2 Prozent. Der Unterschied verteilt sich auf neun Stellen, `git diff --stat` zählt neun eingefügte gegen acht gelöschte Zeilen. Entscheidungsdatensätze und Regeldateien sind unangetastet, wie vorgesehen.

**Streichungen nach Stufe:** eine, und sie ist Stufe 1 (L08, 160 Zeichen ersetzt durch 98). Keine Streichung auf Stufe 2 oder 3, keine Verdichtung.

**Reichweite der vorgeschlagenen Streichungen.** Gestrichen wird an einer einzigen Stelle Text: L08 nimmt einen Halbsatz von 88 Bytes aus `CLAUDE.md`. Die übrigen acht Einträge fügen hinzu oder schreiben um, mit Zuwachs. 88 von 47.809 Bytes sind 0,18 Prozent einer Fläche; die Schwelle von 20 Prozent ist nicht in Sicht. **Kein Reichweiten-Halt.**

## 5. Vergleichszahlen je Flächenpaar

Die Korpuszahlen sind mit Kommandos ermittelt und nicht geschätzt: `ls shared/decisions/*.md circles/*/decisions/*.md | wc -l` → 184; `ls circles/*/_*_circle.md | wc -l` → 19; `wc -l CLAUDE.md` → 194.

| Paar | Auswahlregel | Paare, die sie erzeugt | Davon gelesen |
|---|---|---|---|
| 1. Entscheidung gegen Entscheidung | Zwei Regeln nebeneinander. **(a)** Unter den 42 aktiven Datensätzen (`_o_`, `_a_`) alle Paare, deren Dateinamen mindestens zwei bedeutungstragende Wortmarken teilen. **(b)** Die sieben Datensätze der Runde 18 gegen die elf der Runde 16, weil die Runde 18 die einzige seit dem vorigen Kuratorenlauf ist, die einen Mechanismus einer früheren Runde umkehrt | (a) **3** von 861 möglichen; (b) **77** | (a) 3 von 3, dazu die vier beteiligten Datensätze; (b) das eine Paar mit belegter Umkehrung im Volltext, die übrigen über ihre Titel und `Implemented:`-Zeilen |
| 2. Entscheidung gegen Regeldatei | entfällt: Fläche 2 ist abwesend | 0 | 0 |
| 3. Entscheidung gegen `CLAUDE.md` | jeder Datensatz, den `CLAUDE.md` zitiert; die Zitate mit `grep -oE` aus der Datei gezogen und mit `ls` gegen den Bestand aufgelöst | **24** | 24 von 24. Alle lösen auf; der Marker jedes Ziels ist gegen das gelesen, was `CLAUDE.md` über es sagt |
| 4. Regeldatei gegen Regeldatei | entfällt | 0 | 0 |
| 5. Regeldatei gegen `CLAUDE.md` | entfällt | 0 | 0 |
| 6. `CLAUDE.md` gegen sich selbst | jede Aussage, die ein Kommando, einen Pfad, einen Dateinamen, einen Probennamen, eine Zahl oder eine Aufzählung nennt, die sich als vollständig liest | **110** Pfadmarken, **26** benannte Bezeichner und Proben, **10** als vollständig gelesene Aufzählungen | alle. 110 Pfadmarken aufgelöst, 26 Bezeichner im Baum gefunden, 10 Aufzählungen einzeln nachgezählt |

**Was dieses Urteil deckt und was nicht.** Für Paar 1 gilt das Urteil „kein lebender Datensatz überholt einen anderen ohne Vermerk" **für die Paare, die diese zwei Regeln erreicht haben**, und nicht für den Korpus von 184 Datensätzen. Von 861 möglichen Paaren allein unter den aktiven sind drei gelesen. Eine Umkehrung zwischen zwei Datensätzen, deren Dateinamen kein Wort teilen und die nicht aus den Runden 16 und 18 stammen, hätte dieser Lauf nicht gesehen.

## 6. Inhalt der Entscheidungsdatensätze vor der Bearbeitung

**Dieser Lauf will keinen Entscheidungsdatensatz ändern.** Alle neun Einträge betreffen `CLAUDE.md`. Es gibt deshalb keinen vorherigen Inhalt zu sichern, und die Pflicht läuft hier ins Leere statt übergangen zu werden.

## 7. Widerspruchsbefund

**Ein Widerspruch, und er ist der Anlass dieses Laufs.** `CLAUDE.md` schickt den Leser an drei Stellen ausdrücklich vom eigenen Text weg an den Dateibestand unter `circles/`, und der Dateibestand antwortet seit der Runde 18 unvollständig, ohne das zu sagen. Beide Seiten sind für sich in Ordnung: die Ablage der Runde 18 im gemeinsamen Speicher folgt der Herkunftsregel, und die drei Idiome in `CLAUDE.md` sind genau deshalb eingeführt worden, weil eine Zahl in Prosa in dieser Datei fünfmal veraltet ist. Der Widerspruch liegt zwischen ihnen, und er ist mit einem Satz auflösbar, weil der Nutzer die Auflösung schon gewählt hat. **Kein neuer offener Entscheidungsdatensatz** ist deshalb angelegt worden: die Frage ist als `shared/issues/260826-0149_*_die-runde-18-hat-keinen-circle-datensatz-…` schon abgelegt, der Nutzer hat Möglichkeit 3 gewählt, und ein zweiter Datensatz doppelte den ersten.

**Kein Widerspruch zwischen zwei lebenden Entscheidungsdatensätzen** in den Paaren, die die Auswahlregel aus Abschnitt 5 erreicht hat. Die eine Umkehrung, die dieser Lauf gefunden hat — die Runde 18 kehrt die Asymmetrie „der erkannte Ordner wird gemerkt, ein Unterordner nicht" aus der Runde 16 um —, richtet sich gegen einen **Modulkopf** und gegen den **Spec** der Runde 16, nicht gegen einen Entscheidungsdatensatz. Der Datensatz `shared/decisions/260825-1725_i_liest-eine-zusammenfassung-denselben-unterordner-einmal-oder-je-zeile.md` sagt das selbst und zitiert den Modulkopf statt eines Datensatzes. Ein `Superseded by:` hat damit kein Ziel, und dieser Lauf schreibt keines.

**Kein Fall von „umgesetzt behauptet, offener Defekt sagt fehlt".** Die 42 aktiven Entscheidungen sind gegen die offenen Defektdatensätze gehalten, die `CLAUDE.md` nennen; keiner bestreitet eine Umsetzung, die ein `_i_`-Marker behauptet.

## 8. Die Änderungsliste

Neun Einträge, alle auf `CLAUDE.md`. Nach Folgengruppe, in der Reihenfolge, in der das Gate sie zeigt:

| Gruppe | Einträge | Zahl |
|---|---|---|
| Zusagenrücknahme | L07 | 1 |
| Stufe 3 | — | 0 |
| Stufe 2 | — | 0 |
| Stufe 1 | L01, L02, L03, L04, L05, L06, L08, L09 | 8 |
| Verdichtung | — | 0 |

Jeder Eintrag nennt seinen Beleg als selbst gefahrenes Kommando mit seiner Ausgabe, und acht der neun nennen daneben einen abgelegten Datensatz, der denselben Befund trägt. Die Kennungen `L01` bis `L09` sind hier vergeben und stehen in dieser Datei, damit die Freigabe einzelner Einträge ein Gate übersteht, das die Liste selbst nicht zeigt.

### L01 — Die drei Zählungen über `circles/` sind eine Untergrenze, und die Zeile sagt es jetzt

- **Fläche:** `CLAUDE.md`
- **Datei:** `CLAUDE.md:11`
- **Stufe:** 1
- **Beleg:** Selbst gefahren am 260826-0812: `ls fusion-workbench/circles/*/_[bc]_circle.md | wc -l` → `17`. Der Baum trägt eine achtzehnte gefahrene Runde ohne Circle-Datensatz: `shared/planning/260825-1725_p_plan-vorschau-vertieft-und-zwei-fehler.md` (zehn Schritte, alle `[DONE]`), 26 Commits `20eccd4..e5ec81a`, und der Abgleich `shared/history/260826-0157-reconciliation.md` Abschnitt „Die Runde 18 hat keinen Circle-Datensatz". Abgelegt als `shared/issues/260826-0149_*_die-runde-18-hat-keinen-circle-datensatz-und-jede-zaehlung-ueber-circles-uebergeht-sie.md`, Möglichkeit 3. Bestätigend aus dem Archiv: `archive/260820-2115-safe-cleanup-tier-1/shared/issues/260816-2138_c_claude-md-nennt-zehn-gefahrene-runden-es-sind-elf.md` — dort hat ein früherer Kuratorenlauf die feste Zahl durch eben diesen Glob ersetzt; jetzt ist der Glob selbst unvollständig.
- **Folgengruppe:** tier-1
- **Entfernte Zusage:** keine — die Zeile bekommt einen Vorbehalt und verliert keine Zusage.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Wie viele Runden gefahren sind und wie jede geschlossen hat, sagt der Dateibestand und nicht diese Zeile:** `ls fusion-workbench/circles/*/_*_circle.md`. Beschränkt (`_b_`) und kohärent (`_c_`) geschlossene Runden zählen als gefahren, vorgesehene (`_a_`) und zurückgestellte (`_d_`) nicht. Eine Runde, die gerade läuft, trägt `_t_`: gebaut, solange sie steht, aber noch nicht geschlossen. Die Tabelle darunter ist ein Verweisregister für die Pfadregel im Absatz danach.

**Nachher:**

> **Wie viele Runden gefahren sind und wie jede geschlossen hat, sagt der Dateibestand und nicht diese Zeile:** `ls fusion-workbench/circles/*/_*_circle.md`. Beschränkt (`_b_`) und kohärent (`_c_`) geschlossene Runden zählen als gefahren, vorgesehene (`_a_`) und zurückgestellte (`_d_`) nicht. Eine Runde, die gerade läuft, trägt `_t_`: gebaut, solange sie steht, aber noch nicht geschlossen. **Nicht jede Runde hat einen Circle-Datensatz, und jede Zählung über `circles/` in dieser Datei liefert deshalb eine Untergrenze:** die Runde 18 ist ohne aktiven Circle gefahren, ihre Arbeit liegt nach der Herkunftsregel vollständig unter `fusion-workbench/shared/`, und keiner der Globs dieser Datei sieht sie (`shared/issues/260826-0149_*_die-runde-18-hat-keinen-circle-datensatz-und-jede-zaehlung-ueber-circles-uebergeht-sie.md`). Die Tabelle darunter ist ein Verweisregister für die Pfadregel im Absatz danach.

### L02 — Die Rundentabelle bekommt eine Zeile für die Runde 18 und einen Ort für „kein Circle"

- **Fläche:** `CLAUDE.md`
- **Datei:** `CLAUDE.md:31` (Tabellenzeile 17, die neue Zeile folgt darunter)
- **Stufe:** 1
- **Beleg:** Dieselbe Messung wie L01. Die zweite Spalte trägt in jeder Zeile ein Circle-Verzeichnis, weil der Absatz darunter Pfade der Form `planning/…` relativ zu diesem Verzeichnis liest; für die Runde 18 gibt es keines, und ihre Pfade lesen sich relativ zu `fusion-workbench/shared/`. Beleg: die Artefakte der Runde liegen sämtlich unter `shared/planning/`, `shared/decisions/` (sieben Datensätze `260825-1725_i_*`), `shared/issues/` und `shared/reviews/`.
- **Folgengruppe:** tier-1
- **Entfernte Zusage:** keine — die Zeile kommt hinzu, keine bestehende ändert sich.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> | 17 | `260825-0711-kontextmenue-traegt-zip-unzip-finder` | das Kontextmenü der Dateiliste trägt Zip, Unzip und Finder neben dem Teilen |

**Nachher:**

> | 17 | `260825-0711-kontextmenue-traegt-zip-unzip-finder` | das Kontextmenü der Dateiliste trägt Zip, Unzip und Finder neben dem Teilen |
> | 18 | — kein Circle-Datensatz, alles unter `fusion-workbench/shared/` | die Vorschau vertieft (Platzhalter in der Ortsangabe, `zeigt` an `juengste`, ein Ort eine Lesung, Ordner ohne Auswahl, mehr Auslieferungsprofile), Klick-Fokus und Zip-Zeitstempel behoben |

### L03 — Der Abnahmelauf-Absatz nennt die Runde 18 mit

- **Fläche:** `CLAUDE.md`
- **Datei:** `CLAUDE.md:84`
- **Stufe:** 1
- **Beleg:** Selbst gefahren: `ls fusion-workbench/circles/*/_[bc]_circle.md` liefert siebzehn Datensätze und die Runde 18 nicht. Dass die Runde 18 gegen die zehn Zusagen ungemessen ist, steht im Plan `shared/planning/260825-1725_p_plan-vorschau-vertieft-und-zwei-fehler.md`, Statuszeile („Gebaut, nicht abgenommen") und in `shared/history/260826-0157-reconciliation.md` Abschnitt „Vor dem Auslieferungslauf".
- **Folgengruppe:** tier-1
- **Entfernte Zusage:** keine.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> — welche das sind, sagt `ls fusion-workbench/circles/*/_[bc]_circle.md` gegen das Datum `260810`;

**Nachher:**

> — welche das sind, sagt `ls fusion-workbench/circles/*/_[bc]_circle.md` gegen das Datum `260810`, und die Runde 18 kommt ohne Circle-Datensatz dazu;

### L04 — Die zwei Marker-Globs sagen, dass sie eine Runde auslassen

- **Fläche:** `CLAUDE.md`
- **Datei:** `CLAUDE.md:70`
- **Stufe:** 1
- **Beleg:** Dieselbe Messung wie L01. `ls fusion-workbench/circles/*/_b_circle.md` liefert zwölf, `ls fusion-workbench/circles/*/_c_circle.md` fünf; zusammen siebzehn, und die Runde 18 steht in keiner der beiden.
- **Folgengruppe:** tier-1
- **Entfernte Zusage:** keine.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> Welche das sind, sagt `ls fusion-workbench/circles/*/_b_circle.md`; die kohärent geschlossenen sagt `ls fusion-workbench/circles/*/_c_circle.md`.

**Nachher:**

> Welche das sind, sagt `ls fusion-workbench/circles/*/_b_circle.md`; die kohärent geschlossenen sagt `ls fusion-workbench/circles/*/_c_circle.md`. Beide Listen lassen jede Runde ohne Circle-Datensatz aus.

### L05 — Der `zip`-Satz nennt ein Merkmal, die Wurzel-`Cargo.toml` führt zwei

- **Fläche:** `CLAUDE.md`
- **Datei:** `CLAUDE.md:82`
- **Stufe:** 1
- **Beleg:** Selbst gefahren am 260826-0805: `sed -n '203,206p' Cargo.toml` → `zip = { version = "8.6", default-features = false, features = [ "deflate-flate2", "unreserved", ] }`. Abgelegt als `shared/issues/260825-1859_*_claude-md-nennt-fuer-zip-das-eine-merkmal-deflate-flate2-es-sind-zwei.md` („Falsch ist allein das Zahlwort"), bestätigt von `shared/history/260826-0157-reconciliation.md`, Tabelle „`CLAUDE.md` gegen den Baum". Die Begründung für `unreserved` steht ausgeschrieben in der Wurzel-`Cargo.toml` über der Abhängigkeit.
- **Folgengruppe:** tier-1
- **Entfernte Zusage:** keine — die Warnung vor dem Streichen der scheinbar unbenutzten `flate2`-Zeile bleibt unverändert stehen, und `unreserved` bekommt dieselbe Warnung sinngemäß dazu.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> Bei `zip` lässt das eine Merkmal `deflate-flate2` genau das Verfahren übrig, das jedes Zip-Werkzeug liest,

**Nachher:**

> Bei `zip` lässt `deflate-flate2` genau das Verfahren übrig, das jedes Zip-Werkzeug liest, und `unreserved` daneben hebt die Prüfung auf, die sonst das Zusatzfeld `0x5855` abwiese, das `ditto(1)` als einziges Zeitfeld liest,

### L06 — Die Aufzählung des Bestands unter `Application Support` nennt fünf von sieben Ablagedateien

- **Fläche:** `CLAUDE.md`
- **Datei:** `CLAUDE.md:122`
- **Stufe:** 1
- **Beleg:** Selbst gefahren am 260826-0757: `awk '/pub const ALLE/,/\];/' crates/krk-core/src/ablage/pfade.rs` → `Belegung`, `Lesezeichen`, `Sitzung`, `Einstellungen`, `Leser`, `Zettel(Erster)`, `Zettel(Zweiter)`; die Namen sind `keymap.toml`, `bookmarks.toml`, `session.toml`, `settings.toml`, `readers.toml`, `note-1.txt`, `note-2.txt` (`pfade.rs:243-250`). Der Satz beginnt mit „Alles, was sich die Anwendung merkt" und nennt die Einstellungen und die Leseprofile nicht. Abgelegt als `circles/260823-2208-vorschau-zeigt-profil-zusammenfassung-statt-metadaten/issues/260824-1852_*_zwei-aussagen-in-claude-md-sind-mit-dieser-runde-falsch-geworden-und-kein-datensatz-traegt-sie.md`, zweite Hälfte („nennt die Leseprofile nicht, obwohl `readers.toml` seit Schritt 2 die siebte Ablagedatei ist"); derselbe Datensatz weist die Behebung ausdrücklich dem Kurator zu.
- **Folgengruppe:** tier-1
- **Entfernte Zusage:** keine — die Betriebsregel gegen den Datenverlust und der Verweis auf die Untersuchung bleiben unverändert; die Aufzählung wird vollständig und bekommt ihre Quelle im Baum.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> Alles, was sich die Anwendung merkt — die Lesezeichen, die gesicherte Sitzung, die abweichende Tastenbelegung und die zwei Notizzettel —, hält sie unter `~/Library/Application Support/KRK/`.

**Nachher:**

> Alles, was sich die Anwendung merkt, hält sie unter `~/Library/Application Support/KRK/`: die Lesezeichen, die gesicherte Sitzung, die abweichende Tastenbelegung, die Einstellungen, die von Hand gepflegten Leseprofile und die zwei Notizzettel. **Welche Dateien das sind, sagt `Datei::ALLE` (`crates/krk-core/src/ablage/pfade.rs`) und nicht diese Zeile**; die Aufzählung hier hat die Einstellungen und die Leseprofile übergangen, seit es sie gibt.

### L07 — Die Ersthelfermeldung hat seit der Runde 14 zwei Empfänger, der Absatz nennt einen

- **Fläche:** `CLAUDE.md`
- **Datei:** `CLAUDE.md:140`
- **Stufe:** 1
- **Beleg:** Selbst gefahren am 260826-0752: `crates/krk-ui/src/appkit/anwendung.rs:1225-1230` trägt `fenster.melder_setzen(Box::new(move || { … selbst.aktives_dem_ersthelfer_nachziehen(); selbst.fokusanzeige_nachziehen(); … }))`. Der Kommentar darüber schreibt es aus: „Zwei Empfaenger haengen daran, und der erste ist der neuere." Abgelegt als `shared/issues/260823-1336_*_claude-md-nennt-einen-empfaenger-der-ersthelfermeldung-der-baum-traegt-seit-dem-260819-zwei.md`, Abschnitt „Vorschlag", der genau diese Fassung verlangt und die Änderung ausdrücklich dem Kurator zuweist.
- **Folgengruppe:** Zusagenrücknahme
- **Entfernte Zusage:** die absolute Lesart von „Es ruft weder `anwenden` noch `setHidden`". Sie stand für die **Meldung** und war seit dem 260819 falsch: der ungenannte erste Empfänger geht über `aktives_setzen` bis `Aufteilung::anwenden` durch. Die Zusage selbst bleibt, eingegrenzt auf `fokusanzeige_nachziehen`, wo eine Probe sie hält. Der offene Verhaltensbefund `shared/issues/260823-0732_*` steht auf eben diesem Empfänger.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> Empfänger ist `Anwendungsdelegierter::fokusanzeige_nachziehen`; es schreibt ausschließlich die fünf Rahmenfarben der Aufteilung (`appkit/aufteilung.rs`, ein `NSBox` je Bereich) und den Fenstertitel (`krk-ui/src/fenstertitel.rs`, eine reine Funktion über die fünf Fokuswerte). **Es ruft weder `anwenden` noch `setHidden`**, und der Grund gehört dazu: eine ausgeblendete Ansicht, die den Ersthelfer hält, lässt AppKit den Rang neu vergeben und diese Meldung ein zweites Mal auslösen.

**Nachher:**

> **Am Melder hängen seit der Runde 14 zwei Empfänger, und sie laufen in dieser Reihenfolge** (`krk-ui/src/appkit/anwendung.rs`, der Rumpf von `fenster.melder_setzen`): erst `aktives_dem_ersthelfer_nachziehen`, das über `aktives_setzen` bis `Aufteilung::anwenden` durchgeht, dann `fokusanzeige_nachziehen`. Das zweite schreibt ausschließlich die fünf Rahmenfarben der Aufteilung (`appkit/aufteilung.rs`, ein `NSBox` je Bereich) und den Fenstertitel (`krk-ui/src/fenstertitel.rs`, eine reine Funktion über die fünf Fokuswerte). **`fokusanzeige_nachziehen` ruft weder `anwenden` noch `setHidden`** — die Zusage gilt ihm und nicht der Meldung, gehalten von der Probe `der_nachzug_der_anzeige_ruehrt_die_auslegung_nicht_an` —, und der Grund gehört dazu: eine ausgeblendete Ansicht, die den Ersthelfer hält, lässt AppKit den Rang neu vergeben und diese Meldung ein zweites Mal auslösen.

### L08 — Die Version ist nicht an jedem Tag gestiegen

- **Fläche:** `CLAUDE.md`
- **Datei:** `CLAUDE.md:45`
- **Stufe:** 1
- **Beleg:** Selbst gefahren am 260826-0800: `git for-each-ref --sort=creatordate --format='%(creatordate:short) %(refname:short)' refs/tags` — am **2026-08-22** steht kein Tag (15.08.: `v0.4.0` `v0.4.1` `v0.4.3`; 16.08.: `v0.4.4` `v0.5.0`; 17.08.: `v0.5.1`; 18.08.: `v0.5.2`; 19.08.: `v0.5.3` `v0.5.4`; 20.08.: `v0.5.5`; 21.08.: `v0.5.6`; 22.08.: keiner; 23.08.: `v1.0.0`; 24.08.: `v1.1.0`; 25.08.: `v1.2.0`). Abgelegt als `shared/issues/260823-1649_*_claude-md-sagt-die-version-sei-seit-dem-260815-an-jedem-tag-gestiegen-am-260822-ist-sie-es-nicht.md`, das den ersatzlosen Wegfall des Halbsatzes empfiehlt: „Wer ihn ersatzlos streicht, verliert nichts an der Begründung."
- **Folgengruppe:** tier-1
- **Entfernte Zusage:** keine — die Begründung dafür, dass die Versionszahl hier nicht steht, trägt der zweite Halbsatz allein.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> — seit dem 260815 ist sie an jedem Tag mindestens einmal gestiegen, und wie oft insgesamt, sagt `git tag -l --sort=creatordate` und keine Zahl an dieser Stelle.

**Nachher:**

> — wie oft sie gestiegen ist, sagt `git tag -l --sort=creatordate` und keine Zahl an dieser Stelle.

### L09 — Der zweite zurückgestellte Circle fehlt, und bei ihm heißt `_d_` wirklich „später"

- **Fläche:** `CLAUDE.md`
- **Datei:** `CLAUDE.md:186`
- **Stufe:** 1
- **Beleg:** Selbst gefahren am 260826-0745: `ls fusion-workbench/circles/*/_d_circle.md` liefert zwei — `260804-0933-eingebauter-web-betrachter-im-vorschaufenster` (die Absage) und `260816-2255-befehle-absetzen-und-makros-speichern`. Der zweite war aktiv: `git log --follow` zeigt `_t_circle.md` → `_d_circle.md` in `b8e198e` vom 260817-0451 („die zwoelfte Runde weicht der Loeschabsicherung"). Sein Spec steht unter `shared/planning/260816-2240_o_spec-befehle-absetzen-und-makros-speichern.md`, sein Circle-Datensatz führt elf Nutzerfestlegungen vom 260816. `grep -c 'makro' CLAUDE.md` (ohne Groß-/Kleinschreibung) → `0`: die Datei erwähnt diese Arbeit an keiner Stelle.
- **Folgengruppe:** tier-1
- **Entfernte Zusage:** keine — der bestehende Satz über die Absage bleibt wörtlich stehen, es kommt einer dazu.
- **Rückweg:** `git checkout -- CLAUDE.md`

**Vorher:**

> **Der Circle trägt seitdem `_d_`, und das ist die nächstliegende Entsprechung und nicht die genaue** — das Vokabular kennt für eine Absage keinen eigenen Marker, und wer `_d_` hier als „später“ liest, liest es falsch.

**Nachher:**

> **Der Circle trägt seitdem `_d_`, und das ist die nächstliegende Entsprechung und nicht die genaue** — das Vokabular kennt für eine Absage keinen eigenen Marker, und wer `_d_` hier als „später“ liest, liest es falsch. **Zurückgestellte Circles sagt `ls fusion-workbench/circles/*/_d_circle.md`, und der Marker trägt dort zwei verschiedene Bedeutungen:** neben der Absage steht der Befehlslauf mit Makros (`260816-2255-befehle-absetzen-und-makros-speichern`, Spec `shared/planning/260816-2240_*_spec-befehle-absetzen-und-makros-speichern.md`), und dort heißt `_d_` genau „später“ — die Runde war aktiv und ist am 260817-0451 der Löschabsicherung gewichen (`b8e198e`).

## 9. Kandidaten — nicht zur Freigabe gestellt

Drei Befunde ohne den Beleg, den ihre Stufe verlangt. **Ein Kandidat wird nie angewandt und nie am Gate angeboten.**

### K01 — `CLAUDE.md:136` zitiert einen Circle-Datensatz über seinen Marker statt über sein Verzeichnis

Die Stelle nennt `circles/260813-2332-notizzettel-als-blatt-mit-zwei-zetteln/_b_circle.md`. Der Zeiger stimmt heute; er stirbt, sobald der Marker jenes Datensatzes sich bewegt. Die Konvention, einen Circle über seinen **Verzeichnisnamen** zu zitieren, steht in `$FUSION_PLUGIN_ROOT/rules/rule-file-provenance.md` und gilt dort den Provenienz-Kopfzeilen von Regeldateien, nicht der Prosa von `CLAUDE.md`. **Kandidat, weil:** keine falsifizierte Aussage und kein Datensatz dieses Projekts, der die Form für `CLAUDE.md` verlangt. Der projekteigene Datensatz zur Sternform (`archive/260819-1613-safe-cleanup-tier-1/shared/decisions/260815-1145_i_schreiben-zitate-im-code-den-marker-aus-oder-die-sternform.md`) nennt `CLAUDE.md` in seinem Geltungsbereich, meint aber Zitate der Form `YYMMDD-HHMM_x_thema` und nicht `_X_circle.md`.

### K02 — `shared/decisions/260814-1955_o_sechs-beantwortete-entscheidungsdatensaetze-tragen-im-kopf-weiter-status-open.md`

Die Frage ist vom Rahmenwerk beantwortet: `$FUSION_PLUGIN_ROOT/rules/fusion-workbench-conventions.md` `## Decision Record Template` hat das Kopffeld `Status:` abgeschafft und schreibt vor, ein vorhandenes Feld unverändert stehen zu lassen — „those drifted headers are the evidence the removal was decided on". **Kandidat aus zwei Gründen:** der überholende Text ist eine Regeldatei des Plugins und keine der Belegarten, die Stufe 2 verlangt (Entscheidungsdatensatz, Schließungsnotiz, Entwurfsabschnitt eines Sitzungsprotokolls); und die Regeldatei liegt außerhalb dieses Projekts und ist für es nicht erreichbar. Den Datensatz zu schließen wäre außerdem eine Markerbewegung gegen den Baumstand und gehört damit `agents/reconciler.md`.

### K03 — Der Baumkommentar zu `crates/krk-core/` nennt `leseprofil/` und `operation/` nicht

`CLAUDE.md:57-58` beschreibt den Kern als „Verzeichnisleser, Ordnermodell, Tastennormalisierung, Ablage, `text/`, `stapelumbenennen/`". Der Baum trägt daneben `leseprofil/` (Runde 16) und `operation/` (Runde 1). **Kandidat, weil:** die Aufzählung sich nicht als vollständig ausgibt — sie steht als Kommentar hinter einem Baumzweig und nennt Beispiele. Ohne eine Stelle, die Vollständigkeit behauptet, wäre die Ergänzung eine Änderung aus erneutem Lesen des vorhandenen Textes, und die ist nie zulässig.

## 10. Was dieser Lauf nicht tun darf, und wem es gehört

**Fünf Befunde außerhalb des Auftrags.** Jeder ist hier benannt, keiner ist angefasst.

1. **`shared/issues/260820-2056_o_claude-md-nennt-eine-zaehlprobe-unter-einem-namen-den-der-baum-nicht-traegt.md` ist behoben und steht offen.** `CLAUDE.md:142` nennt die Probe heute richtig `die_zeichenregel_hat_zwei_rufer_und_der_vergleich_drei`; der Baum trägt sie unter diesem Namen (`crates/krk-core/tests/verzeichnis.rs:3186`). Den Marker gegen den Baumstand fortzuschreiben gehört `agents/reconciler.md` und nicht diesem Lauf.
2. **`Cargo.toml` trägt dieselbe überholte Wendung wie `CLAUDE.md:82`.** Die Wurzel-`Cargo.toml` sagt an der Stelle, die die Merkmale führt, „`default-features = false` mit dem einen Merkmal `deflate-flate2`". Eine Bauvorschrift gehört `coder`; abgelegt ist es schon, als `Also seen`-Zeile auf `shared/issues/260825-1859_*_claude-md-nennt-fuer-zip-das-eine-merkmal-deflate-flate2-es-sind-zwei.md`. **Kein neuer Datensatz**, der erste trägt es.
3. **Der Spec der Runde 16 sagt bei C6.1 und C6.7 Zahlen, die die Runde 18 umgekehrt hat.** „Ein Feldbaustein, der seine Datei in einem Unterordner benennt, kostet genau einen Leselauf" und „fünf Verzeichnisleseläufe am größten mitgelieferten Profil" stehen unverändert in `circles/260823-2208-…/planning/260824-0613_o_spec-…md`. Spec- und Plandateien gehören `planner` und dem Abgleich; ein Spec zeichnet außerdem auf, was zugesagt war.
4. **`shared/issues/260826-0149_o_claude-md-sagt-nichts-ueber-die-fuenf-neuerungen-der-runde-18-an-der-vorschau.md` ist keine Kuratorenarbeit nach dem hier geltenden Maßstab.** Der Datensatz verlangt sechs **Ergänzungen** an `CLAUDE.md`, und keine der sechs macht eine dastehende Aussage falsch. Dieser Lauf ändert eine Aussage nur, wenn ein Widerspruch zwischen zwei Flächen oder eine durch die Geschichte belegte Überholung sie trägt; „hier fehlt etwas" ist keins von beidem. Der Eintrag L06 bringt die Leseprofile als Ablagedatei in die Datei, weil dort eine Aufzählung **falsch** ist; die fünf Vorschau-Neuerungen und der Handgriff mit der `readers.toml` bleiben dem Nutzer überlassen und stehen im Datensatz.
5. **Keine Probe und kein Prüfmuster hängt an den Größen von `CLAUDE.md`.** In der Werkbank dieses Projekts gibt es kein Gegenstück zu `hooks/lib/__tests__/fixtures/rules-emission.golden`; `grep -rl 'CLAUDE.md' crates/ xtask/` liefert keine Probe, die Bytes oder Zeilen dieser Datei festhält. Der Anwendungsdurchgang zieht damit keine fremde Prüfung nach sich.

## 11. Ergebnisse

Freigabe des Nutzers am 260826: alle neun Einträge, einzeln vorgelegt. Die Kandidaten K01 bis K03 sind nicht freigegeben und nicht angefasst.

Jeder Eintrag ist in zwei Schritten geschrieben worden: der Vorher-Text unmittelbar vor dem Schreiben frisch von der Platte gelesen und auf genau ein Vorkommen geprüft, danach die Datei neu gelesen und der Nachher-Text byteweise gegen die freigegebene Fassung gehalten. Kein Eintrag ist an einem der beiden Schritte hängengeblieben.

| Eintrag | Ergebnis | Bemerkung |
|---|---|---|
| L01 | applied | Vorbehalt im Rundenzähler-Absatz; ein Vorkommen ersetzt |
| L02 | applied | Tabellenzeile 18 eingefügt, die Zeile 17 darüber unverändert |
| L03 | applied | Halbsatz im Abnahmelauf-Absatz ergänzt |
| L04 | applied | Satz hinter den zwei Marker-Globs ergänzt |
| L05 | applied | `unreserved` benannt, die `flate2`-Warnung dahinter unverändert |
| L06 | applied | Aufzählung auf sieben Ablagedateien gebracht, Quelle `Datei::ALLE` benannt |
| L07 | applied | Zusage auf `fokusanzeige_nachziehen` eingegrenzt, zweiter Empfänger benannt |
| L08 | applied | Halbsatz über die tägliche Versionssteigerung ersatzlos entfernt |
| L09 | applied | zweiter zurückgestellter Circle mit seiner abweichenden Lesart von `_d_` ergänzt |

**Nicht angewandt:** keiner der neun. Kein `skipped`, kein `stale`, kein `failed`.

**Rückweg für den ganzen Durchgang:** `git checkout -- CLAUDE.md`. Der Arbeitsbaum steht so, wie die Änderungen ihn hinterlassen; gestaged ist nichts, eingecheckt wird vom Nutzer.

**Keine fremde Prüfung nachzuziehen.** Abschnitt 10 Punkt 5 hat es vorweggenommen und der Durchgang bestätigt es: keine Probe und kein Prüfmuster dieses Projekts hält Bytes oder Zeilen von `CLAUDE.md` fest.

## 12. Gestrichene Zusagen, in einer Zeile zum Suchen

Eine einzige, und sie ist eine Eingrenzung und keine Streichung:

- **L07:** die absolute Lesart der Zusage „Es ruft weder `anwenden` noch `setHidden`" über die Ersthelfermeldung. Sie gilt künftig `fokusanzeige_nachziehen` und nicht der Meldung; der erste, ungenannte Empfänger `aktives_dem_ersthelfer_nachziehen` geht seit dem 260819 sehr wohl bis `Aufteilung::anwenden` durch.

Kein anderer Eintrag nimmt eine Zusage weg. L08 streicht als einziger Text ersatzlos, und der gestrichene Halbsatz ist eine Tatsachenbehauptung über Versionstags, keine Regel.
