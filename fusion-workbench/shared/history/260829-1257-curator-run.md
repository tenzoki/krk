# Kuratorenlauf — 260829-1257

**Status:** Complete — Erhebung am 260829-1257 gegen `b9d9cbc`; Anwendung am 260829 gegen `6f57c9f` (Arbeitsbaum, `CLAUDE.md` vor der Anwendung byteidentisch zur Erhebung: 53.125 Bytes, 195 Zeilen), Freigabe `all`, vier Einträge angewandt.
**Datum:** 260829-1257 (Erhebung)
**Git HEAD beim Lesen:** `b9d9cbc` (`chore(release): die Version steht auf 1.4.0`, 2026-08-29 12:29). `CLAUDE.md` ist im Arbeitsbaum unverändert gegenüber HEAD (`git status --porcelain -- CLAUDE.md` ist leer).
**Modus:** survey. Dieser Durchgang hat an keine der drei Flächen geschrieben.
**Umfang:** anchored, ohne auflösbaren Anker: `bin/fusion-cadence-anchor` ist nicht ausführbar (`[ -x … ]` schlägt fehl), also gilt als Untergrenze der vorige Kuratorenlauf `shared/history/260826-1637-curator-run.md`, Baumstand `caa3f23`, Anwendung in `69dfa19`. Alles ab diesem Stand ist gelesen; der Bestand davor nur dort, wo eine Aussage von `CLAUDE.md` ihn zitiert.
**Vorheriger Kuratorenlauf:** `260826-1637`, fünf Einträge, alle angewandt. Davor `260826-1100`, `260826-1009`, `260826-0818`, `260821-2231`, `260820-2127`, `260820-1119`, `260819-1500`, `260814-1405`.

**Was seit dem vorigen Lauf dazugekommen ist.** Zwischen `caa3f23` und `b9d9cbc` liegen 66 Commits. Drei davon fassen `CLAUDE.md` an (`69dfa19` Anwendung des vorigen Laufs, `9c02863` und `fc829c8` aus der Behebungsrunde vom 260826); die übrigen tragen vier Runden, jede kohärent (`_c_`) geschlossen und jede mit einem vom Nutzer gefahrenen Abnahmelauf: Runde 19 `circles/260827-0310-vorschau-zaehlt-ordnerinhalt-im-default-profil` (`a5c7a46..d444879`), Runde 20 `circles/260827-2028-vorschau-rendert-pdf-als-betrachter` (`2033626..48cd818`), Runde 22 `circles/260828-2349-cmd-c-und-cmd-x-legen-dateiverweise-ab` (`4bd0084..35b95b3`), Runde 21 `circles/260828-1041-dateilistenfilter-nimmt-eingaben-per-paste` (`79d507a..8d64859`; nach der 22 geschlossen, weil der Circle früher stand). Dazu die Auslieferungen 1.2.2, 1.3.0 und 1.4.0 (`git tag -l --sort=creatordate | tail -3`). Der Reconciler hat in `shared/history/260829-1252-reconciliation.md`, Abschnitt „Gemeldet für den Curator", vier Stellen von `CLAUDE.md` genannt; jede ist hier am Baum nachgeprüft und nicht geglaubt.

**Sprache dieser Datei.** Deutsch. `CLAUDE.md:3` deklariert `**Language:** de` als einzige Sprachzeile; nach `## Project language` der Konventionen steuert sie beide Flächen. Gelesen sind `stilwerk/chat-voice-de.yaml` und `stilwerk/default-voice-de.yaml`.

---

## 1. Die acht Belegquellen, und was in jeder gelesen wurde

| # | Quelle | Gelesen |
|---|---|---|
| 1 | Circle-Datensätze (`circles/`, 23 Verzeichnisse, 23 Datensätze: 13 `_b_`, 9 `_c_`, 2 `_d_`, 0 `_a_`, 0 `_t_`) | **4 gelesen** (Kopf, Marker und Schließungsnotiz der vier neuen Circles vollständig). Die Marker aller 23 sind gegen die Rundentabelle von `CLAUDE.md` gehalten (Abschnitt 3). |
| 2 | Entscheidungsdatensätze (`shared/decisions/` 27, `circles/*/decisions/` 152, zusammen 179) | **27 Köpfe gelesen** (Titel und `Answered:`/`Superseded by:`-Zeile jedes gemeinsamen Datensatzes), dazu die Köpfe und Fragen der sieben Datensätze der vier neuen Circles und des einen neuen gemeinsamen (`260826-1811_o`). Die 12 in `CLAUDE.md` zitierten Datensätze sind auf Existenz und Marker geprüft (Abschnitt 3). |
| 3 | Sitzungsberichte (`shared/history/` 164, `circles/*/history/` 475, zusammen 639) | **7 gelesen**: der vorige Kuratorenlauf (Kopf, Quellentabelle, Flächenpaare), `shared/history/260829-1252-reconciliation.md` (die Abschnitte zu `CLAUDE.md`), die vier Abgleiche der neuen Circles (`260827-1907`, `260828-1044`, `260829-0734`, `260829-1223`, jeweils die `CLAUDE.md`-Zeilen), der Kopf von `260826-2205-reconciliation.md`. |
| 4 | Git-Historie | `git log --follow -- CLAUDE.md`: 35 Commits, davon 3 seit `caa3f23`. `git blame` auf die Zeilen 81 (`8c111ea`) und 143 (`88f18ed`). `git log` über `caa3f23..b9d9cbc` vollständig (66 Commits, Betreffzeilen). Die Commits `2aee690`, `f4ba58d`, `415ef6f`, `3764fb6`, `1b0939a` sind über die Durchsichten erreicht und am Baum nachgeprüft. |
| 5 | Durchsichten und Untersuchungen (`shared/reviews/` 38, `shared/analyses/` 4, `circles/*/reviews/` 67) | **4 gelesen** (die `CLAUDE.md`-Absätze der vier Durchsichten der neuen Circles: `260827-1911`, `260828-1046`, `260829-0051`, `260829-1218`); die zwei neuen gemeinsamen Durchsichten vom 260826 (`-1933`, `-2158`) nur nach `CLAUDE.md` durchsucht, kein Treffer über die Behebungsrunde hinaus. |
| 6 | `orchestrator-events.jsonl` (2021 Zeilen) | **Durchsucht, 9 Treffer auf `curator`**, die letzten zwei Zeilen gelesen (Abschluss der Runde 21). Bestätigend; kein Eintrag stützt sich darauf. |
| 7 | Archiv (`archive/`, fünf Läufe, 167 Dateien; seit dem vorigen Lauf zwei Läufe, `260827-1534` und `260829-1256`, 25 Dateien) | **Durchsucht, 0 gelesen.** Die Zitatprüfung in Abschnitt 3 hält jeden in `CLAUDE.md` zitierten Pfad gegen das Archiv: keiner ist dorthin gewandert. |
| 8 | `**Provenance:**`-Köpfe projekteigener Regeldateien | **0 — die Fläche fehlt.** `ls rules .claude/rules` meldet beide als nicht vorhanden. |

Der Defektbestand als Gegenprobe: `find shared/issues circles/*/issues -name '*_o_*.md'` liefert 332 offene Datensätze. Vollständig gelesen sind die zwei, die `CLAUDE.md` im Titel nennen und seit dem vorigen Lauf entstanden sind (`circles/260827-2028-…/issues/260828-1046_o_claude-md-nennt-sieben-werte-…`, `circles/260828-1041-…/issues/260829-1217_o_claude-md-nennt-die-zaehlprobe-…`); beide stützen je einen Eintrag (L03, L04). Kein offener Defekt behauptet das Gegenteil eines der vier Einträge. `shared/issues/260826-0149_o_claude-md-sagt-nichts-ueber-die-fuenf-neuerungen-der-runde-18-an-der-vorschau.md` verlangt eine Ergänzung, keinen der Einträge hier; er steht neben den zwei Kandidaten unten.

## 2. Flächengrößen

| Fläche | Vorher | Nachher (bei voller Freigabe) |
|---|---|---|
| `CLAUDE.md` | 53.125 Bytes, 195 Zeilen | 54.911 Bytes, 199 Zeilen (auf einer Arbeitskopie im Scratchpad angewandt und gemessen) |
| Entscheidungsdatensätze (`shared/decisions/`) | 27 Dateien, 166.633 Bytes, 2.229 Zeilen | unverändert |
| Projekteigene Regeldateien | Fläche fehlt | Fläche fehlt |

**Sperrschwelle:** kein Eintrag löscht einen Absatz oder eine Zeile. L02 nimmt drei Circle-Namen (rund 170 Bytes) aus einer Aufzählung heraus, die übrigen drei Einträge verlängern. Das größte ersetzte Vorher-Stück (L02) misst 634 Bytes von 53.125, also 1,2 Prozent; die Schwelle von 20 Prozent ist nicht berührt.

## 3. Die sechs Flächenpaare

| Paar | Auswahlregel | Paare erzeugt | Gelesen | Befund |
|---|---|---|---|---|
| Entscheidung gegen Entscheidung | jeder seit `caa3f23` entstandene Datensatz (`ls -t`, 8: `260826-1811_o` gemeinsam; `260827-0311_i` ×2, `260827-1322_o`, `260827-2028_i`, `260828-0712_o`, `260828-1041_o`, `260829-0053_a` in den Circles) gegen jeden gemeinsamen Datensatz gleichen Gegenstands (Titelwort über die 27 Köpfe) | 3 (`260827-1322_o` L7 im Messmodus gegen `260819-2216_a_schuldet-diese-runde-einen-abnahmelauf-gegen-l7`; `260828-1041_o` cmd+v mit Dateiverweis gegen `260811-1552_*_welche-sorten-legt-der-pfadkopierer-…` in Runde 4; `260827-2028_i` Zoomtasten gegen `260826-1223_o` Zehnerblock) | 3 | keiner überholt einen anderen: die L7-Frage baut auf der Antwort von `260819-2216` auf, statt ihr zu widersprechen; `260828-1041_o` schreibt die Reservierung aus `260811-1552` fort und lässt sie stehen; die Zoomtasten liegen nicht auf dem Zehnerblock |
| Entscheidung gegen Regeldatei | — | 0 | 0 | Fläche fehlt |
| Entscheidung gegen `CLAUDE.md` | jeder in `CLAUDE.md` zitierte Datensatz (`grep -oE '(shared\|circles)/[^ ]+\.md' CLAUDE.md`, 26 Pfade, davon 12 Entscheidungen) auf Existenz, Marker und die Aussage, die `CLAUDE.md` über ihn macht; dazu die 7 neuen Datensätze gegen die Absätze, die ihren Gegenstand berühren | 12 + 7 | 19 | alle 26 Pfade vorhanden, keiner archiviert, jeder Marker passt zur Aussage. Ein Befund: `260827-2028_i` (drei Zoombefehle, `Wirkungsbereich::Vorschau`) ist umgesetzt, `CLAUDE.md:81` zählt noch sieben Werte (L03). `260828-1041_o` bleibt offen und `CLAUDE.md` sagt nichts über `cmd+v`; kein Widerspruch |
| Regeldatei gegen Regeldatei | — | 0 | 0 | Fläche fehlt |
| Regeldatei gegen `CLAUDE.md` | — | 0 | 0 | Fläche fehlt |
| `CLAUDE.md` gegen sich selbst | die Rundentabelle gegen `ls circles/*/_*_circle.md` (23 Marker) und gegen den Satz „Was die Runden ab der zweiten hinzugefügt haben, steht in der Tabelle oben" (Zeile 46); jede Zahl und jeder Bezeichner der vier vom Reconciler genannten Absätze gegen den Baum (Abschnitt 4) | 23 Marker, 26 Pfade, 14 Bezeichner | alle | zwei Befunde: vier kohärent geschlossene Runden fehlen in der Tabelle (L01); der Satz „In vieren der kohärent geschlossenen Runden hat der Nutzer den Abnahmelauf gefahren" zählt nach den vier Schließungsnotizen acht von neun (L02) |

Der Befund „kein lebender Datensatz überholt einen anderen" gilt für die Paare, die die Auswahlregel erreicht hat, und nicht für den Bestand von 179 Datensätzen.

## 4. Was am Baum nachgeprüft ist

Gehalten: `Bereich` 5 und `Fokus` 5 Werte (`awk '/enum Bereich /,/^}/' crates/krk-ui/src/fenstermodell.rs`, `awk '/enum Fokus /,/^}/' crates/krk-ui/src/kommandos/fokus.rs`); `Art` 6 Werte mit `Zippen` und `Entpacken`; `Datei::ALLE` mit 7 Einträgen; `Kontextbefehl` mit 3 Werten; `ist_eigene_textflaeche` (`anwendung.rs:2693`) vergleicht weiterhin genau zwei Flächen, Editor und Vorschau-Textanzeige, der PDF-Betrachter ist nicht angemeldet und die Zahl zwei in `CLAUDE.md:137` hält (so auch die Durchsicht `260828-1046`); `ohne_warten_oeffnen` mit fünf Rufern (`text/datei.rs` ×3, `operation/{zippen,entpacken}.rs`) plus dem Verzeichnisleser in `sys.rs:240`, wie `CLAUDE.md` es beschreibt; `waehrend_eines_blattes_kommen_genau_diese_vier_durch` in `zulaessigkeit.rs:941`; Untergrenzen-Abschnitt in jeder Datei unter `appkit/` außer `koordinaten.rs` und `mod.rs`; `Cargo.lock` ohne `cc` und ohne anderes `-sys` als `windows-sys`; `objc2-pdf-kit` wie `syntect`, `two-face` und `zip` mit `default-features = false`; `rust-toolchain.toml` auf 1.97.1; `krk-ui` ohne `src/lib.rs`; `Makefile` mit `tasten`, `menue`, `check`, `ausliefern`, `beglaubigen`; `ls circles/*/_a_circle.md` leer, wie Zeile 187 sagt; alle 26 in `CLAUDE.md` zitierten Workbench-Pfade vorhanden (Glob-Skript, `_*_` als `_?_`), keiner im Archiv.

Falsch geworden: `Wirkungsbereich` 8 Werte statt sieben (`awk '/^pub enum Wirkungsbereich/,/^}/' crates/krk-core/src/tasten/belegung.rs | grep -cE '^\s+[A-Z]'` liefert 8, `Vorschau` seit `2aee690`); die Zählprobe heißt `die_zeichenregel_hat_drei_rufer_und_der_vergleich_drei` (`crates/krk-core/tests/verzeichnis.rs:3694`, `grep -c die_zeichenregel_hat_zwei_rufer crates/` liefert 0); `traegt_die_folge` ist ein Musterabgleich über `Muster` (`filter.rs`, Modulkopf `# Der Vergleich ist seit der Runde 21 ein Musterabgleich`, `Muster::aus` kleinschreibend, ohne Umlautfaltung, an `*` geteilt); die Rundentabelle endet bei 18, `ls circles/*/_c_circle.md` liefert 9 Datensätze und die Tabelle nennt 5 davon; die vier Schließungsnotizen der neuen Circles sagen jede „Der Nutzer hat den Abnahmelauf … gefahren".

## 5. Pre-edit content

Kein Entscheidungsdatensatz wird in diesem Lauf geändert. Die vollständige Vorher-Fassung jedes Eintrags steht im **Before**-Block des Eintrags; für `CLAUDE.md` gilt daneben `git checkout -- CLAUDE.md` gegen `b9d9cbc`.

## 6. Ledger

Reihenfolge nach Folgegruppe, die schwerste zuerst. Keine Constraint-Entfernung, kein Tier-3-Eintrag, kein Tier-2-Eintrag, keine Konsolidierung; vier Tier-1-Einträge, zwei Kandidaten.

### L01 — Die Rundentabelle endet bei 18; vier kohärent geschlossene Runden fehlen

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Zeile 32 (die Zeile der Runde 18; die vier neuen Zeilen folgen darauf)
- **Tier:** 1
- **Citation:** `ls fusion-workbench/circles/*/_c_circle.md | wc -l` liefert 9; die Tabelle nennt fünf `_c_`-Circles (Zeilen 8, 12, 13, 14, 15). Die vier fehlenden: `260827-0310-…` (Schließungsnotiz: „Geschlossen kohärent (`_c_`) am 260827-1920"), `260827-2028-…` („am 260828-1055"), `260828-1041-…` („am 260829-1226"), `260828-2349-…` („am 260829-0737"). Zeile 46 verspricht: „Was die Runden ab der zweiten hinzugefügt haben, steht in der Tabelle oben" — für vier Runden ist das falsch. Gegenstände aus den Schließungsnotizen und den Commit-Betreffzeilen `3ee2638`, `9f91f92`, `bf3a91d`, `5e506e6` (19), `5ff1ee4`, `22b8442`, `2aee690` (20), `f4ba58d`, `3722c89` (21), `1644ada`, `3764fb6` (22). Die Nummern folgen der Zählung der Circles (`shared/history/260829-1252-reconciliation.md`, „die Runden 19–22"); die 21 ist nach der 22 geschlossen und steht trotzdem davor, weil ihr Circle früher stand.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> | 18 | — kein Circle-Datensatz, alles unter `fusion-workbench/shared/` | die Vorschau vertieft (Platzhalter in der Ortsangabe, `zeigt` an `juengste`, ein Ort eine Lesung, Ordner ohne Auswahl, mehr Auslieferungsprofile), Klick-Fokus und Zip-Zeitstempel behoben |

**After:**
> | 18 | — kein Circle-Datensatz, alles unter `fusion-workbench/shared/` | die Vorschau vertieft (Platzhalter in der Ortsangabe, `zeigt` an `juengste`, ein Ort eine Lesung, Ordner ohne Auswahl, mehr Auslieferungsprofile), Klick-Fokus und Zip-Zeitstempel behoben |
> | 19 | `260827-0310-vorschau-zaehlt-ordnerinhalt-im-default-profil` | ein eingebautes Default-Profil zählt den Ordnerinhalt nach Typ und beziffert die versteckten Einträge, drei Zählzeilen unter den sechs Metadatenangaben, `typ` und `versteckt` als Schlüssel in `readers.toml` |
> | 20 | `260827-2028-vorschau-rendert-pdf-als-betrachter` | die Vorschau rendert PDF als Betrachter mit Seitenzähler in der Statuszeile und den drei Zoombefehlen `cmd+plus`, `cmd+minus` und `cmd+0`; `Wirkungsbereich::Vorschau` kommt zurück |
> | 21 | `260828-1041-dateilistenfilter-nimmt-eingaben-per-paste` | `cmd+v` im Dateifenster hängt die Zwischenablage an den Filtertext an, `*` im Filtertext ist ein Platzhalter |
> | 22 | `260828-2349-cmd-c-und-cmd-x-legen-dateiverweise-ab` | `cmd+c` und `cmd+x` im Dateifenster legen Dateiverweise für andere Anwendungen ab |

### L02 — „In vieren der kohärent geschlossenen Runden hat der Nutzer den Abnahmelauf gefahren": es sind acht von neun

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Zeile 71
- **Tier:** 1
- **Citation:** `ls fusion-workbench/circles/*/_c_circle.md | wc -l` liefert 9. Die vier in der Zeile genannten plus die vier neuen Schließungsnotizen (je „Der Nutzer hat den Abnahmelauf aus Schritt N … gefahren": `260827-0310-…/_c_circle.md`, `260827-2028-…/_c_circle.md`, `260828-1041-…/_c_circle.md`, `260828-2349-…/_c_circle.md`) sind acht; die eine Ausnahme `260817-0833-…` bleibt (Schließungsnotiz: geschlossen „auf Entscheid des Nutzers am Rebalance-Gate", kein Abnahmelauf). „Vieren" ist damit falsch. Die Ersetzung nennt keine Zahl (Zeile 71 sagt zwei Sätze später selbst: „Die Schließungsnotiz des jeweiligen Circle-Datensatzes schreibt es je Runde aus"), behält die Ausnahme und die Besonderheit der Runde 15 und lässt drei Circle-Namen aus der Aufzählung fallen, die `ls` liefert. Wer die drei Namen behalten will, lehnt den Eintrag ab; die Zahl bleibt dann falsch.
- **Consequence group:** tier-1
- **Constraint removed:** none (drei Beispielnamen entfallen: `260813-0939-…`, `260818-1615-…`, `260819-2230-…`; keine Regel hängt an ihnen)
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> In vieren der kohärent geschlossenen Runden hat der Nutzer den Abnahmelauf gefahren (`260813-0939-titelleiste-fuehrt-version-und-semantische-tags`, `260818-1615-ordner-angleichen-und-abwurf-aus-fremden-apps`, `260819-2230-auswahl-und-kopieren-in-der-vorschau` und `260821-1644-veroeffentlichen-als-achte-station`, deren Abnahme fünfzehn Nutzerkriterien geprüft hat und für eines mangels zweitem Mac ein Indiz statt einer Abnahme liefert); in einer (`260817-0833-jeder-loeschweg-mit-rueckfrage-und-nur-noch-papierkorb`) ist er **nicht** gefahren

**After:**
> In allen kohärent geschlossenen Runden bis auf eine hat der Nutzer den Abnahmelauf gefahren; welche, sagt die Schließungsnotiz des jeweiligen `_c_circle.md`, und eine Zahl steht hier nicht, weil sie mit jeder kohärent geschlossenen Runde stiege (bei `260821-1644-veroeffentlichen-als-achte-station` hat die Abnahme fünfzehn Nutzerkriterien geprüft und für eines mangels zweitem Mac ein Indiz statt einer Abnahme geliefert). In der einen (`260817-0833-jeder-loeschweg-mit-rueckfrage-und-nur-noch-papierkorb`) ist er **nicht** gefahren

### L03 — `Wirkungsbereich` trägt acht Werte, nicht sieben; die Zahl weicht der Ableitung

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Zeile 81
- **Tier:** 1
- **Citation:** `awk '/^pub enum Wirkungsbereich/,/^}/' crates/krk-core/src/tasten/belegung.rs | grep -cE '^\s+[A-Z]'` liefert 8 (`Dateifenster`, `Leiste`, `Dateibereiche`, `Editor`, `Tabbereich`, `Navigator`, `Vorschau`, `Ueberall`); `Vorschau` seit `2aee690` (Runde 20, Schritt 2). Defekt `circles/260827-2028-vorschau-rendert-pdf-als-betrachter/issues/260828-1046_o_claude-md-nennt-sieben-werte-fuer-wirkungsbereich-der-baum-traegt-acht.md`. Ableitung statt Berichtigung: die Zeile behandelt `Kommando` und `Art` schon so, und `Wirkungsbereich` wächst mit jedem Fokusbereich. `Bereich` und `Fokus` halten (je 5, nachgezählt am 260829) und behalten ihre Zahl mit dem neuen Datum.
- **Consequence group:** tier-1
- **Constraint removed:** none
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> Am 260825 nachgezählt: `Wirkungsbereich` (`krk-core/src/tasten/belegung.rs`) trägt sieben Werte, `Bereich` (`krk-ui/src/fenstermodell.rs`) fünf und `Fokus` (`krk-ui/src/kommandos/fokus.rs`) fünf. Für `Kommando` — dieselbe Datei wie `Wirkungsbereich`, also `krk-core/src/tasten/belegung.rs` — steht hier keine Zahl:

**After:**
> Am 260825 nachgezählt und am 260829 unverändert: `Bereich` (`krk-ui/src/fenstermodell.rs`) trägt fünf Werte und `Fokus` (`krk-ui/src/kommandos/fokus.rs`) fünf. Für `Wirkungsbereich` (`krk-core/src/tasten/belegung.rs`) steht hier keine Zahl mehr: sie stand bis zur Runde 20 auf sieben und ist mit `Wirkungsbereich::Vorschau` (`2aee690`) falsch geworden (`circles/260827-2028-vorschau-rendert-pdf-als-betrachter/issues/260828-1046_*_claude-md-nennt-sieben-werte-fuer-wirkungsbereich-der-baum-traegt-acht.md`); gezählt wird sie mit `awk '/^pub enum Wirkungsbereich/,/^}/' crates/krk-core/src/tasten/belegung.rs`. Für `Kommando` — dieselbe Datei wie `Wirkungsbereich`, also `krk-core/src/tasten/belegung.rs` — steht hier keine Zahl:

### L04 — Der Filterabsatz nennt die Zählprobe mit ihrem alten Namen und den Vergleich als Teilzeichenfolge

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Zeile 143
- **Tier:** 1
- **Citation:** `grep -rn 'die_zeichenregel_hat' crates/krk-core/tests/verzeichnis.rs` liefert allein `:3694:fn die_zeichenregel_hat_drei_rufer_und_der_vergleich_drei()`; der alte Name hat im Baum keinen Treffer. `crates/krk-core/src/verzeichnis/filter.rs`, Modulkopf: „Die Zeichenregel hat drei Aufrufer, … seit der Runde 21 die Reinigung des eingefügten Textes, `crate::zwischenablage::filtertext_aus`" und „# Der Vergleich ist seit der Runde 21 ein Musterabgleich … `*` … an beiden Enden ungebunden"; `Muster::aus` (`filter.rs:140-145`) schreibt klein und teilt an `*`, `traegt_die_folge` (`filter.rs:155-156`) „ohne Rücksicht auf die Schreibung, ohne Faltung von Umlauten und Akzenten". Commits `f4ba58d` (Platzhalter) und `415ef6f` (Proben). Defekt `circles/260828-1041-dateilistenfilter-nimmt-eingaben-per-paste/issues/260829-1217_o_claude-md-nennt-die-zaehlprobe-mit-ihrem-alten-namen-und-den-vergleich-als-teilzeichenfolge.md`, dessen Abnahme genau diese Form verlangt (Verweis auf den Modulkopf statt einer zweiten Beschreibung der Regel).
- **Consequence group:** tier-1
- **Constraint removed:** none (der Satz „ohne Rücksicht auf Groß- und Kleinschreibung und faltet keine Umlaute" bleibt als Eigenschaft des Musterabgleichs stehen)
- **Revert path:** `git checkout -- CLAUDE.md`

**Before:**
> Wie viele Rufer jede hat, sagt die Zählprobe `die_zeichenregel_hat_zwei_rufer_und_der_vergleich_drei` in `crates/krk-core/tests/verzeichnis.rs` und nicht diese Zeile — sie hat während der Runde 10 einen unerlaubten Rufer gefangen, und behoben wurde die Wurzel und nicht die Zahl in der Probe. Der Vergleich ist eine Teilzeichenfolge ohne Rücksicht auf Groß- und Kleinschreibung und faltet keine Umlaute.

**After:**
> Wie viele Rufer jede hat, sagt die Zählprobe `die_zeichenregel_hat_drei_rufer_und_der_vergleich_drei` in `crates/krk-core/tests/verzeichnis.rs` und nicht diese Zeile — sie hat während der Runde 10 einen unerlaubten Rufer gefangen, und behoben wurde die Wurzel und nicht die Zahl in der Probe; ihren heutigen Namen trägt sie seit der Runde 21, deren Reinigung des eingefügten Textes (`krk_core::zwischenablage::filtertext_aus`) der dritte Rufer der Zeichenregel ist. **Der Vergleich ist seit der Runde 21 ein Musterabgleich und keine Teilzeichenfolge mehr:** `*` ist das eine Sonderzeichen des Filtertexts und steht für eine beliebige, auch leere Zeichenfolge, an beiden Enden bleibt der Vergleich ungebunden, er nimmt keine Rücksicht auf Groß- und Kleinschreibung und faltet keine Umlaute; `traegt_die_folge` nimmt dafür ein `Muster` und keinen `&str`. Die Regel im Einzelnen steht im Modulkopf von `filter.rs` und nicht hier.

### K01 — Die Hülle um `NSPasteboard` hat seit der Runde 22 einen zweiten Ausgang und seit der Runde 21 einen dritten Leser (Kandidat, nicht im Angebot)

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Zeile 79
- **Tier:** candidate
- **Citation:** `crates/krk-ui/src/appkit/zwischenablage.rs`, Modulkopf (Diagramm mit `dateiverweise_schreiben`, Runde 22, und `einfuegequelle`, Runde 21); `3764fb6`, `1b0939a`; gemeldet in `circles/260828-2349-…/history/260829-0734-reconciliation.md` und `circles/260828-1041-…/history/260829-1223-reconciliation.md`.
- **Consequence group:** candidate
- **Warum Kandidat:** Kein Satz der Zeile 79 ist falsch: die Hülle ist weiterhin die eine, ist seit der Runde 4 Ziel und nimmt seit der Runde 14 eine hereingereichte Ablage entgegen. Was fehlt, ist eine Ergänzung, und eine Ergänzung ohne falsifizierten oder überholten Satz hat keine Stufe; der Abgleich `260829-1223` nennt es selbst „keine falsche Aussage". Wer die Ergänzung will, formuliert sie in der Datei; der Kurator schlägt sie nicht vor.
- **Constraint removed:** none
- **Revert path:** entfällt

### K02 — `copy:`, `cut:` und `paste:` sind ein dritter Weg ohne Eintrag in der Belegung (Kandidat, nicht im Angebot)

- **Surface:** CLAUDE.md
- **File:** `CLAUDE.md`, Zeile 135
- **Tier:** candidate
- **Citation:** `crates/krk-ui/src/appkit/anwendung.rs:78` („Drei Antworten ohne Kommando: `copy:`, `cut:` und `paste:`"); `crates/krk-ui/src/kommandos/mod.rs:74-79` (zweiter Eingang der Zulässigkeitsregel, drei Selektoren); Schließungsnotiz `circles/260828-2349-…/_c_circle.md` („`copy:`/`cut:` sind ein dritter Weg ohne Taste").
- **Consequence group:** candidate
- **Warum Kandidat:** Der Absatz beschreibt den zweiten Weg der Runde 17 und bleibt in jedem Satz richtig; die drei Selektoren stehen anders als `Kontextbefehl` im Hauptmenü „Bearbeiten" mit `cmd+c`, `cmd+x`, `cmd+v` als Kürzel, sind also über `make menue` findbar, und ob der Satz „an dieser einen Stelle geschlossen" durch `Dateiablage` (`operationen.rs:1146`, zwei Werte) falsch geworden ist, hängt davon ab, ob der Ausführungszweig ohne Auffangzweig über `Dateiablage` verzweigt; `dateiablage_ausfuehren` (`anwendung.rs:3235`) reicht den Wert weiter, und ich habe den Zweig nicht bis zum Ende gelesen. `inference:` ein Ergänzungsabsatz, kein Widerspruch. Ohne Falsifikation keine Stufe.
- **Constraint removed:** none
- **Revert path:** entfällt

## 7. Außerhalb des Auftrags gesehen, nicht angefasst

- `circles/260828-2349-…/decisions/260829-0053_a_…` trägt `_a_`, die Schließungsnotiz sagt „beantwortet, ohne Codeänderung"; ob ein Datensatz, dessen Antwort keine Umsetzung braucht, `_a_` oder `_i_` trägt, ist die offene Frage `shared/issues/260820-2056_o_drei-beantwortete-datensaetze-koennen-nie-umgesetzt-werden-…`. Markerarbeit, Reconciler.
- `shared/issues/260826-0149_o_claude-md-sagt-nichts-ueber-die-fuenf-neuerungen-der-runde-18-an-der-vorschau.md` verlangt eine Ergänzung derselben Art wie K01 und K02; nach dieser Runde eine Neuerung mehr (der Betrachter). Ergänzungen sind Nutzerarbeit an der Datei, nicht Kuratorenarbeit.

## 8. Outcomes

Anwendungsdurchgang am 260829, HEAD `6f57c9f`, Freigabe `all` (L01–L04). Vor jedem Eintrag ist der Vorher-Block gegen die Platte gehalten (genau ein Treffer), nach jedem Schreiben der Nachher-Block byteidentisch wiedergefunden.

- L01 — applied
- L02 — applied
- L03 — applied
- L04 — applied
- K01 — skipped (Kandidat, nicht im Angebot)
- K02 — skipped (Kandidat, nicht im Angebot)

`CLAUDE.md` nachher: 54.911 Bytes, 199 Zeilen (`wc -c -l CLAUDE.md`), wie in Abschnitt 2 vorausgesagt. Entfernte Constraints: keine. Rückweg: `git checkout -- CLAUDE.md`. Kein Commit.
